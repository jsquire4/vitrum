# Hardware (GPU) Validation Needs — vitrum

**Audience:** an agent/engineer on a machine with a **real hardware WebGPU adapter** (Chrome 120+ on a discrete GPU, or any host where `chrome://gpu` shows hardware-backed WebGPU). The dev box this was authored on is **WSL2 with SwiftShader only**, which cannot validate any of the items below.

This file lists every change from the 2026-05-28 complexity-remediation sweep (and its follow-ups) whose **correctness is already pinned by unit tests + code audits** but whose **visual / GPU-runtime confirmation could not be run in WSL**. Each item has: what changed, why it needs a GPU, the test scene, the expected delta, acceptance criteria, and the exact commands.

---

## 0. Why WSL can't do this (verified)
`tools/benchmark-runner` adapter probe (`VITRUM_PROBE_START_SERVER=1 npm run benchmark:pt-webgpu-adapter-probe`) reports, for BOTH the "hardware" and "swiftshader" Playwright profiles in WSL2:
```
maxStorageBuffersPerShaderStage: 10   maxStorageTexturesPerShaderStage: 4
ptWebgpuFullTier: false   ptWebgpuLiteTier: true   hybridCanRun: false
```
Consequences:
- **walkaround-hybrid backend won't initialize** (`hybridCanRun: false` — needs ≥8 storage textures; SwiftShader gives 4). → all DDGI / ReSTIR / shade / stained-glass validation is blocked.
- **pt-webgpu runs lite tier only** (full tier needs ≥23 storage buffers). → BDPT light-subpath + full-tier intersection paths can't run.
- The README warns SwiftShader renders are "slow and likely incorrect" for these scenarios.

**First step on a real GPU:** run the probe and confirm `ptWebgpuFullTier: true` + `hybridCanRun: true` before trusting any capture.

## 1. Harness reference
- Capture adapter: `tools/benchmark-runner/capture-adapter-playwright.mjs` (writes a PNG to `VITRUM_OUTPUT_PNG`).
- Diff/gate runner: `npm run benchmark:gap-closure --workspace @vitrum/benchmark-runner` (compares candidate vs baseline, computes PSNR).
- Image diff helper: `tools/reference-renders/diff-baselines.mjs`.
- WSL→Windows-Chrome bridges (if running WSL vite + Windows GPU Chrome): the `tools/benchmark-runner/run-*-win-chrome.mjs` variants.
- Acceptance-metric JSON generation from PNG pairs: `npm run benchmark:acceptance-metrics` (see `tools/benchmark-runner/README.md` §"Acceptance metrics artifacts").

### Pre/post A/B baseline
Everything in this sweep is now on `main`. The **pre-sweep baseline commit is `bbd32c8`** ("docs(context-sweep): purge stale comments…", the commit `main` sat at before the sweep). The general A/B procedure for any radiometric item below:
```bash
# baseline (pre-sweep)
git stash; git checkout bbd32c8
VITRUM_GPU_CAPTURE=1 VITRUM_ALLOW_BASELINE_GEN=1 \
  VITRUM_CAPTURE_CMD="node ./tools/benchmark-runner/capture-adapter-playwright.mjs" \
  npm run benchmark:gap-closure --workspace @vitrum/benchmark-runner
# (move the generated PNGs to *.pre-sweep.png per the benchmark-runner README §"Sweep verification capture")
# candidate (post-sweep)
git checkout main
VITRUM_GPU_CAPTURE=1 \
  VITRUM_CAPTURE_CMD="node ./tools/benchmark-runner/capture-adapter-playwright.mjs" \
  npm run benchmark:gap-closure --workspace @vitrum/benchmark-runner
```
Acceptance (per `tools/benchmark-runner/README.md` §"Sweep verification capture"): the visual change must be in the direction the math predicts; no NEW artifacts vs the pre-sweep image; a PSNR regression is acceptable only if visually justified (physically-correct darkening/colour-shift).

---

## 2. Items needing GPU validation

> Status legend: **[done-needs-render]** = code shipped + unit-pinned, only visual confirm outstanding.

> **2026-05-29 GPU validation wave (wsl-gpu harness, dzn/RTX-4090 + lavapipe oracle) — authoritative record: `wsl-gpu/WAVE8-RESULTS.md`.** Cleared the hybrid radiometric backlog: **V2** (chroma: near-light red R/G=2.34 vs white; area: sheared monotone with `4·|u×v|`), **V4** (directional→sun cos45°=0.759; azimuth diffuse-washed by DDGI hemisphere probes — measurement limit, dir verified packed), **V5**, **V12**, **V13** (30-frame moving `updatePrimitive`: 0 errors/teardowns, parity holds), **V14**, **V15**, **V16** (within-HEAD: 5–25× lower disocclusion variance — strict vs-fixed-scalar A/B blocked, baseline lacks the V2 fix), and **V20 NRC** (best-effort, BIASED: 3–5× lower converged variance, stable self-training, gate-OFF byte-identical) — all **PASS**, dual-backend cross-checked. Methodology isolated each fix from the `ddgiUpdateDivisor` cadence confound via linear-HDR debug-texture readback + byte-identity controls. **Still blocked on a conformant-Dawn / real-browser-GL path (the wgpu-native harness cannot run them):** V18 (BDPT — needs scene + Dawn), V19 GRIS-ON (`ptr<storage>` naga gap), V6/V9 (WebGL2 device).

### V1 — A3: BDPT bounce-0 tangent fix  [done-needs-render]
- **What changed:** `wi = t*x + b*y + n*z` (was `b*x`; `y` was dead) in `pt-webgl/src/bdpt/bdptSceneEmittersCpu.ts` + `pt-webgpu/src/bdpt/bdptEmitterPickCpu.ts`. The CPU oracle now matches the GPU `cosineHemisphereSample` shader.
- **Why GPU:** the BDPT light-subpath integrator is full-tier pt-webgpu / pt-webgl; lite tier omits it.
- **Test scene / plan:** a BDPT-enabled scene with an area or mesh light (e.g. a Cornell box with BDPT on) at fixed seed, both backends. Capture pre-sweep (`bbd32c8`) vs `main`.
- **Expected delta:** corrected cosine-weighted light-subpath sampling → slightly different (correct) indirect distribution; **no fireflies/bias** introduced. Converged images should be very close (the bug biased low-sample distributions).
- **Acceptance:** converged BDPT render is artifact-free and matches the analytic expectation; A/B shows no systematic darkening/brightening beyond MC noise at high SPP.

### V2 — T16: DDGI emitter radiometry (chroma + area)  [done-needs-render]
- **What changed:** DDGI light collection now maps core emitters directly (`coreEmittersToDDGILights.ts`) instead of round-tripping through THREE — recovers per-emitter **chroma** (a red rect-area light was bleeding **white**) and uses the correct emissive area `4·|u×v|` (was `width·height`). Wired on both the init path and the incremental `updateEmitter` path.
- **Why GPU:** DDGI runs in walkaround-hybrid (`hybridCanRun: false` in WSL).
- **Test scene / plan:** a colour-bleed scene — a saturated (e.g. red) rect-area light near a white wall, hybrid backend, DDGI on. Capture `bbd32c8` vs `main`. Add a sheared-axis rect-area variant (non-orthogonal u/v) to exercise the area-metric fix.
- **Expected delta:** indirect/probe lighting near the coloured light now carries the **light's colour** (red bleed, not white); sheared-rect emitters change brightness by the area ratio (`|u×v|` vs `|u|·|v|`).
- **Acceptance:** the colour bleed visibly matches the emitter colour; brightness change matches the area-ratio math; no probe artifacts.

### V3 — T5: stained-glass shade module (opt-in, default OFF)  [done-needs-render]
- **What changed:** `lo_sun_caustic` + `lo_sky_aperture` extracted from `shade.wgsl` into an opt-in `stainedGlassShade.wgsl` module gated by a `stainedGlassFlags` UBO field; default OFF. `HybridEngineOptions.stainedGlass?: { sunCaustic?, skyAperture? }`.
- **Why GPU:** walkaround-hybrid shade pass.
- **Test scene / plan:** (a) a Cornell-stained-glass scene with `stainedGlass: { sunCaustic: true, skyAperture: true }` — capture `main` vs `bbd32c8`; (b) a generic (non-stained-glass) scene with the flags OFF (default).
- **Expected delta:** (a) with flags ON, output must be **bit-for-bit / visually identical** to pre-sweep (the flag-ON WGSL is already proven byte-identical to the old inline shader — this is a confirmation, the least-risky item); (b) with flags OFF, the sun-caustic + sky-aperture terms must drop to **exactly zero** vs pre-sweep (the intended fix: generic scenes no longer get stained-glass physics).
- **Acceptance:** (a) PSNR ≈ ∞ / pixel-identical; (b) the two terms are gone, no other change.

### V4 — directional → DDGI sun  [implemented this session — needs-render]
- **What changed:** a scene `directional` emitter now drives the DDGI sun with its **real direction** (was hardcoded `(0,-1,0)`) and is single-counted (resolved the setSunIntensityMultiplier-vs-fixture double-count); `directional` re-declared in walkaround's `supportedEmitterKinds` + ledger.
- **Why GPU:** DDGI sun lighting, walkaround-hybrid.
- **Test scene / plan:** a scene with a non-downward `directional` emitter (e.g. dir `(1,-1,0)` normalized), hybrid backend. Capture `main` vs `bbd32c8`.
- **Expected delta:** DDGI probe/indirect sun lighting now comes from the **correct direction** (previously lit as if from straight down); intensity unchanged (single-counted, not doubled).
- **Acceptance:** sun-lit surfaces face the emitter's actual direction; no doubling/over-brightening; a `(0,-1,0)` directional is unchanged vs pre-sweep.

### V5 — DDGI IRR border-texel fill  [implemented this session — needs-render]
- **What changed:** the irradiance border pass now fills all border texels of each 10×10 probe-atlas cell (previously left the 4 bottom-edge texels `lx∈{6,7,8,9}, ly=9` unfilled).
- **Why GPU:** DDGI probe atlas, walkaround-hybrid.
- **Test scene / plan:** any DDGI scene; inspect probe-atlas edges / look for probe-seam artifacts at cell boundaries. Capture `main` vs `bbd32c8`.
- **Expected delta:** probe octahedral border bilinear-filtering is now correct at the previously-unfilled edge → subtle removal of edge-seam artifacts in indirect lighting.
- **Acceptance:** no probe-seam artifacts; indirect lighting at probe-cell boundaries is continuous.

### V6 — pt-webgl instanced-mesh support  [GPU-VALIDATED PASS 2026-05-29]
- **GPU-VALIDATION RESULT (2026-05-29, wsl-gpu WAVE10-RESULTS.md, headless-Chromium WebGL2 / software GL — valid for geometry/transform correctness):** **PASS.** An `InstancedMesh` of N=4 boxes at distinct world transforms (incl. a scaled instance) renders all 4 at their correct per-instance world transforms — float-HDR A/B vs the N-separate-`THREE.Mesh` reference is **38.45 dB** (mean Δ≈0.006/ch = MC noise), the 8×8 instance-placement signature is **structurally identical** (4 distinct screen regions, NOT origin-collapsed — the exact failure mode `expandInstancedMeshes.ts` prevents), tonemapped PNGs match within 1 LSB. Confirms `@vitrum/pt-webgl` boots + renders headless under software GL. The `webgl2-capture/scenes/` driver built for this is now a reusable WebGL2 validation path.
- **What changed:** pt-webgl now renders `instanced-mesh` primitives. The shared `vitrumSceneToThree` still builds ONE `THREE.InstancedMesh` per primitive (unchanged — walkaround-hybrid's TLAS path needs the single InstancedMesh + per-instance transforms). pt-webgl's `setScene` now calls `expandInstancedMeshesInScene(threeScene)` (new file `packages/pt-webgl/src/expandInstancedMeshes.ts`) AFTER `vitrumSceneToThree` and BEFORE the fork's `WebGLPathTracer.setScene` → BVH/geometry generator. Each `THREE.InstancedMesh` is replaced by N standalone `THREE.Mesh` objects, each baking `parent.matrixWorld · instanceMatrix[i]` into its `matrixWorld` (with `matrixAutoUpdate = false`), sharing the InstancedMesh's single geometry + material. This is required because the absorbed three-gpu-pathtracer fork's `convertToStaticGeometry.js` bakes only `mesh.matrixWorld` (line 283) and never reads `instanceMatrix`, so an unexpanded InstancedMesh would collapse all N instances to one copy at the local origin. `instanced-mesh` re-declared in pt-webgl's `supportedPrimitiveKinds` (capability getter) + the core `promiseLedger.ts` pt-webgl row, so `partitionSceneBySupport` now KEEPS instanced-mesh primitives (flows them through to the converter) instead of warn-skipping them.
- **Code/test status (verified, non-GPU):** `pt-webgl` + `core` `tsc --noEmit` clean; full-workspace typecheck clean; new `expandInstancedMeshes.test.ts` (7 tests) pins N→N expansion at correct per-instance world transforms (incl. scaled + composed-parent-transform cases), shared geometry/material, no-op on non-instanced scenes, and the capability/ledger re-declaration; `capabilitiesPartition.test.ts` updated to assert instanced-mesh now flows through (not warn-skipped); full `npm test` green (all workspaces).
- **Why GPU:** the actual render is the pt-webgl path tracer; WSL SwiftShader can't produce a trustworthy capture.
- **Test scene / plan:** a scene with an `instanced-mesh` of N≥3 instances at distinct transforms, pt-webgl backend. Capture `main`; compare against a hand-authored equivalent of N separate meshes (the reference). Add a scaled-instance variant to exercise the non-translation matrix bake.
- **Expected delta:** all N instances render at their correct per-instance world transforms (was: one copy at the origin, or — before this change — warn-skipped entirely). The render must match the N-separate-meshes reference.
- **Acceptance:** instance count + transforms match the N-separate-meshes reference; no missing / origin-collapsed instances; scaled instances render at the right size.

### V7 — Möller-Trumbore unify (pt-webgpu)  [code landed — OUTSTANDING: needs GPU render A/B]
- **What changed:** pt-webgpu's triangle-intersection is now single-sourced on the canonical shared-bvh Möller-Trumbore math. The divergent local copy in `packages/pt-webgpu/src/wgsl/common.wgsl.ts` (`h = cross(dir, e2); det = dot(e1, h)`, strict `u<0||u>1` / `v<0||u+v>1` barycentric tests) is gone; both pt-webgpu's f32-returning `intersectTriangle` wrapper AND the canonical struct-returning `intersectTriangle` in `BVH_INTERSECT_WGSL` now delegate to the new shared `mollerTrumboreCore` fragment (`MOLLER_TRUMBORE_WGSL` in `packages/shared-bvh/src/wgsl/bvhIntersect.wgsl.ts`), which uses `n = cross(e1,e2); det = -dot(dir,n)` with **triEps-tolerant signed barycentric tests** (`u/v/w < -triEps`, triEps = `params.triIntersectEpsilon` = 1e-5). The `__tests__/cpuTracer.ts` oracle was updated to mirror the same canonical core. This is a deliberate **edge-case intersection numerics** change (NOT behaviour-preserving at triangle edges): the hit distance `t` for interior hits is algebraically unchanged, but hits grazing a shared edge by less than triEps are now **accepted** instead of rejected.
- **Code/test status (verified, non-GPU):** workspace `tsc --noEmit` clean; shared-bvh (74) + pt-webgpu (138 + mcConvergence oracle) vitest green; full `npm test` green (608 pt-webgpu pkg tests etc.). Composed pt-webgpu kernel verified to contain exactly one `mollerTrumboreCore` / `TriHit` / `intersectTriangle` / `safeInvDir` (no WGSL duplicate definition) and zero `IntersectionResult` (canonical struct stays out of pt-webgpu, avoiding the struct collision).
- **Why GPU:** full-tier pt-webgpu path tracer — the edge/tolerance delta only manifests at runtime on shared triangle edges / t-junctions.
- **Test scene / plan:** a mesh-heavy scene with grazing/edge-on triangles (where barycentric tolerance matters), pt-webgpu full tier. Capture **`bbd32c8` (pre-change baseline, has the old divergent formula)** vs the post-change build at high SPP.
- **Expected delta:** near-identical except at triangle edges / shared edges (t-junctions), where the canonical formula's tolerance differs — should be **equal or fewer** edge-crack artifacts, not more.
- **Acceptance:** no new edge cracks / light leaks at triangle boundaries; high-SPP A/B within MC noise except at intended edge cases.

### V8 — T9-stepC per-pass WGSL narrowing  [implemented this session — needs shader-compile validation]
- **What changed:** each walkaround pass's `requires` was narrowed from the full `common` aggregate to its minimal WGSL module set.
- **Why GPU:** a wrongly-narrowed pass (missing a needed declaration) only fails at `createShaderModule` — typecheck and the byte-composition test won't catch a genuinely-missing symbol that the pass references.
- **Test plan:** run the hybrid pipeline on a real GPU and confirm **every pass's shader compiles** (no `createShaderModule` errors) — i.e. the existing GPU smoke / hybrid init path. If any pass fails to compile, its `requires` is missing a module; widen it.
- **Acceptance:** full hybrid pipeline initializes + renders a frame on hardware WebGPU with zero shader-compile errors across all passes.

### V9 — real Jakob-Hanika RGB→spectrum upsampling (pt-webgl spectral)  [GPU INSPECT 2026-05-29 — "now CONSUMED" CONFIRMED; clean PASS needs real-GPU high-SPP]
- **GPU-VALIDATION RESULT (2026-05-29, wsl-gpu WAVE10-RESULTS.md, headless-Chromium WebGL2 / software GL):** the **"now CONSUMED" claim is CONFIRMED** — a rough translucent SSS medium (`transmission` + scattering) drives the fork's `sssSample`→`mediumAlbedoHero(u_sssAlbedo)`; gate ON (`spectralAlbedo=[0.5,0.1,0.7]`) gives albedo-matched purple chroma (green = min channel in **64/64** grid cells; tonemapped [174,67,155]) vs achromatic gray OFF (**0/64**; [214,212,210]), with **no `u_jakobCoeffs missing` warning** → the coefficients are uploaded AND consumed (the prior uploaded-but-dead `evalSpectrum` bug is genuinely fixed). **INSPECT, not clean PASS:** the out-of-gamut (CIE→sRGB) negative-green of the spectral-hero estimator doesn't converge at reachable software-GL SPP (negPixFrac 0.59@128spp → 0.72@320spp; no NaN/Inf, OFF has 0 negatives, display-clippable) — the "no new artifacts at convergence" criterion is the residual **real-GPU high-SPP** need.
- **What changed:** `shared-samplers/src/jakobHanika.ts` replaced the placeholder with the genuine Gauss-Newton sigmoid-coefficient solve (round-trip CPU-pinned to ~1e-7 interior / <5e-3 saturated); pt-webgl's `forkUniformBridge` uploads real `u_jakobCoeffs` from `vitrum.ptWebgl.spectralAlbedo` when spectral rendering is on. Capability tag `spectral-jakob-hanika-placeholder` → `spectral-jakob-hanika`. **UPDATE (this session): the fork integrator now actually CONSUMES the coefficients** — before, `evalSpectrum(u_jakobCoeffs,λ)` had ZERO call sites (uploaded-but-dead). A gated `mediumAlbedoHero()` now drives the volume single-scatter (`u_scatterAlbedo`) + SSS single-scatter (`u_sssAlbedo`) albedo through `evalSpectrumAtHero` when spectral upsampling is active (`uSpectralRendering && u_jakobCoeffs != 0`); off-gate it returns the legacy `heroScalarFromRgb` smoothstep, bit-identical. So the A/B below is now actually meaningful (previously the coeffs had no render effect).
- **Why GPU:** the fork's `evalSpectrum(u_jakobCoeffs, λ)` reflectance weighting only manifests in a live spectral render (hero-wavelength accumulation), not exercisable under SwiftShader.
- **Test scene:** a dispersive/transmissive glass scene, `spectralRendering: true` + a saturated `spectralAlbedo` (e.g. `[0.5,0.1,0.7]`), fixed seed, pt-webgl. A/B `bbd32c8` vs `main`.
- **Expected delta:** the medium picks up the correct chromatic reflectance per hero wavelength (vs the prior flat S≡½); no fireflies; converged image consistent with the CPU round-trip (<5e-3).
- **Acceptance:** chroma shift in the math-predicted direction; no new artifacts.
- **NOTE (capture-harness gap, from `plan/fidelity-promotion-playbook.md`):** pt-webgl is WebGL2 — the lavapipe WebGPU device can't validate it; this row needs a real-browser GL capture. And the lavapipe *PNG render-capture* adapter for WebGPU rows is not yet wired (only probe + compute-smoke exist) — building it is a prerequisite for hands-free WSL fidelity captures.

### V10 — BMFR denoiser (Koskela 2019)  [implemented this session — OUTSTANDING: needs real-GPU denoise-quality A/B]
- **What changed:** the previously-unimplemented `denoiser: 'bmfr'` mode (the `@vitrum/core` `EngineOptions.denoiser` union always advertised it, but `walkaround-hybrid` THREW on it) is now a **real BMFR denoiser** — Koskela, Immonen, Mäkitalo, Foi, Viitanen, Jääskeläinen, Kultala, Takala, "Blockwise Multi-Order Feature Regression for Real-Time Path-Tracing Reconstruction," ACM TOG 38(5), 2019. New `@vitrum/shared-denoisers` modules: `wgsl/bmfr.wgsl.ts` (per-32×32-block compute kernel), `bmfrRegression.ts` (CPU regression core / oracle), `bmfrBindings.ts` (UBO), `bmfrConstants.ts`, `bmfrWebGPU.ts` (one-shot host pipeline). New `walkaround-hybrid` registry entry `pipeline/denoisers/bmfr.ts`; the HybridEngine `'bmfr'` throw is removed. **Design:** 32×32 block, feature set `[1, p.xyz, n.xyz, p².xyz]` (10 cols), Householder-QR solve of the normal equations per color channel, reconstruct `color = T·α`, then temporal EMA (α=0.2, reset on camera motion). The realtime entry uses a **screen-space position proxy** (`(pixelX, pixelY, depth)` from `gNormalDepth.w`) so no dedicated world-position G-buffer is needed; the one-shot host path uses a real world-position buffer.
- **Code/test status (verified, non-GPU + lavapipe):** shared-denoisers + walkaround-hybrid `tsc --noEmit` clean; new `bmfrRegression.test.ts` (7 CPU tests: Householder-QR solve, exact-linear recovery, noisy-constant → mean denoise, gradient preservation, rank-deficient stability); `denoiserRegistry.test.ts` + `passLayout.test.ts` updated for the 7th denoiser; full shared-denoisers (146) + walkaround-hybrid (601) vitest green. **Lavapipe compute smoke (Deno + LLVMpipe, headless WSL): the real `BMFR_WGSL` kernel compiles + dispatches and denoises a noisy 1-spp constant block over a smooth surface with 99.66% variance reduction (outVar 0.00018 vs inputVar 0.05302), output finite, block mean 0.598 vs true 0.600.** This proves kernel correctness (incl. a real UBO-size-mismatch bug caught + fixed during validation) but NOT real-pipeline visual quality — lavapipe is a CPU rasteriser.
- **Why GPU:** BMFR runs inside walkaround-hybrid (`hybridCanRun: false` under WSL SwiftShader); the denoise QUALITY (firefly suppression, edge/gradient preservation, temporal ghosting under motion, block-seam visibility) only manifests on a live hybrid render at real SPP.
- **Test scene / plan:** a noisy hybrid scene (e.g. Cornell with low SPP / a moving camera), hybrid backend, `denoiser: 'bmfr'`. A/B against `denoiser: 'svgf-real'` and `'atrous-variance'` on the SAME scene/seed. Also capture a static-camera converged frame and a camera-in-motion sequence (to check ghosting + the motion-reset path).
- **Expected delta:** vs the raw (`'none'`) signal — substantial noise reduction with **preserved geometric gradients** (BMFR fits the spatial features, so it is gradient-preserving, not a box blur) and **crisp material edges** (block-local fit + screen-space position discontinuities at depth edges). vs `svgf-real` — comparable-or-better spatial smoothness on smooth surfaces; watch for block-boundary seams (32×32 grid) and temporal ghosting on motion (mitigated by the camera-move history reset).
- **Acceptance:** (a) clear noise reduction vs `'none'` with no new fireflies; (b) no hard 32×32 block seams in the final composite; (c) gradients + material edges preserved (not over-blurred); (d) under camera motion, no persistent ghost trails (history resets on `isMoving`); (e) a static converged frame is artifact-free. If block seams ARE visible, enable the half-block-offset overlap (blockStride < blockSize, the `blockStride` UBO field) and/or widen the temporal window.

### V11 — GPU-skin bindMatrix fallback  [implemented this session — needs functional GPU check]
- **What changed:** the GPU skinning path (`GpuSkinningSubsystem`) now falls back to the CPU `solveSkin` for a skinned mesh with a **non-identity `bindMatrix`** and no morphs (it was taking the GPU path, whose kernel uses `combineSkinMatrices(bones, boneInverses)` WITHOUT the bind wrapping the CPU solver applies → wrong positions + normals). Identity/absent bind still uses the GPU fast path.
- **Why GPU:** skinning runs in walkaround GPU compute.
- **Test scene / plan:** an animated `SkinnedMesh` authored with a non-identity `bindMatrix` (no morphs), hybrid backend; compare against the CPU `solveSkin` result.
- **Expected delta:** the mesh deforms correctly (matches CPU reference) — previously distorted under non-identity bind.
- **Acceptance:** non-identity-bind skinned mesh matches the CPU reference; an identity-bind mesh is unchanged (still GPU path).

### V12 — duplicate DDGI sun dedup  [implemented this session — needs-render]
- **What changed:** when a scene `directional` (→ DDGI sun) AND a host `opts.lights` `sun` are both present, only ONE sun now reaches DDGI (scene directional wins, host sun dropped + one-time warn). Applied on BOTH the init path (`HybridEngineLifecycle`) and the incremental re-sync path (`HybridEngine._syncDdgiLightsFromThreeRoot`).
- **Why GPU:** DDGI direct lighting, walkaround.
- **Test scene / plan:** a scene with a `directional` emitter AND a host `opts.lights` sun, hybrid backend. A/B `main` vs `bbd32c8`.
- **Expected delta:** sun lighting at the correct SINGLE intensity (was ~2× from double-injection).
- **Acceptance:** brightness matches a single sun; no doubling; scene-directional precedence honored.

### V13 — RC merged-instance refit + filter parity  [implemented this session — needs-render]
- **What changed:** RC merged-mode moving-instance refit is now wired into `propagateBvhToGiSubsystems` (`refitMergedInstance` — in-place positions + node-AABB refit, no teardown). Also `RCSubsystem.setScene` now builds with a permissive all-meshes filter (was PBR-only `DEFAULT_FILTER`) so RC's merged vertex layout matches ReSTIR's by construction (non-PBR meshes were diverging — caught the moment the refit was wired).
- **Why GPU:** RC merged-mode GI, walkaround (`rcEnabled`).
- **Test scene / plan:** RC merged mode (`rcEnabled`, merged BVH) with a moving instance + at least one non-PBR mesh (e.g. MeshBasic), hybrid backend. Move the instance; observe RC GI.
- **Expected delta:** after the instance moves, RC GI tracks the new geometry without a full rebuild; non-PBR meshes are included in RC's BVH (matching ReSTIR).
- **Acceptance:** GI stays correct after instance moves; no RC/ReSTIR geometry divergence; no teardown stutter.

### V14 — resolutionFactor composite upscale (C1)  [implemented this session — needs-render]
- **What changed:** the composite blit is now resolution-independent — the vertex shader emits a `[0,1]²` screen UV and the frag indexes `denoisedTex` via `uv × textureDimensions` (nearest). Previously it indexed the internal-res texture with raw swap-chain `fragCoord`, so `resolutionFactor < 1` rendered into the top-left and left the rest of the canvas BLACK.
- **Why GPU:** composite pass, walkaround.
- **Test scene / plan:** any hybrid scene at `resolutionFactor` 0.5 and 0.75 on a real GPU.
- **Expected delta:** the ENTIRE canvas is covered (no black border/region); nearest-neighbour upscale; `factor == 1` is bit-identical to before.
- **Acceptance:** corner-pixel telemetry confirms full coverage; upscale quality acceptable vs a `factor = 1.0` reference. Worked pixel: 1920×1080@0.5 → bottom-right maps to internal (959,539).

### V15 — DDGI cadence load-bearing + 2→32 preset spread (H1)  [implemented this session — needs-render]
- **What changed:** `ddgiUpdateDivisor` now drives the REAL probe-update round-robin stride (was a dead UBO field; real cadence was a hardcoded stride 8). Preset spread is now ultra=2 / high=4 / medium=8 / low=32. **IMPORTANT: omitting `qualityTier` applies the ultra preset, so the no-preset DEFAULT cadence is now stride 2 — 4× the old hardcoded stride-8.** Deliberate fidelity-forward change.
- **Why GPU:** DDGI probe update, walkaround.
- **Test scene / plan:** a step lighting change (`updateLighting` toggling the sun); capture DDGI convergence over N frames at `ddgiUpdateDivisor` = 2, 8, 32. Telemetry: `window.__DDGI__.activeCount` per frame + atlas-mean.
- **Expected delta:** GI converges proportionally faster at lower divisors (divisor 2 ≈ 4× the per-frame active-probe count of divisor 8 ≈ 8× of divisor 32); the new default (stride 2) shows visibly faster GI response than the old stride-8.
- **Acceptance:** GI response rate scales inversely with divisor; per-frame `activeCount` matches the stride; no probe artifacts/instability at stride 2; the default 8→2 change is faster without temporal-blend breakdown.

### V16 — RC ⊕ ReSTIR-GI per-pixel confidence MIS  [implemented this session — needs-render]
- **What changed:** the indirect RC/ReSTIR-GI blend (`shade.wgsl`) is now a per-pixel confidence-ratio balance heuristic — `c_restir = clamp(Meff/restirGiMClamp)`, `c_rc = rcWeight·(1−m)`, `w_rc = c_rc/(c_rc+c_restir)` — replacing the old fixed host scalar. Unbiased (convex blend of two estimators of the same integral).
- **Why GPU:** shade pass, walkaround (`rcEnabled`).
- **Test scene / plan:** `rcEnabled: true`, moderate `rcWeight` (≈0.5), a disocclusion-heavy scene; capture the first ~10 frames after a fast camera pan. A/B vs the pre-change fixed-scalar build.
- **Expected delta:** less indirect noise in freshly-disoccluded (low-M) regions (RC fills in); converged (high-M) regions match the old result closely (RC fades out).
- **Acceptance:** disoccluded regions visibly cleaner than the fixed-scalar blend; converged regions ≈ unchanged; `rcEnabled: false` is bit-identical to a pre-change build.

### V17 — PPG guided sampling (gi-ris consumes the learned dTree)  [implemented this session — needs-render]
- **What changed:** PPG was train-only (learned a dTree but gi-ris sampled plain cosine; the guide output had no reader). gi-ris now draws GI candidate directions from the learned dTree with a defensive, UNBIASED MIS mixture: with prob α from the dTree else cosine, and the RIS source pdf is `α·p_guide + (1−α)·p_cos` evaluated for the chosen direction. New `ppg/ppgPdf.wgsl.ts` (`ppgEvalPdf` mirrors CPU `dTreePdf`); the 3 PPG buffers packed into the hybridLayers bind group (group 3, bindings 6/7/8, respecting maxBindGroups=4); `ppgEnabled`/`ppgMixAlpha` UBO gate. ppg-OFF (α=0) is bit-identical to the pre-PPG cosine kernel (no extra RNG draw; literal `w = luminance(Lo)`).
- **Why GPU:** walkaround GI; PPG is opt-in (`ppgEnabled`, forbidden on lite tier).
- **Test scene / plan:** a guiding-friendly scene (cornell-box / indirect-dominated, small-aperture lighting), `ppgEnabled` on vs off, fixed seed. Capture per-pixel Welford indirect-channel variance (NOT screenshots) at matched frame counts.
- **Expected delta:** PPG-on shows LOWER indirect variance / faster convergence; the converged indirect-channel MEAN must MATCH PPG-off (unbiasedness — same fixed point). PPG-off telemetry must be bit-identical to a pre-change build.
- **Acceptance:** equal converged mean (unbiased) + measurable variance reduction with PPG on; PPG-off identical to pre-change.

### V18 — Full Veach §10.3 BDPT connection MIS (pt-webgpu + pt-webgl fork)  [GPU-VALIDATED: BDPT-ON dispatches+renders on both backends after TWO conformance fixes]
- **GPU-VALIDATION RESULT (2026-05-29, wsl-gpu WAVE11/WAVE12-RESULTS.md):** validating BDPT-ON surfaced **two latent vitrum conformance bugs** that meant BDPT-ON had NEVER run on any real WebGPU (vitrum's GPU test never dispatched the BDPT pass): (1) `bdptLightPath` was a `texture_storage_2d<rgba32float, read_write>` — rgba32float read-write storage textures are not core WebGPU (gpuweb #4651), rejected at bind-group creation on wgpu-native + Dawn → **fixed `fdb0b95`** (migrated to a `var<storage, read_write> array<vec4f>` buffer); (2) the path-trace + bdptLightSubpath pipelines used `layout:'auto'` (pipeline-exclusive), so the shared bind groups were rejected ("Exclusive pipelines don't match") → **fixed `6bb11f7`** (one explicit shared `GPUPipelineLayout`). At HEAD `cc86c39` (real committed code, no simulation): **PASS both backends** — bind-group creation succeeds, the BDPT subpath pass dispatches with zero device errors, renders non-black + 0 NaN/Inf, fireflies ≤ unidirectional (103/127 vs 144 — the variance-reduction direction). The deeper §10.3-vs-2-strategy variance A/B on a small/hidden-emitter caustic scene (where the extra strategies carry more weight) remains a finer follow-up; the storage-texture→buffer migration also dropped the per-stage storage-texture count 6→5 (the eye-stack buffer note below is superseded — light-path is now a buffer too).
- **What changed:** the GPU connection-MIS weight was upgraded from the 2-strategy approximation to the full Veach §10.3 sweep over all flipped transfer indices. The WGSL/GLSL port mirrors `@vitrum/shared-samplers`' `buildBDPTStrategyPDFs_full` / `bdptConnectionMIS_full` oracle 1:1 (PBRT-v4 `MISWeight` recurrence; destination-cosine-only ConvertDensity). A new TS reimplementation `bdpt/bdptConnectionMisFull.ts` is asserted equal to the oracle to ≤1e-12 (pdf vector + per-strategy weight + partition-of-unity + pRef scale-invariance). The hardcoded `eyePdfFwd=1.0` is replaced by real `brdfDirectionalPdf` scatter densities threaded through a per-pixel eye-vertex scratch stack; the baked `*G` in the light-subpath shader is dropped (both lanes now store bare SA pdfs, Jacobian applied once at connection). A latent primary-hit double-count in pt-webgpu is fixed (now `bounce>0`, mirroring the fork's `!state.firstRay`).
- **Why GPU:** path-trace kernel (pt-webgpu) + `PhysicalPathTracingMaterial` (pt-webgl fork). Full-tier-only, opt-in via `params.bdptEnabled` / `FEATURE_BDPT`; BDPT-off shading math is bit-identical.
- **Three things needing real hardware:**
  1. **WGSL/GLSL shader-compile** — the §10.3 sweep is structurally reviewed but unverified by a real shader compiler here. Run the opt-in `wgslSmoke.gpu.test.ts` (SwiftShader/Dawn) + the fork's smoke on a GPU host.
  2. **Variance A/B** — render a caustic/glossy-bounce scene (BDPT's regime) with full §10.3 vs the old 2-strategy build at matched SPP. Telemetry: per-pixel Welford on the converged image.
  3. **Storage-buffer layout bump** — the eye-stack adds an unconditional `@group(2) @binding(6)` storage buffer, raising the kernel's per-stage storage-buffer count **23→24** *even when BDPT is off* (single pipeline, runtime-gated). Validated full-tier hardware (RTX 4090 / Chrome-class, where 23 already worked) reports buffer limits far above 24 so this is expected-immaterial — but confirm pipeline creation succeeds, and note a theoretical regression risk for any full-tier adapter reporting `maxStorageBuffersPerShaderStage` in the narrow [10,23] band (a boundary that predates BDPT).
- **Memory ceiling (already enforced, confirm the fallback fires):** the eye-stack is W·H·depth·32 B. 1080p×depth-8 = **506 MiB** exceeds the 384 MiB `GpuResources.BDPT_EYE_STACK_MAX_BYTES` ceiling, so BDPT is **skipped that frame with a `console.warn` and falls back to unidirectional** (a 32-byte placeholder keeps the layout valid). 1080p works up to ~depth-6; full-depth 1080p BDPT needs the deferred cap/tile decision.
- **Expected delta:** equal converged MEAN vs the 2-strategy build (both unbiased — same integral) with LOWER variance on caustic/specular-bounce paths where the extra strategies carry weight; BDPT-off bit-identical to a pre-change build.
- **Acceptance:** shaders compile on a real device; converged means match (unbiasedness) with measurable variance reduction on the BDPT-favourable scene; the 384 MiB fallback fires loudly (never silently) at 1080p×depth-8; BDPT-off identical to pre-change.

### V19 — GRIS reconnection-shift reuse on ReSTIR-GI spatial + temporal (walkaround-hybrid)  [GPU-VALIDATED: gate-OFF restored + GRIS-ON divergence found & fixed, converges on both backends]
- **GPU-VALIDATION RESULT — GRIS-ON divergence found & fixed (2026-05-29, WAVE11→WAVE12):** the GRIS-ON path WAS validated after all (the naga `ptr<storage>` gap below was solved by a harness-side faithful alpha-rename of the prefixed scene globals — not a Dawn dependency). Validation then found GRIS-ON was **BIASED + DIVERGENT** — the raw-HDR indirect mean climbed unbounded (0.14→0.37, var 70×) vs a radius-0 per-pixel ground truth (the earlier "+13%" was an LDR-clamp/tonemap artifact masking the runaway). **Root cause = two over-energization bugs, fixed at `cc86c39`:** (1) the reused-reservoir weight divided by `pSrc` (`pdfReconBsdf`) — but GRIS reservoir reuse weights by `m·p̂·W·J` with NO source-pdf division (the producer already baked `pSrc` into `W`); the extra `/pSrc` (≈π…∞) drove the temporal feedback gain >1. (2) the spatial pass's streaming-pairwise MIS didn't partition unity (Σmᵢ≈2.34 for K≈4) → replaced with the exact two-pass generalized balance heuristic. **GPU-CONFIRMED converged (WAVE12, both backends):** grison medianLitLum STABLE (~0.036, climb gone), temporal-only ≈ ground truth (0.0431 vs 0.0432), variance ≤ GRIS-OFF (was 70× higher). Residual ~16% full-spatial darkness (comparable to GRIS-OFF — a bounded reconnection-visibility/double-filter artifact) is a milder follow-up, NOT the divergence. The #1/#2/#3 "needs conformant Dawn" items below are SUPERSEDED — done on the wgpu-native harness via the shim.
- **GPU-VALIDATION UPDATE (2026-05-29):** the original `f8df9a4` "gate-OFF bit-identical" claim was FALSE on real hardware — it added the `@group(1)` scene group UNCONDITIONALLY to the spatial+temporal GI pipelines, regressing the DEFAULT render to a fully black frame on both dzn (RTX 4090) and the lavapipe oracle. **Fixed at `b2ed2e3`** (the `@group(1)` + GRIS branch are now compile-time-conditional on `restirPtReuse`; default OFF composes the byte-for-byte pre-GRIS single-`@group(0)` pipeline). **GPU-CONFIRMED restored:** default Cornell render now nz≈65483/65536, mean RGB dzn (69.0,85.6,107.4) / lavapipe (70.7,87.8,110.2) — matches the pre-GRIS reference (68.7,85.3,107.0). **GRIS-ON could NOT be validated on the wgpu-native/lavapipe harness** — its bundled naga rejects the `ptr<storage>` function params the reconnection-visibility `traceSceneAny` uses (a documented harness/naga gap; legal WGSL expected to compile on real Dawn). So the GRIS-ON items below (#1 compile, #2 converged-mean, #3 variance) require a **conformant-Dawn path (real-browser WebGPU)**, not the wsl-gpu wgpu-native capture worker.
- **What changed:** the ReSTIR-GI spatial (`spatialGi.wgsl`) and temporal (`temporalGi.wgsl`) reuse passes gained an UNBIASED GRIS reconnection-shift path (Lin, Kettunen, Bitterli, Pantaleoni, Jakob, Nowrouzezahrai — "Generalized Resampled Importance Sampling: Foundations of ReSTIR", SIGGRAPH 2022, §5 + Eq. 12), opt-in via `HybridEngineOptions.restirPtReuse` (UBO gate `restirPtReuse` at offset 412, the former `_regirPad`). When ON, each accepted neighbour's reconnection sample is re-rooted onto this pixel's primary vertex via the reconnection shift T, re-weighted by the change-of-variables Jacobian `G(shifted)/G_base` (destination-cosine half-G, base half-G recovered from the Phase-0 cache `cosReconOut/distRecon²`), gated by a **reconnection-visibility ray** through the scene BVH/TLAS (the spatial + temporal pipelines now bind the shared scene group at `@group(1)`), and combined with the canonical sample under a **pairwise generalized-balance MIS** (defensive `1/(K+1)` canonical weight + per-neighbour pairwise weights). The GRIS finalise uses `W = w_sum / p̂` (the MIS weights replace the legacy `1/M` averaging). The WGSL geometry-term + Jacobian + pairwise-MIS arithmetic mirrors the CPU oracle `@vitrum/shared-samplers/reconnectionShift.ts` 1:1 via the TS mirror `pipeline/grisReuseMis.ts`, asserted equal to ≤1e-12 (oracle parity) + partition-of-unity (Σ mᵢ(y) = 1 for a fixed sample). The gate-OFF path is the verbatim Sprint-17 clamped-Jacobian reuse — **OFF is BIT-IDENTICAL** (unit-pinned: every UBO byte unchanged when the gate is 0; only u32[103] flips when ON).
- **Why GPU:** the reuse passes are WebGPU compute kernels; the unbiasedness claim and the variance/disocclusion-bias improvement can only be measured on a real device with a converged accumulation. **Do NOT fabricate renders** — the static gate (`wgslCompose.test.ts` ident-resolution) only proves the WGSL composes with all identifiers declared, not that it produces correct radiance.
- **Three things needing real hardware:**
  1. **WGSL shader-compile** — the spatial + temporal GRIS branch (reconnection-visibility `traceSceneAny`, `grisShiftJacobian`, pairwise-MIS denominators) is structurally reviewed + statically ident-resolved but unverified by a real shader compiler. Compile both pipelines on a GPU host (SwiftShader/Dawn or real adapter) with `restirPtReuse` on; the spatial/temporal pipelines now declare a 2nd bind group (scene BVH/TLAS) — confirm `createComputePipelineAsync` succeeds.
  2. **Converged-mean A/B (unbiasedness)** — render a walkaround GI scene (Cornell-box or a room with strong indirect color-bleed + a disocclusion edge, e.g. a moving occluder or a fresh camera pan) to convergence with `restirPtReuse` ON vs OFF at matched accumulation. **Telemetry:** per-pixel Welford mean on the converged indirect channel (`hdrIndirect`), NOT eyeballed screenshots (temporal pipeline → screenshots capture transient states). The converged MEANS must be EQUAL within noise (both unbiased estimators of the same GI integral) — a mean shift would indicate the Jacobian / MIS / visibility is wrong.
  3. **Variance / disocclusion-bias A/B** — at matched low accumulation (or right after a camera move), ON must show LOWER variance and LESS disocclusion bias than OFF (the legacy reuse leaks neighbour radiance through occluders + over-/under-weights without the proper Jacobian; the visibility ray + exact Jacobian fix both). Telemetry: per-pixel variance map + the disocclusion-edge error vs the converged reference.
- **Storage-buffer budget (confirm, do not assume):** the spatial/temporal GI pipelines now bind the shared scene group, so they reference at most 2 reservoir buffers (group 0) + the scene BVH subset they traverse — **fewer scene buffers than the already-shipping `shade`/`ris` pipelines** (which declare the full 11-entry scene group and work on the full tier). In merged-BVH mode the visibility ray touches only the 3 BVH buffers (nodes/index/position); TLAS slots are declared-but-untraversed (same discipline as shade). Confirm pipeline creation succeeds on the full tier; lite tier forbids the heavy GI features anyway but the GI reuse passes themselves stay within budget. **No budget overflow was introduced** — but verify on a real device.
- **Expected delta:** equal converged MEAN ON vs OFF (unbiased), LOWER variance + less disocclusion bias ON; gate-OFF bit-identical to a pre-change build (already unit-pinned on the UBO bytes).
- **Acceptance:** both pipelines compile with the gate on; converged means match (unbiasedness) with measurable variance / disocclusion-bias reduction; gate-OFF identical to pre-change (byte-level UBO assertion already green).

---

### V20 — Neural Radiance Caching (Müller et al. 2021) — LIVE query + train passes WIRED, compile-time gated (walkaround-hybrid)  [query+train passes landed this session — gate-OFF GPU-CONFIRMED + NRC-ON compiles/renders on dzn; OUTSTANDING: biased-cache quality/perf A/B + a host-side capability gate]
- **GPU-VALIDATION UPDATE (2026-05-29):** gate-OFF GPU-CONFIRMED unregressed — the default (`nrcEnabled` OFF) Cornell render at HEAD is byte-faithful to the pre-NRC reference on both backends (dzn nz=65483, mean (69.0,85.6,107.4); lavapipe (70.7,87.8,110.2)), so the compile-time NRC gate avoided the GRIS-class structural regression (the unit `nrcStructuralGate` guard was confirmed *sufficient* here). **NRC-ON compiles + renders non-black on dzn (RTX 4090):** nz=65491, mean (66.3,85.5,107.0) — the small R-channel dip vs OFF is the expected biased-cache substitution of some DDGI estimates, zero device errors; the inlined MLP forward avoids the `ptr<storage>`-param naga gap that blocked GRIS-ON. **NEW CAPABILITY REQUIREMENT surfaced:** NRC-ON needs `maxBindGroups ≥ 5` (the `@group(4)` NRC group; WebGPU default is 4) and `maxComputeWorkgroupStorageSize ≥ 24576` (the fused-MLP workgroup tiles; WebGPU floor is 16384). dzn exposes 8 / 32768 so it works, but **vitrum should capability-gate `nrcEnabled` on these two limits** (throw a clear error when the host's device lacks them, mirroring the lite-tier forbid — host-owns-lifecycle: the host must request the higher limits at device creation). Tracked as a follow-up. The remaining true-GPU item is the biased-cache QUALITY/PERF A/B below (perceptual closeness + faster convergence + self-training stability), which needs a conformant accumulation run.
- **WS3 UPDATE (2026-05-29, feature-completeness wave) — the hash-grid tables now LEARN:** the previously-frozen multiresolution feature tables are now trained per frame. (a) The fused MLP backward (`wgsl/fusedMlp.wgsl.ts`) emits the layer-0 input gradient `dL/dX` into a new `gradInputFx` atomic buffer (the input layer is linear → no relu' factor; row stride is the RAW encoded width `p.inW`, not the padded `W`). (b) A new standalone compute kernel `wgsl/nrcEncodeBackward.wgsl.ts` reads `dL/dX`, recomputes the 8 trilinear corners from the STORED query world position (records gained +3 f32s: `recordStride = inW + OUT_W + 3`), and scatters `weight·dL/dfeature` into per-level gradient tables via i32 fixed-point atomics (`gradTablesFx`, module-scope binding — WGSL forbids passing storage as a ptr arg, so the scatter is inlined). (c) `NrcSubsystem.trainFromRecords` runs the MLP `trainStep` THEN an encode-backward + separate **table Adam** (own moments, `tableLearningRate` default 0.1 per Instant-NGP §4 lr_embed ≈ 10× lr_mlp). **Lavapipe-validated this session:** `fusedMlpHarness.ts` PART-1b — GPU `dL/dX` == CPU analytic, maxAbsErr **9.4e-7** (f32; FD-confirmed clean — linear input, no ReLU kink); `nrcEncodeBackwardHarness.ts` — encode-backward scatter GPU == CPU `hashGridBackward` oracle (maxAbsErr **4.1e-6**) + **LIVENESS** (8 table-Adam steps move the tables by 0.8, proving they are no longer frozen). **Unit-pinned** (`nrcEncodeBackward.test.ts`, vitest node): analytic `dL/dX` == FD (≤1e-4, observed ~3e-13), chained encode→forward→backward table-grad == FD (≤1e-3), Adam-step formula, CPU liveness, + WGSL codegen pins (module-scope gradTablesFx, hash primes, 2^20 fixed-point). **Gate-OFF byte-identity preserved** — the record-stride change is data-only via `nrcCfg.recordStride` (the OFF path never instantiates `NrcSubsystem`); `nrcGateBitIdentity.test.ts` still green. **STILL NEEDS REAL HARDWARE:** the steady-state effect of the now-trainable encoding on cache quality/convergence (folded into the biased-cache QUALITY A/B below).
- **HONEST BIAS CAVEAT (read first):** NRC is a **BIASED cache** — the MLP prediction *replaces* the true path suffix at the spread-termination vertex; it is a *learned approximation*, NOT an unbiased Monte-Carlo estimate. Unlike V18 (BDPT MIS) / V19 (GRIS) which preserve the converged MEAN, NRC does **NOT** — its value is variance reduction / faster convergence / lower noise with **bounded bias**. The acceptance is *perceptual closeness to the no-NRC reference within tolerance, with measurably faster convergence / lower noise*, **NOT** "equal converged mean." Do not assert mean-equality for NRC. NRC is opt-in / full-tier only — **it cannot run on lite / SwiftShader** (hash-grid feature tables + fused-MLP weight/Adam buffers exceed the lite budget; the ctor throws on `tier:'lite' + nrcEnabled`).
- **What landed THIS session (the LIVE query + train passes — gated-OFF byte-identical, lavapipe-validated):**
  1. **LIVE cache QUERY at the gi-ris suffix vertex.** `buildRisGiNrcModule` (`shaders/risGiNrc.wgsl.ts`) is the NRC variant of the Sprint-16 gi-ris pass. At the reconnection vertex `xs`, it tracks Müller's path spread (§5) along the bounce edge; when `a(x) > c·a₀` fires it REPLACES the DDGI suffix estimate (`Lo = irrAtXs·albedo·INV_PI`) with the MLP's predicted outgoing radiance — an INLINE single-sample MLP forward (`wgsl/nrcQuery.wgsl.ts` `nrcMlpForward`, reading the trainer's f32 master weight/bias buffers + the hash-grid tables, running the multiresolution-hash-grid + one-blob + raw-feature encode INLINE since WGSL forbids storage-pointer function args). Below the spread threshold it keeps the DDGI estimate verbatim (sub-threshold regions are bit-identical to the OFF pass).
  2. **LIVE self-training (host-owns-cadence).** The gi-ris NRC pass writes one record per half-res pixel (encoded suffix input + the DDGI radiance as the §5 self-training target) into a `@group(4)` record-gather buffer. `NrcSubsystem` (`neural/nrc/nrcSubsystem.ts`) owns the `FusedMlpTrainer` + the query resources; each frame the pipeline copies the records into a MAP_READ staging buffer (same encoder, after gi-ris) and runs ONE `trainStep` (re-entrancy-guarded, fire-and-forget). The MLP learns the cache; the query reads the SAME weights.
  3. **COMPILE-TIME structural gate (the GRIS-class-regression discipline, MANDATORY).** `nrcEnabled` is a compile-time engine-creation flag (NOT a runtime UBO branch). When OFF (default): gi-ris compiles `RIS_GI_MODULE` on the 4-group shade layout — **byte-for-byte the pre-NRC pass**, no `@group(4)`, no NRC symbols, no `NrcSubsystem` constructed, no NRC GPU buffers allocated. When ON: gi-ris compiles the 5-group inline-MLP variant + binds `@group(4)`. Threaded `HybridEngineOptions.nrcEnabled` → `_nrcEnabled` → init host → `pipeline.initialize({nrcEnabled})` → `compilePipelines({nrcConfig})` → 5-group layout + `RISGIPass(getter)`. Exactly mirrors how `restirPtReuse` was fixed after the f8df9a4 black-frame regression.
  4. **The UBO gate (unchanged).** `nrcEnabled` still rides UBO offset 364 (former `_ppgPad2`, 416 bytes, no size change). `nrcGateBitIdentity.test.ts` pins OFF≡absent byte-for-byte + ON flips ONLY u32[91].
- **Unit-level guards (green now, in `npx vitest run` @ packages/walkaround-hybrid):**
  - `nrcStructuralGate.test.ts` (NEW — the GRIS-class regression guard): composing gi-ris with `nrcConfig` ABSENT has NO `@group(4)` / NONE of the NRC symbols (and WITH config, they ARE present); the REAL `compilePipelines` against a mock device gives the `risGi` pipeline **4 bind-group layouts OFF / 5 ON**, and the **SET of compiled pipelines is IDENTICAL OFF vs ON** (no NRC-only pipeline added, no pass removed; every non-risGi pipeline's bgl-count is unchanged). This is the unit guard that catches the GRIS-class structural regression.
  - `nrcGateBitIdentity.test.ts` (UBO-byte gate), `nrcEncoding.test.ts` (encode forward/backward vs CPU oracle ~1e-6), `spreadTermination.test.ts` (predicate vs CPU oracle).
- **Lavapipe (CPU-WebGPU) validation done this session:** `neural/nrc/nrcQueryHarness.ts` composes the NRC encode + inline-MLP-forward, COMPILES the kernel on lavapipe, and validates it numerically vs a CPU oracle: **encode GPU↔CPU maxAbsErr 8.9e-8, inline-MLP-forward GPU↔CPU (trainer's concatenated layout) maxAbsErr 1.1e-8.** The fused-MLP TRAIN kernel was already lavapipe-validated (`fusedMlpHarness.ts`: GPU==CPU-analytic, loss falls 92×). So the NEW load-bearing arithmetic (inline forward + encode) is GPU-compiled + numerically pinned; only the FULL composed gi-ris-NRC shader (with scene-traversal deps) and the steady-state cache convergence remain for a real-GPU host.
- **STILL NEEDS REAL HARDWARE (the dominant remaining work is VALIDATION, not implementation):**
  1. **GATE-OFF BIT-IDENTITY — GPU CONFIRM (do NOT skip).** The unit structural guard (4 vs 5 bgl, identical pipeline set) is NECESSARY but, per the GRIS lesson (f8df9a4 passed unit tests yet black-framed on real GPUs), **NOT SUFFICIENT**. Capture the DEFAULT (nrcEnabled OFF) walkaround render on a real GPU and confirm it is byte-identical to a pre-NRC build — i.e. NRC OFF did not perturb the default pipeline. This is the V19-style gate-off proof and is REQUIRED before trusting the gate.
  2. **Full composed gi-ris-NRC WGSL compile** — the complete `buildRisGiNrcModule` shader (inline MLP + encode + scene traversal + DDGI + PPG-pdf deps) compiles on a GPU host with `nrcEnabled` ON (the isolated query module already compiles on lavapipe; the full composition pulls the `@vitrum/*` shared WGSL).
  3. **Biased-cache QUALITY A/B (NOT mean-equality)** — render a strong-indirect walkaround scene (Cornell color-bleed) NRC ON vs OFF at matched accumulation. **Telemetry:** perceptual error (FLIP / rel-MSE) of NRC-ON vs the NRC-OFF converged reference within a stated tolerance; NRC-ON reaches a noise floor in FEWER frames and/or lower per-pixel variance at matched frame count. **Do NOT assert equal converged means** — a small bounded mean shift is expected.
  4. **Self-training stability** — the per-frame `trainStep` keeps the cache stable (no divergence/oscillation) across a camera pan + lighting change; telemetry: training-loss trajectory + held-out cache-prediction error over time.
- **Storage-buffer budget (confirm on real hardware):** the NRC ON gi-ris pipeline binds the existing 4 groups PLUS `@group(4)` (4 read-only storage + 1 read_write storage + 1 uniform). gi-ris reuses the shade-layout scene/frame groups (16 storage buffers at the full-tier floor) so the +5 NRC storage buffers push gi-ris well above the 16-buffer floor — **full-tier-only, and the host MUST confirm the gi-ris pipeline fits the adapter's `maxStorageBuffersPerShaderStage` / `maxBindGroups` (5 groups needs ≥5)** when `nrcEnabled`. If a full-tier adapter caps at 16 storage buffers, the NRC scene group may need slimming; flagged for the GPU pass.
- **Expected delta:** gate-OFF **bit-identical** to a pre-NRC build (unit-pinned on UBO bytes + the structural guard; GPU-confirm pending per #1); gate-ON perceptually close to the NRC-OFF reference within tolerance with faster convergence / lower noise — **biased, not mean-preserving**.
- **Acceptance:** OFF byte-identical (unit + structural-guard green now; GPU-confirm pending); encode/forward/spread match CPU oracles ~1e-8/1e-6 (lavapipe green now); the query/train passes are wired + compile-time-gated (green now); the full gi-ris-NRC shader compiles on a real GPU + the NRC-ON image is within perceptual tolerance of the NRC-OFF reference with measurable convergence/variance improvement (GPU pending).

### V24 — Differentiable RT inverse session (WS5: Phase-0 FD loop + Phase-1 BSDF adjoint)  [Phase-0 loss-descent GPU-VALIDATED on dzn — param recovery + monotone descent confirmed; OUTSTANDING: Phase-1 GPU adjoint dispatch A/B]
- **What landed (pt-webgpu + @vitrum/core + @vitrum/engine):**
  1. **`@vitrum/core` contract** (`core/src/inverse.ts`): `InverseParam` / `InverseSessionOptions` / `InverseStepResult` / `InverseSession` + optional `Engine.createInverseSession?`, exported from the package index, forwarded conditionally through the `createEngine` idempotent-dispose proxy (throws after dispose). Default frame path is byte-unchanged (no `FrameInput` / `frameParamsLayout` / kernel-RNG edits — `frameSeed` was ALREADY a required, RNG-consumed `FrameInput` field, so path-replay just re-renders with a frozen seed).
  2. **Phase 0 (finite-difference) — fully wired** in `PtWebgpuInverseSession` (`pt-webgpu/src/inverse/inverseSession.ts`): each `step()` renders N SPP at the target resolution with a FROZEN per-sample seed sequence (`#renderAndReadbackForInverse` in `index.ts`), reads the accum texture back via the existing `readOidnInputsFromTextures`, computes the L2/L1 loss, forward-differences the gradient by ±ε re-renders, Adam-steps the tiny flat param vector, and pushes updates through `updatePrimitive`/`updateEmitter`. Optimizable paths: `materials.<id>.{baseColor,roughness,metallic,emissive,emissiveIntensity}` and `emitters.<id>.{color,intensity}`.
  3. **Phase 1 (path-replay BSDF adjoint) — analytic partials VALIDATED + GPU adjoint WGSL emitted, dispatch gated.** CPU oracle (`pt-webgpu/src/inverse/brdfAdjoint.ts`) computes analytic `dBrdf/dBaseColor` (3×3 diagonal Jacobian) + `dBrdf/dRoughness` for the FROZEN sampled `wi`; the unit gate `brdfAdjoint.test.ts` confirms **analytic == central-finite-difference of `evaluateBrdf` to ≤1e-4** across 5 shading configs (the critical Phase-1 gate — PASSING), plus an analytic-Lambert `(1-F)/π` closed-form cross-check. The WGSL twin (`wgsl/pathTrace/pathTraceAdjoint.wgsl.ts`) emits the SAME arithmetic (codegen-shape-pinned) and uses the NRC i32 fixed-point grad-atomic convention (`ADJOINT_GRAD_FP = 2^20`). **The GPU adjoint *dispatch* is not yet wired into a render pass** — a pt-webgpu session resolves the effective method to `'finite-difference'` and reports it via `session.method` (no silent wrong-gradient path), pending the real-GPU replay A/B below.
- **Unit guards (green now, `npx vitest run` @ packages/{core,engine,pt-webgpu}):** `brdfAdjoint.test.ts` (8 — the ≤1e-4 FD gate + Lambert + WGSL pins), `inverseSession.test.ts` (21 — contract shape, path resolution/validation throws, Phase-0 convergence on a 1-material fit [loss → <5% of initial, baseColor → target], gradient-sign, frozen-RNG-replay determinism, idempotent dispose, pure loss/Adam helpers), `inverseSessionProxy.test.ts` @ engine (3 — proxy forwards conditionally + throws after dispose).
- **Phase-0 loss-descent GPU-VALIDATED (2026-05-29, wsl-gpu `tests/v24-inverse-fit.mjs`, dzn RTX-4090):** a new worker command (`inverse-fit` → `capture-worker/render-inverse-fit.ts`) drives the real `createInverseSession` Phase-0 FD loop end-to-end against a REAL path-traced forward model: it renders a Cornell target at a GOAL baseColor, reads it back to linear-HDR RGB via a new HDR-preserving `readbackRGBA16FAsRGBA32F` (the SAME space the session reads its own renders into, so the L2 minimum sits at the goal), resets to a wrong START color, opens the session fitting `materials.<id>.baseColor`, and drives 50 `step()`s at 40 spp. Result: **loss monotone-decreases 0.0206 → 0.0173 and the fitted baseColor recovers from param-error 0.622 → 0.245** (all 3 channels move toward the goal: red 0.40→0.62, green 0.45→0.16, blue 0.55→0.14 toward goal [0.85,0.2,0.2]). The residual (red plateaus ~0.62, not 0.85) is the genuine MC-noise floor + the wall's low red-channel illumination — not a bug; the fit is unbiased and converges in the right direction on every channel. Confirms the readback + the FROZEN-SEED finite-difference gradient + Adam are all correct on hardware. The picked target is the highest-coverage diffuse primitive (a Cornell wall) so its albedo dominates the image-loss signal.
  - **Two real bugs the GPU run surfaced + fixed (both now unit-pinned):** (1) `InverseParam.min/max` are SCALARS but the doc said "per-component"; passing an array silently produced `Math.max(x, [0,0,0]) = NaN` in `clampParams` — doc corrected in `core/src/inverse.ts`. (2) the image-loss functions read `rendered[i] ?? 0`, which guards `undefined` but NOT `NaN`/`±Inf`; a single firefly pixel (Inf is representable in rgba16float) NaN-poisoned the whole mean loss → NaN gradient → silent optimizer stall. `l2Loss`/`l1Loss`/`lossValue` now map non-finite samples to 0 via `finiteSample` (`optimizer.ts`), pinned by a new `inverseSession.test.ts` case (26 tests green).
- **STILL NEEDS REAL HARDWARE:**
  1. **Phase-1 GPU adjoint dispatch + A/B** — wire the `pathTraceAdjoint.wgsl` partials into an adjoint compute pass, replay the forward path with the frozen seed, accumulate per-param gradients; telemetry: the GPU analytic gradient within ~5× of a GPU finite-difference gradient on the same scene (the analytic == FD equivalence is already CPU-proven to ≤1e-4; the GPU item confirms the replay determinism + atomic reduction).
  2. **Dispose clean** — no leaked GPU buffers after `session.dispose()` (Phase-0 creates no session-owned persistent buffers — VALIDATED, the dzn run disposed cleanly; Phase-1 adjoint buffers must be freed).
- **Acceptance:** core types exported + `createEngine` proxy forwards (green now); default frame path byte-unchanged (no kernel/UBO edits — verified by code-read); the ≤1e-4 BSDF-adjoint FD gate green (now); **Phase-0 loss descent + param recovery on a real GPU (DONE — dzn, 2026-05-29)**; Phase-1 adjoint-within-5×-of-GPU-FD (pending).

---

### V23 — Volumetric subsurface scattering (pt-webgpu, WS4)  [GPU-VALIDATED: SSS-on composition compiles + dispatches + renders translucent output on dzn; radiometric slab A/B still pending]
- **What landed (WS4):** the crude Beer-Lambert + `0.02` forward-scatter fudge in `kernel.wgsl.ts` is replaced by a real homogeneous participating-media random walk: free-flight distance sampling `t = -ln(1-ξ)/σ_t` (hero-channel σ_t in spectral mode, max-channel otherwise with per-channel transmittance re-weight), single-scatter albedo `σ_s/σ_t` throughput on a real collision, Henyey-Greenstein phase sampling (`sampleHenyeyGreenstein`, mean cosθ = +g forward scatter), in-medium next-event estimation to the directional light with phase↔light power-heuristic MIS, and `exp(-σ_t·dist)` fall-through to the surface BSDF on a no-collision flight. σ_a is derived host-side from `attenuationColor`/`attenuationDistance` (`materialPacking.ts`, new vec4 #22; `MATERIAL_VEC4_STRIDE` 22→23 in TS+WGSL lockstep), or the spectral-attenuation curve when authored; σ_s from `scatteringCoefficient(RGB)`; g from `scatteringAnisotropy`. Medium enter/exit is driven by the dielectric REFRACTION branch (`BounceSample.enteredMedium/exitedMedium`). The caustic specular chain (`caustic.wgsl.ts`) gained per-segment Beer-Lambert medium extinction.
- **STRUCTURAL (compile-time) BDPT gate — energy conservation:** the volumetric walk + the HG phase helpers are compiled in ONLY when BDPT is OFF (`composePtWebgpuTraceWgsl(bdptEnabled)` / `composePathTraceKernelWgsl({ volumetricSss })`; the pipeline picks the variant from `gpuResources.#bdpt`). The BDPT-on shader carries NO SSS symbols (`var inMedium`, `freeFlightDist`, `fn sampleHenyeyGreenstein` all absent) and falls back to per-channel Beer-Lambert absorption — because the BDPT light subpath has no medium logic, so attenuating only the eye path would break energy conservation. Lite tier keeps Beer-Lambert absorption only (no walk). UBO byte size is unchanged (σ_a lives in the materials buffer).
- **GPU status (real Dawn, this session):** BOTH gated compositions (SSS-on and BDPT-on) **compile without errors** on the wsl-gpu Dawn device — `wgslSmoke.gpu.test.ts` now exercises both and is green (5/5). (En route, a pre-existing latent BDPT bug surfaced: `bdptConnection.wgsl.ts` used the non-existent WGSL builtins `isNan`/`isInf` — replaced with the portable `x != x` / `abs(x) > 3.4e38` idiom; this had been silently failing the SSS-on parse test on the base branch too.)
- **GPU-VALIDATED (2026-05-29, wsl-gpu `tests/vitem-check.mjs --scenario=cornell-sss`, dzn RTX-4090):** the SSS-on path-trace kernel **compiles, the volumetric-walk pass dispatches with zero device errors, and renders non-black + finite** translucent output (Cornell-SSS 256² 48spp: nonzero 55.4%, meanRGB [56.1, 54.1, 54.7], fireflies 3380 — the higher firefly count vs plain Cornell is the expected variance signature of the unbiased single-scatter NEE+phase MIS estimator). The `cornell-sss` scenario authors a transmission=0.55 material with σ_s RGB [0.16,0.20,0.24]/g=0.35, exercising the free-flight + HG-phase + in-medium NEE walk. Drives the modernized typed-options `render-pt-webgpu.ts` (WS6: typed `spectral`/`bdpt`). So the random-walk restructure dispatches + renders on hardware; only the quantitative slab-transmittance A/B remains.
- **Unit-pinned (green now, `npx vitest run` @ packages/pt-webgpu, `volumetricSss.test.ts` 16 tests):** HG normalizes to 1 over the sphere (MC) + g=0 ⇒ exactly 1/(4π); HG sampler IS-weight ≈ 1 + sampled mean cosθ = g; free-flight CDF mean = 1/σ_t + survival fraction = exp(-σ_t·d); no-scatter slab transmits exp(-σ_a·d); single-scatter albedo σ_s/σ_t; MIS partition-of-unity; σ_a packing round-trip; **structural gate** (SSS symbols absent when BDPT on, FrameParams UBO byte-stable, default export == SSS-on composition).
- **STILL NEEDS REAL HARDWARE (radiometric A/B):** render a translucent slab / dense-medium scene SSS-on; confirm the exit-face chrominance tracks the analytic single-scatter transmittance/albedo within a stated tolerance (≈5%), forward-scatter materials (g>0) brighten the forward lobe, and the **BDPT-ON path is byte-unchanged** from the pre-WS4 build (the gate proof). Telemetry (linear-HDR readback), not screenshots.
- **Acceptance:** both gated WGSL variants compile on a real GPU (DONE — Dawn, this session); CPU oracles green (DONE); radiometric slab A/B within tolerance + BDPT-on byte-unchanged (GPU pending). NOTE: V21 (smooth normals, WS1) and V22 (many-light IS, WS2) are tracked by their own workstreams.

---

### V21 — Smooth shading normals (walkaround-hybrid, WS1 2026-05-29)  [GPU compile+render VALIDATED via the T1 hybrid smoke on lavapipe+dzn; dedicated faceted→smooth + acne A/B still needs a curved low-poly scene]
- **What landed:** the walkaround real-time primary-shading passes (`shade`/`ris`/`risGi`/`risGiNrc`) now use a SMOOTH barycentric per-vertex shading normal — `normalize(w·n0 + u·n1 + v·n2)·hit.side` (`smoothShadingNormal` in `sceneTraversal.wgsl.ts`, mirroring the DDGI precedent `probeUpdateRays.wgsl.ts:443-454`) — instead of the faceted geometric face normal. The GEOMETRIC normal (`hit.normal`) is KEPT for ray-origin offsets / backface bias (shadow + bounce rays) in every pass. A new per-vertex `bvh_normal` storage buffer (`@group(1) @binding(11)`, data = `shared.normals`) was added to the scene bind group; to stay at the `maxStorageBuffersPerShaderStage = 16` floor, the per-tri `bvh_beer` Beer-Lambert tint moved from a storage buffer to an `r32uint` texture (`@group(1) @binding(5)`, `pipeline/bvhBeerTexture.ts`) — textures don't count against the storage limit, so the net scene-group storage count is unchanged (11). The GPU-skin compute kernel (`gpuSkinBvh.wgsl.ts`) now writes its inverse-transpose skinned normals into the SHARED `bvh_normal` buffer at `baseVertex+vi` (was a dropped per-mesh `vi` write — computed-but-unconsumed); the smooth-normal blend therefore tracks skinned deformation.
- **TLAS deferral (documented):** in TLAS mode `bvh_normal` holds LOCAL-space BLAS normals and the per-instance world transform is not carried out of `traceTlasFirstHit`, so the passes GATE on `ubo.bvhMode == 1u` and keep the geometric normal there. Smooth normals are merged-world-BVH-only this wave; a TLAS-mode smooth normal (transform the blended normal by the instance's inverse-transpose) is a follow-up.
- **Unit-level guards (green now, `npx vitest run` @ packages/walkaround-hybrid):** `shaders/__tests__/smoothNormals.test.ts` — barycentric-blend CPU oracle (unit-length over 2000 random triples; side-flip), codegen pins (all 4 passes declare + CALL `smoothShadingNormal`; each keeps a `geoNormal` for the offset; shade reads beer via `textureLoad`, no storage `bvh_beer`), compose-level pins (the flattened shade/ris/risGi emit `smoothShadingNormal` exactly once; `BVH_BEER_TEX_WIDTH` declared once; no dangling `&bvh_beer`), scene-group storage budget (≤11 storage; beer is `tex:uint`, normal is `storage-ro`), and a GPU-skin inverse-transpose CPU oracle (`mat3InverseTranspose`·restNormal, Rz(90°) maps +X→+Y). `bindGroupDescriptorParity.test.ts` updated to the new layout (beer texture @5, normal storage @11) and green; `gpuSkinNormals.test.ts` updated to pin the `skinnedNormals[outIdx]` redirect.
- **GPU compile+render VALIDATED (2026-05-29, wsl-gpu `scripts/t1-smoke.mjs`, lavapipe + dzn):** the entire ReSTIR pipeline set — `shade`/`ris`/`risGi`/`risGiNrc` now carrying `smoothShadingNormal` + the relocated bind layout (new `bvh_normal` storage @group(1)@binding(11), `bvh_beer` moved to an `r32uint` texture @binding(5) to hold the scene-group storage count at the 16 floor) — **compiles ("[ReSTIR] All pipelines compiled successfully") and renders the Cornell-DDGI hybrid frame non-black on BOTH backends, PSNR 999 dB vs golden** (no regression vs the pre-WS1 golden, i.e. the merged-world-BVH DDGI scene which has flat walls reads byte-identically — the smooth blend is a no-op on flat faces, exactly as intended). So the bind-group-layout reshuffle + the `smoothShadingNormal` WGSL are device-valid on naga (lavapipe/dzn) and Dawn. The faceted→smooth *visual* delta needs a curved low-poly mesh the DDGI golden scene doesn't contain.
- **STILL NEEDS REAL HARDWARE:**
  1. **Faceted→smooth render A/B** — a low-poly curved mesh (coarse sphere / cylinder) under direct + indirect light: confirm the faceted triangle banding on the OFF reference is gone with smooth normals ON, no new shading artefacts.
  2. **No normal-offset acne** — confirm keeping the GEOMETRIC normal for the ray offset (not the smooth one) leaves shadow + GI-bounce rays acne-free at silhouette/grazing angles (the smooth normal can dip below the surface there — the whole reason the offset stays geometric).
  3. **Skinned-normal liveness** — a skinned hero mesh deforming: the smooth shading normal tracks the deformation (the kernel writes into the shared `bvh_normal` each frame), not a frozen rest pose.
- **Expected delta:** smoother shading on curved low-poly geometry (no faceting); shadow/GI rays unchanged in acne behaviour (geometric offset preserved); TLAS-mode renders byte-unchanged (geometric normal retained there).
- **Acceptance:** ON render removes faceting on the curved test mesh with no acne; geometric-offset behaviour preserved; TLAS-mode image unchanged; unit + compose + parity tests green (all green now).

---

### V22 — Many-light importance sampling (pt-webgpu NEE, WS2)  [GPU-VALIDATED on dzn: unbiased (blurred PSNR 32 dB @ 256spp) + 60% direct-lighting variance reduction @ 8spp — AND surfaced + fixed two real bugs in the light-tree path]
- **What changed:** pt-webgpu's per-event direct-light pick was uniform-random (`floor(rand·lightCount)`, compensated `·lightCount`). WS2 replaces it (FULL TIER ONLY) with a power-weighted light-tree descent: `sampleLightTree(hitPos, LT_DIST2_FLOOR, nodeCount, &rng)` over a `@vitrum/shared-samplers` tree (Conty Estévez & Kulla 2018 power × spatial-proximity descent over a Shirley-1996 power-as-cost median-split tree), and divides the NEE contribution by the tree's branch-product selection pdf `lt.pdf` instead of `·lightCount`. The tree node array is uploaded to a DEDICATED group(3) storage buffer (full tier only; lite keeps the uniform pick on its separate `kernelLite`). Leaf `emitterIndex` is built in the SAME order the kernel NEE walk visits lights (directional · point · spot · rect · mesh · env), so `picked` indexes the same slot whichever path produced it. Gated by `params.lightTreeEnabled` (built with ≥ 2 selectable lights) + `params.lightTreeNodeCount`; degenerate draws fall back to the uniform pick that iteration. Incremental `updateEmitter`/`updateEnvironment` rebuild + re-upload the tree.
- **Why GPU:** the selection runs in the WebGPU path-trace compute kernel; the variance-reduction claim + group(3) shader-compile can only be measured on a real device with a converged accumulation. The CPU oracle (`sampleLightTreeCPU`/`lightTreePdfCPU`) unbiasedness + variance-reduction is unit-pinned (`lightTreeImportance.test.ts`), and the WGSL consumption + gating is structurally pinned (`lightTreeConsumption.test.ts`), but neither runs the real shader.
- **Both estimators UNBIASED** (this is V18/V19-class, NOT V20/NRC): the power-weighted pick and the uniform pick are both unbiased single-sample NEE estimators of the same direct-lighting integral, so the converged MEAN must MATCH; the win is VARIANCE at equal SPP.
- **GPU-VALIDATED (2026-05-29, wsl-gpu `tests/vitem-check.mjs --scenario=cornell-ptwgpu`, dzn RTX-4090):** the full-tier trace with the `@group(3) @binding(0) lightTree` storage binding **compiles, the 4-bind-group pipeline layout is accepted by `createComputePipeline`, the kernel dispatches with zero device errors, and renders non-black + finite** on the 2-light `cornell-ptwgpu` scenario (RectAreaLight + PointLight → `lightTreeEnabled` with ≥2 selectable lights; 256² 48spp: nonzero 50.0%, meanRGB [38.9,37.3,46], fireflies 883). So item #1 below (full-tier group-3 shader-compile + dispatch) is DONE; the converged-mean + variance A/B (items #2/#3) still need the ≥8-light spread scene. The BDPT light-subpath pipeline that shares the layout also bound group 3 without an "unbound group" error (V25 BDPT-ON rendered on the same harness).
- **FULLY GPU-VALIDATED (2026-05-29, wsl-gpu `tests/v22-lighttree-variance.mjs`, dzn RTX-4090).** A host-facing `lightTreeImportanceSampling` engine option (default ON) was added to A/B the tree against a forced-uniform pick on the SAME scene; a 16-point-light `cornell-manylights` scenario (~60:1 power spread, no dominant overhead area light so the power/dist² importance correlates with per-pixel irradiance) is rendered K× at varying seeds, direct-lighting only (bounces=1), per-pixel Welford mean+variance:
  1. **Full-tier shader-compile (group 3)** — DONE: the 4-bind-group pipeline (incl. `@group(3) @binding(0) lightTree`) compiles + dispatches on dzn.
  2. **Variance reduction @ low spp (the operating regime)** — tree-ON per-pixel direct-lighting variance is **60.5% lower** than forced-uniform at matched 8 spp (ratio 0.395). This is also the discriminator vs the pre-fix bug, which RAISED variance.
  3. **Unbiasedness @ high spp** — once both estimators converge (256 spp), the firefly-suppressed (box-blurred) means agree at **32.1 dB PSNR** ON↔OFF — both integrate the same direct-lighting integral, so the `/lt.pdf` divisor + leaf-index alignment are correct.
- **TWO REAL BUGS the GPU A/B surfaced + fixed (the unit/CPU oracle never hit them — it built the tree from explicit emitter lists, not the kernel's default-emitter packing):**
  1. **Light-tree directional-slot misalignment → biased pick.** The kernel iterates the directional NEE slot whenever `lightDir.w = mean(directionalIrradiance) > 1e-6`, but the tree's `hasDirectional` ALSO required an explicit `directional` emitter to exist. With the (then-default) phantom directional irradiance, the kernel shaded a directional slot the tree omitted → every subsequent leaf's `emitterIndex` was one slot off the kernel walk → the tree weighted light *j* by light *k*'s pdf. Fixed: the tree gate now mirrors the kernel EXACTLY (`mean(dirIrr) > 1e-6`, no `.some()`).
  2. **Phantom `[1,1,1]` directional in EVERY directional-less scene.** `defaultDirectionalIrradiance` returned `[1,1,1]` when no directional emitter existed → the kernel shaded a fabricated directional light in every such scene (physically wrong) AND the tree's directional leaf got the union-AABB of all positional lights (dist²≈0 inside the scene → its importance dominated the descent, starving the real lights → ~76% variance INCREASE). Fixed: returns `[0,0,0]` (no emitter ⇒ no light); removes the phantom from both the kernel NEE and the tree.
  Both fixed in `packages/pt-webgpu/src/scene/emitterPacking.ts`, pinned by 3 new `lightTreeImportance.test.ts` cases (16 green).
- **KNOWN LIMITATION (documented, NOT a bug — a tuning follow-up):** the power/dist²-only tree has NO orientation-cone / cosine / visibility term and NO minimum-pdf clamp, so it has a HEAVY TAIL — at HIGH spp its rare `1/pdf` spikes (from near-zero-pdf lights that a surface's cosine/visibility actually makes relevant) converge slower than uniform, and on occlusion-heavy scenes with a dominant overhead area light it can lose. The win is in the low-spp operating regime where importance sampling matters. Production mitigations (Conty-Estévez orientation cone, min-pdf floor) are the tuning follow-up.
- **Lite tier untouched:** the lite trace composes NEITHER the group(3) binding NOR `sampleLightTree` (structurally pinned); lite renders are unaffected by the toggle.
- **Acceptance:** full-tier group-3 compile (DONE); 60% variance reduction @ low spp (DONE); unbiased converged means (DONE); two correctness bugs fixed + unit-pinned (DONE). Orientation-cone / min-pdf tuning = follow-up.

---

### V25 — BDPT light-subpath workgroup-race fix (pt-webgpu, `ee8c1fc`)  [GPU-VALIDATED: compiles + dispatches + renders on dzn — variance-WIN A/B still needs a caustic scene]
- **What changed:** `bdptExtendLightSubpath` ran one workgroup per light-subpath column, each reading column-1's vertex from the buffer — a cross-workgroup DATA RACE (workgroup execution order within a single `dispatchWorkgroups` is spec-undefined). It only "worked" because drivers serialize tiny dispatches (which is precisely why the V18 BDPT validation passed). Fix: build the WHOLE subpath in ONE invocation via a sequential `col = 0..maxB` loop; each column reads the previous vertex THIS thread just wrote → sequentially correct, no inter-workgroup hazard. Host now issues `dispatchWorkgroups(1,1,1)`. The per-column emitter-pick + extend + write logic is byte-identical (control-flow-only restructure).
- **GPU-VALIDATED (2026-05-29, wsl-gpu `tests/v25-bdpt-capture.mjs`, dzn RTX-4090):** the refactored single-workgroup kernel **compiles, the BDPT subpath pass dispatches with zero device errors, and renders non-black + finite** (Cornell 256² 32spp: BDPT-ON nonzero 49.5% vs unidirectional 49.0%). So the restructure did not break compilation or chain construction. Drives the modernized `render-pt-webgpu.ts` typed-`bdpt`-option path (WS6).
- **Unit-pinned:** typecheck clean; 236 pt-webgpu tests green incl. the BDPT light-subpath oracle (`bdptLightSubpathOracle.test.ts`) + full Veach §10.3 connection MIS (`bdptConnectionMisFull.test.ts`) — the subpath math the loop preserves.
- **STILL NEEDS A CAUSTIC SCENE:** on this large-area-light Cornell, BDPT-ON fireflies (716) were comparable-to-slightly-above unidirectional (576) — expected, since a big area light is already efficient for unidirectional NEE and under-exercises BDPT's connection strategies (the V18 follow-up note). The variance-WIN A/B (BDPT-ON variance < unidirectional) needs a small/hidden-emitter or caustic scene where the extra strategies carry weight. Not a regression — the subpath construction is the unchanged, oracle-tested math; this row validates the race fix's compile/dispatch/render correctness.
- **Acceptance:** refactored kernel compiles + dispatches + renders correctly on real hardware (DONE — dzn); deeper §10.3 variance win on a caustic/hidden-emitter scene (follow-up, same as V18's open item).

---

## 3. Suggested session order
Run the **probe** (confirm full tier + hybrid). Then V8 (cheap: does everything still compile?), then the render A/Bs V1–V7, V9, V10 (BMFR), and the later-wave items V11–V17 (skin bind, sun dedup, RC merged-refit, resolutionFactor coverage, DDGI cadence/2→32 default, RC⊕ReSTIR-GI MIS, PPG guided sampling). The cheapest functional checks (V8 compile, V11 skin-routing, V14 canvas-coverage) gate the rest. Adopt the post-sweep PNGs as new baselines per the benchmark-runner README §5 once each passes visual sign-off. **Note:** several V11–V16 items default-ON now (e.g. V15's stride-2 default cadence, V16's confidence MIS), so a plain `main` capture already exercises them.
