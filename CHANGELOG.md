# Changelog

All notable changes to this monorepo will be documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html) once packages enter the public release channel (see root `README.md`). Until then, versions may remain `0.0.0` and breaking API moves are noted here without minor/major bumps.

## [Unreleased]

### Current package naming note

- The active WebGL2 hero backend is `@vitrum/pt-webgl2`. Historical bullets below that
  mention `@vitrum/pt-webgl`, `@vitrum/three-bindings`, or `@vitrum/three-gpu-pathtracer`
  describe pre-`e14000c` work on packages that were later removed in this same
  `[Unreleased]` section. They are not current import paths or API promises.

### Fixed (glTF decode reports + inverse render-regime gates, 2026-06-16)

- **glTF texture decode reports now carry self-contained dimensions and provenance:** `GltfTextureDecodeReportEntry` includes decoded width/height, power-of-two status, original dimensions, resize/max-size metadata, glTF texture/image/sampler indices, image URI/mime data, and selected texture-source extension provenance. `loadGltfAndDecodeTextures()` also reconciles backend compatibility after a successful spec-gloss alpha roughness bake, so direct asset callers no longer see the stale `specularGlossinessTexture.glossinessAlpha` approximation after the baked CPU-readable `roughnessMap` exists.
- **`@vitrum/pt-webgpu` path-replay inverse sessions now gate on the actual forward render regime:** the real engine reports effective bounce count and spectral mode to `PtWebgpuInverseSession`; requested path replay downgrades with `path-replay-unsupported-render-regime` when the baseline was multi-bounce or spectral, matching the current single-bounce RGB direct-light adjoint instead of producing a mismatched analytic gradient.

### Fixed (pt-webgpu inverse fallback diagnostic specificity, 2026-06-16)

- **Path-replay inverse fallbacks now classify normal/env scalar controls explicitly:** requested `method:'path-replay'` sessions for `materials.<id>.normalScale`, `bumpScale`, and `clearcoatNormalScale` now report `path-replay-unsupported-normal`, while `materials.<id>.envMapIntensity` reports `path-replay-unsupported-environment`. These controls still optimize through finite difference; the change makes arbitrary-asset inverse diagnostics more machine-readable.

### Fixed (glTF skin attribute compatibility truthfulness, 2026-06-16)

- **glTF skin attributes now report the same semantics the importer uses:** `analyzeGltfAsset()` no longer predicts `skinned-mesh` support from raw `JOINTS_0` / `WEIGHTS_0` attributes alone. Skinning now requires a node that binds `skin` plus both joint and weight streams; unbound skin attributes report `skinAttributesWithoutNodeSkin=unsupported`, incomplete streams report `incompleteSkinAttributes=unsupported`, and best-effort import returns structured `ignored-skin-attributes` / `incomplete-skin-attributes` diagnostics while importing the primitive as a static mesh.

### Fixed (glTF instanced morph/skinning compatibility truthfulness, 2026-06-16)

- **glTF compatibility preflight now rejects instanced skinned/morphed meshes explicitly:** `analyzeGltfAsset()` marks `EXT_mesh_gpu_instancing` on skinned or morphed meshes as `EXT_mesh_gpu_instancing.skinnedOrMorphed=unsupported`, matching the importer behavior that falls back to a single skinned/morphed primitive because the core Scene contract has no instanced skinned/morphed primitive kind yet. `loadGltfForEngine(..., compatibilityMode:'reject-unsupported')` now fails before constructing an engine for this combined case instead of relying on the later `ignored-gpu-instancing` degraded import diagnostic.

### Fixed (glTF opacity report truthfulness, 2026-06-16)

- **glTF feature reports now match opaque alpha conversion semantics:** `analyzeGltfAsset()` only reports `opacity` for `baseColorFactor[3]` / legacy spec-gloss `diffuseFactor[3]` when the material uses `alphaMode:"MASK"` or `"BLEND"`, matching `gltfToScene()`'s conversion rule. Opaque materials with alpha below 1 keep the RGB base color but no longer imply a converted opacity field in backend compatibility reports.

### Fixed (glTF emissive-map PDF truthfulness, 2026-06-16)

- **glTF backend compatibility now surfaces emissive-texture emitter PDF approximation explicitly:** `evaluateGltfBackendCompatibility()` reports an `emissiveMap.texelPdf` approximate material issue whenever a backend imports glTF `emissiveTexture` but cannot guarantee exact texel-space emitter alias tables/PDFs across all direct, GI, RC, DDGI, and fallback sampling paths. `pt-webgpu-lite` still reports the base `emissiveMap` row as unsupported instead of adding this narrower approximation issue. A focused pt-webgl2 test now ties CPU-readable emissive-map micro-triangle packing to the mesh-area forward/sample PDF parity proof. Exact texel-alias emitter PDFs remain a promotion tail; this closes the silent planner/reporting ambiguity.

### Fixed (glTF material sweep render gate, 2026-06-16)

- **The material-heavy glTF sweep now has a focused `pt-webgpu` render/readback gate:** `tools/gltf-material-sweep` exposes its synthetic base/KHR texture-map fixture and decode hooks, and `tools/behavioral-gate` adds `pt/gltf-material-sweep`. The new lane drives the same asset through `loadGltfForEngine({ decodeTextures:true, textureTarget:'cpu-linear' })`, asserts all 18 decoded texture-report rows and CPU-readable handles survive controller attachment, boots the real `pt-webgpu` backend, renders 8 spp at 64², and requires finite non-black readback with zero GPU validation errors. On lavapipe this proves API/decode/controller/backend boot-readback for the material-heavy asset; full-tier rich-material fidelity and golden-PNG comparison remain promotion evidence.

### Fixed (pt-webgpu emitter inverse replay slice, 2026-06-16)

- **`@vitrum/pt-webgpu` inverse sessions now accept renderer-consumed scalar map controls:** `materials.<id>.normalScale`, `bumpScale`, `clearcoatNormalScale`, `aoMapIntensity`, `lightMapIntensity`, and `envMapIntensity` can now be optimized through the finite-difference baseline, seeded from the scene, clamped to sane default ranges, and patched back through the incremental material hook. Requested `method:'path-replay'` still downgrades the normal/env-map controls with structured unsupported-field diagnostics until those analytic terms are mirrored; `aoMapIntensity` now has a scoped base-color-chain path-replay derivative, and `lightMapIntensity` path-replays the primary-hit baked-radiance partial.
- **`@vitrum/pt-webgpu` inverse sessions now replay vec2 thin-film thickness ranges through thickness maps:** `materials.<id>.iridescenceThicknessRange` is a first-class `kind:'vec2'` inverse parameter, seeded from the scene, clamped to non-negative thicknesses, and patched back through the incremental material hook. Thickness ranges now stay on the scoped path-replay adjoint for both map-free and readable `iridescenceThicknessMap` cases by differentiating the sampled thin-film thickness and chaining it to min/max endpoints through `V·H` or the sampled G-channel texel.
- **`@vitrum/pt-webgpu` path-replay inverse sessions now differentiate deterministic direct emitter color/intensity:** the adjoint descriptor format admits emitter-domain slots for delta directional, point, spot, rect-area, disc-area, and uncapped explicit mesh-area emitters, and `adjointPass` scatters `emitters.<id>.color` / `emitters.<id>.intensity` gradients through the scoped direct-light BRDF value plus the same attenuation/geometric factors used by material direct-light replay. Mesh-area targets use a source-of-truth packed triangle range only while their explicit triangles remain contiguous; capped/reordered or unmapped mesh emitters still downgrade with structured diagnostics. The session router keeps soft directional, unreplayed receiver materials, environment lighting, indirect transport, alpha/normal/transmission/displacement visibility, layered/volume/spectral receivers, exact texel-PDF mesh emission, and forward light-selection MIS parity on finite-difference/proof tails.
- **`@vitrum/pt-webgpu` path-replay adjoint no longer center-samples finite area lights:** rect/disc and mesh-area direct-light replay now samples emitter surfaces with the same area-measure formulas as the forward NEE path, including Shirley-Chiu disc sampling, uniform triangle barycentrics, solid-angle area PDFs, and the existing shadow gates. The scoped adjoint still sums every eligible direct light rather than replaying the forward light-selection MIS branch; environment, indirect, alpha/normal/transmission/displacement, layered/volume/spectral receivers, exact texel-PDF mesh emission, and validation recaptures remain open.
- **Path-replay downgrade diagnostics now classify finite-difference-only inverse fields:** `materials.<id>.ior`, `transmission`, `thickness`, `attenuationColor`, and `attenuationDistance` now report `path-replay-unsupported-transport`, while `opacity` and `alphaCutoff` report `path-replay-unsupported-visibility`. Hosts no longer have to infer transport-vs-visibility limitations from the generic unsupported-field code or message text.

### Fixed (walkaround emitter/OIT + inverse scalar tails, 2026-06-16)

- **Walkaround ReSTIR-DI material-backed emissive maps now split into bounded micro-emitters:** readable `emissiveMap` triangles use the shared barycentric subdivision helper in the ReSTIR-DI emitter list, carry per-micro-triangle CDF/light-tree power, and store subdivision metadata in the existing `EmitterTri` padding lanes so RIS/final shade map the winning `xi` back to parent-triangle UVs before sampling the material atlas. This narrows textured-emitter PDF error for merged, TLAS, instanced, and mirrored-TLAS material emitters without changing the 80-byte emitter stride. Full texel alias tables and GI/RC/DDGI texel-space emission remain promotion tails.
- **DDGI mesh-area emitter NEE now samples mapped source texels when TLAS metadata is available:** the DDGI emitter-triangle packer carries optional source-triangle/subdivision metadata in the existing `EmitterTri` padding lanes, and `ddgiEmitterNEE` uses the material atlas to modulate sampled mesh-area emitter radiance at the emitter sample's UV. Rect/disc emitters and unmapped mesh-area emitters continue to use scalar `Le`; exact texel alias/PDF sampling and full GI/DDGI texel-space transport remain promotion tails.
- **RC probe-cast emitter NEE now samples material-backed emissive texels:** `@vitrum/walkaround-rc` adds optional material-atlas bindings to the RC cast pass, and `HybridEngineRC` forwards the main walkaround atlas/meta views so `rcEmitterNEE` can map `EmitterTri` source-triangle/subdivision metadata back to UV0 emissive-map texels instead of always using averaged `Le`. Omitted atlas bindings, unmapped emitters, and UV1-only maps fall back to scalar `Le`; exact texel alias/PDF sampling remains a promotion tail.
- **Walkaround now emits a structured runtime warning for exact emissive-map texel-PDF approximation:** `HybridEngine.setScene()` and material `updatePrimitive()` patches report `walkaround-hybrid.emissive-map-texel-pdf-approximation` once per primitive set when a material-backed emitter uses nonzero `emissive` + `emissiveMap`. The warning preserves the implemented UV-local/micro-emitter behavior while making the remaining exact texel-alias/PDF promotion tail visible to non-glTF hosts too.
- **Transparent OIT shadow rays now use the canonical atlas alpha-transmittance helper:** transparent layer sun, point/spot, and finite-emitter lighting no longer use a private 32-hit shadow walker that could leak after its budget; they share the shade/ReSTIR alpha/cast-shadow path, including the post-budget opaque blocker fallback. Transparent layers are still camera-visible approximations, not ReSTIR/GI/DDGI/RC transport vertices.
- **`@vitrum/pt-webgpu` inverse sessions can now finite-difference transport controls:** `materials.<id>.transmission`, `materials.<id>.thickness`, `materials.<id>.attenuationColor`, and `materials.<id>.attenuationDistance` are admitted as inverse parameters, seeded from the scene, clamped to physical ranges, and patched through the incremental material hook. Requested `method:'path-replay'` still downgrades them with structured diagnostics until transmission/thickness/Beer-Lambert transport is mirrored in the adjoint pass.
- **`@vitrum/pt-webgpu` inverse sessions can now finite-difference scalar alpha controls:** `materials.<id>.opacity` and `materials.<id>.alphaCutoff` are admitted as scalar inverse parameters with physical clamp ranges and material patching. Requested `method:'path-replay'` still downgrades them with structured diagnostics until alpha visibility/replay terms are mirrored in the adjoint pass.

### Fixed (pt-webgl2 finite-light and rough-transmission path tails, 2026-06-16)

- **Finite analytic area lights are now camera-visible path terminals in `@vitrum/pt-webgl2`:** rect/disc area-light intersections pick the nearest light before scene geometry, add raw emission for primary/specular-transmission camera paths, MIS-weight ordinary BSDF arrivals against analytic-light NEE, and terminate the path instead of also accumulating background or geometry behind the light.
- **Rough transmissive samples now feed the accumulated roughness filter:** the GLSL megakernel computes a Disney transmission half-vector for rough glass samples, so transmissive lobes participate in the same firefly/glossy-filter state as reflective lobes.

### Fixed (pt-webgpu inverse downgrade diagnostics, 2026-06-16)

- **Path-replay inverse fallbacks now report structured reasons:** `InverseSessionOptions.onDiagnostic` and `InverseSession.diagnostics` expose exact downgrade causes when `@vitrum/pt-webgpu` falls back from requested `method:'path-replay'` to finite difference: missing adjoint hook, emitter-domain params, unsupported fields, non-triangle/non-identity primitives, unsupported material transport/maps, environment lighting, and soft directional lights. This improves arbitrary-scene predictability; it does not promote the remaining adjoint math tails.

### Fixed (emissive-map selection power + DDGI alpha probe hits, 2026-06-16)

- **Material-backed emissive meshes now use UV-local selection power:** shared CPU helpers sample readable `emissiveMap` payloads at KHR-transform/wrap-aware UVs and estimate per-triangle emission with deterministic quadrature. Walkaround ReSTIR-DI uses that estimate for emitter CDF/light-tree power while keeping scalar packed `Le` for hit-local shader texel evaluation; `pt-webgpu` and `pt-webgl2` implicit mesh-area lights now pack UV-local triangle radiance instead of whole-texture averages. Same-day follow-ups split readable mapped implicit triangle lights into bounded barycentric micro-triangles on both PT backends and walkaround ReSTIR-DI material-backed emitter selection, preserving total area while localizing textured emission. This narrows textured-emitter selection error; exact texel-alias PDFs remain a promotion tail.
- **Walkaround ReSTIR-DI flat-CDF candidates now divide by the actual sampled CDF segment:** the shader no longer recomputes flat-branch PMF from scalar `EmitterTri.Le`, which was wrong for material-backed textured emitters whose CPU CDF was built from map-aware selection power.
- **DDGI probe primary rays now skip alpha-mask holes and fractional blend layers for opaque probe hits:** `probeUpdateRays` uses a DDGI-local alpha-aware first-hit walk, matching the material-atlas alpha semantics already used by DDGI shadow transmittance.

### Fixed (walkaround DDGI emissive maps + alpha transmittance, 2026-06-16)

- **DDGI probe-hit material emission now samples readable emissive maps:** `@vitrum/walkaround-hybrid` forwards the material texture atlas snapshot into `ProbeUpdatePass`, binds the atlas/meta textures in the probe-ray pass, and modulates material-emissive direct probe hits by the hit-local emissive-map texel. This closes the DDGI probe-hit side of mapped emissive radiance; exact texel-PDF emitter selection and full texel-space GI/RC/DDGI sampling remain promotion tails.
- **Walkaround direct/GI visibility now has a shared alpha-transmittance path:** the material-atlas shadow walk exposes deterministic fractional transmittance for `alphaMode:'blend'` blockers. Analytic/sun direct lighting, ReSTIR-DI producer visibility, ReSTIR-GI producer visibility, and NRC GI producer visibility use it while preserving `castShadow:false`, mask, scalar-glass, and full-occluder behavior. Transparent layers still are not true ReSTIR/RC/DDGI/GI transport vertices.

### Fixed (pt-webgpu additive-map inverse replay routing, 2026-06-16)

- **Path-replay adjoint no longer falls back for unrelated additive primary-hit maps:** `@vitrum/pt-webgpu` keeps BRDF/unlit path-replay sessions when the target material has additive `emissiveMap` or `lightMap` terms that do not change the optimized local derivative, and keeps emissive parameter replay when a baked light map is present. Transport/geometry-changing maps such as alpha, transmission/thickness, normal/bump, displacement, and clearcoat-normal still route to finite difference or unsupported paths.
- **Light-map intensity now participates in scoped path-replay adjoint:** `@vitrum/pt-webgpu` mirrors the forward first-hit light-map convention by sampling the readable linear light-map texel and scattering `dot(dLossDRendered, lightMapRadiance)` for `materials.<id>.lightMapIntensity`. This is intentionally a camera-visible baked-radiance derivative, not direct-light, GI, or texel-PDF emitter sampling; normal/bump/clearcoat-normal/env-map controls remain structured finite-difference downgrades.

### Fixed (pt-webgpu AO-map adjoint replay, 2026-06-16)

- **AO maps and AO intensity now participate in scoped path-replay adjoint:** `@vitrum/pt-webgpu` mirrors the forward full-tier `aoMap` sampler in `adjointPass`, including linear R-channel sampling, `aoMapIntensity`, per-map UV/transform/wrap metadata, and the local chain-rule multiplier on `baseColor` gradients for lit direct-light and unlit primary-hit fits. `materials.<id>.aoMapIntensity` now scatters through the derivative of `mix(1, aoR, intensity)`. Normal/bump/alpha/transmission/thickness/displacement maps, clearcoat-normal maps, layered/volume/spectral cases, environment/env-map terms, soft-sun terms, indirect paths, and forward light-selection MIS parity remain finite-difference or unsupported tails.

### Fixed (glTF runtime backend profile bridge, 2026-06-16)

- **Adapter-only glTF loads can now validate against the actual pt-webgpu runtime tier:** `loadGltfForEngine()` accepts `runtimeProfile`, constrained to the selected backend family, so hosts that construct `pt-webgpu` outside `@vitrum/engine/gltf` can validate strict loads against `pt-webgpu-lite` before engine creation. Cross-backend profile mismatches throw immediately instead of silently validating the wrong profile.

### Fixed (walkaround texture-alpha shadow predicates, 2026-06-16)

- **Walkaround opaque/ReSTIR/GI shadow visibility now evaluates atlas alpha coverage:** `@vitrum/walkaround-hybrid` routes shade direct-light shadows, ReSTIR-DI visibility, ReSTIR-GI visibility, NRC GI visibility, and GRIS temporal/spatial reconnection visibility through a material-atlas shadow walk. The new path preserves `castShadow:false` and `skipGlass` semantics while evaluating readable `baseColorMap.a`, `alphaMap.r`, vertex alpha, opacity, and mask/blend coverage at the actual blocker hit. Transparent layers still do not participate as ReSTIR/GI/DDGI/RC transport vertices, and exact texel-PDF emissive selection remains an open tail.

### Fixed (glTF WebGPU spec-gloss roughness bake, 2026-06-16)

- **WebGPU-target glTF texture decode now bakes legacy spec-gloss alpha roughness:** `decodeSceneTextures(target:'webgpu')` / `loadGltfAndDecodeTextures({ textureTarget:'webgpu' })` now synthesize the same linear `roughnessMap` from `KHR_materials_pbrSpecularGlossiness.specularGlossinessTexture.a` that the CPU-linear path already produced, while preserving the decoded `specularColorMap` handle in sRGB form for WebGPU upload.
- **Decoded glTF loads now keep inactive material variants decoded:** `loadGltfAndDecodeTextures()` decodes the importer `convertedMaterials` table in addition to the active scene, and `loadGltfForEngine()` forwards that decoded table into `GltfSceneController`. Switching a `KHR_materials_variants` material after a decoded load no longer reintroduces raw/opaque texture handles through an incremental `updatePrimitive()` patch, and `textureDecodeReport` includes `gltf-material-N` rows for decoded inactive variant materials.

### Fixed (glTF bridge instancing animation metadata, 2026-06-16)

- **Bridge-created glTF controllers now preserve `EXT_mesh_gpu_instancing` animation metadata:** `loadGltfForEngine()` forwards `instancingBindings` from `loadGltfAsset()` into `GltfSceneController`, so the one-call API patches `InstancedMeshPrimitive.instances[]` when an instanced node or ancestor animates instead of emitting an invalid ordinary `transform` patch for the instanced primitive. `gltfAssetApi.test` now covers the public bridge path, complementing the direct controller test.
- **High-level glTF loading now honors point/line fallback sizing:** `loadGltfAsset()` forwards `pointLineFallbackRadius` into `gltfToScene()`, so the public loader and `loadGltfForEngine()` path use the host-selected generated-mesh thickness for glTF `POINTS` / line topology fallback instead of silently falling back to the derived radius.
- **Compressed glTF hook tests now include real codec smoke coverage:** `gltfCompression.test` keeps the runtime adapter dependency-free, but uses dev-only `draco3d` and `meshoptimizer` fixtures to encode/decode tiny Draco and meshopt payloads through the documented host hook APIs.

### Fixed (walkaround GRIS bias wording, 2026-06-16)

- **Stale centroid-pHat bias text was reconciled:** `HybridEngineOptions` and `@vitrum/walkaround-hybrid` README now describe the old centroid-pHat row as closed by the xi-aware `restir_di_compute_phat_xi()` path instead of listing it as an active ReSTIR-DI/GI bias.

### Fixed (glTF animated tangent patches, 2026-06-16)

- **glTF morph and skin animation patches now carry solved tangents:** `GltfSceneController` includes `solveSkin()`'s posed tangent stream when it emits morph-weight or skeleton animation patches, so tangent-space material maps do not keep stale rest-pose tangents after runtime glTF animation. `sceneController.test` covers both morph `TANGENT` deltas and skinned pose patches.

### Fixed (pt-webgl2 skinned tangent consumption, 2026-06-16)

- **CPU-solved skinned tangents now reach the WebGL2 path tracer:** `@vitrum/pt-webgl2` preserves `solveSkin()`'s posed tangent stream for skinned meshes, including `morphTargetTangents` blended by core, instead of dropping tangents and forcing the attribute packer to derive a fallback frame. `skinSolve.test` now pins the backend pre-pass against the shared core solver.

### Fixed (pt-webgl2 analytic primitive fallback, 2026-06-16)

- **Analytic primitives now render on `@vitrum/pt-webgl2` via generated mesh fallback:** `setScene()` accepts core `AnalyticPrimitive` shapes (`sphere`, `box`, `capsule`, `cylinder`, `h-channel-came`) and tessellates them with `analyticPrimitiveToMesh()` before the WebGL2 triangle-BVH upload path. Capabilities and `BACKEND_PROMISE_LEDGER` now report `analytic` / analytic-shape rows as `fallback-generated-mesh` instead of unsupported, and `ledgerVsCapabilities` plus `engineContract` pin the runtime behavior.

### Fixed (glTF point/line fallback geometry, 2026-06-16)

- **Point and line primitive modes now import as generated mesh fallback:** `@vitrum/gltf-adapter` converts glTF `POINTS`, `LINES`, `LINE_LOOP`, and `LINE_STRIP` into deterministic triangle meshes (tiny cubes for points, thin rectangular prisms for line segments) instead of skipping them. The adapter preserves source-path diagnostics as `fallback-generated-primitive-mode`, reports compatibility as `fallback-generated-mesh`, keeps `reject-unsupported` usable for these assets, and still lets `reject-degraded` reject the topology approximation. Generated vertices replicate UV, UV1, COLOR_0, skin weights, and morph deltas from their source vertices where those streams exist.

### Fixed (glTF high-UV material remap, 2026-06-16)

- **Single high material UV sets now import predictably:** `@vitrum/gltf-adapter` remaps a primitive material that references one `TEXCOORD_N` beyond UV1 into the core `uv1` lane when that primitive has a readable `TEXCOORD_N` accessor and no material-visible `texCoord:1` conflict. The adapter clones the primitive material refs to `texCoord:1`, feeds tangent generation from the remapped UVs, and compatibility analysis no longer rejects those lossless cases. Conflicting or missing high-UV cases now drop the affected texture fields with structured `ignored-material-texcoord` diagnostics instead of silently sampling UV0.

### Fixed (pt-webgpu iridescence/anisotropy-map adjoint replay, 2026-06-16)

- **Iridescence and anisotropy maps now participate in scoped path-replay adjoint:** `@vitrum/pt-webgpu` mirrors KHR_materials_iridescence `iridescenceMap` R and `iridescenceThicknessMap` G sampling, collapses the film thickness range to the sampled forward value, mirrors KHR_materials_anisotropy map sampling with B-channel strength and RG-derived rotation offset, evaluates direct-light BRDF partials with those hit-local mapped lobe values, and applies the correct local chain factors to `iridescence` and `anisotropy` gradients. `iridescenceIor` and `anisotropyRotation` use the mapped lobe state without extra multiplier because they are IOR/offset controls. Clearcoat normal maps, normal/visibility/path-selection maps, alpha/light/transmission maps, layered/volume/spectral cases, environment/soft-sun terms, indirect paths, and forward light-selection MIS parity remain finite-difference or validation tails. AO maps are covered by the later same-day AO replay entry.

### Fixed (pt-webgpu clearcoat/sheen-map adjoint replay, 2026-06-16)

- **Clearcoat and sheen maps now participate in scoped path-replay adjoint:** `@vitrum/pt-webgpu` mirrors KHR_materials_clearcoat and KHR_materials_sheen map sampling in `adjointPass`, including linear `clearcoatMap` R, linear `clearcoatRoughnessMap` G, sRGB `sheenColorMap` RGB, and linear `sheenRoughnessMap` A with each map's UV/transform/wrap metadata. The direct-light replay evaluates BRDF partials with the hit-local mapped lobe terms and applies the same local chain factors to `clearcoat`, `clearcoatRoughness`, `sheenColor`, and `sheenRoughness` gradients. Clearcoat normal maps, normal/visibility/path-selection maps, transmission/layered/volume/spectral cases, environment/soft-sun terms, indirect paths, and forward light-selection MIS parity remain finite-difference or validation tails.

### Fixed (pt-webgpu specular-map adjoint replay, 2026-06-16)

- **Specular color/intensity maps now participate in scoped path-replay adjoint:** `@vitrum/pt-webgpu` mirrors KHR_materials_specular map sampling in `adjointPass`, including sRGB `specularColorMap` RGB and linear `specularIntensityMap` A with each map's UV/transform/wrap metadata. The direct-light replay now evaluates BRDF partials with the hit-local mapped specular terms and applies the same local chain factors to `specularColor` and `specularIntensity` gradients. Extension-lobe maps are covered by later same-day entries; normal/visibility/path-selection maps, transmission/layered/volume/spectral cases, environment/soft-sun terms, and indirect paths remain finite-difference or validation tails.

### Fixed (pt-webgpu roughness/metallic-map adjoint replay, 2026-06-16)

- **Roughness and metallic maps now participate in scoped path-replay adjoint:** `@vitrum/pt-webgpu` binds the linear material texture array into `adjointPass`, mirrors the forward ORM sampler for roughness G and metallic B with each map's UV/transform/wrap metadata, evaluates lit direct-light BRDF partials with the hit-local mapped roughness/metallic values, and applies the same local G/B chain factors to scalar `roughness` and `metallic` gradients. Normal/bump/AO/light/alpha/transmission and other nonlocal or path-changing maps remain finite-difference tails until their source terms are mirrored and validated.

### Fixed (pt-webgpu baseColor-map adjoint replay, 2026-06-16)

- **Base-color texture and `COLOR_0` factors now participate in scoped path-replay adjoint:** `@vitrum/pt-webgpu` binds the existing vertex-color stream into `adjointPass`, mirrors the baseColorMap texture descriptor/UV/transform/wrap sampler used by the material path, evaluates lit direct-light BRDF partials with `baseColor * baseColorMap * COLOR_0`, and applies the same local chain-rule factor to RGB `baseColor` gradients, including unlit primary-hit fits. Other material maps, alpha visibility, transmission, layered/volume/spectral cases, environment/soft-sun terms, indirect paths, stochastic area sampling, and GPU inverse-fit recaptures remain finite-difference or validation tails.

### Fixed (pt-webgpu anisotropy adjoint breadth, 2026-06-16)

- **Map-free anisotropy scalars now participate in path-replay adjoint:** `@vitrum/pt-webgpu` adds CPU finite-difference-checked and WGSL-mirrored local partials for KHR_materials_anisotropy `anisotropy` and `anisotropyRotation`, reading the scalar descriptor lanes and differentiating the anisotropic GGX specular lobe in the existing opaque direct-light replay domain. Anisotropy maps are covered by a later same-day entry; normal/alpha/transmissive/layered/volume/spectral materials, environment/soft-sun terms, indirect paths, stochastic area sampling, and GPU inverse-fit recaptures remain finite-difference or validation tails.

### Fixed (walkaround transparent OIT scalar sky parity, 2026-06-16)

- **Transparent blend sky lighting now uses material lobes even without HDRI:** `@vitrum/walkaround-hybrid` removes the diffuse-only no-HDRI fallback from `transparentOit.wgsl.ts`, so scalar/procedural sky radiance now flows through the same deterministic five-tap GGX/specular/clearcoat/sheen/anisotropy/iridescence estimate used for HDRI-backed transparent layers. Transparent ReSTIR direct-light reservoir participation and transparent GI remain approximation tails.

### Fixed (pt-webgpu iridescence adjoint truthfulness, 2026-06-16)

- **Map-free scalar iridescence now participates in path-replay adjoint:** `@vitrum/pt-webgpu` adds CPU finite-difference-checked and WGSL-mirrored partials for KHR_materials_iridescence `iridescence`, reading packed material vec4 #25 and differentiating the thin-film-modified base F0 in the existing opaque direct-light replay domain. `iridescenceIor` now joins that same safe slice via a local symmetric derivative of the thin-film F0 term, still inside a single replay pass rather than a full-render finite-difference probe. `inverseSession` only routes targeted scalar iridescence/iridescenceIor fits to `path-replay`; coupled BRDF+iridescence sessions, iridescence maps, thickness-range/thickness-map gradients, anisotropy, transmission, layered/volume/spectral materials, alpha visibility, environment/soft-sun terms, indirect paths, and stochastic area sampling still degrade to finite difference. The same routing guard now rejects analytic primitives, instanced meshes, and transformed meshes because the replay shader intersects raw triangle buffers only.

### Fixed (glTF/walkaround asset-tail closures, 2026-06-16)

- **glTF generated tangents now honor the selected tangent-space UV set:** `@vitrum/gltf-adapter` generates fallback tangents from `TEXCOORD_1` when a normal/clearcoat-normal/bump map selects `texCoord:1`; `texCoord:2+` and mixed tangent-space map UV channels now emit structured diagnostics instead of silently generating a wrong UV0 tangent frame.
- **Walkaround emissive maps no longer double-apply readable map averages and now feed mirrored TLAS DI payloads:** `packBVHEmissiveLeFromCore()` stores scalar production Le in the per-triangle glow buffer and leaves hit-local emissive-map modulation to the material atlas sampler. ReSTIR-DI source-triangle payloads now cover merged, TLAS, instanced, and mirrored-TLAS material-backed emitters by encoding reversed barycentric orientation in the existing source-triangle lane. A later same-day follow-up replaces material-backed direct-emitter selection averages with UV-local quadrature and fixes the flat-CDF PMF; exact texel-PDF selection, analytic/extra emitter mapped payloads, and GI/RC/DDGI texel-space emission remain promotion tails.

### Fixed (pt-webgpu sheenColor adjoint breadth, 2026-06-16)

- **Map-free `sheenColor` now participates in path-replay adjoint:** `@vitrum/pt-webgpu` adds CPU finite-difference-checked and WGSL-mirrored diagonal RGB partials for KHR_materials_sheen `sheenColor`, scatters a dedicated adjoint field code across the deterministic direct-light replay loops, and routes map-free opaque inverse sessions for `materials.<id>.sheenColor` to `path-replay`. Sheen maps, transmission, layered/volume/spectral fields, environment/soft-sun terms, stochastic area sampling, indirect paths, and GPU inverse-fit recaptures remain finite-difference or validation tails.

### Fixed (pt-webgpu sheen adjoint breadth, 2026-06-16)

- **Map-free scalar sheen now participates in path-replay adjoint:** `@vitrum/pt-webgpu` adds CPU finite-difference-checked and WGSL-mirrored partials for the additive KHR_materials_sheen Charlie lobe (`sheen` and `sheenRoughness`), reads packed material vec4s #23/#24 in the adjoint pass, scatters explicit field codes across the existing deterministic direct-light replay loops, and routes map-free opaque inverse sessions for those fields to `path-replay`. Sheen maps, transmission, layered/volume/spectral fields, environment/soft-sun terms, stochastic area sampling, indirect paths, and GPU inverse-fit recaptures remain finite-difference or validation tails.

### Fixed (walkaround transparent OIT finite emitters, 2026-06-16)

- **Transparent blend layers now receive finite mesh-emitter direct light:** `@vitrum/walkaround-hybrid` transparent OIT reads the existing ReSTIR emitter list, uses four fixed stratified uniform-area samples per finite emitter as a deterministic estimate, evaluates the same atlas-backed GGX/specular/clearcoat/sheen/aniso/iridescence payload used by opaque shade, samples emissive-map-aware `Le` where source-triangle payloads are available, and uses alpha-aware direct shadow transmittance per sample. Reservoir-backed transparent ReSTIR direct-light participation, transparent GI/RC/DDGI/ReSTIR-GI participation, exact texel-PDF emissive selection, and GPU A/B promotion remain open tails.

### Fixed (walkaround transparent OIT shadow filtering, 2026-06-16)

- **Transparent blend layers now attenuate direct shadows through alpha coverage:** `@vitrum/walkaround-hybrid` transparent OIT replaces its binary cast-shadow query for direct sun, analytic point/spot, and finite-emitter direct lights with a material-atlas shadow walk. The new path skips `castShadow:false` and scalar-glass blockers like the old shadow query, hard-blocks opaque/mask hits, and deterministically multiplies visibility by `1 - alphaCoverage` through `alphaMode:'blend'` blockers. Transparent ReSTIR/GI participation and GPU A/B promotion remain open tails.

### Fixed (pt-webgpu emissive-map adjoint replay, 2026-06-16)

- **Camera-direct emissive maps now participate in path-replay adjoint:** `@vitrum/pt-webgpu` binds the existing full-tier UV/texture descriptor/sRGB texture resources into `adjointPass`, mirrors the forward emissive-map sampler including per-map UV set, transform, wrap, heterogeneous-layer fit, and explicit LOD, and multiplies both RGB `emissive` and scalar `emissiveIntensity` gradients by the hit-local emissive texel. `inverseSession` now keeps opaque emissive/emissiveIntensity fits with `emissiveMap` on `path-replay`; alpha maps, BRDF/material maps, transmission, environment/indirect paths, stochastic area terms, and extension-lobe adjoints remain finite-difference tails.

### Fixed (glTF secondary vertex color diagnostics, 2026-06-16)

- **`COLOR_1+` is no longer silently ignored:** `@vitrum/gltf-adapter` now emits structured `ignored-vertex-color-set` diagnostics from `gltfToScene()` for secondary vertex color attributes and reports them as unsupported primitive compatibility issues from `analyzeGltfAsset()`. `COLOR_0` remains the imported core color stream; multi-color-set rendering is still unsupported rather than silently collapsed.

### Fixed (walkaround transparent OIT analytic lights, 2026-06-16)

- **Transparent blend layers now receive analytic point/spot direct light:** `@vitrum/walkaround-hybrid` transparent OIT binds the existing `analytic_lights` texture and adds a deterministic point/spot NEE loop using the atlas-backed GGX/specular/clearcoat/sheen/aniso/iridescence payload, inverse-square falloff, spot cone attenuation, and the shared cast-shadow-aware visibility query. Transparent GI/RC/DDGI/ReSTIR-GI participation and first-hit emissive/light-map semantics remain explicit approximation tails.

### Fixed (pt-webgpu clearcoat adjoint breadth, 2026-06-16)

- **Map-free clearcoat scalars now participate in path-replay adjoint:** `@vitrum/pt-webgpu` adds CPU finite-difference-checked and WGSL-mirrored partials for `clearcoat` and `clearcoatRoughness`, reads packed material vec4 #23 in the adjoint pass, scatters explicit field codes across the existing deterministic direct-light replay loops, and routes map-free opaque inverse sessions for those fields to `path-replay`. Clearcoat maps/normal maps, sheen, iridescence, anisotropy, transmission, environment, indirect, and stochastic area sampling remain finite-difference or validation tails.

### Fixed (walkaround emissive-map DI payloads, 2026-06-16)

- **Merged and TLAS ReSTIR-DI now evaluate material emissive-map texels at the selected light sample:** `@vitrum/walkaround-hybrid` packs a valid source-triangle lane for material-backed emitters and uses a shared WGSL helper in RIS, pHat reuse, and final shade to sample `emissiveMap` at the stored triangle `xi`. TLAS emitters map the world-expanded light triangle back to the local BLAS material-atlas triangle; mirrored TLAS instances and analytic/extra emitters conservatively keep the averaged `Le` fallback. Material-backed CDF/light-tree power is now narrowed by UV-local quadrature; exact texel-PDF selection plus GI/RC/DDGI texel-space emission remain approximate.

### Fixed (pt-webgl2 scattering coefficient semantics, 2026-06-16)

- **`scatteringCoefficientRGB` is now consumed as per-channel σ_s:** `@vitrum/pt-webgl2` no longer packs the field as SSS albedo or ignores RGB-only scattering during activation. The material texture now stores a scalar-majorant `sssSigmaT` plus per-channel `sssSigmaS`, and the GLSL SSS path derives medium albedo as σ_s/σ_t using attenuation-derived σ_a. The backend ledger still marks the row approximate until the scalar-majorant single-scatter model has visual promotion.

### Fixed (glTF/OIT predictable-tail closures, 2026-06-16)

- **Progressive glTF loading now honors strict pt-webgpu runtime tiers:** `loadGltfWithProgressiveEngine()` now applies the same `pt-webgpu` full-vs-lite compatibility gate as `loadGltfWithEngine()` before creating the progressive handoff engine, so `reject-unsupported` / `reject-degraded` cannot pass a lite adapter through the progressive subpath and fail later.
- **Optional Draco fallback analysis matches import behavior:** `analyzeGltfAsset()` no longer marks declaration-only Draco usage or optional Draco primitives with complete uncompressed fallback accessors as `requires-hook`. Required Draco assets and optional Draco primitives without fallback accessors still require a host decoder hook.
- **Transparent OIT sky lighting is material-aware:** `@vitrum/walkaround-hybrid` transparent blend layers now use a deterministic material-lobe HDRI estimate for camera-visible sky/environment lighting, including specular, clearcoat, sheen, anisotropy, iridescence, and `envMapIntensity`. The scalar no-HDRI fallback remains conservative diffuse, and transparent ReSTIR/GI participation is still a validation/promotion tail.
- **Unreadable walkaround material maps now use structured warnings:** `packMaterialTextureAtlas()` carries diagnostics for CPU-unreadable texture handles, and `HybridEngine` emits them through `onWarning` as `walkaround-hybrid.unreadable-material-texture-map` during initial scene BVH publication and material-rebuild updates instead of relying on a packer-local console warning.

### Fixed (inverse/walkaround learned tails, 2026-06-16)

- **Scoped adjoint + transparent/NRC startup tails closed:** `@vitrum/pt-webgpu` path-replay inverse sessions now keep map-free opaque `shadingModel:'unlit'` `baseColor` on the analytic route as a primary-hit contribution identity while mapped or non-baseColor unlit targets still downgrade to finite difference. `@vitrum/walkaround-hybrid` transparent-OIT direct sun now casts through the shared cast-shadow-aware scene visibility query instead of using an unshadowed sun BRDF. Opt-in NRC now exposes `trainedSteps/warmupSteps` in its config UBO and keeps DDGI suffix radiance in the visible reservoir until completed trainer windows satisfy the warm-up gate, while still gathering spread-fired training records immediately.

### Fixed (glTF EXT_mesh_gpu_instancing native import, 2026-06-16)

- **glTF GPU instancing now imports and animates natively:** `@vitrum/gltf-adapter` maps valid node-level `EXT_mesh_gpu_instancing` TRANSLATION/ROTATION/SCALE accessors to core `InstancedMeshPrimitive` records, baking `nodeWorld * instanceTRS` into each instance matrix. `extensionsRequired: ['EXT_mesh_gpu_instancing']` is now accepted, feature reporting predicts the native `instanced-mesh` primitive kind, `GltfSceneController` patches `instances[]` for animated instanced nodes/ancestors, and malformed/skinned/morphed instancing paths keep structured fallback diagnostics instead of silently dropping instance intent.

### Fixed (pt-webgl2 emissive mesh NEE parity, 2026-06-16)

- **Plain glTF-style emissive meshes now feed WebGL2 triangle-light NEE:** `@vitrum/pt-webgl2` now synthesizes mesh-area triangle lights from nonzero material `emissive * emissiveIntensity`, so emissive meshes no longer rely only on path-hit luck unless hosts authored an explicit `mesh-area` emitter. CPU-readable `emissiveMap` handles now modulate synthesized triangle radiance through UV-local quadrature; black readable maps suppress the implicit light, and opaque/unreadable handles warn while keeping scalar emissive fallback. Scalar `updatePrimitive({ material })` fast paths now repack the mesh-light texture when material emission changes, without rebuilding BVH geometry.

### Fixed (pt-webgpu emissive-map emitter power, 2026-06-16)

- **Readable emissive maps now drive implicit mesh-area NEE strength:** `@vitrum/pt-webgpu` implicit emissive-mesh synthesis now multiplies scalar `emissive * emissiveIntensity` by UV-local CPU-readable emissive-map quadrature, decoded through the same sRGB role used by the material texture array. Black readable emissive maps no longer synthesize phantom mesh-area lights, and opaque/unreadable map handles emit a warning while keeping the previous scalar fallback.

### Fixed (walkaround mutation GI invalidation, 2026-06-16)

- **TLAS transform refits now invalidate DDGI probes:** `HybridEngine.updatePrimitive(id, { transform })` in TLAS mode already reset accumulation, marked DDGI instances dirty, and re-synced the shared BVH, but it did not invalidate the DDGI probe atlas. TLAS and merged transform refits now share the physically correct invalidation behavior, and the mutation-matrix seam asserts it.
- **Skinned pose patches no longer no-op in walkaround:** `HybridEngine.updatePrimitive(id, { bones | boneInverses | morphWeights })` now re-solves skinned poses with the canonical `solveSkin` path, routes the solved positions/normals through the existing TLAS/merged refit path, preserves the submitted pose fields in scene state, and invalidates DDGI for GPU-skinned TLAS refits.

### Fixed (glTF animation invalidation, 2026-06-16)

- **glTF controller incremental patches now reset engine temporal state:** `GltfSceneController` calls an Engine-like target's optional `reset()` hook after successful incremental animation or material-variant `updatePrimitive()` batches, while `setScene()` fallback keeps the full-scene invalidation path. `ProgressiveHandoffCoordinator` exposes the same reset hook on its synthetic glTF controller target, so realtime/PT handoff animation remains synchronized and stale temporal/converged histories are invalidated through one predictable mutation path.

### Changed (glTF texture decode reporting, 2026-06-16)

- **Texture decode reports now expose decoded handle color space:** `textureDecodeReport.entries[]` separates the material role `colorSpace` from the decoded payload `handleColorSpace` when the handle supplies a Vitrum color-space hint. This makes `target:'cpu-linear'` vs `target:'webgpu'` auditable: base-color/emissive maps still report the glTF sRGB role, while the decoded handle reports either linear CPU-atlas payloads or sRGB-preserved WebGPU upload payloads. Target-specific decode diagnostics now name the active target instead of hard-coding `cpu-linear`.

### Tests (pt-webgpu ReSTIR-PT specialty proof, 2026-06-16)

- **Static ReSTIR-PT specialty-lobe reference fixture:** `tools/radiometric-ab/ab-restir-pt-specialty.mjs` now pins a deterministic CPU identity check for clearcoat, sheen, iridescence, and anisotropy in the one-sample ReSTIR-PT producer/finalize/resolve path. The focused pt-webgpu test verifies the fixture, producer payload coverage, reservoir serialization fields, and resolve BRDF consumption. This is not a V28 GPU recapture; material-furnace/reference-render promotion remains a validation tail.

### Fixed (ledger truthfulness, 2026-06-16)

- **Walkaround material-support comments no longer carry stale proxy wording:** `consumedMaterialFields.ts` now reflects the implemented GI receiver-lobe material target path, the transparent-OIT direct-sun material-lobe closure, and the existing unsupported-field warning surface. The Road D1 helper-policy row is reconciled as closed: the named helpers are kept with explicit public/test/harness wording instead of being treated as dead runtime code.

### Fixed (pt-webgpu compatibility diagnostics, 2026-06-16)

- **pt-webgl2 and full pt-webgpu now consume face-specific layered normal maps:** `frontLayer.normalMap` / `backLayer.normalMap` and their scales are packed into append-only material descriptor lanes, sampled with the selected face's UV/wrap/transform metadata, and used instead of the top-level normal map when authored. pt-webgl2 also applies the same selection in the stained-glass attenuation path. The pt-webgpu lite tier still emits structured warnings for these nested fields.
- **pt-webgpu no longer silently drops the direct-Scene lite vertex-color case:** the lite tier now warns when primitives carry authored `COLOR_0` vertex colors that require the full-tier vertex-color binding. Full-tier layered normal-map fields are native rather than warned, and material update patches rebuild instead of attempting an unsafe descriptor fast path.
- **Direct glTF bridge can target runtime profiles:** `loadGltfForEngine({ ... }, { backend: 'pt-webgpu-lite' })` now validates against the constrained lite profile while still passing `backend:'pt-webgpu'` to the injected engine factory. Strict adapter-only callers can now reject lite-unsupported assets such as `COLOR_0` scenes before constructing an engine, matching the engine-wrapper runtime-tier gate.
- **Required-extension failures are structured:** unknown entries in `extensionsRequired` now throw `GltfImportError` with an `unsupported-required-extension` diagnostic and exact `extensionsRequired[i]` path, instead of a plain unstructured `Error`.
- **glTF import tangent/camera diagnostics are source-pathed:** tangent generation, missing-UV tangent failure, tangent-generation failure, and ignored cameras now emit `GltfImportDiagnostic` entries with exact `meshes[i].primitives[j].attributes.*` / `cameras[i]` paths while preserving the legacy warning text.
- **glTF texture decode diagnostics now point back to source material slots:** imported `TextureRef`s carry adapter-local source metadata, so `textureDecodeReport`, `decodePixels` context, strict engine texture-readiness failures, max-size resize warnings, NPOT-repeat warnings, and spec-gloss bake diagnostics now report paths such as `materials[i].pbrMetallicRoughness.baseColorTexture` or `materials[i].extensions.KHR_materials_clearcoat.clearcoatTexture`. Hand-built `Scene` inputs still fall back to `scene.primitives[i].material.*`.

### Fixed (walkaround Welford allocation hygiene, 2026-06-16)

- **Non-Welford denoisers no longer allocate full-size Welford aux textures:** `@vitrum/walkaround-hybrid` now threads a Welford ping-pong allocation policy through frame resource creation. The default `atrous-variance` path keeps full-resolution `varianceBufferAux` and `atrousVarianceEstimateTexture`, while `svgf-real`, `oidn-final`, `neural`, and raw/no-denoiser modes receive legal 1x1 placeholders for those unused aux resources. The always-read `varianceBuffer` stays full-resolution, and `hdrTotalTexture` remains full-size because shade writes it through the standard frame bind group.

### Fixed (walkaround transparent OIT composition, 2026-06-16)

- **Camera-visible `alphaMode:'blend'` is no longer stochastic-only:** `@vitrum/walkaround-hybrid` now has a transparent-OIT pass between `indirect-combine` and `temporalAccum`. The opaque shade pass skips fractional blend layers, the new pass ray-walks those layers front-to-back using atlas-backed baseColor/alphaMap/vertex alpha coverage, composites them over the denoised opaque result, and feeds that composited radiance into temporal accumulation. The transparent direct-sun term now reuses the atlas-backed material payload plus the extension-aware GGX/clearcoat/sheen/aniso/iridescence BRDF instead of a diffuse-only sun approximation. The alpha warning now reflects the remaining approximation: transparent sky/light-map terms plus ReSTIR/GI/shadow participation, not missing camera-visible composition.

### Fixed (pt-webgpu direct-light adjoint breadth, 2026-06-16)

- **Path-replay adjoint now covers more direct-light source families:** `@vitrum/pt-webgpu` adjoint replay now differentiates material base/specular params under delta directional, point, spot, and stochastic area-measure rect/disc/mesh-area direct lighting. The pass consumes packed directional/spot/mesh-area buffers, honors point/spot/rect/disc/mesh emitter `castShadow:false` lanes, and uses native disc area plus packed mesh-triangle area terms. Inverse-session routing now allows those covered source families while keeping soft-sun angular diameter, environment, indirect, mapped/transmissive/layered/volume/spectral, forward light-selection MIS parity, and extension-lobe material domains on finite difference until their adjoints are implemented and validated.
- **Metallic path-replay adjoint coverage:** the same scoped direct-light adjoint path now differentiates scalar `metallic` through the opaque base-BRDF diffuse fade-out and F0 blend. The CPU oracle finite-difference test, emitted WGSL shape pins, engine field code, inverse-session routing, pt-webgpu suite, and shader gate cover the new field; GPU inverse-fit recapture remains part of the broader adjoint proof tail.
- **Emissive-strength path-replay adjoint coverage:** the primary-hit emission adjoint now also supports scalar `emissiveIntensity` beside RGB `emissive`. The private adjoint descriptor carries unfactored emissive RGB so the derivative remains valid when intensity is zero; mapped emissive materials still degrade to finite difference.

### Fixed (VitrumCanvas glTF bridge parity, 2026-06-16)

- **React glTF loading now uses the engine-aware bridge:** `<VitrumCanvas gltf=...>` now calls `loadGltfWithEngine()` instead of the asset-only loader, so decoder hooks, texture decode options, compatibility modes, strict pt-webgpu tier checks, and controller metadata use the same path as `@vitrum/engine/gltf`. `attachVitrum` now accepts a preconstructed engine plus structural scene controller, uses that engine for the RAF lifecycle, and re-targets the controller after device-loss auto-recreate.

### Fixed (engine glTF strict existing-engine path, 2026-06-16)

- **Existing pt-webgpu engines now honor strict glTF runtime-tier gates:** `loadGltfWithEngine()` no longer bypasses `pt-webgpu-lite` compatibility checks when the caller supplies an already-created engine. Existing engines are loaded unattached, checked against the actual runtime tier for `reject-unsupported` / `reject-degraded`, then attached only after the gate accepts the asset. This prevents strict one-call glTF loads from validating through the factory path while silently attaching an incompatible lite-tier engine.

### Fixed (walkaround ReSTIR-GI receiver-lobe targets, 2026-06-16)

- **Receiver-lobe ReSTIR-GI target/reuse:** `@vitrum/walkaround-hybrid` GI RIS/NRC producers and temporal/spatial default+GRIS reuse now evaluate candidate and final `pHat` through a material-aware receiver-lobe helper. Diffuse receivers algebraically reduce to the old `luminance(Lo) * cosTheta / pi` target, while rich receivers add the same specular/clearcoat/sheen BRDF proxy used by the GI material-payload path. Default and GRIS GI reuse now bind the scene/material group for receiver recasts; temporal previous-domain recast remains best-effort and falls back to the geometric target when the current UBO cannot reconstruct the previous camera ray exactly. Structural tests, the shader gate, and the full walkaround test suite pass; GPU smoke passed the lavapipe DDGI/RC/ReSTIR-TLAS/DDGI-BVH oracles but the dzn/RTX path timed out during pipeline init, so browser/dzn recapture remains a validation tail.

### Tests (pt-webgpu material-lobe proof, 2026-06-15)

- **Extension-lobe CPU reference oracles:** `@vitrum/pt-webgpu` now has focused CPU tests for clearcoat zero-default/scalar-linearity, sheen zero-default/color/scalar behavior, normalized base/clearcoat/sheen sampled-PDF accounting, and iridescence F0 zero-default behavior. The tests also lock the current sheen PDF as an explicitly documented cosine approximation until a true Charlie-lobe sampler lands. GPU material-furnace/reference A/B remains a promotion tail, not implied closed by these CPU oracles.

### Fixed (adjoint + learned-system tails, 2026-06-15)

- **Rect-area direct-light path-replay adjoint promotion:** `@vitrum/pt-webgpu` now allows path-replay inverse sessions for material adjoint params under point and center-sampled rect-area direct lighting, matching the implemented `adjointPass.wgsl.ts` source terms. Environment, directional, spot, mesh-area, mapped/transmissive/layered/spectral/volume material cases still fall back to finite difference.
- **Neural/NRC proof hardening:** `@vitrum/walkaround-hybrid` now loads and validates the tracked `starter-v1.vitrum-model` and `v2-random.vitrum-model` checkpoints in tests, and NRC record unpacking treats a slot as empty only when the entire encoded-input prefix is zero rather than dropping valid records whose first feature is zero.

### Fixed (glTF WebGPU texture decode bridge, 2026-06-15)

- **WebGPU-target texture decode is no longer a no-op:** `decodeSceneTextures(target:'webgpu')` now resolves raw glTF image handles through the host `decodePixels` hook into CPU-readable `Float32Array` texture handles that pt-webgpu can upload deterministically. Unlike the `cpu-linear` target, WebGPU-target decode preserves backend upload color space: sRGB material maps stay sRGB-valued for WebGPU sRGB texture formats, while normal/scalar/data maps stay linear. `textureDecodeReport` now reflects those handles as CPU-readable instead of leaving `{ kind:'raw-image' }` refs opaque.

### Fixed (walkaround neural/NRC posture, 2026-06-15)

- **Neural checkpoint validation and fallback:** `@vitrum/walkaround-hybrid` now validates `denoiser:'neural'` checkpoints against the U-Net spec before GPU allocation, rejecting missing, unknown, wrong-sized, duplicate, or non-finite layer payloads instead of allowing placeholder buffers. Neural dispatch failures now fall back to raw HDR and surface a fallback `denoiserState` rather than throwing the frame. Enabling `nrcEnabled:true` now emits a structured `walkaround-hybrid.nrc-experimental-biased` warning, tracked research checkpoints are covered by loader/spec tests, and README/ledger wording now distinguishes repo-only research checkpoints from production neural weights.

### Fixed (walkaround material truthfulness, 2026-06-15)

- **Texture-driven alpha blend diagnostics:** `@vitrum/walkaround-hybrid` now emits `walkaround-hybrid.alpha-blend-approximation` for `alphaMode:'blend'` materials whose partial coverage comes from `baseColorMap` or `alphaMap`, not just scalar `opacity`, while still skipping the fully transparent endpoint. The walkaround ledger text was reconciled to the current DI/GI material implementation: rich DI, GI suffix payloads, and receiver-lobe GI targets are implemented, but finite-emitter power, light-map scope, transparent lighting/GI/shadow scope, and GPU A/B promotion evidence keep the rows `approximate`.

### Fixed (walkaround ReSTIR-GI material parity, 2026-06-15)

- **Material-aware ReSTIR-GI suffix radiance:** `@vitrum/walkaround-hybrid` default and NRC GI-RIS producers now apply atlas-backed smooth/normal/bump normals at primary, post-glass, and bounce vertices, and compute reconnection-vertex `Lo` through the shared material payload. Default diffuse suffixes still use mapped albedo × irradiance/π; rich suffix materials (metal/glossy/specular/clearcoat/sheen/anisotropy/iridescence) use the extension-aware GGX/clearcoat/sheen BRDF proxy. NRC training records now use the same mapped albedo/roughness payload as the suffix query. The 2026-06-16 receiver-lobe target wave builds on this path; remaining work is GPU A/B promotion evidence rather than an unimplemented receiver target.

### Fixed (pt-webgpu lite emitter shadows, 2026-06-15)

- **Lite directional `castShadow:false` parity:** `@vitrum/pt-webgpu` now sign-encodes the first directional emitter's `castShadow:false` flag into the existing `cameraPos.w` angular-diameter UBO mirror and decodes it in the lite directional NEE path, so lite directional lights can skip the visibility ray just like packed full-tier directional records. The pt-webgpu emitter-shadow ledger remains `approximate` until the remaining off-default caustic/BDPT shadow-flag proof is closed.

### Fixed (glTF engine bridge, 2026-06-15)

- **Actual-backend strict compatibility:** `loadGltfForEngine()` now reads `engine.backendId` from existing or factory-created engines, reports that actual backend in the bridge result, and reruns strict compatibility against it before `setScene()`. Best-effort mode can still tolerate factory fallback, but strict loads can no longer validate against one backend and silently attach another.
- **Strict import diagnostics and texture readiness:** `loadGltfForEngine()` now folds structural converter diagnostics and backend texture-readiness rows into `reject-unsupported` / `reject-degraded`. Skipped geometry such as missing `POSITION`, unresolved compression, unreadable indices, and unsupported primitive modes reject before engine construction, while opaque texture handles reject in `reject-degraded` unless the host explicitly opts in with `opaqueTextureHandlesReady`.

### Fixed (progressive engine handoff, 2026-06-15)

- **Coordinator scene fallback seeding:** `createProgressiveEngine()` now passes its initial scene into `ProgressiveHandoffCoordinator`, enabling the coordinator's existing `setScene()` fallback when a realtime or converged backend cannot apply an incremental primitive patch. This keeps progressive mutation orchestration predictable instead of throwing solely because the factory path omitted the authoritative scene.

### Fixed (walkaround ReSTIR-DI material parity, 2026-06-15)

- **Extension-aware ReSTIR-DI target scoring:** `@vitrum/walkaround-hybrid` now threads material-atlas payloads through DI `PrimarySurface` reconstruction and evaluates RIS candidate weights, finalization, temporal reuse, and spatial reuse with the same extension-aware BRDF used by shade-owned direct lighting. ReSTIR-DI pHat now consumes base/vertex/roughness/metallic maps, normal/bump/clearcoat-normal perturbations, specular scalar/color maps, clearcoat, sheen, anisotropy, iridescence, and env-map intensity instead of the old albedo/rough/metal-only proxy. Reservoir storage is unchanged; this is render-changing for rich glTF materials and still needs GPU A/B promotion evidence.

### Tests (pt-webgpu BDPT, 2026-06-15)

- **BDPT light-side material payload numeric oracle:** `bdptGlossyLightSubpath.test.ts` now supplements the A9 WGSL structure pins with independent arithmetic checks for row-4 tri/front-face payload packing, mapped base/vertex/AO/ORM/transmission/extension material transforms, front/back layer selection, thin-film tint mixing, Cauchy IOR, and Jakob-Hanika spectral reflectance override. This hardens the BDPT material parity proof without changing renderer behavior; GPU material-furnace/radiometric A/B evidence remains a validation-tail item.

### Fixed (pt-webgpu inverse adjoint, 2026-06-15)

- **KHR_materials_specular path-replay adjoint coverage:** `@vitrum/pt-webgpu` now differentiates `specularColor` and `specularIntensity` in the path-replay adjoint fast path, reads packed material vec4 #27 in the adjoint pass, and keeps `baseColor` / `roughness` derivatives aligned with non-default dielectric F0. Path-replay selection is now field/material/light aware: point and center-sampled rect-area direct-light scenes can use the adjoint path; rich mapped, transmissive, layered, anisotropic, unlit, environment-lit, disc-area, spot/directional/mesh-lit, and non-emissive extension-lobe cases degrade to finite difference instead of taking a scoped analytic path outside its domain.

### Fixed (pt-webgpu material PDFs, 2026-06-15)

- **Layered transmissive BSDF source/PDF coherence:** `@vitrum/pt-webgpu` transmissive dielectric materials now share the same normalized base/clearcoat/sheen sampled-density model as opaque materials. The base lobe remains the Fresnel reflect/refract dielectric partition, while clearcoat and sheen can now be sampled as same-side reflection lobes on glass; `brdfDirectionalPdfFullSampledWithClearcoatNormal()` no longer drops transmissive materials to a base-only PDF. This is render-changing for glass materials with clearcoat/sheen and still needs reference A/B promotion evidence.

### Fixed (glTF strict runtime profile, 2026-06-15)

- **`@vitrum/engine/gltf` runtime lite-profile enforcement:** strict one-call glTF loads now evaluate the actual runtime `pt-webgpu-lite` compatibility row before constructing an engine. `reject-unsupported` rejects lite-tier assets with unsupported rows, `reject-degraded` rejects any unsatisfied non-native lite row, best-effort remains non-blocking, and decoded spec-gloss roughness bakes / required host hooks are honored before deciding whether a lite-row issue is still active.
- **Core material-map contract drift:** `MaterialMapFields` now includes `thicknessMap`, matching the first-class `MaterialSpec.thicknessMap` / glTF KHR volume import path and preventing adapter/backend material-map slices from accidentally omitting the field.

### Fixed (walkaround memory layout, 2026-06-15)

- **`@vitrum/walkaround-hybrid` compact default ReSTIR-GI reservoirs:** the default `restirPtReuse:false` path now allocates and compiles the compact 20-u32 Sprint-16/17 GI reservoir layout instead of paying for the 30-u32 GRIS reconnection cache. Opt-in GRIS/ReSTIR-PT reuse still compiles the widened layout, GI snapshot import/export checks the live stride, and the PPG training kernel bakes the same stride as the active GI reservoir variant.

### Fixed (pt-webgl2 diagnostics, 2026-06-15)

- **Mesh-area material mutation coherence:** `@vitrum/pt-webgl2` material fast-path patches on a primitive referenced by a `mesh-area` emitter now preserve the folded emissive radiance in the GPU material texture while still updating the authored scalar material fields. This keeps camera/path-hit emission and mesh-light NEE in sync without rebuilding BVH geometry.
- **Mutation fallback diagnostics:** `@vitrum/pt-webgl2` now emits deduped structured `EngineWarning`s when `updatePrimitive()` transform/positions/topology/material-map patches, `addPrimitive()`, or `removePrimitive()` take the supported scene-texture/BVH rebuild path instead of a targeted native patch. Scalar material, emitter, and environment fast paths remain quiet.
- **Structured texture/HDRI ingestion warnings:** `@vitrum/pt-webgl2` now routes unreadable material texture handles, unreadable HDRI payloads, and ambiguous texture pixel strides through the public `EngineWarning` channel during `setScene()` and the `updateEnvironment()` fast path, while preserving standalone packer `console.warn` behavior. This closes the old H7 silent-drop diagnostic gap for pt-webgl2 texture/environment ingestion.
- **Road ledger reconciliation:** source-verified and struck stale pt-webgl2 H7 residuals for pinned internal trace flags, honest caustic-strategy approximation labeling, and lite-tier behavior, and updated `@vitrum/dev` README to reflect the shipped `MaterialInspector` click-to-pick path.
- **Reference-render tooling docs:** stale reference-render READMEs and the PR-hybrid manifest no longer advertise removed `capture:refs:*`, `seed-wg0`, or `pr-hybrid` workflows; they now point at the live `benchmark:gap-closure` capture-adapter flow, `validate:gpu:smoke`, or explicitly mark old artifacts as archive/provenance.

### Tests (glTF behavioral gate, 2026-06-15)

- **One-call decoded glTF render gate:** `tools/behavioral-gate` now forces the `pt/gltf-textured-pbr` fixture through `loadGltfForEngine({ decodeTextures:true, textureTarget:'cpu-linear', decodePixels })`, asserts decoded texture counts and a backend-ready `textureDecodeReport`, verifies the engine attaches the decoded controller scene, and renders the decoded pt-webgpu asset.
- **Spec-gloss roughness bake bridge pin:** `loadGltfForEngine()` now has a focused best-effort regression proving decoded `KHR_materials_pbrSpecularGlossiness.specularGlossinessTexture.a` creates a generated CPU-linear `roughnessMap` and attaches that decoded scene to the engine, matching the existing direct `loadGltfAndDecodeTextures()` bake coverage.

### Added (glTF sampler metadata, 2026-06-15)

- **`@vitrum/core` + `@vitrum/gltf-adapter` sampler intent preservation:** `TextureRef` now carries authored `magFilter`, `minFilter`, and `mipFilter` metadata in addition to UV channel, `KHR_texture_transform`, and wrap modes. The glTF adapter maps all six glTF minification constants plus NEAREST/LINEAR magnification into those fields, and `textureDecodeReport` exposes the same sampler policy with `usesMipmaps` for authored mipmapped modes. Backend per-texture filter/mipmap enforcement remains a separate renderer policy rather than silently implied by import success.
- **glTF sampler compatibility diagnostics:** `analyzeGltfAsset()` now inventories authored sampler policies per material texture, and backend compatibility adds `*.samplerPolicy` approximate issues when a selected backend imports the texture but cannot guarantee exact per-texture filter/mipmap behavior. `loadGltfForEngine(..., compatibilityMode:'reject-degraded')` rejects those sampler-policy degradations before constructing an engine.

### Added (pt-webgl2 OIDN final-pass, 2026-06-15)

- **`@vitrum/pt-webgl2` OIDN final denoiser:** `denoiser: 'oidn-final'` now has a real async final-pass path instead of degrading to disabled. The backend reads linear HDR plus full-tier albedo/normal MRT aux into OIDN RGB tensors, runs the shared OIDN dispatcher once converged, reports `FrameStats.denoiserState`, exposes `getLatestDenoised()`, invalidates on reset, and releases cache state on dispose. The promise ledger now marks pt-webgl2 `oidn-final` as `native`; realtime denoisers still warn/degrade as unsupported.

### Added (pt-webgpu inverse rendering, 2026-06-15)

- **Finite-difference extension-lobe material params:** `@vitrum/pt-webgpu` inverse sessions now accept common scalar/RGB material extension fields for optimization, including specular, clearcoat, sheen, iridescence, and anisotropy controls. This was the initial finite-difference-safe landing; later 2026-06-15/16 bullets promote specular controls plus map-free clearcoat and sheen controls into the scoped path-replay direct-light domain, while sheen maps, iridescence/aniso, and mapped variants remain finite-difference.

### Fixed (D1 source reconciliation, 2026-06-15)

- **Walkaround/RC stale documentation cleanup:** active READMEs and source comments now reflect the raw WebGPU package boundary, live PPG sTree splitting, intentionally truncating `expandIndicesToStride4()` helper behavior, and CPU-oracle-only PPG helper functions. Package-boundary tests now reject the old `three/webgpu`, single-global-cell PPG, and RC receiver-export claims.

### Fixed (glTF diagnostics, 2026-06-15)

- **`@vitrum/gltf-adapter` double-sided material diagnostics:** `gltfToScene()` now emits a structured `double-sided-material` diagnostic with the original `materials[i].doubleSided` path while preserving the flag in `MaterialSpec.extensions.doubleSided`. The adapter README also now reflects current COLOR_0 support: pt-webgl2 and full-tier pt-webgpu consume vertex colors natively, while pt-webgpu lite and walkaround-hybrid report unsupported compatibility rows.

### Fixed (walkaround material diagnostics, 2026-06-15)

- **`@vitrum/walkaround-hybrid` unsupported material-field diagnostics:** analytic primitives now participate in the unconsumed-material scan before their render-scene mesh fallback, and `updatePrimitive(id, { material })` runs the same structured `walkaround-hybrid.unconsumed-material-fields` warning path as `setScene()`. Unsupported displacement fields remain unsupported, but no longer slip through analytic or material-only mutation paths silently.
- **`@vitrum/walkaround-hybrid` fractional alpha-blend mutation diagnostics:** `updatePrimitive(id, { material })` now emits the same structured `walkaround-hybrid.alpha-blend-approximation` warning as `setScene()` when a material patch introduces fractional `alphaMode:'blend'`, so incremental material edits cannot silently bypass the transparent lighting/GI/shadow approximation warning.

### Tests (mutation observability, 2026-06-15)

- **`@vitrum/pt-webgpu` mutation matrix observability:** router tests now assert that incremental `updateEmitter()` writes the emitter GPU buffer, commits scene state, and resets accumulation, while same-sized HDRI `updateEnvironment()` writes the environment texel/CDF buffers, updates environment metadata, commits scene state, and resets without falling through to `setScene()`.

### Fixed (pt-webgpu lite area-light MIS, 2026-06-15)

- **`@vitrum/pt-webgpu` lite rect/disc area-light MIS:** the lite trace now pairs rect/disc next-event estimation with a BSDF-sampled ray-light connection against the same `liteLightTex` records. `kernelLite` restores the light-sampled power heuristic, `connectLite` intersects BSDF directions against packed rect/disc records, and `oracle.liteRectMis.test.ts` now proves the paired light-sampled + BSDF-sampled shares recover the independent solid-angle ground truth.

### Fixed (pt-webgpu directional lights, 2026-06-15)

- **`@vitrum/pt-webgpu` in-medium directional NEE:** volumetric single-scatter NEE now loops the packed N-directional light buffer instead of the legacy scalar `params.lightDir.w` mirror, preserving RGB irradiance, multiple directionals, and `castShadow:false` for directional emitters. `volumetricSss.test.ts` pins the no-scalar-regression path, and `npm run shader-gate` compiles the updated full/composite trace shaders.
- **`@vitrum/pt-webgpu` ReSTIR-PT suffix direct lighting:** `rptDirectAtVertex()` now loops the same packed N-directional RGB records instead of the scalar lightDir mirror, preserving chroma, multiple directionals, and directional `castShadow:false` in ReSTIR-PT suffix Lo. `restirPtReuseContract.test.ts` pins the no-scalar-regression path.
- **`@vitrum/pt-webgpu` MNEE transmissive cone-search approximation:** the legacy glass cone-search fallback now iterates packed RGB directional-light records and honors the directional shadow-disable sign bit instead of using the scalar `params.lightDir.w` mirror. `mneeRefractionCaustic.test.ts` pins the no-scalar-regression path; the path remains documented as an approximation pending true manifold parity.
- **`@vitrum/pt-webgpu` BDPT light-subpath bounce-0:** BDPT now counts and power-samples the packed N-directional RGB light records, emits directional/environment pseudo-distant vertices from current scene center/radius instead of `-dir * 50.0`, and mirrors the policy in the CPU BDPT oracle. `bdptGlossyLightSubpath.test.ts`, `bdptEmitterPickCpu.test.ts`, `scenePack.test.ts`, and FrameParams layout/packing tests pin the no-scalar/no-fixed-radius path.

### Tests (WebGPU stub validation, 2026-06-15)

- **`@vitrum/pt-webgpu` GPU stub hardening:** the size-validating mock `GPUDevice` now records buffer usage flags and bind-group layouts, rejects zero/invalid usage, validates bind-group buffer ranges, enforces `minBindingSize`, and checks that uniform/storage bindings carry the required `GPUBufferUsage` bit. This closes the pt-webgpu side of the H53 dummy-buffer recurrence class; adapter-backed WebGPU shader/PASS_ORDER gates remain tracked separately.
- **`@vitrum/walkaround-hybrid` sizing GPU stub hardening:** the walkaround mock `GPUDevice` now records layout/bind-group calls, rejects invalid buffer usage, validates missing/duplicate/unknown bind-group entries against the layout, catches range overflows and empty ranges, enforces layout-derived `minBindingSize`, rejects buffer resources in texture slots, and checks required `UNIFORM`/`STORAGE` usage bits. `dummyBufferSizing.test.ts` now pins those H53 guard rails.
- **WGSL/PASS_ORDER gate reconciliation:** source-verified that root `npm run shader-gate` already compiles the pt-webgpu, walkaround-hybrid PASS_ORDER, shared-denoiser, and walkaround-rc WGSL inventory, and that CI runs it with Mesa lavapipe. The Road now narrows the remaining D10/H56 proof work to a stronger pipeline-layout creation gate plus non-mirrored WGSL behavior oracles, not a missing parse gate.
- **WGSL pipeline-creation gate:** `tools/shader-gate/gate.mjs` now goes beyond `createShaderModule` and creates adapter-backed compute/production pipeline variants: pt-webgpu full/lite/ReSTIR-PT/SPPM entries, shared-denoiser kernels, walkaround-rc kernels, and walkaround production default/GRIS/PPG/ReGIR/NRC layouts through the real `compilePipelines()` path.
- **`@vitrum/pt-webgpu` cpuTracer drift tripwire:** `cpuTracerDriftTripwire.test.ts` now stores literal frozen hashes for the mirrored WGSL function bodies instead of deriving its "frozen" table from live shader strings at module load. Shader edits to the mirrored functions now fail unless the CPU mirror and hash pins are updated together.
- **`@vitrum/pt-webgpu` material-texture sampler oracle:** `materialTextures.test.ts` now pins repeat/clamp/mirror wrapping, KHR/THREE UV transform math, and post-wrap UV-fit scaling with CPU expectations instead of only shader substring checks.
- **`@vitrum/pt-webgl2` environment-none upload guard:** `uploadGapGuard.test.ts` now proves `environment:'none'` uploads `envMapInfo.totalSum=0` and `environmentIntensity=0`, while a raw HDRI scene uploads a positive environment sum and authored intensity. This closes the pt-webgl2 side of the phantom-sky regression class with a pipeline-level guard.
- **`@vitrum/pt-webgpu` ReSTIR-PT temporal weight oracle:** `restirPtReuseContract.test.ts` now numerically pins pairwise-GRIS current/previous weights, proves reused-reservoir weight is independent of `pdfSrc`, and verifies final GRIS `W = w_sum / pHat` is capped without `M` normalization.
- **`@vitrum/pt-webgpu` MNEE half-vector CPU oracle:** `mneeNewton.test.ts` now verifies the analytic flat-mirror reflection point independently of WGSL substrings: the mirror-image intersection zeroes the half-vector residual, while a shifted vertex remains nonzero.
- **`@vitrum/pt-webgpu` volume-thickness behavior oracle:** `volumetricSss.test.ts` now pins material vec4 #28 packing and the `materialAttenuationDistance` slab clamp with independent CPU expectations, including negative-segment handling and finite Beer-Lambert absorption for an infinite ray segment through an authored KHR volume slab.
- **`@vitrum/walkaround-hybrid` Welford/sample-budget behavior oracle:** `sprint9-10a-welford.test.ts` now checks Welford mean/M2 variance against an independent two-pass sample-variance reference and pins sample-budget first-frame plus threshold-tier semantics, reducing H55's formerly string-only adaptive-sampling residue.
- **`@vitrum/walkaround-hybrid` GTAO upsample behavior oracle:** `gtaoQuarterRes.test.ts` now checks the per-channel bilateral upsample against an independent CPU reference for equal-surface averaging, depth-edge weighting, and the zero-weight fallback average.
- **`@vitrum/walkaround-hybrid` OIDN failure degradation proof:** `oidnFinalDenoiser.test.ts` now pins dispatch-time `denoiseFinal()` failure behavior: the denoiser falls back to raw HDR, reports a retryable `failed` state with the OIDN error reason, and retries successfully on the next dispatch.

### Fixed (glTF spec-gloss texture alpha, 2026-06-15)

- **`@vitrum/gltf-adapter` archived specular-glossiness texture bake:** `loadGltfAndDecodeTextures()` / `decodeSceneTextures()` now synthesize a linear `roughnessMap` from `KHR_materials_pbrSpecularGlossiness.specularGlossinessTexture` alpha using `roughness = 1 - glossinessFactor * alpha`, preserving texCoord, `KHR_texture_transform`, and sampler wrap metadata. Pre-decode planning still reports the legacy extension as approximate because a host pixel decoder is required for the bake.

### Fixed (skinned morph tangent consumption, 2026-06-15)

- **`@vitrum/core` + skinned backends:** `solveSkin()` now returns a solved `tangents` stream when rest tangents exist, blends glTF `morphTargetTangents` before skinning, and preserves base tangent handedness. `@vitrum/pt-webgpu` carries those solved tangents through initial scene packing and bones/morph-weight mutation fast paths, while `@vitrum/walkaround-hybrid` routes tangent-bearing skinned meshes through the CPU skin fallback until its GPU skin kernel grows tangent writes. glTF compatibility remains `approximate` for GPU-native tangent skinning, not because tangent deltas are dropped.

### Tests (material contract gates, 2026-06-15)

- **Material promise gate hardening:** added pt-webgpu descriptor/WGSL regression pins for native `aoMapIntensity`, `lightMapIntensity`, and `envMapIntensity` scalar lanes, plus a walkaround-hybrid `setScene` structured-warning fixture for unsupported displacement and environment-intensity material fields.

### Fixed (pt-webgpu ReSTIR-PT emitter shadows, 2026-06-15)

- **`@vitrum/pt-webgpu` ReSTIR-PT / MNEE `castShadow:false` parity:** the ReSTIR-PT producer consumes packed point, spot, rect/disc, and mesh-area emitter shadow-disable lanes before firing suffix direct-lighting visibility rays. Follow-up closure now mirrors emitter shadow flags through BDPT bounce-0 endpoints and all point/finite-area MNEE light-leg visibility checks. The ledger remains `approximate` only for the off-default SPPM photon-map no-shadow source-treatment model.

### Fixed (pt-webgl2 mesh-area emitter shadows, 2026-06-15)

- **`@vitrum/pt-webgl2` mesh-area `castShadow:false` NEE path:** mesh-area triangle-light slots now pack the emitter shadow-disable flag in `uMeshLights` s5.g, `sampleMeshAreaLight()` carries it through `LightRecord`, and the mesh-area next-event-estimation branch skips attenuation/occlusion for those lights just like analytic emitters. Follow-up closure added a folded mesh-emitter material flag so ordinary forward emissive-hit MIS skips shadow-disabled emitters while camera/specular-visible emission remains visible; the pt-webgl2 promise row is now native.
- **`@vitrum/pt-webgl2` BDPT emitter shadow metadata:** the BDPT light-subpath texture now has a fourth metadata row carrying emitter-endpoint `castShadowDisabled`, and BDPT direct connections to that endpoint skip visibility when the source light has `castShadow:false`.

### Added (walkaround bump maps, 2026-06-15)

- **`@vitrum/walkaround-hybrid` bump-map visible-normal perturbation:** readable `bumpMap` handles now ride the material atlas as linear height fields with uv0/uv1, wrap, and texture-transform metadata preserved. `bumpScale` is stored in atlas metadata, and `shade.wgsl` applies a finite-difference height-gradient perturbation after normal-map application. The promise ledger grades these rows `approximate`, not `native`, because ReSTIR/GI candidate PDFs and payloads still use the base visible-normal path.

### Added (walkaround volume thickness maps, 2026-06-15)

- **`@vitrum/walkaround-hybrid` KHR_materials_volume thickness maps:** readable `thicknessMap` handles now ride the material atlas as linear data with uv0/uv1, wrap, and transform metadata preserved. The shade/transmitted-GI/tinted-visibility glass paths sample `thicknessTexture.g` and exponentiate the scalar Beer-Lambert tint by that factor, so the promise ledger grades the row `approximate` rather than `unsupported` because walkaround still uses its pre-baked scalar attenuation model instead of exact thin-shell volume integration.

### Fixed (pt-webgpu BDPT finite-area light tracing, 2026-06-15)

- **`@vitrum/pt-webgpu` BDPT finite-area emitter extension:** the light-subpath extension now applies the required `cos/pdfΩ = π` factor after tracing from a finite area emitter sampled in area measure. `oracle.bdptConnectionCosine.test.ts` now contains independent rendering-equation oracles for the emitter endpoint, one-bounce diffuse light tracing, and a non-Lambertian light-vertex connection, closing `PTWG-BDPT-01` without relying on a mirrored shader assembly.

### Added (walkaround procedural sky bake, 2026-06-15)

- **`@vitrum/walkaround-hybrid` Preetham procedural sky:** `procedural-sky` environments now bake through the shared Preetham equirect helper into the same directional IBL map/CDF path used by raw HDRI payloads. Runtime `updateEnvironment()` uploads the baked 256x128 sky, threads it to DDGI probe misses when env bindings are available, and keeps scalar `skyTint`/`skyIrradiance` only as a fallback average. The ledger remains `approximate` for finite bake/model limits rather than scalar-only data loss.

### Added (walkaround authored tangent consumption, 2026-06-15)

- **`@vitrum/walkaround-hybrid` authored/generated tangent TBN:** walkaround now carries tangent.xyzw through both BVH modes. `packSceneFromCore()` tangents feed TLAS scenes, `mergeWorldSpaceFromCore()` transforms merged-world tangents and flips handedness for mirrored transforms, `BvhBufferHost` uploads the stream as a scene-bound `rgba32float` tangent texture, and `materialAtlas.wgsl` prefers authored/generated tangents for normal and clearcoat-normal maps before falling back to UV-gradient derivation. The affected material rows remain `approximate` only because ReSTIR/GI payload/PDF parity is still narrower than visible shade-owned lobes.

### Added (shared Preetham procedural sky, 2026-06-14)

- **`@vitrum/shared-samplers` + PT backends procedural sky:** `bakePreethamSkyEquirect()` now provides a shared CPU Preetham daylight bake. `@vitrum/pt-webgl2` uses it to route `procedural-sky` environments through the existing equirect HDRI/CDF upload path, promoting that environment kind from unsupported to approximate; `@vitrum/pt-webgpu` now consumes the same shared bake helper so the two PT backends cannot drift on sky radiance.

### Added (glTF proof coverage, 2026-06-14)

- **`@vitrum/engine` + `@vitrum/gltf-adapter`:** progressive glTF loading tests now assert `textureDecodeReport` and warning passthrough, while the texture sweep covers enabled `MSFT_texture_dds` alternate-source selection through `loadGltfAsset()` without losing material-map report entries.
- **`@vitrum/gltf-adapter`:** `loadGltfAndDecodeTextures()` now invokes `decodeSceneTextures()` when a host `decodePixels` hook is supplied, returning decoded/unchanged texture counts, structured diagnostics/warnings, and a refreshed backend-readiness report instead of acting as a report-only alias.
- **`@vitrum/gltf-adapter` + `@vitrum/engine/gltf`:** glTF import warnings now include a structured `diagnostics` array with stable codes and source paths for converter-owned degradations such as ignored cameras, rest-pose skins, point/line fallback topology, ignored GPU instancing, missing scenes, unresolved compression, and skipped required geometry. `loadGltfForEngine()` and `loadGltfWithProgressiveEngine()` pass those diagnostics through.
- **`@vitrum/gltf-adapter` texture decode max-size handling:** `decodeSceneTextures(target:'cpu-linear', { maxTextureSize })` now downsamples oversized decoded raw-image payloads with deterministic nearest-neighbor sampling before backend upload, while preserving a structured source-path diagnostic with original and resized dimensions.

### Added (pt-webgl2 resize API, 2026-06-14)

- **`@vitrum/pt-webgl2`:** added `PTEngineWebGL2.setSize(width, height)` as a native render-target resize path. It stores the explicit host canvas size, reallocates existing accumulation/present targets without scene/BVH rebuild, and resets accumulation; the core promise ledger now advertises pt-webgl2 resize as `native`.

### Fixed (engine H31 residue, 2026-06-14)

- **`@vitrum/engine` attach/progressive diagnostics:** `attachVitrum()` handles now expose a live `backendId` getter so hosts can see the currently selected backend even after auto-recreate swaps the underlying engine. `createProgressiveEngine()` now has a focused regression test proving final WebGPU canvas `configure()` failures surface through `onError` as `{ phase:'canvas-configure', backend:'walkaround-hybrid', recoverable:true }`.
- **`@vitrum/gltf-adapter` UV-set compatibility diagnostics:** material textures that request `TEXCOORD_2+`, including `KHR_texture_transform.texCoord` overrides, now produce structured `unsupported` compatibility issues with exact source paths. This makes the current core `uvs` / `uv1` limit explicit during backend ranking instead of allowing higher UV sets to degrade silently.
- **`@vitrum/gltf-adapter` texture-source hook diagnostics:** `analyzeGltfAsset()` now reports `extensions.textureSourceUses[]` entries for `KHR_texture_basisu`, `EXT_texture_webp`, and `MSFT_texture_dds`, including texture index, alternate image index, source path, required/fallback status, MIME type, and whether the use actually requires a host hook. This keeps compressed-source policy explicit without pretending Vitrum bundles a KTX2/Basis/WebP/DDS transcoder.
- **`@vitrum/engine/gltf` progressive helper:** new `loadGltfWithProgressiveEngine()` loads a glTF asset, enforces the `pt-webgpu` compatibility profile used by the converged progressive backend, constructs the glTF scene controller, and wires that controller into `createProgressiveEngine()` for a one-import glTF → realtime/progressive handoff path.
- **`@vitrum/shared-denoisers` one-shot WebGPU cleanup:** `runHdrLuminanceBilateralWebGPU`, `runAtrousVarianceWebGPU`, and `runSVGFRealWebGPU` now destroy transient textures/buffers and release ephemeral devices from `finally` when upload/dispatch/readback/remodulation fails. A new cleanup regression test forces readback failure across all three entry points, and `ATROUS_VARIANCE_WGSL` now has a package-level gate proving the direct luminance helper include remains standalone.

### Added (walkaround roughness/metallic/AO atlas slice, 2026-06-14)

- **`@vitrum/walkaround-hybrid` roughness/metallic/AO map consumption:** the material texture atlas now packs readable `roughnessMap`, `metallicMap`, and `aoMap` handles alongside `baseColorMap`, preserving per-map uv0/uv1 selection, wrap modes, and `KHR_texture_transform` metadata. `shade.wgsl` samples roughness from the glTF G channel, metallic from the B channel, and AO from the R channel, with `aoMapIntensity` carried in the per-triangle material word and applied with glTF occlusion-strength semantics. The promise ledger grades these rows `approximate` because upstream reservoir/GI payloads still use scalar packed lanes.
- **`@vitrum/walkaround-hybrid` scalar KHR_materials_specular controls:** the material texture atlas now packs `specularColor.rgb` plus `specularIntensity` as per-triangle metadata, and shade-owned direct, analytic, sun, and glossy-indirect GGX paths modulate dielectric F0 from those controls. The promise ledger grades these rows `approximate` because ReSTIR candidate PDFs/payloads and specular maps are not yet specular-control complete.
- **`@vitrum/walkaround-hybrid` KHR_materials_specular texture maps:** readable `specularColorMap` and `specularIntensityMap` handles now ride the material texture atlas, preserving per-map uv0/uv1, wrap, and texture-transform metadata. Shade-owned GGX paths multiply scalar specular color by the sRGB RGB map and scalar specular intensity by the linear alpha map; the ledger grades the rows `approximate` because ReSTIR candidate PDFs/payloads remain scalar.
- **`@vitrum/walkaround-hybrid` clearcoat and sheen texture maps:** readable `clearcoatMap`, `clearcoatRoughnessMap`, `sheenColorMap`, and `sheenRoughnessMap` handles now ride the material texture atlas with per-map uv0/uv1, wrap, and texture-transform metadata. Shade-owned top-coat and Charlie sheen lobes multiply scalar controls by glTF R/G/RGB/A channels respectively; the ledger grades these rows `approximate` because ReSTIR candidate PDFs/payloads remain base-lobe-only.
- **`@vitrum/walkaround-hybrid` clearcoat normal maps:** readable `clearcoatNormalMap` handles now ride the material atlas with `clearcoatNormalScale` metadata and perturb a separate top-coat normal for shade-owned clearcoat direct, analytic, sun, and glossy-indirect paths. The ledger grades the rows `approximate` because ReSTIR candidate PDFs/payloads remain base-lobe-only.
- **`@vitrum/walkaround-hybrid` anisotropy controls:** scalar `anisotropy` / `anisotropyRotation` plus readable `anisotropyMap` handles now ride the material atlas and switch shade-owned GGX direct, analytic, sun, and glossy-indirect paths to an anisotropic branch. The ledger grades these rows `approximate` because ReSTIR/GI reservoir PDFs/payloads remain isotropic.
- **`@vitrum/walkaround-hybrid` iridescence controls:** scalar `iridescence` / `iridescenceIor` / `iridescenceThicknessRange` plus readable `iridescenceMap` / `iridescenceThicknessMap` handles now ride the material atlas and modify shade-owned GGX F0 with the thin-film helper used by pt-webgpu. The ledger grades these rows `approximate` because ReSTIR/GI reservoir PDFs/payloads remain base-lobe-only.
- **`@vitrum/walkaround-hybrid` scalar clearcoat lobe:** the material texture atlas now packs scalar `clearcoat` plus `clearcoatRoughness` as per-triangle metadata, and shade-owned direct, analytic, sun, and glossy-indirect paths add a fixed-F0 GGX top-coat lobe. The promise ledger grades these rows `approximate` because ReSTIR candidate PDFs/payloads and clearcoat maps/normal maps still use the base-lobe path.
- **`@vitrum/walkaround-hybrid` scalar sheen lobe:** the material texture atlas now packs scalar `sheen`, `sheenRoughness`, and `sheenColor.rgb` as per-triangle metadata, and shade-owned direct, analytic, sun, and glossy-indirect paths add a Charlie/Neubelt-Pettineo sheen lobe. The promise ledger grades these rows `approximate` because ReSTIR candidate PDFs/payloads and sheen maps still use the base-lobe path.

### Added (glTF morph target tangent preservation, 2026-06-14)

- **`@vitrum/core` + `@vitrum/gltf-adapter` morph target tangents:** glTF morph-target `TANGENT` VEC3 deltas now import into `SkinnedMeshPrimitive.morphTargetTangents` alongside position/normal deltas and weights. Compatibility reporting now grades these assets as `approximate` rather than data-loss unsupported because the data is preserved and consumed by CPU-solved skinned paths, while GPU-native tangent skinning remains a fallback path.

### Fixed (pt-webgpu lite static multi-primitive packing, 2026-06-14)

- **`@vitrum/pt-webgpu` lite-tier static geometry:** lite `setScene()` now packs mesh, skinned-mesh, and instanced-mesh primitives through a merged world-space BLAS (`mergeWorldSpaceFromCore`) so the lite shader's root-0 traversal sees multi-primitive and transformed static scenes without TLAS bindings. Static `instanced-mesh` is now advertised as native on lite; material patches fall back to a merged-scene repack, and transform/topology mutation rows remain unsupported because those incremental paths are still TLAS-oriented.

### Added (pt-webgpu COLOR_0 vertex colors, 2026-06-14)

- **`@vitrum/pt-webgpu` full-tier vertex colors:** shared-bvh now packs core `colors` as rgba vertex data with white defaults, pt-webgpu uploads/binds the stream at group(3)/binding(11), and the full-tier material paths multiply `COLOR_0` into base color plus alpha pass-through. glTF compatibility now treats `pt-webgpu` full as native for vertex colors while keeping `pt-webgpu-lite` and walkaround-hybrid as structured unsupported profiles.

### Fixed (pt-webgpu roughness/metallic texture parity, 2026-06-13)

- **`@vitrum/pt-webgpu` distinct roughness/metallic maps:** the full-tier material texture descriptor now carries separate `roughnessMap` and `metallicMap` slots with independent UV channel, `KHR_texture_transform`, wrap, and heterogeneous-layer UV-fit metadata. A canonical glTF combined metallicRoughness texture still maps both slots to the same linear-array layer, while assets that author distinct texture handles no longer drop `metallicMap`. The promise ledger now marks pt-webgpu `roughnessMap` and `metallicMap` native.

### Added (glTF texture decode bridge, 2026-06-13)

- **`@vitrum/gltf-adapter` `decodeSceneTextures()`:** new public helper normalizes raw-image `TextureRef` handles into backend-ready CPU-linear RGBA `Float32Array` payloads when hosts provide a `decodePixels` hook, applying the adapter's sRGB/data-map policy and preserving alpha. It returns a refreshed `textureDecodeReport`, structured source-path diagnostics for missing decoders / unsupported handles / max-size / NPOT-repeat hazards, and a `webgpu` target that deliberately leaves opaque handles for backend upload. Built-in PNG/Basis transcoders, automatic downsampling/mip generation, and non-baseColor walkaround map consumption remain Road-to-100 follow-ups.

### Added (walkaround alphaMap atlas cutout, 2026-06-14)

- **`@vitrum/walkaround-hybrid` alphaMap cutout consumption:** readable alpha maps now pack into the material texture atlas as linear R-channel layers with per-triangle alphaMode/opacity/alphaCutoff metadata. RIS, shade, temporal/spatial primary casts, ReSTIR-GI, and NRC GI now use `traceSceneFirstHitAlphaMaskTextured`, so `alphaMode:'mask'` applies `opacity * alphaMap.r < alphaCutoff` before reservoir writes. Fractional `alphaMode:'blend'` is now camera-composited by the transparent-OIT pass, with transparent lighting/GI/shadow participation still approximate.
- **`@vitrum/walkaround-hybrid` baseColorTexture alpha coverage:** textured alpha traversal now multiplies baseColorMap `.a` with optional `alphaMap.r`, so ordinary glTF `alphaMode:'MASK'|'BLEND'` assets that store coverage in `pbrMetallicRoughness.baseColorTexture.a` are honored without fabricating a separate `alphaMap`. `gltfAdapter.test.ts` pins the adapter boundary and `materialTextureAtlas.test.ts` pins the atlas/shader path.
- **`@vitrum/walkaround-hybrid` emissiveMap visible glow:** readable emissive maps now pack into the material texture atlas as sRGB-decoded layers with per-map UV, transform, and wrap metadata. The shade pass multiplies camera-visible emissive surface glow by the map. A later 2026-06-16 DI follow-up samples mapped radiance for merged-BVH ReSTIR-DI candidates and final shade, while selection power, TLAS/analytic emitters, and GI/RC/DDGI payloads still use averaged radiance; the promise ledger therefore keeps the row approximate.
- **`@vitrum/walkaround-hybrid` transmissionMap glass gating:** readable transmission maps now pack into the material texture atlas as linear R-channel layers with per-map UV, transform, and wrap metadata. Shade, RIS, ReSTIR-GI, and NRC GI classify glass from `scalarTransmission * transmissionMap.r`. Direct-light candidate payloads, emitter power, and GI payload lanes still use scalar transmission, so the promise ledger grades this as approximate.
- **`@vitrum/walkaround-hybrid` normalMap visible shading:** readable normal maps now pack into the material texture atlas as linear tangent-space layers with per-map UV, transform, wrap metadata, and `normalScale`. The shader now prefers authored/generated tangent.xyzw when present and falls back to a per-triangle UV-gradient frame before perturbing the camera-visible smooth normal. Reservoir/GI payload parity remains open, so the promise ledger grades this as approximate.
- **`@vitrum/walkaround-hybrid` lightMap visible baked radiance:** readable light maps now pack into the material texture atlas as linear layers with per-map UV, transform, wrap metadata, and `lightMapIntensity`. `shade.wgsl` adds the sampled map as first-hit baked outgoing radiance, matching the PT backend camera-visible convention. ReSTIR emitter power and GI remain scalar/unaware, so the promise ledger grades this as approximate. Material updates that change `lightMap`, `lightMapIntensity`, or atlas alpha metadata (`alphaMode` / `opacity` / `alphaCutoff`) now force an atlas rebuild instead of leaving stale metadata.

### Added (walkaround baseColorMap atlas first slice, 2026-06-13)

- **`@vitrum/walkaround-hybrid` baseColorMap consumption:** readable raw/DataTexture-shaped `TextureRef` handles on uv0/uv1 now pack into a linear RGBA32F texture array with per-triangle metadata, bind through scene bindings 20-21, and modulate shade-visible albedo with wrap + `KHR_texture_transform` semantics. `CONSUMED_MATERIAL_FIELDS` and the core promise ledger now grade walkaround `baseColorMap` as `approximate`; glass Beer tint, fractional alpha blend, normal/thickness/extension maps, and tangent-space map support remain Road-to-100 follow-ups.
- **Walkaround material patch safety:** `HybridEngine.updatePrimitive(material)` keeps the scalar fast path for scalar-only edits, but `baseColorMap` handle/UV/wrap/transform changes route through a full BVH/material-atlas rebuild so cached scene bind groups cannot keep stale atlas resources.
- **glTF texture diagnostics for walkaround:** `textureDecodeReport.backendReadiness.walkaroundHybrid` now reports atlas-backed walkaround fields (`baseColorMap`, `normalMap`, `roughnessMap`, `metallicMap`, `aoMap`, `alphaMap`, `emissiveMap`, `transmissionMap`, `thicknessMap`, `lightMap`, `specular*Map`, clearcoat/sheen/iridescence/anisotropy maps, and `bumpMap`) as `ready` for CPU-readable handles and `opaque` for undecoded/opaque handles; non-atlas fields such as `displacementMap` remain `ignored`. A public `loadGltfAsset()` sweep test now pins every imported texture field's report entry.

### Fixed (glTF texture color-space policy, 2026-06-13)

- **pt-webgl2 material texture atlas color roles:** color/tint maps (`baseColorMap`, `emissiveMap`, `sheenColorMap`, `specularColorMap`) are now inverse-sRGB decoded into the linear RGBA32F atlas, while scalar/data maps remain linear. A single glTF texture handle used in both roles now receives distinct sRGB/linear atlas layers instead of aliasing one decoded payload.
- **glTF texture diagnostics:** `textureDecodeReport` entries now expose the adapter's per-map `colorSpace` policy so hosts can see whether a texture is treated as sRGB color or linear data before choosing a backend.

### Added (pt-webgl2 material maps, 2026-06-13)

- **pt-webgl2 alpha-map contract reconciliation:** `alphaMap` is now ledgered as native after source verification: the 111-texel material record packs alpha transforms at texels 93/94, decodes per-map wrap modes, and samples alpha maps through transform+wrap in both surface-record and attenuation paths.
- **KHR_materials_anisotropy map on `@vitrum/pt-webgl2`:** `anisotropyMap` now packs into the material atlas/stride, preserves per-map UV channel, `KHR_texture_transform`, and wrap modes, and the GLSL surface path samples B-channel strength plus RG rotation offset before feeding anisotropic GGX sampling/eval/PDF. The promise ledger now marks pt-webgl2 `anisotropyMap` native.
- **KHR_materials_volume thickness texture on `@vitrum/pt-webgl2`:** `thicknessMap` now packs alongside scalar `thickness`, preserves per-map UV channel, `KHR_texture_transform`, and wrap modes, and the GLSL surface/attenuation paths sample the KHR volume G channel to clamp Beer-Lambert attenuation distance. The promise ledger marks pt-webgl2 `thicknessMap` approximate, not native, because the backend still uses closed-surface attenuation rather than exact thin-shell volume integration.

### Added (SHADOW-01 shadow flags, 2026-06-11)

- **Primitive `castShadow` backend-wide:** pt-webgpu now skips `castShadow:false` geometry in every any-hit (occlusion) traversal on BOTH tiers (material vec4 #25 .w lane + `triShadowCastDisabled` in the shared `traceMeshBvh` any-hit path) while camera/radiance rays still hit it; walkaround-hybrid honors it on the ReSTIR **DI** shadow predicates (RIS candidate + shading visibility, analytic point/spot NEE, direct-sun NEE) via a new bvh_material bit-0 lane + shared-bvh cast-shadow-masked traversal variants (`BVH_CAST_SHADOW_MASK_WGSL`, derived from the canonical traversal strings with anchored-surgery guards) — GI-side occlusion (ReSTIR-GI/GRIS/DDGI/RC) intentionally still occludes ('approximate'). pt-webgl2 was already native.
- **Emitter `castShadow` (`EmitterBase`):** pt-webgl2 packs the host value into the lights-texture s5.g lane (former IES pad) and `directLightContribution` skips the occlusion test for `castShadow:false` analytic lights (rect/disc/spot/point/directional; mesh-area NEE excluded). pt-webgpu packs per-light lanes (directional sign-encoded angularDiameter; point/spot/rect/disc/mesh) consumed by the default kernel + kernelLite NEE loops and the connect.wgsl BSDF-MIS area connections; off-default integrators (BDPT/ReSTIR-PT/caustics) still shadow-test ('approximate'). walkaround warns (`walkaround-hybrid.unsupported-emitter-cast-shadow`).
- **`receiveShadow` honesty closure:** stays `@reserved`/unsupported on all three backends (non-physical for GI); structured `*.reserved-receive-shadow` warnings fire when a scene sets it false.
- **`BackendSupportDetails.shadows`** new capability sub-map (`primitiveCastShadow` / `emitterCastShadow` / `receiveShadow`) with per-backend rows in the promise ledger, pinned by `engineContract.test.ts`. Default (flag-less) scenes are unchanged: every new lane packs 0.0 / the prior raw value, and the WGSL gates short-circuit on it (pt-webgpu trace SHA re-pins are string-only; behavioral gate 29/29, naga 51/51, GLSL 6/6 green).

### Fixed (complexity-sweep remediation R0–R7, 2026-06-10/11 — commits 8ffd061..HEAD)

- **pt-webgl2 first-sample lights clobber (latent, pre-existing):** the hand-written scene-texture binding sequence evaluated the dummy-texture factory inline as a bind argument; on the first frame the factory's raw `gl.bindTexture` clobbered the still-active `lights.tex` unit, so **sample 1 of every accumulation rendered with a black lights texture** — a permanent ~1/spp under-bias of the direct-light term in the accumulated mean. Found by GPU A/B when the binding sequence became table-driven (`run-ptwebgl2-h1`: lit meanLum 0.26176 → 0.26931 at 24 spp = the corrected first sample). Converged renders asymptotically identical.
- **pt-webgl2 `materialIndex` per-vertex → per-triangle (D10.9):** retires the three-mesh-bvh fork idiom; `faceIndices.w` (the triangle index) now indexes the material table at all 5 GLSL fetch sites, structurally eliminating the last-writer-wins hazard on vertices shared across material boundaries (previously only guarded by a dev-mode assertion). GPU A/B bit-identical on well-formed scenes.
- **`createProgressiveEngine` dropped `onError` (I1.5):** construction-phase errors in the realtime/converged sub-builds now reach the host's `onError` callback.

### Changed (complexity-sweep remediation R0–R7 — behavior-preserving structural work)

- **Layout/stride single-source-of-truth (theme A):** `BVH_NODE_FLOATS`/`VERTEX_STRIDE_F32`/`MAT4_STRIDE_F32` named constants adopted across 4 packages (12+ former magic literals); WGSL stack depths/strides TS-sourced and template-interpolated; NEW pin-test class (stride parity, WalkaroundUBO per-field sentinel round-trip, FrameParams/RestirPtParams/SppmStats field-order vs WGSL struct, RC light layout parsed from live WGSL) — the H1/H41/F-TLAS1 drift class now fails unit tests instead of rendering black.
- **God-file decomposition (theme B):** HybridEngine/WalkaroundGPUPipeline/gpuResources/glResources/probeUpdatePass/cascadeDispatch/gltfToScene/dev-vanilla decomposed move-only (config parsing, GI-state, DDGI sync dedup, frame-context/dispatch split, capture helper, Reservoir/Sppm/Present sub-objects, per-extension glTF parsers, per-overlay dev modules). pt-webgpu group-0 layout deduped from 3 hand-copies to 1 (the historical F1 crash site).
- **Engine facade hardening (theme C):** `createEngine` split into a dispatch table + per-backend constructor modules; bidirectional proxy↔ledger conformance gate (the R10 "facade forgets to forward" class now fails CI); `GIStatePersistable` generic moved to `@vitrum/core`; ledger gains 5 verified method entries.
- **WGSL dedup (theme D):** DDGIGridUBO/sampleDDGIAtPoint/finaliseGIReservoirW/clampCoord/tangent-frame/anisotropy-axes/MNEE-Jacobian/concentric-disc/rotateY deduped (naga 51/51 + T1 GPU smoke green throughout); harness WGSL split into `*.harness.wgsl.ts` siblings; dead `sppmGather` deleted; `SPPM_GROUP4_BINDINGS_WGSL`→`SPPM_GROUP3_BINDINGS_WGSL` (stale name).
- **Subsystem facades (theme E) + extensibility tables (theme F):** DDGI atlas export/import behind the DDGI facade; PIPELINE_SPECS/PASS_LABELS/destroyQueue/BGLCache/SCENE_BUFFER_REGISTRY/SCENE_TEXTURE_BINDINGS/UNIFORM_MANIFEST tables replace grow-per-variant chains; pt-webgl2 uniform/texture upload is now exhaustiveness-gated at compile time (the upload-gap bug class).
- **Dead code (theme G):** ~80 verified-dead exports/files/shims removed (verify-true-deadness protocol; knip config fixed; pt-webgl2 GLSL barrels + THREE-era leftovers deleted); 5 unused deps dropped, playwright pinned directly.
- All 29 behavioral-gate configs pass; T1 GPU smoke (lavapipe+dzn) green at every round; GI brute-force oracles 100%; lint 0 errors / warnings below pre-sweep baseline.

### Added (trust-remediation rounds R7a–R7d, 2026-06-10 — commits a1b85b1, ba1429d, 3f3aa6d, 9c3e6ba, 1a8ab08)

- **`onError` engine error surface (R7d, 9c3e6ba):** `Engine.onError(cb)→unsubscribe` added to the core contract. `EngineError` carries `kind: 'gpu-validation' | 'gpu-internal' | 'device-lost' | 'context-lost' | 'render'`, `fatal`, and `raw`. All three backends wire `uncapturederror` (deduped, once per distinct message per 32 frames) and `device.lost` (fatal; state→`'error'`). `attachVitrum` gains an `onEngineError` option; `<VitrumCanvas>` gains an `onEngineError` prop. The audit's worst systemic finding — walkaround ran with a validation error every frame with no host visibility — is now structurally closed.
- **`@vitrum/gltf-adapter` new package (R7d, 9c3e6ba):** zero-dependency glTF 2.0 → `@vitrum/core` `Scene` adapter. Handles GLB + JSON, sparse accessors, flat-normal generation, node-hierarchy transform flattening, and multi-primitive meshes. Full `pbrMetallicRoughness` + 9 KHR material extensions mapped to `MaterialSpec`; pluggable image decode (ImageBitmap default). Honest v1 exclusions (animations/skins-as-rest-pose/cameras/morphs/draco) warn per-file. 30 fixture tests; root suite now 13 packages.
- **`captureFrame` pixel-readback API (R7d, 1a8ab08):** `Engine.captureFrame(opts)→Promise<CapturedFrame>` on the core contract. Returns `{ data: Float32Array, width, height }` in linear HDR RGBA, top-left origin. `'linear'` mode (default) reads the pre-tonemap accumulation buffer; `'output'` reads the post-tonemap present surface. pt-webgpu reads `accumTexture`/`presentTexture`; pt-webgl2 uses `gl.readPixels` with row-flip. walkaround reads `resolvedTexture` for `'linear'` and rejects `'output'` (swap-chain is host-owned — documented honest limitation). `attachVitrum` handle passthrough.
- **`pickPrimitive` real on all 3 backends (R7d, 1a8ab08):** the core-contract stub is now a real CPU raycast on all backends. Shared `pickPrimitiveCpu` helper in `@vitrum/shared-bvh` uses Möller–Trumbore over world-space triangles with instanced transforms, analytic sphere intersection, and skinned rest-pose documentation. pt-webgl2 gains its first debug surface (`capabilities.debugSurface` now `true`).
- **Anisotropic GGX on `@vitrum/pt-webgpu` (R7b, ba1429d):** `material.anisotropy` + `material.anisotropyRotation` now actually render. Heitz VNDF generalized to (αx, αy) with Burley aspect ratio + rotation tangent-frame; eval/sample/pdf trio consistent. `anisotropy=0` reduces exactly to the isotropic path (byte-identical). 10-test MC harness pins sampler/pdf CV agreement at 0/0.5/0.9 anisotropy.
- **Behavioral gate (R7a, a1b85b1):** `tools/behavioral-gate/` permanent — 26-config matrix (17 pt-webgpu + 8 walkaround + 1 spectral×photon-map) on lavapipe. Asserts zero GPU errors + non-black per config; `npm run behavioral-gate`; CI job. 26/26 PASS.
- **`CameraLike`, `QualityTier`, quality presets public (R7d, 9c3e6ba):** `CameraLike` exported from `@vitrum/engine`; `QualityTier`, `QUALITY_PRESETS`, `resolveQualityPreset` exported from `@vitrum/walkaround-hybrid`. 10 export smoke tests.
- **`examples/README.md` + black-frame debugging runbook (R7d, 9c3e6ba):** 7-app inventory with run instructions, URL params, and capture protocol. `docs/debugging-black-frames.md` — 8-check runbook built from the repo's real failure catalog (symptom→check→fix, behavioral-gate quick command).
- **giState v4 — PPG warm restore (R7c, 3f3aa6d):** `giState` export/import now includes an optional PPG section: the trained sTree/dTree guiding distributions serialize with bounds/cap compatibility validation; import restores warm guided sampling without retraining. Transient window state deliberately excluded (documented). v3 snapshots still import (cold PPG). 8 byte-identity round-trip tests.
- **Spectral × photon-map gather spectralized (R7c, 3f3aa6d):** `sppmGather` now resolves photon RGB flux at the path's hero wavelength in spectral mode (matching all other RGB emission sources). Non-spectral byte-identical. `pt/spectral+photon` behavioral config now passes.

### Changed (trust-remediation rounds R7a–R7d)

- **SPPM streaming-window correction (R7a, a1b85b1):** the Wave 4 SPPM was clearing cell counters per frame (discarding photons; variance diverged as r→0). Fixed to accumulate. **Semantics clarification:** the current estimator is a streaming-window (frozen radius, insertion-normalized flux), not Hachisuka progressive SPPM. True progressive SPPM (A4-progressive) is a follow-up item.
- **`envMapIntensity` unified on `@vitrum/pt-webgpu` (R7b, ba1429d):** the BSDF-escape env miss path now scales by the last-shaded surface's `envMapIntensity`, matching the NEE half and `@vitrum/pt-webgl2`'s design.
- **`lightMap` depth-gating unified (R7b, ba1429d):** `@vitrum/pt-webgpu` now applies `lightMap` on camera-visible hits only (bounce == 0), matching `@vitrum/pt-webgl2`.
- **pt-webgl2 `capabilities.debugSurface` now `true` (R7d, 1a8ab08):** honest flip — pt-webgl2 now has a real `pickPrimitive` implementation.
- **walkaround procedural-sky ledger grade `'unsupported'`→`'approximate'` (R7b, ba1429d):** matches the scalar-degrade-on-walkaround reality.

### Removed (trust-remediation rounds R7a–R7d)

- **IES profile dead chain removed (R7b, ba1429d):** the IES GLSL function, uniform, struct field, texture unit, and packer lane were dead (wired to hardcoded null; never in the core contract; fork residue). Removed. The packer lane is now documented padding.

### Fixed (trust-remediation rounds R7a–R7d)

- **BDPT emitter-vertex throughput bias (R7a, a1b85b1):** `fPrev = vec3f(1.0)` while the body multiplied `cosPrev/pdfFwd` (=π for cosine-sampled emitters) — throughput ×π per emitter extension; isotropic branch had a similar bug. `fPrev = INV_PI` restores unity; `pdfRev` now stores the true reverse density (swapped-argument `brdfDirectionalPdf`).
- **Checkerboard × real-denoiser gap-fill (R7a, a1b85b1):** a new `cb-prefill` compute pass gap-fills stale parity pixels before the denoiser slot when checkerboard + svgf-real/bmfr/neural/oidn-final. Checkerboard-off byte-identical; pass graph 18→19.
- **Procedural-sky Preetham NaN (R7a, a1b85b1):** `Math.min(30, undefined) = NaN` caused the Preetham bake to NaN 16 896 texels when optional fields were omitted. Per-field defaults added; regression tests pin partial + bare specs.
- **walkaround `rcEnabled` naga validation error (R7a, a1b85b1):** the naga-compat rename table was missing five `rc_tlas_*` → canonical TLAS identifier mappings; ptr-storage stripping left undefined identifiers → GPU validation error every frame on `rcEnabled`. Fixed in `nagaFix.mjs`.
- **Spectral CMF upload guard fail-loud (R7b, ba1429d):** missing CMF upload now warns + degrades instead of the previous silent 1e-6-floor overbright.
- **`fresnel` "blown out pixels" TODO resolved (R7b, ba1429d):** explained by B9 Kulla-Conty multiscatter. 50 lines of dead commented-out GLSL predecessors + a dead `transmissionEval` block removed.
- **`environmentTexture` churn fix (R7b, ba1429d):** same-size uploads now reuse the GPU allocation (no destroy/recreate) and preserve bind-group identity.
- **dof × equirect regime guard (R7c, 3f3aa6d):** physically-undefined DOF + equirect combination now warns and forces DOF off.
- **Analytic × lite phantom count (R7c, 3f3aa6d):** `frameParams` zeroes `analyticCount` on lite tier — no phantom UBO analytic-light count for the analytic-less lite kernel.

### Added (v1-closure campaign, 2026-06-10 — commits 6e90443…caab499)

- **Real SPPM photon map (A4, `06910e2`):** stochastic progressive photon mapping replaces the per-pixel 32-photon approximation on `@vitrum/pt-webgpu`. Persistent spatial hash grid (group-3 bindings 6–8), photon-emission pass from point/spot lights through the BVH, camera-hit SPPM gather with α=2/3 Hachisuka-Jensen progressive radius shrink seeded from the scene AABB. The 0.35 world-unit gather radius and ×1.25 brightness fudge are gone. Full-tier only (warn+degrade on lite); off-path gated; 23 hash-grid/radius tests.
- **Preetham procedural sky on `@vitrum/pt-webgpu` (`06910e2`):** real Preetham 1999 analytic daylight (Perez coefficients, zenith polynomials, turbidity/rayleigh/mie/HG sun aureole, finite sun disk) baked to 256×128 equirect through the existing HDRI importance-sampling path. Heuristic tint fudge deleted; 11 tests pin numerical invariants.
- **Env pillar complete on `@vitrum/walkaround-hybrid` (`caab499`):** `envImportanceSample` is now a DI NEE candidate in the walkaround RIS loop (M_ENV=1 sentinel, measure-consistent source pdf, spatial reuse); `risGiNrc` GI-escape reads `envRadiance`; DDGI probe misses sample the real HDRI (group-2 bindings 6/7, rotationY convention consistent); `updateEnvironment` rebuilds directional CDFs at runtime. `hdri` ledger grade promoted `approximate→native`.
- **Radiance Cascades finished (A7, `caab499`):** RC receiver replaced with correct MC irradiance estimator `E=(4π/N)·ΣL·cos` (was ray-count-dependent `Le/Wsum·N·0.5` — fixed ~2.55× error at N=16); real env map bound into the last cascade; point/spot lights added to the RC light model; chromatic sun from scene's directional emitter; scene-scale shadow bias. N=16/N=64 now agree ≈π in tests.
- **Skinning implemented on `@vitrum/pt-webgl2` + `@vitrum/pt-webgpu` (`1d31f0b`):** CPU `solveSkin` (bones + bindMatrix + morph targets) runs at ingestion time on both PT backends — pt-webgpu via `applySolveSkinToScene` + bones-patch fast path in the mutation router; pt-webgl2 via `solveSkinPrimitives` pre-pass inside `buildSceneTextures`. Ledger `skinned-mesh` promoted `approximate→native` on both. 9 tests pin solved results against independent CPU expectations.
- **Tonemap/exposure/outputColorSpace on `@vitrum/walkaround-hybrid` (`1d31f0b`):** new 16-byte composite UBO; operators from `@vitrum/shared-samplers` (`aces`/`agx`/`reinhard`/`linear`/`none`); unset-quality default proven bit-identical to the old hardcoded ACES@1.0+sRGB. 20 tests.
- **BDPT estimator coherence on both PT backends (`1d31f0b`):** (pt-webgpu) light-subpath extension now samples ONE real-BSDF direction used for both the trace and stored throughput/pdfFwd — was a sampled-then-discarded direction causing biased MIS densities. (pt-webgl2) subpath pass re-seeds row-independently so the three rows of one vertex trace the same path — were three independent paths (garbage vertices).
- **In-repo shader compile gate (`1d31f0b`, `tools/shader-gate`):** deno+lavapipe naga gate over 48 composed WGSL shaders (pt-webgpu full/lite/ReSTIR-PT passes, walkaround pipeline passes, shared-denoisers exports) — 48/48 OK; `npm run shader-gate` + CI job.
- **Lifecycle-edge hardening (`06910e2`):** pipeline `dispose()` clears `_initialized`; OIDN resize generation-guards stale writes; neural denoiser resize keeps InferenceGraph size guard; PPG resize retains `ppgMaxSpatialCells` + generation-guards async flux readback; `SampleBudget` reads fresh Welford ping-pong side; GI-state import validates grid origin/spacing/dims; `attachVitrum` ResizeObserver updates canvas backing store; `advanced.device/canvas/context` stripped with warn; `progressiveHandoff` passes destination dims per contract.
- **Contract honesty sweep (`0dbaff5`, `44ada1a`):** pt-webgl2 `emissiveIntensity` default 0→1 (cross-backend consistent); pt-webgl2 `denoiser` option now warn-degrades; walkaround `supportsAuxBuffers` corrected false; `partitionSceneBySupport` honours `AnalyticPrimitive.fallbackMesh` (+3 tests); walkaround `consumedMaterialFields` allowlist module (+11 tests); caustic stride constants single-sourced via POINT/SPOT_LIGHT_VEC4_STRIDE (H1/H41 class fix); photon spot emission axis negation removed; `hdrLuminanceBilateral` i32/u32 type error fixed; CI rewritten (dead fork checkout removed).
- **DDGI emitter NEE complete (`0dbaff5`):** rect/disc-area fixture point-proxy removed from `coreEmittersToDDGILights` (was double-counting against H18 NEE triangles); mesh-area emitter triangles expand into the DDGI probe NEE list (`collectMeshAreaEmitterTrisFromCore` + `setEmitterTris`); emissive-mesh scenes get nonzero DDGI indirect.

### Changed (v1-closure campaign)

- **BEHAVIOR CHANGE — pt-webgl2 default output (`06910e2`):** `@vitrum/pt-webgl2` previously output raw linear HDR. The contract default (`aces` tonemap @ exposure 1.0 @ sRGB output) now applies. Hosts that relied on raw HDR should pass `tonemap: 'none'` + `outputColorSpace: 'linear'` in `EngineOptions.quality`.
- **BEHAVIOR CHANGE — pt-webgpu default output (`caab499`):** `@vitrum/pt-webgpu` previously returned raw linear HDR in `primaryRadiance`. The contract default (`aces` tonemap @ exposure 1.0 @ sRGB) now applies. Adjoint/OIDN readbacks stay linear. Same migration: `tonemap: 'none'` + `outputColorSpace: 'linear'`.
- **Photon-map caustics upgraded to real SPPM (`06910e2`):** `causticStrategy: 'photon-map'` on `@vitrum/pt-webgpu` is no longer the per-pixel 32-photon approximation. It is now a real SPPM gather over a persistent spatial hash grid with progressive radius shrink. The `pt-webgpu-photon-map-approximate` experimentalFeature flag is retired; the mode is no longer approximate.
- **Skinning: rest-pose upgrade to native on PT backends (`1d31f0b`):** `@vitrum/pt-webgl2` and `@vitrum/pt-webgpu` previously packed rest-pose geometry for skinned meshes. Both backends now solve the current pose via `solveSkin` at ingestion and re-solve on `bones`/morphTargets patches. Ledger grade `approximate→native`.
- **Radiance Cascades receiver rescale (`caab499`):** the RC irradiance estimator is now correct (`(4π/N)·ΣL·cos`). Scenes with RC enabled will see different GI energy. The prior `Le/Wsum·N·0.5` formula was both wrong and ray-count-dependent.
- **Env map on `@vitrum/walkaround-hybrid` now directional (`caab499`):** HDRI environments are now importance-sampled as DI NEE candidates in the RIS loop and into DDGI probe misses. Scalar-tint fallback active when no env map is loaded. Ledger grade `approximate→native`. `updateEnvironment` now rebuilds CDFs at runtime.

### Removed

- **`@vitrum/pt-webgl` (fork-backed WebGL2 backend) removed (`e14000c`, 2026-06-09).**
  The package, its GLSL/fork entry point, and all Three.js bridge subpaths were deleted
  as part of the complete THREE-decouple. **Replacement:** `@vitrum/pt-webgl2` is the
  native WebGL2 path tracer with the same contract surface and no Three.js dependency.
- **`@vitrum/three-bindings` removed (`e14000c`, 2026-06-09).** The Three.js scene-graph
  adapter package (`sceneFromThreeJS`, `loadGltfScene`, `VITRUM_USER_DATA_KEYS`, etc.)
  was deleted. Host adapters belong outside the core runtime graph; the material extension
  fields they mapped are now first-class fields on `@vitrum/core` `MaterialSpec`.
- **`@vitrum/three-gpu-pathtracer` (fork) removed (`e14000c`, 2026-06-09).** The forked
  upstream shader library is no longer in the dependency graph; `@vitrum/pt-webgl2` uses
  its own native GLSL shaders.
- **`examples/` Three.js example apps removed (`e14000c`, 2026-06-09).** The former
  Three.js-coupled example apps (hero-viewer, hero-lighting-designer, hero-product-viz,
  neural-denoiser, cornell-box) were deleted. Acceptance fixtures now live under
  `tools/reference-renders/` and `tools/benchmark-runner/`.
- **Three.js bridge subpaths removed.** The `@vitrum/walkaround-hybrid/three`,
  `@vitrum/walkaround-rc/three`, and Three.js-coupled entry points are gone. The raw
  WebGPU compute paths are the only dispatch paths. TSL-side helpers (`applyDDGIShading`,
  `GIReceiver`) remain as optional Three.js coupling points in `walkaround-hybrid`.

### Fixed

- **pt-webgl2 analytic lights (H1/H4, `5f81433`, 2026-06-09):** `lights.count` was
  never uploaded to the GLSL shader; all point/spot/rect/disc-area lights were silently
  inert (only mesh-area emitters lit anything via the emissive fold). Fixed by adding
  `GlProgram.setUint` + uploading `lights.count` each frame. Residual MIS bias on
  multi-light scenes with unequal powers is documented (H4 follow-up: stored selection
  pdf needed for full correctness).
- **pt-webgl2 spectral CMF upload (H2, `7e7b0c5`, 2026-06-09):** CIE CMF tables, CDFs,
  and integrals were never uploaded; spectral mode rendered all-black. Fixed by uploading
  CIE X/Y/Z tables each frame when the spectral feature flag is set.
- **pt-webgl2 backgroundAlpha upload (H3, `16430a8`, 2026-06-09):** `backgroundAlpha`
  was never uploaded; directly visible sky/HDRI environments accumulated nothing (black
  sky). Fixed by uploading `backgroundAlpha = 1.0` each frame.
- **pt-webgl2 BDPT crash fix (H5, `940c886`, 2026-06-09):** `bdpt:true` caused a crash
  via binding an unbound `usampler2D`. Fixed by dummy-binding the texture when BDPT is
  off. BDPT itself remains inert (no host driver called); `causticStrategy:'bdpt'` now
  gates off honestly with a warning.
- **pt-webgl2 env intensity (H6, `f2b20b7`, 2026-06-09):** HDRI `environment.intensity`
  was ignored. Fixed by reading `environment.intensity ?? 1` before upload.
- **pt-webgl2 frameTimeMs + dummy-texture leak (H7, `c7d4661`, 2026-06-09):** `frameTimeMs`
  was returning 0 (hardcoded) instead of the actual elapsed time; `dispose()` was not
  releasing the placeholder 2D and 2D-array dummy textures. Both fixed.
- **DDGI coloured-bounce now Lambertian (2026-06-07, `8aa444a`):** the DDGI probe-ray bounce shading was over-estimating bounce energy by ~π/albedo (~4× at ρ=0.85) and zeroing colour bleed by storing irradiance E directly. The producer-side bounce surface now applies its own Lambertian BRDF — `radiance = (direct + indirect) · baseColor / π` (`probeUpdateRays.wgsl.ts`) — restoring M7's (e66429d) Change 3 in corrected form while keeping Changes 1/2/4/5. Producer (bounce surface) and receiver (shaded point) are two different surfaces' BRDFs; both are required. GPU-validated vs a CPU f64 path-trace anchor (dzn RTX-4090 + lavapipe): coloured-wall residual 23–34% (this form) vs 244–365% for the prior white-bounce model. Evidence: `wsl-gpu/captures/queue-2026-06-07/ddgi-white-bounce/RESULTS.md`. The axis-aligned octahedral-sampling under-read it surfaced is tracked separately (open; not this fix).
- **RC out-of-light-model regime — full fix (2026-06-07):** Radiance Cascades sampled only sun + emissive geometry + environment, never the abstract rect-area emitter list, so a rect-area-only scene produced all-zero cascades — and the RC⊕ReSTIR-GI confidence MIS then handed RC its weight on the host toggle alone, letting an empty cascade REPLACE the correct ReSTIR-GI indirect with zero (darkened/black indirect). Two parts:
  - **PART A (`596c341`):** `shade.wgsl` now gates the RC confidence weight `cRc` on RC-has-energy (`select(0, 1, Lo_rc > 1e-6)`) — an empty cascade forces `cRc = 0` so ReSTIR-GI keeps full weight. Bit-identical when RC is off (rcWeight=0 AND Lo_rc=0) or on-with-energy (gate=1, blend unchanged).
  - **PART B (`1e893fa`):** RC probe-ray hits now run one-sample-per-emitter NEE (`rcEmitterNEE`) against the world-space rect-area emitter tris — sample point, opaque shadow test through RC's own BVH, Lambertian area-form estimator. Emitters are shared world-space tris (no re-upload); `emitterCount == 0` is byte-identical to RC's prior light model. GPU-proven HARM→NEUTRAL→BENEFICIAL arc.
  - **Two-scene RC acceptance gate (`7a46acf`):** the emitter-NEE path changed which scene exercises RC, so the official gate is now two scenes — Scene 1 emitter-NEE (enclosed Cornell, rect light; `rcDeltaMean` 0.0087 > 0.005) + Scene 2 directSun (open Cornell, engine sun; 0.0010 > 0.0005). Supersedes the single W8 cornell-down-directional pair; 4 dzn 512²/48f reference PNGs under `tools/reference-renders/rc-gate-2026-06-07/`.

- **BDPT GPU light-subpath feedback loop (2026-05-28):** `PathTracingRenderer.renderBdptLightSubpathPass` renders to a scratch RT and copies columns via `readRenderTargetPixels` / `writeTexture` so bounce k>0 can read prior columns without sampling the active render target (fixes dark ~8 KiB BDPT on ANGLE/WSL GPU).
- **BDPT WSL capture harness (2026-05-28):** `benchmark:bdpt-layered-refs-gpu` uses WSL Vite + WSL headed Playwright with `vitrumBdptCpuFill=1` (not Windows Playwright); promoted `bdpt-layered-mechanical` PNGs (~490–545 KiB). Experimental Windows path: `benchmark:bdpt-layered-refs-gpu-win`.

> **Catch-up note (2026-05-17):** entries from `T3.A` through `b87e2e4` (premium-grade refactor plan doc) absorbed from 49 commits on main. The ~80 feature/fix branches that were queued for merge off W1-R6 at the time have all since been merged + pushed to `origin/main` (verified 2026-05-30; no outstanding unmerged branches remain).

### Added

- **Backend-typed engine factories + `getScene?()` + `negotiateWebGPUDevice` (2026-06-07, `c9d88fa`):** three additive (non-breaking) contract additions. (1) `EngineFactory<TOptions, TEngine = Engine>` gains a 2nd type param so the named backend factories return typed intersections (`PTEngineWebGL2Surface`, `PTEngineWebGPUSurface`, `HybridEngineGISurface`); `createEngine` stays erased. (2) Optional `Engine.getScene?(): Scene | null` — frozen-reference semantics (no defensive copy), returns the capability-filtered SUPPORTED core `Scene`, `null` pre-`setScene`/post-`dispose`; implemented on all 3 backends + facade-forwarded; `BACKEND_PROMISE_LEDGER` updated. (3) `negotiateWebGPUDevice(options?)` in `@vitrum/engine` — a device-acquisition convenience parallel to `attachVitrum`, with NO hidden ownership (the host destroys the device), reusing `probeAdapterProfile` + the existing required-limits builders.
- **Checkerboard rendering — OFF-default, bit-identical (2026-06-07, `ba8d3c8` + `1182bd1`):** half-res checkerboard shading + temporal-resolve reconstruction, shipped INERT (the RC/PPG/NRC ship-off-then-enable pattern). `HybridEngineOptions.checkerboardRendering?: boolean` (`@default false`, false in all quality presets) threads to `WalkaroundGPUPipeline`; `shade.wgsl` early-outs at gap pixels when `checkerboardOn == 1 AND (x+y)&1 != frameParity`, and `ResolvePass` reprojects/fills the gap. OFF is byte-identical (grisReuseUbo 416-byte golden green; frameParity/checkerboardOn ride repurposed f32 pad slots written 0 when off). The hybrid temporal+spatial gap-fill (`1182bd1`) blends the motion reproject with a 4-shaded-neighbour spatial average weighted by motion magnitude — the `checkerboard-motion-ab` oracle measured the prior motion-reproject-only fill failing under fast pan (22.9 dB) and the hybrid fix restoring it. Enable-by-default on `medium`/`low` tiers is gated on a real shade-pass GPU-time saving measurement (HVN follow-up).
- **Checkerboard rendering — completed into a real degradation knob + enabled on `medium`/`low` (2026-06-08, `b8c4cfe` + `3fb6812` + `839010e`):** the shade-only checkerboard above was GPU-timestamp-measured to save ~nothing — the shade pass is only ~5% of the frame, so compacting it (with a full-res dispatch + warp-divergent gap early-return) was actually a slight regression. Profiling the per-pass GPU timings found the real cost is the three full-res ReSTIR-DI passes — `spatial-1`/`spatial-2`/`ris` (~62% of the frame, 6 `castPrimary` BVH re-casts/pixel). Those now **compact their dispatch** to the active-parity pixels when checkerboard is on (genuinely ~half the threads — one per kept pixel — not a warp-wasting early-return; `SpatialReservoirPass`/`RISPass`/`ShadePass` + `spatial.wgsl`/`ris.wgsl`/`shade.wgsl`), reconstructing the gap via the existing temporal reproject + a configurable per-frame motion fallback (`HybridEngineOptions.checkerboardMotionThresholdSq`, default 0.004) that forces full-rate under fast camera motion. Measured on dzn RTX-4090 (per-pass GPU timestamp, interleaved-paired, both swap orders): spatial-1 1.87× / spatial-2 1.88× / ris 1.90× / shade 1.66× → **~31% whole-frame GPU-time saved** (and grows with resolution); quality is static-identical (temporal accumulates the full candidate set at convergence), sustained-motion ≈ full-rate, with a sub-perceptible motion-onset transient (worst ~43.6 dB, gap error 0.001 luma) that recovers in 2-3 frames. Now **ON in the `medium`/`low` quality presets** (the degradation tiers), full-rate on `ultra`/`high`; `CheckerboardPerfProof` records the measured whole-frame evidence (`status:'measured'`, `wholeFrameSpeedupRatio`, per-pass + quality), and `CHECKERBOARD_SUPPORT_DETAILS.presetEnabled` is now per-tier. Default (no preset, `checkerboardRendering` unset) byte-identical; an explicit `checkerboardRendering:false` still overrides medium/low back to full-rate. Verified end-to-end on dzn: preset render-proof (medium engages 1.43×, ultra full-rate ≈1.00×), motion-A/B quality, T1 GPU smoke (naga + OFF golden 66.4/67.7 dB unchanged). Harnesses: `wsl-gpu/scripts/checkerboard-{spatial,ris}-perf-ab.ts`, `checkerboard-preset-engage-ab.ts`, `checkerboard-motion-ab.ts`, `walkaround-passprofile.ts`.
- **Phase 0 oracle suite (2026-06-07 — validation infrastructure):** the objective-oracle acceptance suite the completion plan (`plan/completion-plan-2026-06-07.md`) builds first, so each later fidelity fix is gated by a ground-truth reference rather than a string golden. **In-tree:** `0-F` denoiser fidelity oracle — `packages/shared-denoisers/__tests__/denoiserFidelityOracle.test.ts` (`d583514`): identity + variance-reduction-without-bias, discrimination-proven (rejects passthrough + biased). **In the `~/projects/wsl-gpu` harness (external GPU validation infra, not in this repo):** `0-D` ReSTIR-DI/GI fidelity oracle (`restir-fidelity-oracle.ts` — reuse-unbiased + p̂-consistency + variance-reduction; lead-verified clean-PASS / `--inject-bug`-FAIL); `0-E` cross-backend ground-truth A/B (`xbackend-groundtruth-ab.ts` — pt-webgpu converged vs walkaround GI, GI-channel-isolated + exposure-anchored; rejected zero-GI 1038× and wrong-bounce 72.6×, and SURFACED a default-path P0-suspect: ReSTIR-GI indirect reads ~0 on an enclosed Cornell with the default config, root-cause pending); `0-G` pt-webgl fork WebGL2 capture (feasible + ~90% scaffolded via Playwright + headless Chrome + ANGLE/SwiftShader). Plus regression-coverage adds for the previously zero-test packages (`scene-lighting`, `stained-glass-extensions` — `2c0242d`) and `three-bindings` G-P0.4 round-trips (`d6f7d78`).
- **Reference baselines (2026-05-27):** `session-20260527` cornell + hero quick captures promoted to `tools/reference-renders/baseline/`; `scripts/promote-ref-baseline.sh` + `npm run capture:promote-baseline`.
- **BDPT / RC captures (2026-05-27):** `isSoftwareGlRenderer` helper; refreshed `bdpt-layered-mechanical` PNGs; RC acceptance via `benchmark:rc-acceptance-gpu` (WSL vite + Windows Chrome).
- **Cornell baseline refresh (2026-05-27):** `capture:refs:quick` session promoted to `tools/reference-renders/baseline/` (6 Cornell + 3 hero PNGs).
- **BDPT GPU light-subpath resolution restore (2026-05-27):** `renderBdptLightSubpathPass` restores `resolution` after the 3×N light-path fill so the eye-path pass is not left at 3×3 (fixes ~11 KiB dark BDPT captures on hardware GL).
- **BDPT Windows Playwright capture (2026-05-27):** `capture-cornell-scenarios.mjs` + `benchmark:bdpt-layered-refs-gpu` runs cornell Vite in WSL and node captures on Windows for hardware WebGL (≥50KB promotion gate).
- **BDPT WSL+Windows capture (2026-05-27):** `benchmark:bdpt-layered-refs-gpu` runs cornell-box Vite in WSL and Playwright on Windows Chrome; promotion skips sub-50KB BDPT stubs.
- **RC mechanical fixtures (2026-05-27):** `write-rc-mechanical-fixtures` writes 64×64 solids under `W8-rc-mechanical-{off,on}/` so `verify:mechanical` no longer clobbers GPU PNGs in `W8-rc-{off,on}/`.
- **BDPT fill regression (2026-05-27):** removed redundant per-frame `driveFork` in `fillBdptLightPath` (setScene already compiles FEATURE_BDPT); kept resolution restore after GPU light-subpath fill.
- **BDPT FEATURE_BDPT compile fix (2026-05-27):** `forkUniformBridge` sets `FEATURE_BDPT=1` when BDPT is opted in even before `lightPathTex` exists so `renderBdptLightSubpathPass` can run after `setScene`; `uBdptEnabled` still gates eye-path connections until the texture is bound. `npm run benchmark:bdpt-layered-refs-gpu` for headed hardware captures.
- **BDPT GPU light-subpath pass (2026-05-27):** fork `renderBdptLightSubpathPass` + `uBdptLightSubpathPass` shader branch; `PTEngineWebGL2.fillBdptLightPath()`; cornell-box wired.
- **BDPT WebGL fixes (2026-05-27):** `forkUniformBridge` toggles `FEATURE_BDPT` + binds `uBdptLightPathTex` only when active; SwiftShader skips float light-path bind (unidirectional PT stays bright); `fillFromScene` no longer clears the shared render target; `packBdptLightPathColumnsWebGL` + tests; capture adapter uses hardware Chromium launch; parity BDPT refs in `benchmark:bdpt-layered-refs`.
- **Complexity remediation wave-0 (2026-05-27):** `bdptLightSubpathOracle.test.ts` pins `packBdptLightPathColumns`; `rcParamsCodegen.test.ts` pins `RCParamsOffset` vs `packRCParams`; un-exported `collectDDGIPointLightsFromRoot`; removed redundant `resolveReSTIRBvhMode` re-export from `bvhCompute.ts`; BDPT capture script doubles SPP and sets `vitrumSpf=16` when `vitrumBdpt=1`.
- **pt-webgl BDPT CPU light-path fill (2026-05-27):** `BdptLightPathBuffer.fillFromScene()` + `fillBdptLightPathWebGL` write bounce-0 vertices from `@vitrum/core` emitters; cornell-box `?vitrumBdpt=1` calls it each frame before `bdptAdvanceFrame()`. Extension bounces still need the fork light-subpath draw pass.
- **GPU reference captures (2026-05-27):** `capture-cornell-suite.sh` writes `cornell-layered-bdpt.png` when `--bdpt`; `benchmark:bdpt-layered-refs` promotes GPU PNGs into `bdpt-layered-mechanical/`; real 512×512 layered capture landed (~578KB). `session-20260527/` quick cornell + hero PNGs committed; cornell suite promoted into `baseline/`. **RC GPU PNGs:** `npm run benchmark:rc-acceptance-gpu` — Vite in WSL + Playwright on Windows host (~86KB off / ~75KB on).
- **BDPT/layered mechanical acceptance (2026-05-27):** fixture PNGs under `bdpt-layered-mechanical/`; `npm run benchmark:bdpt-layered-mechanical` + gated `bdptLayeredAcceptance.gpu.test.ts`; in `verify:mechanical`.
- **pt-webgl TLAS capability tag (2026-05-27):** `experimentalFeatures` includes `merged-bvh-only` when `getSceneTlasAudit().needsTlas`; `@vitrum/engine` re-exports `auditSceneNeedsTlas`.
- **RC mechanical acceptance (2026-05-27):** committed 64×64 fixture PNGs under `W8-rc-{off,on}/`; `npm run benchmark:rc-acceptance-mechanical` runs metrics + `rcAcceptance.gpu.test.ts` without GPU; wired into `verify:mechanical`.
- **Hybrid denoiser A/B toggle (2026-05-27):** `engine.debug.setDenoiserEnabled` / `isDenoiserEnabled` gates `DenoiserAdapterPass`; `@vitrum/dev` `DenoiserABToggle` works on walkaround-hybrid.
- **RC acceptance full gate (2026-05-27):** `npm run benchmark:rc-acceptance-full` — capture + metrics + `rcAcceptance.gpu.test.ts` (`VITRUM_RC_REQUIRE_GPU=1` default).
- **createEngine TLAS routing (2026-05-27):** `auditSceneNeedsTlas` in `@vitrum/core`; `pickBackend` steers `quality` and warns when multi-mesh scenes would land on merged-BVH pt-webgl without WebGPU.
- **W8 RC acceptance harness (2026-05-27):** `npm run benchmark:rc-acceptance` captures Cornell walkaround PNGs (`rcEnabled` off/on) and runs `benchmark:acceptance-metrics`; two-engines URL params `rcEnabled` / `rcWeight`; `npm run benchmark:bdpt-layered-refs` wraps layered + BDPT cornell captures.
- **pt-webgl TLAS audit (2026-05-27):** `auditPtWebglSceneForTlas` + `PTEngineWebGL2.getSceneTlasAudit()` warn on multi-mesh / instanced scenes (merged BVH limitation).
- **C2 TLAS deepening (2026-05-27):** `fingerprintBuffers` + split `blasContentVersion`/`tlasContentVersion`; DDGI/RC TLAS-only `writeBuffer` refit on transform; `isTlasOnlyVersionBump` / `fingerprintTlasBuffers` in `@vitrum/shared-bvh`; `isRestirTlasOnlyRefit` shared by DDGI + RC; `makeRestirBvhSnapshot`; RC `rcTraceFirstHit`; topology rebuild RC bounds on TLAS; `updatePrimitive` + `publishBvh` sync DDGI ReSTIR BVH immediately (not only on `renderFrame`); unit pins for TLAS `transformRefit` / `positionsRefit` / eager DDGI sync; pt-webgpu `uploadScenePackTlasOnly` (transform) + `uploadScenePackBlasOnly` (BLAS splice when TLAS fingerprint unchanged).
- **PR-7 GPU skin refit (2026-05-27):** `applyGpuSkinnedRefit` runs one CPU `solveSkin` after GPU LBS (no duplicate LBS in `GpuSkinningSubsystem`).
- **PR-8 pt-webgl incremental patches (2026-05-27):** `updatePrimitive({ material })` → `updateMaterials()`; `{ transform }` → fork BVH refit (PR-8b); `{ positions }` same topology → geometry regen (PR-8c); `updateEmitter` → `updateLights()`; ledger `transform` + `positions` + `material` + `emitter: true`.
- **PR-5.5 hybrid RC on transform (2026-05-27):** merged-mode `transformRefit` returns `rcRefitBounds` from live `bvhPositions` so `RCSubsystem.refitCascadeBounds` runs without full `setScene`.
- **PR-7 GPU skinning same-frame (2026-05-27):** `gpuSkinBvh.wgsl.ts` LBS writes merged `bvhPositions` on GPU; `refitSkinnedMeshAfterGpuWrite` + `refreshBvhNodesOnly` refit nodes without position re-upload (no frame-lagged readback).
- **WG-7 BDPT on pt-webgpu (2026-05-27):** GPU `bdptExtendLightSubpath` (bounce-0 power-weighted emitter pick incl. spot + environment/HDRI + extension bounces); `extensions['vitrum.ptWebgpu.bdpt']`; FrameParams `bdptEnabled` / `bdptMaxLightBounces`. CPU `bdptEmitterPickCpu` mirrors GPU weighted pick for `fillBdptLightPathCpu` test oracles.
- **PR-7 TLAS skinning (2026-05-27):** GPU skin writes local BLAS positions when `bvhMode: 'tlas'`; `refitSkinnedMeshAfterGpuWrite` refits TLAS instance bounds + RC AABB without position re-upload.
- **WG-9 svgf-real on pt-webgpu (2026-05-27):** `SVGFRealDispatcher` wired when `denoiser: 'svgf-real'` on full trace tier.
- **auto backend (2026-05-27):** `pickBackend('auto')` selects **pt-webgpu** at ≥500k triangles when WebGPU is available.
- **PR-D6 GPU captures (2026-05-27):** `benchmark:pr-hybrid-gpu` + Windows wrapper; merged `PR-hybrid/manifest.json` (perf + PNG).
- **PR-D6 polish (2026-05-26):** `npm run benchmark:pr-hybrid-refs` writes smoke PNGs under `tools/reference-renders/PR-hybrid/`.
- **Gap-closure mechanical CI (2026-05-26):** `buildGapClosureCornellThreeScene` in `@vitrum-examples/shared`; two-engines `vitrumGapScenario` capture wiring; `npm run benchmark:gap-closure-mechanical`; smoke baselines for `rfe07` / `rfe08`.
- **pt-webgpu WG-4/5/6 parity (2026-05-26):** layered front/back `activeLayerWeightRgb` at hero λ, η² transmission MIS in `brdfDirectionalPdf`, translucent SSS gate (`isTranslucent`), same-topology `positions` BLAS splice via `rebuildPrimitiveBlas`; core promise ledger `positions: true` for pt-webgpu.
- **Program signoff docs (2026-05-26):** `plan/archive/WG-signoff-2026-05-26-archived-2026-05-28.md`, `plan/archive/PR-signoff-2026-05-26-archived-2026-05-28.md`; fidelity + maturity matrices updated.
- **pt-webgpu lite + full trace tiers (2026-05-26):** lite layout for SwiftShader-class adapters (≤10 buffers / ≤4 textures); full layout splits path-trace bindings across three groups (≤10 buffers each) so Chrome-class GPUs (16/8) get TLAS, HDRI, area lights, motion/variance aux, and caustics without the old single-group 23-buffer requirement.
- **Hybrid TLAS production path (2026-05-26):** `InstancedMesh` through `@vitrum/three-bindings`, uuid-aware `updatePrimitive` / ReSTIR material resolution, `hybridTlasTraverse` tests, and Windows GPU PR-hybrid bench runner (`run-gpu-host-windows.mjs`).
- **WG-0 baseline (2026-05-26):** committed `tools/reference-renders/baseline/ptwgpu-parity-material-fields.png`; strict hash verification via `VITRUM_STRICT_GAP_CLOSURE=1` on hardware (`npm run benchmark:gpu-windows -- run-gap-closure-verification.mjs …`).
- **WG-1 pt-webgpu OIDN (2026-05-26):** `denoiser: 'oidn-final'` with HDR + albedo + normal-depth GPU readback (`packages/pt-webgpu/src/denoise/`), `getDenoisedFrame()`, `extensions['vitrum.ptWebgpu.oidnModelUrl']`.

- **Wave-4 reliability gate runners (2026-05-26):**
  - `@vitrum/benchmark-runner` gained `run-lifecycle-soak.mjs` for repeated lifecycle churn checks (navigation/quality/size permutations with telemetry-progress assertions and optional dev-server auto-start).
  - Added `run-wave4-hardening.mjs` orchestration gate (`hardening:wave4`) combining mechanical checks, strict lifecycle soak, and optional quality-mode smoke pass into a single report artifact under `tools/benchmark-runner/results/wave4/`.
  - Added benchmark-runner workspace validation scripts (`npm run typecheck --workspace @vitrum/benchmark-runner`, `npm test --workspace @vitrum/benchmark-runner`) using syntax checks across all local runner entrypoints.

- **Coherence + contract truthfulness sweep (2026-05-25):**
  - `EngineCapabilities` gained explicit capability matrix fields: `supportedPrimitiveKinds`, `supportedEnvironmentKinds`, `incrementalPatchSupport`, `presentationMode`, and `experimentalFeatures`.
  - New `@vitrum/stained-glass-extensions` package extracted stained-glass-specific wire contracts (`SURFACE_TEXTURE_ID`, `VITRUM_USER_DATA_KEYS`, came UBO packer).
  - Area-light PDF robustness guards landed in fork shader sampling (`light_sampling_functions.glsl.js`) with smoke-check coverage to prevent NaN/Inf regressions.
  - SVGF-real object-ID fallback switched to conservative history rejection (`currObjId=0`, `prevObjId=1`) until true object-ID buffers are wired.

### Changed

- **pt-webgpu `causticStrategy:'photon-map'` formally demoted to APPROXIMATE/stylized (2026-06-07, honest-labeling only — zero rendering change).** GPU A/B (dzn RTX-4090) vs the forward-traced oracle that validated MNEE shows photon-map recovers only ~21% of true caustic energy and fires on ~1% of caustic pixels (vs `manifold-nee` 98.7% / 99.4%), with a hardcoded world-unit `gatherRadius=0.35` (~6× firing-rate swing under a radiometrically-null scene rescale) and a flat `1+0.25·transmission` brightness fudge (~20% of its reported energy). `causticStrategy:'manifold-nee'` is the validated reference caustic. Docs/comments updated at the two constant sites (`caustic.wgsl.ts`), `core` `causticStrategy` JSDoc (`factory.ts`), `pt-webgpu/README.md`, and `plan/renderer-fidelity-matrix.md`; the `pt-webgpu-photon-map-approximate` experimentalFeatures flag already advertised this. Evidence: `wsl-gpu/captures/queue-2026-06-07/photon-map/RESULTS.md`.
- Benchmark-runner env parsing now fail-fast for invalid numeric configuration values (quality-mode bench + lifecycle soak), preventing silent NaN-driven false positives.
- Project maturity wording is normalized from "pre-alpha/prototype" to "experimental backend" across active architecture docs (`README.md`, `AGENTS.md`, `CLAUDE.md`, `plan/library-architecture.md`).

- `FrameInput` swap-chain handles are now strictly branded backend textures; host callsites must brand `swapChainView` / `swapChainFormat` using `asBackendTexture` and `asBackendTextureFormat`.
- `@vitrum/pt-webgl` and `@vitrum/pt-webgpu` now expose patch methods through the engine surface and truthfully report patch granularity via `incrementalPatchSupport`.
- `@vitrum/walkaround-hybrid` root export surface is trimmed to engine-facing API; broad internal shader/PPG/RC exports were removed from the package root.

### Breaking

- `@vitrum/core` no longer exports `_resetCacheUnsafe`.
- `@vitrum/walkaround-hybrid` package-root internals are no longer part of the stable API surface; import only documented public symbols from package root.

- **Single-wave contract and merge-race remediation (2026-05-24):**
  - Re-landed shared WGSL primitive modules (`pcg.wgsl.ts`, `bsdfPrimitives.wgsl.ts`) in `@vitrum/shared-samplers` and removed duplicated local copies in walkaround-hybrid/pt-webgpu common shader sources.
  - Re-landed branded core contract types in `@vitrum/core`: `Mat4` (`asMat4`/`isMat4`), branded backend texture helpers, and `FrameOutput` discriminated union (`FrameRendered | FrameSkipped`).
  - Restored pt-webgl fork-access indirection (`forkAccess.ts`) and routed bridge/engine private-fork reads through it.
  - Aligned incremental API exposure: `@vitrum/engine` now forwards `updatePrimitive`/`updateEmitter` only when `supportsIncrementalScene === true`; `@vitrum/walkaround-hybrid` now implements `updateEmitter` and material patches via incremental APIs.
  - Hardened acceptance/testing contracts: GPU-gated RC/neural acceptance tests now read and assert harness JSON metrics, and `pt-webgpu` multi-light MIS test now checks an independent analytic expectation (removing tautological sum assertions).

- **C2 — TLAS module in `@vitrum/shared-bvh`** (`5cf642b`, 2026-05-19): top-level acceleration structure builder, refit, and CPU reference traversal. `buildTlas(instances, opts?)` runs binned SAH (K=16) over instance world AABBs and emits the same 32-byte node layout as the BLAS so `bvhIntersect.wgsl` primitives can be reused with only the leaf-payload decode varying. `refitTlas(data, newAabbs)` is the O(nodes) bottom-up bounds refresh used when instance transforms change but topology is unchanged. `tlasIntersect(...)` is the CPU reference oracle for tests (returns candidates; with `maxLeafInstances: 1` the candidate set equals the precise hit set). Signed-int32 leaf-flag check (`0xFFFF0000 &` is `-65536` in JS unless routed through `>>> 0`) caught and fixed in the same pass — 17 tests pin build / refit / traversal / candidate-semantics / error paths. Pipeline wiring (WGSL traverse-into-BLAS + bvh-common adapter to emit per-mesh BLAS roots) is the multi-week follow-up; this module is the load-bearing foundation that lets that work proceed without rebuilding from scratch.

- **C1 — SkinnedMesh end-to-end (contract + CPU solver + morph targets)** (`7d0f4d7` + `f3a91ce`, 2026-05-19): `SkinnedMeshPrimitive` added to `@vitrum/core`'s `ScenePrimitive` discriminated union; `sceneFromThreeJS` now converts `THREE.SkinnedMesh` instead of throwing. Adapter extracts skinIndex/skinWeight, flattens `skeleton.bones[i].matrixWorld` + `skeleton.boneInverses[i]` into single Float32Arrays, captures rest-pose positions/normals/uvs/tangents/indices, and pulls morph attributes (auto-converts absolute morphs to deltas per `morphTargetsRelative === false`). `solveSkin(prim)` is the per-frame linear-blend skinning solver — applies morph deltas BEFORE bone transform per glTF 2.0; precomputes `combined = bones · boneInverses` once per call (boneCount × mat4mul) instead of per-vertex×bone. Normals use the upper 3x3 of the skin matrix (rigid-bone correct; inverse-transpose for scaled bones deferred). `vitrumSceneToThree` pre-solves at scene-build time and emits a static THREE.Mesh; per-frame pose updates flow through `engine.updatePrimitive(id, { positions, normals })` which already exists. Downstream consumers updated: `@vitrum/engine` `sceneAABB` uses rest-pose AABB for skinned-mesh; `@vitrum/walkaround-hybrid` `_coreSceneSuppliesMeshes` includes skinned-mesh; `@vitrum/pt-webgpu` `summarizeScene` exposes `skinnedMeshPrimitiveCount`. 19 tests pin conversion + solver math (identity / translation / rotZ-90 / two-bone-blend / in-place writes / 4 morph cases / shape errors).

- **`@vitrum/walkaround-rc` package** (`b065676`, 2026-05-18; W8 follow-up): extracted the Radiance Cascades subsystem from `@vitrum/walkaround-hybrid/src/rc/` into its own workspace package. Owns cascade pyramid layout, BVH compute, dispatch state machine, buffer manager, receiver material wrapper, and raw WGSL strings. `walkaround-hybrid` depends on it via `file:../walkaround-rc` and re-exports the public surface for back-compat. Composition with DDGI / ReSTIR-GI (Track-A MIS) still happens in `walkaround-hybrid` via `HybridEngineRC.ts`. 10 source files + 2 test files git-moved cleanly; workspace went 11 → 12 packages.

- **A3 positions-only BVH refit fast path** (`@vitrum/walkaround-hybrid`, `426a4c2`, 2026-05-19): `HybridEngine.updatePrimitive` now routes positions-only geometry patches (new vertex positions on the same index buffer + same vertex count) through a new `positionsRefit` path that runs `refitBvhBounds` (~1 ms / 30k tris) instead of the full SAH rebuild (~50 ms). The dispatcher now has three branches: transform-only, positions-only, true topology. Positions patches with vertex-count mismatch correctly fall through to the rebuild path. Adds 3 tests pinning the routing.

- **PPG W9 Phase 2 — per-pixel position from ReSTIR-GI reservoir** (`@vitrum/walkaround-hybrid`, `2f7155b`, 2026-05-18): `ppgGuide.wgsl` now reads `xv` (primary-hit world position) from the half-res ReSTIR-GI reservoir at new `@group(0) @binding(4)`, mapping full-res pixels to half-res reservoir indices. Closes sweep bug #5 (pre-Phase-2 used `sceneCentre` as a placeholder, mapping every pixel to a single sTree cell). Pinned by 7 structural tests in `ppgGuidePerPixelPosition.test.ts`.

- **Premium-grade refactor plan from 2026-05-17 complexity sweep** (b87e2e4, `plan/premium-grade-refactor-20260517.md`): 13-workstream plan across 4 waves synthesising 178 raw findings from `memory/complexity-sweep-20260517.md` into Themes A–I (god-files, mode-switch chains, duplication, contract leaks, hidden globals, scaffold completions, stale/misplaced/dead code, per-pass BGLs). Scaffold decisions locked: RC → `@vitrum/walkaround-rc`; PPG → finish GPU dTree traversal; neural denoiser → finish custom UNet; OIDN → wire `'oidn-final'` as a real mode. First PR: W1 — Pass + Resource + Denoiser registry abstraction (foundation every other workstream depends on). Numerical correctness (per 2026-05-11 sweep) is explicitly out of scope.

- **T3.A — `@vitrum/engine` package + unified `createEngine()` factory** (47e22d8): new thin-facade package exposing the five-line drop-in `const engine = await createEngine({ canvas, scene, camera });`. Probes WebGPU vs WebGL2 via `@vitrum/core`'s `detectGpu`, picks `walkaround-hybrid` (WebGPU realtime GI) or `pt-webgl` (WebGL2 path tracing) per a `prefer: 'auto' | 'realtime' | 'quality'` hint (`'auto'` uses walkaround for scenes <500k tris on WebGPU, pt-webgl otherwise). Accepts either `THREE.Scene` or `@vitrum/core` `Scene`; auto-converts THREE via `@vitrum/three-bindings`. Computes scene AABB diagonal `D` and derives scale-sensitive defaults (`cameraMoveResetThresholdSq = (D·1e-3)²`, `emitterDist2Floor = (D·1e-4)²`, `triIntersectEpsilon = D·1e-6`, `temporalAccumAlpha = 0.01`) so Cornell-tuned numerics no longer leak into jewellery- or city-block-scale scenes. Owns the device handle it creates; idempotent dispose via a thin proxy. 15 unit tests across `pickBackend`, `deriveScaleDefaults`, `computeSceneAABB`. Plan deviation: factory lives in a new `@vitrum/engine` package rather than `@vitrum/core` because the factory has to construct backend engines and `@vitrum/core` has no backend deps; placing it in core would force circular deps or brittle dynamic imports.

- **T3.E — `engine.onFrame()` / `engine.onProgress()` telemetry hooks** (`@vitrum/core` + `@vitrum/walkaround-hybrid` + `@vitrum/pt-webgl`, 16adbe1): two optional methods on the `Engine` contract — `onFrame?(cb: (stats: FrameStats) => void): () => void` and `onProgress?(cb: (progress: ProgressStats) => void): () => void`. Both throw-safe (subscriber throws are swallowed so the render loop stays alive). `walkaround-hybrid` fires `onFrame` at end of each successful `renderFrame()` with `frameTimeMs`, `spp=1`, and per-pass `gpuTimeMs` pulled from timestamp queries when available; `onProgress` intentionally absent. `pt-webgl` emits `{frameTimeMs, spp}` and `{kind: 'pt-spp', current, target, fraction}` per frame. `@vitrum/engine` `wrapWithIdempotentDispose` proxy conditionally forwards both methods via the exactOptionalPropertyTypes-friendly spread pattern; post-dispose subscribe returns a no-op teardown. 6 tests in `__tests__/telemetryProxy.test.ts`.

- **T3.D — `loadGltfScene()` loader pipeline** (`@vitrum/three-bindings`, e060af7): wraps three.js's `GLTFLoader` so a host can hand vitrum a URL / Blob / File / ArrayBuffer and get back a vitrum `Scene` plus the first embedded camera. Decodes the source, calls `GLTFLoader.parse()` with an optional `configure(loader)` hook (DRACO / KTX2 / Meshopt extensions), wraps a bare Group in `THREE.Scene`, auto-computes flat vertex normals on any meshes missing the NORMAL attribute (common in baked / procedural glTFs) before `sceneFromThreeJS` would otherwise throw, and surfaces `gltf.cameras[0]` as `{viewMatrix, projMatrix, cameraPosition}` from the camera node's world matrix. 5 unit tests covering Blob / ArrayBuffer / embedded-camera / no-camera-fallback / bad-source. Fixture is a hand-built minimum glTF 2.0 (one triangle, embedded data-URI buffer) — no external assets, no network in CI.

- **T3.F — `attachVitrum()` vanilla helper + `<VitrumCanvas>` React component** (`@vitrum/engine`, 9c806dc): two new entrypoints under `@vitrum/engine/lifecycle` and `@vitrum/engine/react` for hosts that want a fully-managed RAF loop without writing the lifecycle boilerplate. `attachVitrum` drives the RAF loop, packs `viewMatrix / projMatrix / cameraPosition / prevView / prevProj / viewport` per frame (so ReSTIR temporal reuse + motion vectors work out-of-box), tracks the canvas's CSS pixel size via `ResizeObserver`, and defaults to pause-on-hidden via `document.visibilitychange`. `<VitrumCanvas>` wraps `attachVitrum` in a single component; `scene + camera + prefer` flow through the dependency array, `onFrame / onProgress / quality` flow through refs to avoid engine churn. Strict-Mode-safe via a cancelled flag. `react` / `react-dom` are OPTIONAL peer deps (peerDependenciesMeta) — vanilla hosts pay nothing for them. Plan deviation: originally scoped to `@vitrum/three-bindings`; moved to `@vitrum/engine` to avoid a circular dep.

- **T3.G — `@vitrum/dev` debug-overlay package** (2e7d2d3): new devDep-tier package with six opt-in debug overlays (React + vanilla `attachDebugOverlays`): `FrameTimeHUD` (live frame time + 60-frame moving average + pass breakdown via `engine.onFrame()` with rAF-delta fallback), `DDGIAtlasViewer`, `BVHVisualizer`, `GISignalSplit`, `DenoiserABToggle`, `MaterialInspector`. `react / react-dom / three` are OPTIONAL peer deps. Single source of truth for the host↔engine contract is the new `EngineDebugSurface` interface. 24 tests pass / 5 skipped (DOM-required tests skip in node env until happy-dom — added by the unskip-tests follow-up below).

- **T3.G followup — `engine.debug` surface implemented** (`@vitrum/core` canonical owner + `@vitrum/walkaround-hybrid`, 38463d1): `EngineDebugSurface` interface now lives in `@vitrum/core/engine.ts` as an optional `debug?: EngineDebugSurface` field on the `Engine` interface. `HybridEngine.debug` implements four of seven methods: `atlasTexture()` / `visibilityAtlasTexture()` return live DDGI atlases via `getReadAtlasGPUTextures()`; `bvhNodes()` unpacks the 32-byte/node three-mesh-bvh binary layout into the public 8-floats-per-node Float32Array contract `[minX, minY, minZ, maxX, maxY, maxZ, depth=0, pad=0]`; `giSignalTextures()` returns the persistent `hdrColorTexture` (direct), `hdrIndirectTexture` (indirect), and `aoFullTexture` from the live pipeline. `pickPrimitive` and `setDenoiserEnabled / isDenoiserEnabled` intentionally remain absent — they need a real GPU picking pass and a denoiser-bypass shader path respectively. `@vitrum/dev` no longer redefines the types; re-exports from `@vitrum/core`. `@vitrum/engine` proxy passes `engine.debug` through. 5 tests in `__tests__/debugSurface.test.ts`.

- **T3.C — three hero example apps** (d40b2a3, `examples/hero-viewer`, `examples/hero-lighting-designer`, `examples/hero-product-viz`): three new Vite projects each runnable end-to-end with `cd examples/hero-X && npm install && npm run dev`. **hero-viewer** (~230 LoC): drop-in glTF viewer via `attachVitrum` + drag-drop `loadGltfScene`, OrbitControls, realtime/quality toggle that recreates the engine. **hero-lighting-designer** (~190 LoC): procedural Cornell-style room with 3 draggable point lights, debounced setScene per slider edit, FPS HUD via `engine.onFrame()`. **hero-product-viz** (~270 LoC): procedural shelf + glass orb, `engine.onProgress()`-driven SPP bar, material editor (roughness / metalness / transmission / IOR), 4K render-to-PNG button (256 SPP convergence, `canvas.toBlob('image/png')` + synthetic anchor click). All three exercise the public surface from T3.A/D/E/F.

- **T3.J — npm publish prep (gating, no actual publish)** (4be0931, `RELEASING.md` + `packages/*/package.json`): adds `"private": true` everywhere as a publish-safety belt (previously only `pt-webgpu` had it); adds `repository.directory` so npm pages link directly to package source; adds `author`, `keywords`, and `files` (`["src", "README.md"]`); aligns `three-mesh-bvh` peer-dep range across packages (`shared-bvh` was `^0.9.9`, `pt-webgl` was `>=0.7.4` — now both `>=0.7.4`). `npm pack --dry-run --workspaces` verified packed sizes: `@vitrum/core` 19 KB, `@vitrum/engine` 11 KB, `@vitrum/three-bindings` 22 KB, `@vitrum/walkaround-hybrid` 228 KB (full WGSL shader corpus); all others <70 KB. New `RELEASING.md` documents the pre-publish checklist, the `@vitrum` npm scope claim (currently unregistered), the `pt-webgl` → `file:../../../three-gpu-pathtracer` sibling-repo dep resolution options, versioning policy, and rollback procedure. Per `CLAUDE.md`, npm publish is a user-explicit-instruction step — this commit makes the repo publish-ready but does not publish.

- **T3.I — animation / dynamic-geometry support status doc** (`plan/animation-support-status.md`, 5fbef72): honest audit of what works, what doesn't, and at what cost when the host animates the scene. Camera animation works on both backends; light animation is limited (`updateLighting()` invalidates DDGI + resets accumulator; per-emitter patches are `never`); mesh transform animation is not supported (`updatePrimitive` is `never`; only setScene → full reinit); skinning / morph targets are not supported. Surprising findings flagged: `WalkaroundGPUPipeline.updateEmitters()` is fully implemented but orphaned (no public path reaches it); `cameraMoveResetThresholdSq=1.0` default silently mis-tunes any non-Cornell scene (T3.A's `createEngine()` now derives it from the AABB diagonal); hosts omitting `prevViewMatrix` get zero-bias temporal reuse; `setScene()` is not guarded against re-entry. New `__tests__/cameraAnimationReset.test.ts` — 8 analytic tests via a CPU mirror of the reset criterion; no GPU dependency; 2 ms runtime.

- **8 previously-skipped tests now run** (95cb3b8, `@vitrum/dev` + `@vitrum/shared-denoisers` + `@vitrum/pt-webgpu`): three problem areas resolved. (1) `@vitrum/dev` DOM tests: switched env to `happy-dom ^15.10.2`; 5 tests now run (`@vitrum/dev` 24→29). (2) `@vitrum/shared-denoisers` WebGPU tests: split into `vitest.config.ts` (node, excludes `*.gpu.test.ts`) + new `vitest.gpu.config.ts` (vitest browser mode via Playwright Chromium with `--enable-unsafe-webgpu --use-vulkan=swiftshader --enable-features=Vulkan` — SwiftShader's Vulkan ICD gives `navigator.gpu` real semantics in the test runner). Added `@vitest/browser ^1.6.1` + `playwright ^1.59.1`. Both `runAtrousVarianceWebGPU` tests pass under SwiftShader. (3) `@vitrum/pt-webgpu` WGSL smoke test: surfaced 2 latent bugs that string-only tests could not catch — 13 `if (cond) statement;` lines (WGSL requires braces, all 13 wrapped) and 18 storage buffers in the bruteforce shader (default limit 8; SwiftShader 10; consumer GPUs ≥16); test now requests the adapter's max via `requiredLimits.maxStorageBuffersPerShaderStage` and splits into a WGSL-parses case (always runs) + a pipeline-create case (branches on adapter limit; asserts the EXACT limit-exceeded error string on SwiftShader so a future regression that silently fits under the cap is detectable). Workspace state: 907 pass / 1 skipped (was 894 / 8). The remaining skip is an `it.skipIf(hasWebGpu)` inverse-guard ("throws when WebGPU is missing") — contextual-by-design.

- **`SURFACE_TEXTURE_ID` wire-contract enum exported** (`@vitrum/walkaround-hybrid`, b1ee92d): the integer values stamped on `userData.surfaceTextureId`, packed into the low 3 bits of `bvhIndex[*].w` by `packBVHIndexW`, and dispatched by the WGSL `surfaceTextureMod` switch all encode the same 0..7 mapping. The canonical table previously lived in the consumer host (stainedGlass's physics package) and the WGSL switch was a hand-mirrored copy — renumbering either side silently corrupted rendering. The canonical table now lives in `@vitrum/walkaround-hybrid` (the consumer that owns the wire format) and is pinned against the WGSL switch labels via a runtime test that fails loudly if either side drifts. Hosts that author scenes should re-export `SURFACE_TEXTURE_ID` from `@vitrum/walkaround-hybrid` instead of maintaining their own copy. 4 new wire-contract assertions; 313 walkaround-hybrid tests pass. <!-- Superseded by the coherence sweep (2026-05-25): SURFACE_TEXTURE_ID's canonical home is now @vitrum/stained-glass-extensions; three-bindings re-exports from there. -->

- **MIS host implementation across X/Y/Z CMFs (Wilkie 2015 §3.3)** (`@vitrum/shared-samplers` + `@vitrum/pt-webgl`, bf161f4): pairs with three-gpu-pathtracer@5598783 (one-sample MIS across X/Y/Z CMFs). `shared-samplers`: new `sampleHeroWavelengthMIS(uStrategy, uLambda)` returns the mixture pdf for use as the MC denominator (shares inverse-CDF helper with legacy `sampleHeroWavelength`, kept for backward compat); new `X_CMF_CDF` / `Z_CMF_CDF` exports + `X_CMF_INTEGRAL` / `Z_CMF_INTEGRAL` constants mirrored to the GLSL shader's new uniforms. 15 new unit tests cover per-strategy chromatic targeting, 3-band mixture coverage, unbiasedness of the MC estimator (constant L(λ)=1 recovers `∫Y dλ` within 5%). `pt-webgl/forkUniformBridge.ts`: refactored `computeYIntegralWavelengthSpace` → generic `computeCmfIntegralWavelengthSpace`; new `buildCmfCdf` replaces the old Y-only `buildYCdf`; uploads `uXCmfCdf` / `uYCmfCdf` / `uZCmfCdf` + matching integrals to the fork material every frame init. Re-captured `cornell-spectral.png` at 12 SPP: mean RGB (4, 55, 0) → (41, 99, 63); non-zero blue pixels <0.1% → 55.6%.

- **Sprint 10c vitrum-side BDPT bridge** (`@vitrum/pt-webgl`, 398dfce): exposes the fork's Sprint 10c BDPT integrator path through the vitrum-layer API and uniform bridge. New `ForkBridgeBdptOptions` export (`enabled`, `maxLightBounces`, `lightPathTex`); `driveForkMaterialUniforms()` gains optional `bdptOptions` arg and threads `uBdptEnabled / uBdptMaxLightBounces / uBdptLightPathTex` to fork uniforms. Safety guard: null `lightPathTex` forces `enabled=false` (prevents sampling an unbound texture slot in `FEATURE_BDPT=1` mode). `PTEngineWebGL2` reads `vitrum.ptWebgl.bdpt` (boolean) + `vitrum.ptWebgl.bdptMaxLightBounces` (number 1–3) from extensions and threads to the bridge on `setScene()`. Hosts supply `lightPathTex` per frame via direct `driveForkMaterialUniforms()` after their light-subpath draw pass. 3 new Sprint 10c tests in `__tests__/forkUniformBridge.test.ts`.

- **H6.6 / SG.D — spectral pipeline wire-format tests (Sprint 12 COMPLETE)** (`@vitrum/pt-webgpu`, f165a99): adds the missing test coverage to lock the wire-format contract between fork-side `MaterialsTexture.js` and vitrum-side `stampVitrumExtensions()`. New `__tests__/materialsTextureSpectral.test.ts` (9 tests) covers: wire-format `MATERIAL_PIXELS` self-check, no-spectral fallback (`hasSpectralAttenuation` bit clear, bins=0), constant-μ curve (all 32 bins ≈ μ, feature flag set), ramp-curve linearity (`μ(380)≈0`, `μ(780)≈1`, midpoint correct), multi-material stride (spectral bins at correct `STRIDE` offset for `mat[1]`), `scatteringCoefficient + anisotropy` → sample 15.r/g, thin-film stack (enabled, layerCount, incidentIor, angleDependent), `hasFrontLayer / hasBackLayer` feature flag bits, PBR non-corruption invariant (IOR + attenuationDistance unchanged). Sprint 12 moves PARTIAL → COMPLETE in `external_requests/IMPLEMENTATION-STATUS.md`; stainedGlass audit Gap 2 RESOLVED. 49 pass / 3 skipped; `tsc --noEmit` clean.

- **`DICHROIC_REFLECTANCE_LUT` + `DICHROIC_TRANSMITTANCE_LUT` userData consumption** (`@vitrum/three-bindings`, e5981ab): adds two new `VITRUM_USER_DATA_KEYS` entries and wires `convertMaterial` to forward them via `Material.extensions.dichroicLUTs`. Consumer is the stainedGlass app's physics baker, which stamps `userData.vitrumDichroicReflectanceLUT` + `userData.vitrumDichroicTransmittanceLUT` as `THREE.DataTexture` (256×1 RGBA float) LUTs containing per-angle RGB triplets baked from TMM × CIE 1931 2° observer convolution of the dichroic stack's spectral response. Filed alongside `external_requests/11-dichroic-lut-consumption.md` documenting the consumption contract + raster-side fragment shader extension follow-up. 37 tests pass — no regressions.

- **`MeshBasicMaterial` accepted via flat-emissive synthesis** (`@vitrum/three-bindings`, 460a05e): new `convertBasicMaterial` adapter so host code can pass `MeshBasicMaterial` (unlit overlay meshes — panel-mount preview, debug grids) through `convertMesh` without manually synthesising a `MeshStandardMaterial`. Maps the unlit colour to `emissive = color, emissiveIntensity = 1, roughness = 1, metallic = 0` — reads as a flat self-lit surface under both PT and walkaround. `convertMesh` now accepts `{Std, Phys, Basic}`; dispatch picks the right converter via the `is*Material` flag.

- **Cornell-box real-GPU canvas capture + 6 reference renders** (c91653e + 6bfd901): the cornell-box example was capturing PNGs via the existing Playwright adapter but the resulting PNGs were always transparent black on real-GPU runs because (1) `THREE.WebGLRenderer` defaults to `preserveDrawingBuffer: false` (canvas backing store invalidated between blit and external readback) and (2) `toDataURL` blobs got blocked by the MCP base64 filter and Chrome downloads require user gesture. Fixes: opt-in to `preserveDrawingBuffer: true` only when `isCapture` (keeps interactive mode's perf characteristics); new `vitrum-capture-sink` vite middleware accepts `POST /__capture?name=foo` with a PNG body and writes `<repo>/tools/reference-renders/post-sweep-20260512/<foo>.png`. Six new reference renders captured on RTX 4090 / ANGLE D3D11 at 512×512 (`cornell-{glass,caustic,spectral,layered,sss,parity}`), initially 16 SPP, then re-rendered at 64 SPP via a new `vitrumSpf=N` URL param threading `samplesPerFrame + maxSamplesPerFrame` into engine extensions (enables headless background-tab capture without rAF throttling). Drive-by portability: AO full-res output `r16float → rgba16float` (base-spec WebGPU disallows `r16float` as storage texture without the `texture-formats-tier1` optional feature, which three.js's WebGPURenderer doesn't request); SVGF history-length texture `r16uint → r32uint` (same reason; counter stays well under u16 max).

- **`capture-cornell-suite.sh` — one-shot 6-scenario render harness** (be42e87): wraps the benchmark-runner's Playwright capture adapter in a single shell script. Starts cornell-box vite dev server, captures all 6 cornell scenarios in sequence, writes PNGs to a dated output dir, and cleans up the dev server on exit. Defaults: 1280×720 / 512 SPP / 8 bounces. Flags: `--quick` (512×512 / 64 SPP / 4 bounces — smoke), `--hero` (1920×1080 / 2048 SPP / 12 bounces — final-render), `--bdpt` (adds `&vitrumBdpt=1` query param), `--only` (comma-separated scenario subset), `--out` (custom output dir).

- **CI shader-compile-ci — headless-Chromium smoke for all 7 cornell scenarios** (ba6d565): launches headless Chromium under SwiftShader, navigates cornell-box to each of the 7 scenarios at 64×64×1spp/1bounce, waits for `VITRUM_CAPTURE_READY` (only fires after the shader has been compiled, linked, run, and written to the canvas). Console errors + pageerrors matched against a small set of shader/WebGL/WGSL error patterns; any match fails the run. Each scenario hits a different fork shader-define permutation (transmission, dispersion, clearcoat, subsurface, etc.) so the guard covers the union of compile paths the library actually ships. Catches what `fork-shader-smoke` (string-pattern check on GLSL source) cannot: (a) uniforms the fork references but `PhysicalPathTracingMaterial` no longer declares, (b) bind-group-layout / WGSL signature mismatches, (c) accidental opt-in to optional WebGPU features (e.g. `r16float` storage), (d) WGSL reserved-word collisions, (e) any uncaught JS during scene/material setup. Verified locally: all 7 scenarios pass in ~8 minutes under SwiftShader. Wired into `verify:mechanical`.

- **Fork-side Sprint 6 + H6.3 verification — tracking** (76b8b86 + 3b68360): Sprint 6 (rough refraction GGX lobe) fork patch APPLIED at fork commit `e9c7516` — `IMPLEMENTATION-STATUS.md` updated; `sprint-6-pt-fork-patch.md` marked APPLIED. H6.3 verification (Sprints 7/8/12/14 clean post-MRT migration at fork commit `ff1dace`): all 4 prior sprints route through `gColor.rgb` accumulation; no `gl_FragColor` stale references; no forward-port needed. Verified by shader-smoke + grep audit.

- **Sprint 10c BDPT fork patch — APPLIED** (ff46186): `IMPLEMENTATION-STATUS.md` Sprint 10c entry moved BLOCKED → APPLIED with full details: fork commit `98f4446`, vitrum commit `398dfce`, vertex storage approach (3×3 RGBA32F ping-pong), MIS faithfulness note, and 4 documented known gaps (pdfRev approximation, full strategy enumeration, GPU A/B, reference renders). `sprint-10c-pt-fork-patch.md` status header + DoD checklist updated (6 of 8 items complete; 2 pending GPU run).

- **SG.C — `HybridEngine.updateLighting()` — runtime sun + sky update API** (`@vitrum/walkaround-hybrid`): eliminates the engine-recreation workaround for time-of-day scrubbing in stainedGlass (audit Gap 1). New `LightingOptions` interface (`primaryLightDir`, `primaryLightIntensity`, `skyTint`, `skyIrradiance`); `HybridEngine.updateLighting(opts: Partial<LightingOptions>)` re-uploads the WalkaroundUBO at next frame start (UBO uploader reads live engine fields each frame — no frozen snapshot), invalidates the DDGI probe atlas (`invalidateProbeCache()` on `DDGI` — resets `_frame`/`_ready` so the blend kernel re-converges over ~8 frames at default hysteresis), and resets the temporal accumulator (`requestAccumReset()` on `WalkaroundGPUPipeline` — sets `_accumFrameIndex = 0` so α=1 on the next frame). No pipelines or GPU buffers recreated. Cost: 1 UBO write at next frame + ~133 ms DDGI re-convergence + 1 frame temporal-accumulator reset. 9 tests in `__tests__/hybridEngineLighting.test.ts`.

- **SG.B — three.js peer dep relaxation** (`packages/*/package.json`): audited the full vitrum three.js API surface to determine the minimum viable version. `three/webgpu` and `three/tsl` (required by `@vitrum/walkaround-hybrid`) were first published as top-level package exports in **0.167.0**; all other APIs (core classes, `three/addons`, `three/examples/jsm/objects/Sky.js`, `NoColorSpace`/`SRGBColorSpace`) are present in 0.152+. Floor set to `>=0.167.0 <0.190` for all four packages that carry a `three` peer dep (`three-bindings`, `pt-webgl`, `walkaround-hybrid`, `shared-bvh`). `devDependencies.three` and `devDependencies.@types/three` aligned to the same `>=0.167.0 <0.190` range. stainedGlass at 0.171.0 satisfies the new constraint; host bridge call sites can remove the `as any` casts and restore TypeScript structural type-checking. No source-code changes — packaging metadata only. Addresses stainedGlass audit Gap 4.

- **SG.A — anisotropy + anisotropyRotation round-trip** (`@vitrum/core` `Material` + `@vitrum/three-bindings`): adds `anisotropy?: number` and `anisotropyRotation?: number` to the `Material` contract; `convertMaterial()` reads both fields directly off `THREE.MeshPhysicalMaterial` (not via userData — the stainedGlass baker stamps them directly on the material); `vitrumSceneToThree()` writes them back onto the reconstructed THREE material so the fork receives the correct anisotropy on every `setScene`. Fixes ripple/waterglass cells rendering as isotropic in PT final-render mode (stainedGlass audit Gap 5).

- **T2.H2 — Neural U-Net denoiser revival** (`@vitrum/walkaround-hybrid` + `@vitrum/core`): Rebuilds the neural denoiser from scratch, fixing all 8 correctness blockers from the Sprint 13 scaffold that was deleted during the sweep-2026-05-11 review. The `'neural'` denoiser mode is **opt-in** — default remains `'atrous-variance'`. No trained weights are shipped; users collect a dataset and run `tools/neural-denoiser-training/train.py`. The 8 bugs fixed vs. the prior scaffold: **(1) Skip-connection spatial mismatch** — `unetArchitecture.ts` now uses pre-downsampling encoder feature maps (enc1_feat, enc2_feat, enc3_feat) as skip sources, ensuring every skipAdd layer's operands have matching (H, W, C); tests verify this analytically at 64×64 and 128×64. **(2) `enc_input` never assembled** — `inputPacker.ts` (new file) provides a GPU compute shader that packs noisyColor + albedo + normals (3+3+3 channels) into a per-pixel interleaved H×W×9 buffer; `InferenceGraph` calls this packing pass before dispatching the graph. **(3) Binding index mismatch** — All 5 WGSL kernels (`conv2d`, `transposedConv2d`, `relu`, `skipConnection`, `bilinearUpsample`) declare the canonical binding order input=0, weights/inputB=1, biases=2, output=3, params=4; the host dispatch in `InferenceGraph._buildBindGroup` uses the same order; Test 2 parses `@binding()` declarations from WGSL source and asserts consistency. **(4) Uniform buffer never written** — `InferenceGraph.initialize()` calls `device.queue.writeBuffer()` with the actual shape params (`inputH`, `inputW`, `inC`, `outC`, `kH`, `kW`, `stride`, `padding`) immediately after allocation, AND re-writes per layer on each `run()` call; `_uniformWriteCount` counter is exposed for test instrumentation; Test 3 verifies the counter and the packed param layout. **(5) `train.py.md` not executable** — `tools/neural-denoiser-training/train.py` is a real Python file with a `UNetDenoiser` (PyTorch) matching the architecture, L1+SSIM composite loss (Chaitanya 2017), Adam + cosine LR schedule, and exports `weights.bin` at training end; `python -m py_compile` verified. **(6) Bind-group cache not stable** — `InferenceGraph.run()` tracks buffer identity via `cachedBufKeys` and rebuilds the bind group if any buffer changes (e.g. after a resize via re-`initialize()`). **(7) `dispose()` doesn't clear slot-by-slot** — `dispose()` nulls each `cachedBindGroup` slot individually via a for-loop, then reassigns `_layerStates = new Array(N).fill(null)` so GPUBindGroup references release before the underlying buffers are destroyed. **(8) No `'neural'` mode in `HybridEngine`** — `HybridEngineOptions.denoiser` union extends to include `'neural'`; `HybridEngine` constructor validates `neuralWeights` is provided (throws with a helpful error pointing to `tools/neural-denoiser-training/README.md`); `HybridEngine._initPipeline` creates and initializes the `InferenceGraph` before pipeline init; `WalkaroundGPUPipeline.initialize` stores the graph and dispatches it in `renderFrame` when `_denoiserMode === 'neural'`. New binary format: `.vitrum-model` (header: `u32 magic=0xDEAF1984`, `u32 version=1`, `u32 layerCount`; per-layer: nameLen+name+weightCount+weights+biasCount+biases); weight layout OIKW for Conv2D, IOKW for ConvTranspose2D matching PyTorch defaults. `tools/neural-denoiser-training/export_weights.py` (new, standalone): converts any `.pth` checkpoint to the vitrum binary without running train.py. `@vitrum/core` `EngineOptions.denoiser` union extended with `'neural'`. 21 tests in `__tests__/neural.test.ts` across 5 groups. References: Ronneberger et al. 2015 (U-Net, MICCAI, https://arxiv.org/abs/1505.04597); Chaitanya et al. 2017 (RDA, SIGGRAPH, https://doi.org/10.1145/3072959.3073601).

- **T2.H1 — Real Schied 2017 SVGF denoiser mode `'svgf-real'`** (`@vitrum/shared-denoisers` + `@vitrum/walkaround-hybrid`): implements the full spatiotemporal variance-guided filter from Schied et al. 2017 as a new `denoiser: 'svgf-real'` option alongside the existing `'atrous-variance'` mode. Three new WGSL compute kernels: `svgfReprojection.wgsl.ts` (Eq. 1–4: bilinear 4-tap gather of prev-frame colour + moments, per-tap depth/normal/objId disocclusion rejection, per-pixel history-length increment or reset, EMA α=max(α_min, 1/(h+1))), `svgfVarianceFromMoments.wgsl.ts` (Eq. 5: `Var = max(0, M2 - M1²)` for h ≥ 4), `svgf7x7SpatialFallback.wgsl.ts` (7×7 box-filter spatial luminance variance for h < 4, pass-through otherwise). Host pipeline `svgfRealWebGPU.ts`: reprojection → variance-from-moments → 7×7 fallback → à-trous wavelet filter ×5 (reuses `ATROUS_VARIANCE_WGSL`). GPU memory budget at 1080p: historyLength r16uint ≈4 MB, momentsHistory rg32float ≈16 MB, prevRadiance rgba16float ≈16 MB, motionVec rgba16float ≈16 MB (~52 MB total persistent). Ping-pong texture pairs (A/B) used for historyLength, moments, and prevRadiance to avoid WebGPU read-write hazards. `WalkaroundGPUPipeline` gains `_svgfPingPong` counter and `_svgfReprojUboRef`. `FrameResources` gains 8 new persistent textures. `@vitrum/core` `EngineOptions.denoiser` and `HybridEngineOptions.denoiser` unions updated to include `'svgf-real'`. New exports: `SVGF_REPROJECTION_WGSL`, `SVGF_VARIANCE_FROM_MOMENTS_WGSL`, `SVGF_7X7_SPATIAL_FALLBACK_WGSL`, workgroup size constants, `SVGFReprojUniforms`, `packSVGFReprojUniforms`, `SVGF_REPROJ_DEFAULT_UNIFORMS`, CPU emulation functions (`svgfReprojCPU`, `svgfVarianceFromMomentsCPU`, `svgf7x7FallbackCPU`), `runSVGFRealWebGPU`. Tests: 9 test groups in `__tests__/svgfReal.test.ts` covering identity reprojection, convergence, disocclusion reset, object-ID mismatch, variance-from-moments Eq. 5, 7×7 spatial fallback, end-to-end 50-frame convergence, WGSL export content, and `packSVGFReprojUniforms` round-trip.

- **T2.H4 — Full Veach §10.3 BDPT MIS** (`packages/shared-samplers/src/bdptMIS.ts`): implements `buildBDPTStrategyPDFs_full` + `bdptConnectionMIS_full` + `geometricTermG` and the `BDPTFullVertex` interface. `buildBDPTStrategyPDFs_full` performs the complete Veach Algorithm 10.4 recursive-ratio sweep — O(k) enumeration of all k+1 strategy PDFs for a BDPT path of length k (PBR4e Eq. 16.16). Left sweep: `p_{s−1} = p_s · pdfRev[s] / (pdfFwd[s] · G(v_{s−1}↔v_s))`; right sweep: `p_{s+1} = p_s · pdfFwd[s+1] · G / pdfRev[s+1]`. Specular vertices zero out all strategies whose sweep passes through them (§10.3.5). `geometricTermG` implements `|cos θᵢ · cos θⱼ| / ‖xᵢ − xⱼ‖²` (§8.3.2). Existing `_partial` API retained as deprecated alias — no breaking changes; all 36 prior `bdpt.test.ts` tests continue to pass. New `__tests__/bdptVeachFull.test.ts`: 31 tests across 6 groups (T1 path-length-2 sum-to-1; T2 equal-PDFs 1/N recovery; T3 specular zero-weight; T4 recursive-ratio invariance vs. independent manual accumulation; T5 analytic G formula spot-checks; T6 camera/light endpoint and degenerate-input guards). Tolerances: 10–12 decimal places for ratio invariance; analytic G checks to 12 decimal places.

- **T1.C — DDGI pipeline behavior tests (CPU emulation)** (`packages/walkaround-hybrid/__tests__/ddgiPipeline.test.ts`): CPU mirror of the full DDGI producer→blend→border→receiver chain. Mirrors: `haltonSO3Rotation` (Shoemake 1992 SO(3), probeUpdateFrameParams.ts:19-60), `ddgiRayDirection` / `uniformSphere` / `rotateAngleAxis` (hammersley.wgsl.ts), `cosineBlend` with EMA (probeUpdateBlend.wgsl.ts post-M7 cosine kernel), `receiverMultiply` (applyDDGIShading.ts albedo/π receiver), and `borderMirror` (reused from ddgiBorderMirror.test.ts). Four behavior tests: (1) uniform white room atlas per-cell EMA converges to expected weighted-mean radiance within ±5% after 50 frames at hysteresis=0.97; (2) receiver multiply `(albedo/π)·E = albedo` for albedo=0.5, E=π within 1e-3; (3) Halton SO(3) decorrelation — frames 0 and 1 produce <50% direction overlap at 0.1 rad threshold (49.5% observed, deterministic); (4) cosine-kernel non-negativity — no per-channel atlas value is negative for positive inputs. Plus a 5-test SO(3) matrix sanity suite (det=1, R·Rᵀ=I) for 100 frame indices.

- **T1.D — Test gap fills** (`feat/sweep-2026-05-12-followup`): three surgical test additions with no production-code changes. (1) *D1 — VNDF importance-sampling normalization* (`packages/pt-webgpu/src/__tests__/energyConservation.test.ts`): new `describe('T1.D1 …')` block; uses importance sampling against the VNDF itself (Heitz 2018 §3) to verify `(1/N) · Σ D(h_i)·(wo·h_i)/(N·wo)/p_VNDF(h_i)` converges to analytic `1/G1(wo)` within ±2% at N=10000, for roughness ∈ {0.1, 0.5, 0.9}. (2) *D2 — 2-light sum-MIS correctness* (`packages/pt-webgpu/src/__tests__/multiLightMis.test.ts`, new file): CPU mirror of the WGSL per-light NEE evaluation (Item 15); 4 tests: 2-light sum equals A_only + B_only (5% tol), symmetric positions give equal contributions (1% tol), doubling intensity doubles contribution (1% tol), MIS-weighted sum matches raw sum when lightPdf ≫ brdfPdf (5% tol). (3) *D3 — BVH 200-tri complex-scene round-trip* (`packages/shared-bvh/src/__tests__/bvhEncoding.test.ts`): extends 33-G with a 10×10 procedural grid plane (200 triangles); traces 1000 random rays, asserts BVH traversal tHit matches brute-force oracle to within 1e-5 for every hit ray. 0 mismatches observed.

- **T1.B — CPU mini-ray-tracer + MC convergence tests** (`@vitrum/pt-webgpu`, sweep-2026-05-12 follow-up Phase B): adds `packages/pt-webgpu/src/__tests__/cpuTracer.ts` — a test-only, BVH-free TypeScript path-tracer (~250 lines) that mirrors the core WGSL math from `pathTraceBruteforce.wgsl.ts` and `common.wgsl.ts`. Helpers: `cosineHemisphereSample` (ONB + Malley's method), `sampleGgxVndfTangent` (Heitz 2018 Algorithm 1), `frDielectric` (PBR4e §9.3), `schlickFresnel`, `intersectTriangleMT` (Möller-Trumbore), `intersectAabb` (Williams 2005 slab test + `safeInvDir`), `powerHeuristic` (Veach β=2), `integratePath` (NEE direct + cosine-hemisphere next-bounce + Russian roulette). All functions use a deterministic Park-Miller LCG; no `Math.random()`. New `packages/pt-webgpu/src/__tests__/mcConvergence.test.ts` with 4 tests: (1) Lambertian disc under unit directional light — mean radiance ≈ ρ/π ≈ 0.2228, ±5% at N=2000; (2) single-bounce diffuse from point-light proxy of area light — mean radiance ≈ (ρ/π)·L/r², ±15% at N=2000; (3) white furnace (ρ=1, env=1, open scene) — per-channel mean ≈ 1.0, ±1% at N=5000; (4) perfect mirror reflection — deterministic reflect-then-env-lookup, all N=100 samples return exactly env(reflected\_dir), ±0.1%. All 4 tests pass. Timeout 60 s per test.

- **Albedo demodulation in the à-trous indirect chain** (sweep 2026-05-11, Item 24): implements Schied 2017 §4.1 albedo demodulation for the indirect GI channel. `shade.wgsl.ts` now writes the albedo-demodulated lighting signal (`Lo_indirect / albedo`) to `hdrIndirectOut` and the visible-point diffuse albedo to a new `hdrAlbedoOut` texture (binding 14 in the frame BGL). `indirectCombine.wgsl.ts` re-multiplies the denoised lighting by albedo at combine time: `output = direct + filtered_lighting × albedo`. This keeps high-frequency albedo variation (texture edges, material boundaries) out of the à-trous spatial filter, preventing material-boundary bleed into the denoised lighting. `runAtrousVarianceWebGPU` gains an optional `albedoRgb` parameter: when supplied, it demodulates the input before uploading to the GPU and re-modulates the output after readback. New resources: `albedoTexture` (rgba16float, full-res) added to `FrameResources` with matching create/destroy lifecycle. Files changed: `shade.wgsl.ts`, `indirectCombine.wgsl.ts`, `resourceManager.ts`, `bindGroupLayouts.ts`, `bindGroupBuilders.ts`, `WalkaroundGPUPipeline.ts`, `atrousVarianceWebGPU.ts`. Tests: +11 in `shared-denoisers` covering demodulate/remodulate helpers, black-surface clamp, checkerboard round-trip invariant, and WGSL contract assertions; `albedoRgb` buffer-shape validation added to `assertAtrousVarianceWebGPUBufferShapes`.

- **`@vitrum/walkaround-hybrid` GTAO Jiménez 2016 slice integral** (sweep 2026-05-11, Item 23): `gtao.wgsl.ts` replaces the simplified Bavoil-style HBAO formula `(h1+h2)/π` with the full Jiménez 2016 §4.2 Eq. 11 closed-form slice integral, verified against Intel XeGTAO.hlsli. Per slice: surface normal is decoded from the G-buffer and projected onto the slice plane to derive γ (the signed projected-normal angle from the view axis); horizon angles are clamped to ±π/2 around γ to restrict integration to the upper hemisphere relative to the surface normal; each side's contribution is `(cos(γ) + 2·h·sin(γ) − cos(2·h − γ)) / 4`; the per-slice result is weighted by `projNormalLen` before averaging over slices. New `gtaoSliceIntegral(h, n, cosN)` helper. Multi-bounce (Jiménez §5.2) deferred — albedo G-buffer not wired into GTAO pass. 12 new tests added to `sprint15-gtao.test.ts`: 4 structural shader-string checks + 8 analytical CPU-mirror tests of the slice integral.

- **`@vitrum/walkaround-hybrid` RC octahedral solid-angle normalization** (sweep 2026-05-11, Item 22): replaced uniform `4π/N` Monte-Carlo normalization in `walkaroundDiffuseLighting.ts` with per-bin solid-angle weights `Ω_i` from a precomputed `computeOctahedralSolidAngles(gridSize)` lookup (new `rc/octahedralSolidAngles.ts`). The octahedral grid is not solid-angle-uniform — bins near fold edges subtend ~15% less solid angle than central bins at N=4. The receiver irradiance integral `E = Σ L_i · cos(θ_i) · Ω_i` now uses per-bin `Ω_i` rather than the constant average. Formula: per-bin solid angle computed numerically by decoding the four corner directions and summing the two-triangle spherical quad area (`(‖d1‖ + ‖d2‖)/2`), subdivided 16×16 per cell for < 0.05% error. Reference: Cigolle et al. 2014, JCGT §A.2.
- **`@vitrum/walkaround-hybrid` RC merge integral weighted by actual solid-angle** (sweep 2026-05-11, Item 21, Path A): the `cascadeMerge.wgsl.ts` kernel previously averaged 4 children with equal weight (`÷4`), which is correct only when each parent bin covers exactly 4× the child solid angle (Sannikov paper assumption). The current `CASCADE_DIMS` use non-Sannikov scaling (~2.7–7.2× probe-count ratio, not the paper's /8; ~2.5–3× interval ratio, not ×4) so the ÷4 assumption violated energy conservation across cascade levels. Fix: new `octCellSolidAngle(cx, cy, N)` WGSL helper computes per-bin solid angle inline using the octahedral corner-decode approach; merge is now `merged = Σ child_i · Ω_i / Σ Ω_i`. The non-Sannikov scaling is now explicitly documented in `cascadePyramid.ts` header. References: Sannikov 2023, §3; Cigolle et al. 2014, JCGT §A.2.
- **`@vitrum/walkaround-hybrid` RC solid-angle test suite** (sweep 2026-05-11, Items 21–22): new `__tests__/rcSolidAngles.test.ts` — 14 tests: length-N² return for gridSize ∈ {4,8,16,32}; sum ≈ 4π within 1e-3 relative for all grid sizes; all weights positive and bounded by 2π; 4-way octahedral symmetry holds within 1% for N=4 and N=8.
- **`@vitrum/walkaround-hybrid` DDGI atlas border fill pass** (sweep 2026-05-11, Item 3): new `ddgi/wgsl/probeUpdateBorder.wgsl.ts` implements `probeUpdateBorderIrradiance` and `probeUpdateBorderVisibility` compute kernels that populate the 1-pixel border ring of every probe's octahedral atlas cell per Majercik 2019 §3.2 / Cigolle 2014 §A.1. Fixes systematic darkening at octahedral seams where bilinear sampling previously read zero-initialized border pixels. Implementation: after each `probeUpdateBlend` dispatch, the host copies the blend-output atlas into a per-atlas scratch texture (`irrScratchTex` / `visScratchTex`) to sidestep the WebGPU restriction against simultaneous `texture_2d` read + `texture_storage_2d` write on the same texture, then dispatches the border pass reading from scratch and writing to the write-side atlas. Two new pass-layout slots (`ddgi-border-irr`, `ddgi-border-vis`) appended after `indirect-combine`; `MAX_PASS_COUNT` raised from 26 to 28.
- **`@vitrum/walkaround-hybrid` DDGI border-mirror CPU test suite** (sweep 2026-05-11, Item 3): `__tests__/ddgiBorderMirror.test.ts` — 24 tests covering: (1) every border texel maps to an interior source for both N=8 (irradiance) and N=16 (visibility); (2) eight spot-check mirror coordinates match reference formulas; (3) uniform interior → uniform borders (no zero-pollution); (4) bilinear sampling at left/right/top seam returns interior value, not 50% darkened; (5) interior pixels are not modified by the fill; (6) pass-layout slot presence and ordering assertions.

- **`OIDNDenoiseOptions.tensorNames`** — optional ONNX I/O tensor name overrides (`@vitrum/shared-denoisers`).
- **`scripts/run-fork-shader-smoke.mjs`** + root **`npm run fork-shader-smoke`** — runs sibling `three-gpu-pathtracer` shader string regression (`VITRUM_FORK_DIR` override).
- **`tools/benchmark-runner/scenario-presets.mjs`** — canonical scenario metadata for gap-closure captures.
- **`plan/renderer-fidelity-matrix.md`** — capability honesty matrix (WebGL vs WebGPU).
- **`CONTRIBUTING.md`** — contributor commands and evidence gates.
- **`.github/workflows/ci.yml`** + root **`npm run release:dry-run`** — CI-equivalent mechanical checks and package dry-run scaffold.
- Workspace **`npm run typecheck`** — runs `tsc --noEmit` in each package that defines a `typecheck` script.
- **`plan/binding-babylon-sketch.md`** — future second-binding checklist against `@vitrum/core`.
- **`plan/walkaround-without-three.md`** — module-by-module note on THREE coupling and RC re-composition scope.
- **`@vitrum/walkaround-hybrid`**: exported **`hostScene/types.ts`** seam types (`WalkaroundBVHSceneRoot`, `WalkaroundDDGIScene`, `WalkaroundThreeHostScene`).
- Unit tests: **`@vitrum/three-bindings`** (`sceneFromThreeJS` smoke), **`@vitrum/pt-webgl`** (factory validation).
- **`@vitrum/shared-bvh` BVH encoding round-trip tests** (Test 33-G): new `packages/shared-bvh/src/__tests__/bvhEncoding.test.ts` with 5 tests: (1) pt-webgpu builder relative-offset invariant on a unit-box mesh; (2) `validateBvhEncoding` passes on a valid buffer and throws on an invalid absolute-encoded buffer; (3) cross-package traversal identity — 1 000 random rays through a unit-box BVH produce self-consistent hit results (hit points lie on box surface within 1e-4); (4) canonical ray through box front face hits at the expected distance (t ≈ 1.0 within 1e-4). Includes a test-only TS mirror of the WGSL relative-offset traversal loop (slab test + Möller-Trumbore + relative-offset child indexing).
- **`@vitrum/shared-bvh`**: `validateBvhEncoding(nodeBytes, totalNodes)` — exported validation helper; asserts every interior node's `rightChildOrTriOffset ∈ [1, totalNodes)`. Throws with a diagnostic message on first violation.
- **`@vitrum/pt-webgpu`**: initial functional backend prototype (no longer stub), including:
  - progressive accumulation compute path,
  - CPU-built BVH + GPU BVH traversal,
  - multi-bounce diffuse/specular/emissive baseline shading,
  - directional + point direct-light support,
  - package test suite (`scenePack`, `buildCpuBvh`).
- **`packages/pt-webgpu/README.md`** documenting current capabilities, limits, and next steps.
- **`@vitrum/core`**: `GpuDetection.adapterKind` (`WgpuAdapterKind`), optional `detectGpu({ publishToWindow })`, `probeWebGPU()` returns `adapterKind`; `probeWebGPU` / `isSwiftShaderAdapter` re-exported; JSDoc on `Material` mutability vs `readonly material` on primitives.
- **`@vitrum/walkaround-hybrid`**: `HybridEngineOptions.verbose` gates ReSTIR pipeline init / compile `console.log` calls (still enabled when `debug` is true).
- **`@vitrum/pt-webgpu`**: WGSL sampling via `@vitrum/shared-samplers` (`HAMMERSLEY_WGSL`, `OCTAHEDRAL_CORE_WGSL`); deprecated re-export alias `OCTAHEDRAL_WGSL`.

- **Sprint 10c — BDPT sampler library** (`@vitrum/shared-samplers`, 2cf887a): `BDPTVertex` layout (3 RGBA32F texels = 12 floats per vertex) + pack/unpack utilities + kind constants; `buildBDPTStrategyPDFs` with Veach 1997 power-heuristic MIS (β=2 default). 35 BDPT tests. Fork patch spec in `plan/sprint-10c-pt-fork-patch.md` (texture-ping-pong chosen over MRT; 9 color attachments exceeds `MAX_DRAW_BUFFERS=8`).
- **Sprint 12 — hero-wavelength spectral sampler** (`@vitrum/shared-samplers`, 2cf887a): CIE 1931 2° CMF tables (81 entries, 380–780 nm @ 5 nm) + D65 illuminant + Bradford `xyzToLinearSRGB`; `sampleHeroWavelength` via piecewise-linear Y-CMF CDF inversion (PBRT §4.6.2); `cauchyIOR` + `abbeNumber` + 3 glass presets (crown/flint/lead-crystal). 50 spectral tests.
- **Sprint 13 — custom WebGPU neural denoiser scaffold** (`@vitrum/walkaround-hybrid`, 2cf887a): 5 WGSL inference primitives (`conv2d`, `transposedConv2d`, `relu`, `skipConnection`, `bilinearUpsample`); `InferenceGraph` orchestrator; `WALKAROUND_DENOISER_UNET_SPEC` at 24→48→96→192→96→48→24→3 channel widths (~1.63 MB weights, within 1–3 MB DoD). 101 neural tests. Training scaffold in `tools/neural-denoiser-training/`. (Note: scaffold later deleted in sweep-M2.3 due to 8 correctness blockers — see Removed.)
- **External RFE-01..05 — `@vitrum/core` API contract additions** (2cf887a): `SpectralCurve`, `SurfaceAbsorptionLayer`, `ThinFilmLayer`, `ThinFilmStack` types in `scene.ts`; `Material` gains `spectralAttenuation`, `dispersionAbbeNumber`, `scatteringCoefficient*`, `frontLayer`/`backLayer`, `thinFilmStack`; `EngineOptions` gains `causticStrategy`, `mneeMaxIterations`, `mneeMaxChainLength`; `EngineCapabilities` reflects `causticStrategy`.
- **Sprint 15 — half-res GTAO + bilateral upsample** (`@vitrum/walkaround-hybrid`, 8308451): `gtao.wgsl.ts` horizon-based AO (4 angular slices × 6 log-spaced steps, depth-aware horizon tracking, jittered base angle); `gtaoUpsample.wgsl.ts` joint bilateral upsample (depth+normal edge stops, Kopf 2007). AO modulates non-emissive terms in `shade.wgsl`. New `aoHalfTexture` (r16f half-res) and `aoFullTexture` (r16f full-res, seeded 1.0). (Slice integral subsequently upgraded to Jiménez 2016 in sweep Item 23.)
- **Sprint 16 — ReSTIR-GI RIS pass + reservoir storage** (`@vitrum/walkaround-hybrid`, 1203df9 + 273994c): `ReservoirGI` storage helpers (half-res buffer, pack/unpack); per-pixel half-res RIS over M=8 cosine-hemisphere candidates with DDGI atlas reconnection-vertex radiance; visibility ray zeros occluded samples. `shade.wgsl` reads GI reservoir at `gid.xy/2` for `Lo_indirect`. Reference: Majercik 2021 §4.2.
- **Sprint 17 — ReSTIR-GI temporal + spatial reuse** (`@vitrum/walkaround-hybrid`, 7d307f6): `gi-temporal` pass reprojects half-res visible point through previous view matrix, rejects on depth/normal mismatch, M-clamps history at 20, Jacobian reconnection-shifts previous sample, combines via standard RIS update. `gi-spatial` (2× ping-pong): pulls K=5 random neighbours within a 12-px disc, geometric-reject, Jacobian-shift, combine. End-of-frame copy `reservoirGiCurrent → reservoirGiPrevious`. References: Majercik 2021 §§4.4, 5.2; Bitterli 2020 §5.2.
- **Sprint 18 — per-channel SVGF split (direct + indirect)** (`@vitrum/walkaround-hybrid`, 5bd6cd1): `shade.wgsl` splits radiance into `hdrColorTexture` (direct+emit) and `hdrIndirectTexture` (indirect); existing à-trous chain denoises direct with tight edge stops; new `indirect-combine` pass applies broader 5×5 bilateral blur to the ReSTIR-GI signal then sums into `combinedDenoisedTexture`. Per-channel split transparent to PPG training and downstream temporal accumulator.
- **Sprint 18 follow-up — real per-channel à-trous chain** (`@vitrum/walkaround-hybrid`, 1edb689): replaced the single-channel à-trous chain with a true dual-channel chain — separate variance estimation and 3-iteration à-trous passes for each of direct and indirect, with channel-appropriate edge-stop sigmas.
- **`@vitrum/walkaround-hybrid` — adaptive-sampling tier wired into ReSTIR-GI RIS** (025e15f): Welford-variance `tierTexture` output from the `sample-budget` pass now drives per-pixel candidate counts in `gi-ris` (converged pixels draw 2 candidates; high-noise draw 8).
- **`examples` — stress/scale test scene + WebGL2 fallback walkaround page** (c59a36d, 72c3ca7): `?scene=complex` loads a procedural stress scene; new `walkaround-webgl2/` page drives `pt-webgl` as a cross-engine fallback for the walkaround-mode slot.
- **`@vitrum/walkaround-hybrid` — per-engine isolated pages + robustness harness** (3f41e95): per-material DDGI packing extracted to pure `packDDGIMaterials` function (9 tests); `?cameraMotion=1` automated camera orbit; `globalThis.__vitrumResetScene()` scene lifecycle stress test; per-pass GPU timing telemetry (`9d951d3`).
- **Sprint 9 adaptive sampling wired into pipeline** (`@vitrum/walkaround-hybrid`, ef0c79b): `sample-budget` pass (prepended before RIS) computes per-pixel tier from Welford variance and writes r32uint `tierTexture`; `resolve` pass (between `temporalAccum` and `composite`) reads accumulated radiance and motion vectors and writes `resolvedTexture`.
- **RFE-09 — `@vitrum/pt-webgl` fork uniform bridge** (c6e4a88): new `forkUniformBridge.ts` drives `PhysicalPathTracingMaterial` uniforms from `vitrum.Material` stamps: Sprint 7 volume/SSS uniforms (`u_volumeDensity`, `u_sssSigmaT`, `u_anisotropyG`, `u_scatterAlbedo`, `u_sssAlbedo`, `u_sssAnisotropyG`), Sprint 8 dispersion/IOR uniforms + Cauchy coefficients, Sprint 12 CMF tables (81-entry CIE XYZ arrays, uploaded once per scene), RFE-14 thin-film uniforms.
- **`@vitrum/walkaround-hybrid` library-generality UBO tunables** (0088a78): `WalkaroundUBO` gains 7 new host-overridable f32/u32 fields (`emitterDist2Floor`, `directFireflyClamp`, `causticBoost`, `causticVisClamp`, `temporalMClampDI`, `spatialReuseRadiusPx`, `spatialDepthTolFloor`); GTAO UBO gains `bilateralDepthSigma`. Audit findings B1, B3, B4, M1, M6, M7, M8, M12 all closed. `HybridEngineOptions` surface additions: `emitterDist2Floor`, `directFireflyClamp`, `caustic: { boost, visClamp }`, `temporalMClampDI`, `spatialReuseRadiusPx`, `spatialDepthTolFloor`, `gtao: { radiusPx, intensity, depthThresholdWorldUnits, bilateralDepthSigma }`.
- **`@vitrum/walkaround-hybrid` host-side library-generality options** (328a659): `HybridEngineOptions` gains `cameraMoveResetThresholdSq` (B8), `temporalAccumAlpha` (M3), `targetFrameIntervalMs` (M4); `defaultIsSceneReady` heuristic changed from `total >= 200` to `total > 0` (M5).
- **`@vitrum/walkaround-hybrid` adaptive-sampling thresholds + PPG cell cap** (bd7a7d1): `HybridEngineOptions.adaptiveSamplingThresholds?: [low, high]` plumbs through to sample-budget UBO (default `[0.01, 0.10]`); `HybridEngineOptions.ppgMaxSpatialCells?` plumbs through `createFrameResources → createPPGBuffers` (default 10 000 cells / 3.4 MB). Audit M2 and M10 closed. All 27 library-generality audit findings now closed.
- **Fork patches Sprint 7 + 8 + 12 applied to `three-gpu-pathtracer`** (191a87c): Sprint 7 volume march (`volume_march.glsl.js`): `sampleExponential`, `equiAngularPdf`, `hg_phase`, `sampleHG_glsl`, `volumeMarch` — GLSL mirror of vitrum's HG+equiAngular; Sprint 8 spectral dispersion: Cauchy-B IOR + `dispersionStrength` uniform; Sprint 12 spectral accumulator fork patch (RFE-08): `uCmfX/Y/Z[81]` + `uYCmfCdf[82]` + `uYCmfIntegral` hero-wavelength uniforms uploaded once per scene set.

### Changed

- **Three.js pinned to 0.171.0 via `overrides` to match stainedGlass app** (76f7593, root `package.json`): vitrum's transitive resolution previously installed `three@0.184.0` (top of the `>=0.167.0 <0.190` range). When the stainedGlass host (pinned at `three@0.171.0` exactly) imports vitrum via `file:` link, both copies loaded in the browser and React / R3F warned "Multiple instances of Three.js". The two-instance case isn't just a warning: scene primitives constructed against one three's classes don't pass `instanceof` checks against the other's, breaking `sceneFromThreeJS` round-trips, `WebGPURenderer` scene traversal, and material baking — load-bearing root cause for the stainedGlass walkaround/PT regressions investigated 2026-05-12. Override is one line; lockfile rebuilt to dedupe. Verified: pt-webgl 55/55, three-bindings 56/56, walkaround-hybrid 332/332 — all tests green at 0.171.0.

- **T3.B — root `README.md` rewrite as drop-in quick-start + capability matrix** (dd67889): replaces the package-architecture-first README with a developer-first README oriented around the new `@vitrum/engine` facade. Leads with the 5-line hello world (`attachVitrum`) and the React variant (`<VitrumCanvas>`). Adds a capability matrix table (walkaround-hybrid vs pt-webgl across GI quality, bounces, materials, caustics, animation, hardware, convergence) — honest about limitations (animation column links to `plan/animation-support-status.md`). When-to-use-which-engine paragraph with the `prefer='auto' | 'realtime' | 'quality'` decision matrix. Public API table — which package to import what from. Architecture diagram updated to show `@vitrum/engine` on top + `@vitrum/dev` as a leaf. Performance budgets table with actual cornell-box times captured at 64 SPP. Novel-techniques section adds hero-wavelength MIS, SVGF, and ReSTIR-GI to the citation list.

- **Factory bootstrap `setScene` rationale documented** (`@vitrum/walkaround-hybrid` factory, c4c306a): the factory `createWalkaroundEngine_Hybrid` calls `engine.setScene({ primitives: [], emitters: [], environment: { kind: 'none' } })` as a bootstrap to kick off `_initPipeline()`. With the new in-flight init guard (below) in place, this is no longer dangerous. Decision: KEEP the bootstrap; comment-only commit documenting why. Two concrete callers depend on it: (1) hosts that pass `threeScene` at construction and never call `setScene` themselves (e.g. `examples/two-engines-one-scene/src/main.ts`) — without the bootstrap, `_initPipeline` never fires and the engine stays `'uninitialized'` forever; (2) hosts that call `setScene` right after the factory (e.g. `@vitrum/engine.createEngine` line 200) — the host's setScene fires init-B which races and bumps `_initSeq`; the bootstrap init-A's checkpoints see the seq mismatch and dispose their locals via the `finally` block (bootstrap is wasted work but safe).

- **`@vitrum/pt-webgpu` BVH encoding canonicalized to relative offsets** (Foundations #26 / D10): `buildCpuBvh` now stores `rightChild − nodeIndex` (relative) instead of the absolute right-child node index. The WGSL traversal in `pathTraceBruteforce.wgsl.ts` updated to match (`let rightChild = nodeIdx + node.rightChildOrTriOffset`). Both sides updated atomically — a partial update would silently produce wrong pixels. Encoding now matches `shared-bvh/normalizeBvhInteriorOffsets` and walkaround-hybrid's `common.wgsl` convention. Dev/test mode: post-build invariant check (`1 ≤ offset < totalNodes` per interior node) added inline. `buildCpuBvh.test.ts` extended with relative-offset assertion.
- **`@vitrum/shared-bvh` index-buffer stride contracts formalised** (Foundations #27): exported `BvhIndexStride = 3 | 4` type; added `bvhIndexStride: 3` field to `SceneBVHCommonResult` (`buildSceneBVH` always returns stride 3 — callers that need stride 4 must post-process). `validateBvhEncoding(nodeBytes, totalNodes)` helper exported for cross-package invariant enforcement. pt-webgpu `uploadSceneBuffers.ts` adds an upload-time assertion (`indices.byteLength % 16 === 0`) and documents the stride-4 zero-fill contract on `PackedSceneData.indices`.
- **`@vitrum/pt-webgpu` BVH builder replaced with binned SAH** (Foundations #31 / D10): `buildCpuBvh.ts` median-split replaced with Wald 2007 binned Surface Area Heuristic (K=16 bins per axis). All three axes are evaluated; the cheapest split plane is chosen. If no split is cheaper than a leaf, a leaf is created early (median-split never exploits this). Relative right-child offsets preserved throughout. Hit results are identical to median-split for the same geometry (different BVH topology, same intersections).

- **`@vitrum/shared-denoisers` + `@vitrum/walkaround-hybrid`** (D3 sweep): renamed `svgf` → `atrous-variance` throughout. The shipping denoiser is à-trous wavelet filtering + per-pixel Welford variance scalar — never a Schied 2017 SVGF implementation (no bilinear reprojection, no disocclusion detection, no per-pixel history length). Real SVGF design tracked in `plan/sprint-svgf-real-future.md`.
  - **`shared-denoisers`**: `svgf.wgsl.ts` → `atrousVariance.wgsl.ts`; `svgfConstants.ts` → `atrousVarianceConstants.ts`; `svgfBindings.ts` → `atrousVarianceBindings.ts`; `svgfWebGPU.ts` → `atrousVarianceWebGPU.ts`. All exported symbols renamed (`SVGF_WGSL` → `ATROUS_VARIANCE_WGSL`, `SVGF_COMPUTE_WORKGROUP_SIZE` → `ATROUS_VARIANCE_COMPUTE_WORKGROUP_SIZE`, `runSvgfWebGPU` → `runAtrousVarianceWebGPU`, etc.). WGSL struct names renamed: `SVGFVarianceUBO` → `AtrousVarianceVarianceUBO`, `SVGFAtrousUBO` → `AtrousVarianceAtrousUBO`.
  - **`walkaround-hybrid`**: `pipelineCompiler.ts`, `bindGroupBuilders.ts`, `timestampQueries.ts`, `WalkaroundGPUPipeline.ts`, `resourceManager.ts` updated. Pass slot labels renamed (`svgf-variance` → `atrous-variance-variance`, `svgf-atrous-0..2` → `atrous-variance-atrous-0..2`). Frame resource field `svgfVarianceEstimateTexture` → `atrousVarianceEstimateTexture`.
  - **`@vitrum/core`**: `EngineOptions.denoiser` union extended with `'atrous-variance'`.
  - **`HybridEngineOptions.denoiser`**: `'svgf'` kept as deprecated alias with one-time `console.warn`; normalizes to `'atrous-variance'` internally.
- **Docs / packaging honesty**: root `README.md` (denoisers list, walkaround capability wording), `@vitrum/shared-denoisers` / `@vitrum/pt-webgpu` package descriptions, `packages/walkaround-hybrid` README + `package.json` + `src/index.ts` header — align marketing with shipped code (BMFR not exported; RC toolbox vs `HybridEngine` composition).
- **`@vitrum/walkaround-hybrid`**: Sprint 10a **atrous-variance** path wired in `WalkaroundGPUPipeline` (default): temporal Welford luminance pass + `svgfVarianceMain` + three `svgfAtrousMain` iterations; `HybridEngineOptions.denoiser` (`'atrous-variance'` default, `'atrous'` legacy). New frame textures: Welford ping-pong, `atrousVarianceEstimateTexture`, zero `motionVectorTexture`. `welfordTemporal.wgsl.ts` + expanded GPU timestamp pass slots (14).
- **`@vitrum/shared-denoisers`**: `svgf.wgsl` **à-trous** pass now decodes packed G-buffer like walkaround `atrous.wgsl` (normal `xyz*2-1`, depth `.w`).
- **`tools/benchmark-runner/README.md`**: documents `VITRUM_STRICT_GAP_CLOSURE=1` strict exit behavior (with `run-gap-closure-verification.mjs`).
- **`plan/library-architecture.md`**: fork consumption via sibling `file:` `three-gpu-pathtracer`; BMFR call-out; `@vitrum/pt-webgl` / `pt-webgpu` dependency bullets per actual `package.json`.
- **`@vitrum/pt-webgpu`**: `pathTraceBruteforce.wgsl` — shared `traceMeshBvh` / `traceAnalyticShapes` backing for `traceClosest` and `traceAny` (D2 deduplication).
- **`@vitrum/walkaround-hybrid`**: Sprint 11 PPG spatial lookup — kd-tree buffer + GPU traversal in `ppgSample` and `ppgUpdate`; host `writePpgKdTree` and `buildPpgKdTreeGpuBytes` (E6).
- **Fork (`three-gpu-pathtracer`)**: spectral Beer–Lambert reads packed μ(λ); NEE threads hero wavelength; `transmissionEval` PDF uses Walter-style GGX Jacobian with matching GGX half-vector sampling (shader smoke checks extended).

- **`@vitrum/three-bindings` RFE userData round-trip** (1036a8c): `material.ts` now projects `userData.vitrum*` stamps to `vitrum.Material` fields on THREE→vitrum conversion (`vitrumDispersionAbbeNumber`, `vitrumScatteringCoefficient*`, `vitrumSpectralAttenuation`); `vitrumSceneToThree` writes the same stamps back so fork-side material packers see fresh data after a scene rebuild. Closes RFE-10.
- **`@vitrum/walkaround-hybrid` `ProbeGrid` decoupled from THREE/WebGPU** (c7f7638): `ProbeGrid` no longer imports THREE or the WebGPU backend; it operates on plain numeric inputs so it can be unit-tested and composed without a full renderer context.
- **`@vitrum/walkaround-hybrid` + `@vitrum/pt-webgl` tier-A refactors** (e9d52f4): `pt-webgl` `transmissiveBounces` now `min(per-frame bounces, maxBouncesLimit)` — removes hardcoded 12-cap vs structural limit; walkaround-hybrid shared PBR-scalar helper extracted to `@vitrum/three-bindings` (f8a36dd / P3-B.3).
- **`@vitrum/pt-webgpu` FrameParams repack + module split** (e9dd0b6, efdd5e0): dropped legacy `first*` accumulator fields; `FrameParams` UBO slots recompacted; `uploadSceneBuffers.ts` split into focused modules (geometry, materials, lights, BVH upload).
- **`@vitrum/walkaround-hybrid` audit surgical correctness** (7b159c0): B5 — removed empirical `* 0.08` sky aperture multiplier in `shade.wgsl` (double-scaling physical Lambertian); B6 — DI+GI reservoir `copyBufferToBuffer` folded into main command encoder before `queue.submit()` to close a next-frame temporal-read race at high FPS; B7 — `HybridEngine` constructor now throws `TypeError` for unknown `denoiser` string.
- **`@vitrum/walkaround-hybrid`**: RC `cascadeDispatch` / `bvhCompute` / `nodeMaterialUpgrade` typing cleanups (backend view, `instanceof` material packing, `Record` copy).
- **`@vitrum/babylon-bindings`** (removed 2026-05-11): stub package deleted — zero external consumers and no implementation work scheduled. Babylon binding remains a future option via `plan/binding-babylon-sketch.md`.
- **`@vitrum/pt-webgpu`**: material stride **22** vec4s with thin-film extinction + incident IOR; **multi-light** storage buffers and WGSL direct-light loops; `brdfDirectionalPdf` includes an active transmission-hemisphere term; Playwright adapter passes scenario query params and viewport size from env.
- **`@vitrum/pt-webgl`**: package is explicitly `private` while it depends on the sibling fork via `file:`.
- **`@vitrum/three-bindings`**: RFE `userData` stamping tests on production `vitrumSceneToThree`; duplicate `pt-webgl` scene converter removed.
- **`HybridEngine`**: RC note in file header now points to `plan/walkaround-without-three.md` instead of a dangling TODO.
- Repo docs now reflect that **`@vitrum/pt-webgpu`** has moved from stub to active prototype (`README.md`, `plan/library-architecture.md`, `plan/phase-6-status.md`).
- **`@vitrum/pt-webgpu`**: **`disc-area`** emitters → rect-area prototype packing (orthogonal half-spans √(π)·r/2 so quad patch area equals π·r²; not uniform disc sampling); **`supportedEmitterKinds`** includes **`disc-area`**; **`renderFrame`** reuses **`GPUBindGroup`** across frames until **`setScene`**, accum texture/realloc or **`dispose`**; **`paused`** **`isConverged`** uses **`samplesTarget`** like the accumulating path.
- **`@vitrum/three-bindings`**: **`vitrumSceneToThree`** **`disc-area`** surrogate + **`mesh-area`** radiance merged into **`MeshPhysicalMaterial.emissive`**; **`sceneFromThreeJS`** adds **`mesh-area`** for emissive meshes and zeros primitive **`emissive`** to prevent double emission.
- **`@vitrum/walkaround-hybrid`** (`InferenceGraph`): clarified buffer contract; **`run()`** enforces **`inputTensors`/`outputTensors`** presence in maps; per-layer **`GPUBindGroup`** cache reduces allocation churn until **`dispose`**.
- **`@vitrum/pt-webgl`**: **`transmissiveBounces`** now **`min(per-frame`** **`bounces,`** **`maxBouncesLimit)`** (**no** hardcoded **`12`** cap vs structural limit).

### Removed

- **`RCDispatcher.dispatchFrame` + `RCDispatchOpts` + `gpuBufferOf` (THREE-tied path)** (`@vitrum/walkaround-rc`, `7f55b6b` + `f329c1d`, 2026-05-18; W8 sub-task 2): dropped the legacy compute-dispatch entry that extracted raw `GPUBuffer` handles via the three.js WebGPU renderer's internal `StorageBufferAttribute.__gpuBuffer` property. The W8 Phase 1B refactor had already added a parallel `dispatchFrameRaw(opts: RCDispatchOptsRaw)` path; `RCSubsystem` (the only consumer) was already calling the raw entry. Grep confirmed zero non-self consumers of `dispatchFrame` / `RCDispatchOpts` / `gpuBufferOf` across packages + examples + _staging. Drops ~150 LOC including `WebGPUBackendView` interface, `_buildEnvBinding` THREE-aware env-texture resolver, `_debugFill` method, and 5 THREE imports. Pre-alpha breaking change. `cascadeDispatch.ts` went 696 → 473 LOC.

- **`EngineCapabilities.supportsMotionBlur` + `FrameInput.shutterTime` re-dropped** (`@vitrum/core` + all engines, 4836da8, W3-D18 redo): the original D18 drop (9ea12c9, 2026-05-17) was silently undone the next day when the A-7 sibling-files split (c2a0006) copied the pre-D18 `engine.ts` shape verbatim into the new `engine/capabilities.ts` file. CLAUDE.md still claimed D18 shipped, so this commit restores the intended post-D18 state. Verified by grep across packages/ + examples/ + tests: 0 readers of either field at the time of the drop, all 3 engines were hard-coding `supportsMotionBlur: false`. Pre-alpha breaking change with no external consumers; motion-blur becomes a future opt-in if a real per-shutter PT engine ever lands.

- **Deprecated `'svgf'` denoiser alias** (`@vitrum/core` + `@vitrum/walkaround-hybrid`, `a659367`): the alias was deprecated for several sprints in favour of `'atrous-variance'` (cheaper) / `'svgf-real'` (full Schied 2017). Pre-alpha breaking change; HybridEngine's coerce-and-warn block is gone. Hosts now get a clear `TypeError` at construction.

- **Deprecated `Material` type alias** (`@vitrum/core`, `614e463`): `Material` was the pre-W3-D1 name kept as an alias for `MaterialSpec`. Four in-tree consumers (MaterialInspector, vitrumSceneToThree, the round-trip test, engine barrel) migrated to `MaterialSpec`; alias dropped.

- **Deprecated `isHardwareGpu` boolean** (`@vitrum/core`, `57c3f16`): `GpuDetection.isHardwareGpu` and `WgpuProbeResult.isHardwareGpu` dropped. Hosts compute the same discriminator as `adapterKind !== 'swiftshader'` (already the implementation behind the boolean).

- **Deprecated process-global `iblBaker` free functions** (`@vitrum/pt-webgl`, `a071e15`): `bakeSkyEquirect()`, `clearSkyEquirectCache()`, and `_skyEquirectCacheSize()` were back-compat shims for the pre-W6-E2 module-level singleton. Zero non-self consumers. Hosts use the per-engine `PTEngineWebGL2.bakeSkyEquirect` method or instantiate `new IblBakerCache()` directly. Pre-alpha breaking change.

- **`@vitrum/walkaround-hybrid` orphaned `PPG_SAMPLE_WGSL`** (24d847a): deleted dead WGSL shader constant that was never imported after the PPG path was refactored; confirmed zero references in the codebase before deletion.
- **`@vitrum/walkaround-hybrid` dead DDGI consumer code in `shade.wgsl`** (ff277b2): removed the legacy DDGI irradiance consumer path in `shade.wgsl` that was kept as a fallback during Sprint 16 bring-up; the ReSTIR-GI reservoir read is now the sole indirect-GI source.
- **`@vitrum/walkaround-hybrid` 2 dead SVGF à-trous pass-layout slots** (6bf2b75): trimmed two unused `pass-layout` placeholder entries that remained from the Sprint 18 channel-split refactor but were never dispatched.
- **three-bindings spectral path** (Foundations #35 / D11): deleted the deprecated `Float32Array` branch from `spectralAttenuation` parsing. Pre-alpha breaking change — no external consumers. Use `{ wavelengthStart, wavelengthEnd, values: Float32Array }` shape. (`@vitrum/three-bindings`)
- **Neural denoiser scaffold deleted** (`@vitrum/walkaround-hybrid`): removed `walkaround-hybrid/src/neural/` (entire directory: `InferenceGraph.ts`, `unetArchitecture.ts`, `wgsl/conv2d.wgsl.ts`, `wgsl/transposedConv2d.wgsl.ts`, `wgsl/relu.wgsl.ts`, `wgsl/skipConnection.wgsl.ts`, `wgsl/bilinearUpsample.wgsl.ts`), `__tests__/sprint13-neural.test.ts`, and `tools/neural-denoiser-training/` (README.md, dataset_spec.md, export_weights.md, train.py.md). All neural re-exports removed from `walkaround-hybrid/src/index.ts`. The scaffold had 8 enumerated correctness bugs (skip-connection shape mismatch, missing input-packing layer, bind-group binding mismatch, never-written uniform buffer, `train.py.md` was a Markdown file pretending to be Python, etc.) that prevented it from running, and was not wired into `HybridEngine` (`denoiser` only accepted `'svgf'` / `'atrous'`). Architecture preserved in `plan/sprint-neural-denoiser-future.md`.

### Fixed

- **Barycentric Gram-determinant divide-by-zero floor in surfaceTextures shadow-ray path** (`@vitrum/walkaround-hybrid`, `fbb8bab`, 2026-05-19): the recomputed-from-hit-position barycentric calculation at `surfaceTextures.wgsl.ts:223` used `denom = d00 * d11 - d01 * d01` unfloored. Möller-Trumbore's `det` precheck doesn't gate Gram-det = 0 (colinear-vertex triangles produce nonzero `det` for non-parallel rays but zero Gram det). Brought in line with `pt-webgpu/src/wgsl/pathTrace/intersection.wgsl.ts:317`'s `max(d00 * d11 - d01 * d01, 1e-8)`, eliminating a NaN/Inf path through `surfaceTextureMod` that the May-17 audit had flagged as a "potential consistency target".

- **`safeInvDir` WGSL false-positive for exact-zero direction components** (`@vitrum/shared-bvh` `3327e8c` + `@vitrum/pt-webgpu` `2740d92`, 2026-05-19): two duplicate WGSL `safeInvDir` helpers (shared-bvh's `bvhIntersect.wgsl.ts:119` + pt-webgpu's `common.wgsl.ts:64`) both used `sign(d.x) * 1e30` for near-zero directions. WGSL `sign(0) == 0` — so an exact-zero direction yielded inv-component `0`, collapsing the X slab's t0/t1 contribution to 0 regardless of origin position. Net effect: slab tests for axis-aligned rays whose origin sat OUTSIDE the AABB on the parallel axis gave false positives (tNear=tFar=0, tFar≥0), and traversal would descend the bogus node and waste cycles on ray-triangle tests for a ray that geometrically misses. Rare in PT (jittered directions are almost never exactly axis-aligned) but probe-ray paths in DDGI / RC using Halton / Hammersley sequences can hit it. Fix: `select(-1e30, 1e30, d.x >= 0.0)` picks a definite sign even when d.x is exactly zero (+0 ≥ 0 in IEEE 754). pt-webgpu's `cpuTracer.ts` CPU mirror updated to match. The duplication exists because W2-C6's shared-WGSL-primitives hoist was lost in the May-17 merge race (`items_to_fix.md` E4) — re-applying E4 would deduplicate both copies into shared-samplers. New 5-test pin in `__tests__/safeInvDir.test.ts` — a CPU mirror that matches the post-fix WGSL semantics, including an explicit regression test for the outside-slab axis-aligned case.

- **W7-G5 `validateBvhEncoding` un-export — re-applied after merge-race loss** (`@vitrum/shared-bvh`, `7f15cba`, 2026-05-19): one of seven 2026-05-17 sprint commits whose changes were silently dropped from HEAD when subsequent commits merged from pre-feature parents (audit details: items_to_fix.md Section E). `shared-bvh/src/index.ts` had reverted to `export * from './bvhCommon.js'` after the W7-G5 commit (95d029a) was supposed to switch to selective named exports. Re-applied: now exports only `buildSceneBVH` / `refitBvhBounds` + the two interface types from bvhCommon.js; `validateBvhEncoding` leaves the public surface. Test that imported it from `'../index.js'` switched to `'../bvhCommon.js'`. Downstream typecheck across walkaround-hybrid / walkaround-rc / pt-webgpu / pt-webgl / engine all clean. The six other audit findings (W2-C6, W3-D6, W3-D7, W3-D19, W6-E1, W6-E6) are documented in items_to_fix.md E1–E6 but deferred — none affects runtime correctness.

- **`engine.debug.bvhNodes()` signed-int32 leaf-flag bug** (`@vitrum/walkaround-hybrid`, `318cb19`, 2026-05-19): the depth-pass in `HybridEngine.bvhNodes()` did `(splitWord & 0xFFFF0000) === 0xFFFF0000` — JS bitwise `&` returns int32, so `splitWord & 0xFFFF0000` evaluates to `-65536`, while the literal `0xFFFF0000` on the right is `4294901760` (a number stored as double). Equality fails, every leaf was classified as interior, and the DFS walked past leaves into their `triOffset` (interpreted as a node index) — either out-of-range-rejecting or thrashing `depths` via revisits. Switched to `(splitWord >>> 16) === 0xFFFF` (the convention already used by `buildArrayBvh.ts` and the TLAS module). Extracted the depth-pass into `debug/packBvhNodesForDebug.ts` for testability; 6 new tests pin the leaf-flag check, the int32-sign-bit collision (triCount=1 → split word 0xFFFF0001 → int32 -65535), and the infinite-loop risk when a leaf's triOffset points back into the tree.

- **`gpuTextureBlit` log-once-per-failure-type now actually dedups** (`@vitrum/dev`, `43fdf37`, 2026-05-19): the comment claimed dedup but the catch unconditionally called `console.warn`, so a sustained failure (lost-device, texture state invalidated) would flood the console at 10 Hz × overlay count. Added a per-blit `Set<string>` keyed by `${err.name}:${err.message}`.

- **GPU-tests opt-in regression re-applied** (test-infra, `48a0ce8`, 2026-05-18): the original `npm test`-doesn't-need-Playwright fix (`006debc`, 2026-05-17) was silently undone 26 minutes later by the W2-C2 BVH-builder merge, which reverted both `packages/pt-webgpu/package.json` and `packages/shared-denoisers/package.json` back to `"test": "vitest run && vitest run --config vitest.gpu.config.ts"`. CLAUDE.md kept claiming the opt-in was in place but it wasn't — fresh clones / CI without `npx playwright install chromium` would crash. Now `npm test` runs node-only across all packages; `npm run test:gpu` and `npm run test:all` provide the explicit opt-ins; CONTRIBUTING.md documents the split.

- **`<VitrumCanvas>` `quality` prop now propagates live without engine teardown** (`@vitrum/engine`, b18fd7a): pre-fix, the React `<VitrumCanvas>` component maintained a `qualityRef` and a `useEffect` that updated it on every `props.quality` change — but `attachVitrum`'s RAF tick captured `opts.quality` once at construction and never re-read the ref, so the qualityRef pattern was dead code and quality prop updates required a full component remount via `key`. `AttachVitrumOptions.quality` now accepts a value OR a getter (`() => quality | undefined`); the RAF tick resolves it every frame via the extracted `resolveQualityOption` helper, so React hosts (and any caller that wants live updates) can pass `() => ref.current`. The value form is still accepted, so the three hero example apps that pass a static quality object are unaffected. Pinned by 4 new tests in `__tests__/qualityPropagation.test.ts`.

- **ReSTIR-GI W-cap reference in `temporalGi.wgsl`** (`@vitrum/walkaround-hybrid`, 4a0158b): `RESTIR_GI_W_CAP` was migrated from a hardcoded WGSL const to the WalkaroundUBO field `restirGiWCap`. The migration updated `risGi.wgsl` and `spatialGi.wgsl` but missed `temporalGi.wgsl:178`, which kept referencing the deleted identifier. The `wgslCompose` test suite verifies only include-graph topology (not WGSL validity), so this would only surface as a shader-compile failure at runtime on a real GPU. Fix matches the sibling shaders: `min(W_raw, ubo.restirGiWCap)`.

- **`applyDDGIShading` StorageTexture wrappers now disposable** (`@vitrum/walkaround-hybrid`, 76d6659): the module-level `_slotStorageTextureCache` WeakMap kept a StorageTexture per AtlasTextureSlot. When the slot key was GC'd the WeakMap entry vanished, but the underlying GPUTexture stayed allocated — `StorageTexture` owns a GPU resource that only `.dispose()` releases. For multi-engine apps this leaked StorageTextures from disposed engines indefinitely. New `disposeApplyDDGIShadingCache()` public export walks every cached StorageTexture via a parallel Set, calls `.dispose()`, and resets the WeakMap. Idempotent. Mirrors the parallel-Set + destroy-before-clear pattern already in `probeUpdatePass._trackedCacheTextures`.

- **`HybridEngine.setSize()` for cheap canvas resize** (`@vitrum/walkaround-hybrid`, d4fb73b): pre-fix, `HybridEngine` had no `setSize` API — hosts had to recreate the entire engine on every canvas resize or DPR change. React's `ResizeObserver` storms compounded this: each tick churned the BVH (50–500 ms CPU build), every compiled pipeline shader (50–500 ms), every DDGI atlas, and ~1 GB of `FrameResources` textures. Fix: new `WalkaroundGPUPipeline.resize(W, H)` destroys and recreates only the per-frame `FrameResources` (full-res rgba16float textures, reservoir buffers, variance buffers, GTAO half/full, SVGF persistent textures), resets ping-pong indices + frame counters, and preserves BVH buffers, all compiled compute pipelines, bind-group layouts, DDGI atlases, per-pass UBOs, timestamp state. Bind groups are rebuilt per-frame in `renderFrame()` so the resize automatically picks up next frame. New `HybridEngine.setSize(W, H)` updates `_width / _height` and delegates to `_pipeline.resize()` when live; defensively no-ops on identical / zero / negative sizes and pre-init pipeline. Cost: 5–30 ms on a 4K surface vs 500–2000 ms for full teardown + re-init. DDGI atlas survives unchanged (world-space, not screen-space). NOT added to the core `Engine` interface — `pt-webgl` resizes internally via `FrameInput.viewport.{width,height}`; each backend manages its own resize semantics. 9 tests in `__tests__/hybridEngineSetSize.test.ts`.

- **`HybridEngine.dispose()` is honest about late-resolving init chains** (`@vitrum/walkaround-hybrid`, f68191c): pre-fix, `dispose()` was synchronous and tore down only what was already published (`_pipeline`, `_bvhBuffers`, `_ddgiTraversalScene`). If an `_initPipeline()` chain was awaiting `pipeline.initialize()` (~50–500 ms of shader compilation), its post-await publish would still write the just-built pipeline into `_pipeline` AFTER `dispose()` had run — engine state was `'disposed'` but the GPU resources were live and orphaned (~1 GB leak on 4K). Fix: `dispose()` now checks `_initRunning` (set true at start of every `_initPipeline` IIFE, false in its `finally`). When `_initRunning` is true, dispose sets `_pendingTeardown = true` + `_disposed = true`, transitions state to `'disposed'` immediately, and DEFERS the actual `_teardownPipeline()` + `_ddgi.dispose()` to the in-flight chain's `finally` block. Idempotent (a second `dispose()` call is a no-op). Deliberately does NOT bump `_initSeq` from `dispose()` — the in-flight chain captured `mySeq` at start and relies on `mySeq === this._initSeq` in the `finally` to know whether it is the latest writer responsible for finalising teardown. `dispose()` remains synchronous (no `Promise<void>` API ripple). 5 new tests in `__tests__/hybridEngineDispose.test.ts`.

- **In-flight init guard prevents concurrent setScene leaks** (`@vitrum/walkaround-hybrid`, 01bf92e): `HybridEngine._initPipeline()` was fire-and-forget with no in-flight guard. When the host (or vitrum's own factory bootstrap) called `setScene()` while a previous `_initPipeline()` was still running, both async chains wrote to shared state (`_bvhBuffers`, `_pipeline`, `_ddgiTraversalScene`) at multiple points after several `await` boundaries — last writer won; the loser leaked ~1 GB of GPU resources (18 rgba16float full-res textures + BVH buffers + DDGI atlases). React StrictMode + window resize storms compounded this. Fix: every `_initPipeline()` captures a monotonic `mySeq = ++this._initSeq` at entry; before each shared-state write the chain re-checks `mySeq === this._initSeq`. On mismatch it disposes its locals (BVH + synthesized THREE root + pipeline) in a `finally` block and bails without mutating shared state. Two checkpoints cover the two await boundaries (`buildReSTIRSceneBVH` and `pipeline.initialize()`). Adds `_pendingTeardown` flag so `dispose()` (above) can keep its synchronous contract. 5 new tests in `__tests__/hybridEngineInitRace.test.ts`.

- **`PTEngineWebGL2.updateEnvironment()` — env-only fast path, no BVH rebuild** (`@vitrum/pt-webgl` + `@vitrum/core`, ac0e5ce): host-driven `timeOfDay` scrubs in stainedGlass were paying for a full `setScene()` (BVH rebuild + geometry/material re-upload) on every env intensity change. The PT engine has nothing engine-internal to rebuild for env swaps — `WebGLPathTracer.updateEnvironment()` refreshes only the IBL uniforms and resets the accumulator. New optional `updateEnvironment?(env: SceneEnvironment | null)` method on the `Engine` interface (optional so HybridEngine and other backends without a cheap env path can skip implementing it; hosts MUST typeof-check before calling). `PTEngineWebGL2.updateEnvironment` mutates the engine's internal `THREE.Scene` env / intensity / rotation in place via a newly-exported `applyEnvironment` helper (extracted from `vitrumSceneToThree`), then calls the fork's `WebGLPathTracer.updateEnvironment()` (which internally calls `reset()`). Throws before `setScene()` or after `dispose()`; null env treated as `{kind: 'none'}`. 6 tests in `__tests__/updateEnvironment.test.ts` (including a regression assertion that `WebGLPathTracer.setScene` call count does NOT change while `updateEnvironment` call count increases).

- **Preserve environment intensity + rotation on three-bindings round-trip** (`@vitrum/three-bindings`, 473a3bf): the PT engine env-update path (and any THREE → vitrum → THREE round trip) silently dropped `scene.environmentIntensity` and `scene.environmentRotation`, causing PT mode to render with the wrong sky brightness after a `timeOfDay` scrub on the host side. `resolveEnvironment` now captures intensity (from `scene.environmentIntensity`) and rotationY (from `scene.environmentRotation.y`) on HDRI environments; `applyEnvironment` writes the captured intensity to BOTH `environmentIntensity` (drives IBL contribution) and `backgroundIntensity` (drives the visible HDRI background brightness), matching THREE's "background = environment" texture-sharing convention; rotationY written onto `environmentRotation.y` and `backgroundRotation.y` so equirect orientation survives the round trip. 5 new `environment.test.ts` cases (resolve) + 4 new `vitrumSceneToThree.test.ts` cases (round-trip).

- **Round-trip dichroic LUT stamps in `stampVitrumUserData`** (`@vitrum/three-bindings`, 7b290f9): dichroic LUT keys (`vitrumDichroicReflectanceLUT`, `vitrumDichroicTransmittanceLUT`) were read by `convertMaterial` into `Material.extensions.dichroicLUTs` but never re-stamped on the vitrum → THREE direction. The `sceneFromThreeJS → vitrumSceneToThree` round trip used by the PT engine's `setScene` path silently dropped the LUT handles, forcing dichroic raster backends downstream to fall back to in-shader TMM evaluation. `stampVitrumUserData` now reads `Material.extensions.dichroicLUTs` and writes both reflectance and transmittance handles back into `mat.userData` under the canonical `VITRUM_USER_DATA_KEYS`. Absent LUTs leave userData clean (no phantom keys). 5 new cases in `material-vitrum-roundtrip.test.ts`.

- **Skip transparent / invisible UI hit-zone meshes in `sceneFromThreeJS`** (`@vitrum/three-bindings`, 9849113 + 195aef0): `sceneFromThreeJS` was including transparent `MeshBasicMaterial` meshes (`EdgeHotZone`, `TracingImageOverlay` opacity=0 planes used for pointer hit-testing) as opaque flat-emissive geometry, occluding the actual scene. Now any `MeshBasicMaterial` with `transparent === true` or `opacity <= 0.01` is dropped from the converted vitrum Scene. Extended in 195aef0 with two more cases that were also occluding as opaque flat-emissive: R3F-style pointer-capture planes (10000×10000 plane with `visible={false}` on the Object3D) and selection-handle overlays (`material.visible = false`). Visible basics still convert via the existing `convertMesh` path. Drive-by from the panel-black bug 2026-05-12.

- **React-stable BVH dirty check + value-deduped material LUT** (`@vitrum/shared-bvh`, 09a2534): two related fixes for React/R3F hosts that re-render scenes. (1) `sceneBvh.ts` — drop `mesh.id` from the geometry-version hash: React reconciliation can construct fresh `THREE.Mesh` objects per render (JSX-declared `<mesh>` inside a `.map()`); each new Mesh bumps three.js's global `mesh.id` counter; the previous additive hash (`version + mesh.id`) treated identity churn as structural change and rebuilt the BVH every frame (~79 ms on a livingRoom-scale scene). Now hashes only `posAttr.version` (which bumps when `position.needsUpdate = true` or the attribute is replaced — the actual signal we want) plus `meshes.length * 1000003` to detect add/remove without depending on per-mesh identity. (2) `bvhCommon.ts` — value-dedup the material LUT: identity-based dedup blew the LUT up because React-rendered hosts with per-edge `useEdgeMaterial` + 70+ inline `<meshStandardMaterial>` declarations created 356 unique `THREE.Material` instances vs ~30 unique value-signatures, overflowing DDGI's 64-slot cap. New signature hashes the fields consumers actually read (`baseColor, emissive, emissiveIntensity, roughness, metalness, transmission, ior, map.uuid, normalMap.uuid`); two materials with identical signatures alias to the same canonical slot. Both fixes are observed-behaviour-preserving for the existing non-React tests.

- **WGSL reserved word `pass` → `passIdx`** (`@vitrum/walkaround-hybrid`, 2b4bfee): in `probeUpdateBorderIrradiance` and `probeUpdateBorderVisibility` the loop counter was named `pass`, a WGSL reserved keyword. Some WGSL frontends parse this without complaint but the spec lists `pass` as reserved and stricter validators (Tint debug builds, future browser versions) reject it. Renamed to `passIdx`; behaviour byte-identical.

- **Thread `triIntersectEpsilon` as parameter (cross-UBO link)** (`@vitrum/walkaround-hybrid`, 8a29f2b): `intersectTriangle` and the BVH traversal helpers (`bvhIntersectAny`, `bvhIntersectFirstHit`) used to read `ubo.triIntersectEpsilon` directly. That worked when the only callers were shaders binding `WalkaroundUBO` (which has the field) but fails to compile at link time when `COMMON_WGSL` is concatenated with shaders binding a different UBO struct — atrous binds `AtrousUBO`, which has no `triIntersectEpsilon` member. Fix: thread the value through as a parameter. Helpers no longer touch the global; every call site (ris, risGi, shade, spatial, temporal, surfaceTextures) passes `ubo.triIntersectEpsilon` explicitly. Atrous-style shaders can now reuse `COMMON_WGSL` without dragging in a UBO field they don't have. Behaviour unchanged (same value flows everywhere the helpers previously read directly). 301 walkaround-hybrid tests pass.

- **Post-sweep cleanup: cornell-box stale SVGF imports + H6.6 test path** (4d762df): two bugs surfaced when smoke-testing the GPU verification harness. (1) `examples/cornell-box/src/main.ts` imported 3 symbols the M2.2 sweep had renamed (`SVGF_DEFAULT_ATROUS_ITERATIONS → ATROUS_VARIANCE_DEFAULT_ATROUS_ITERATIONS`, `SVGF_FRAME_COUNT_INPUT_GUARD_MAX → ATROUS_VARIANCE_FRAME_COUNT_INPUT_GUARD_MAX`, `SVGF_MAX_ATROUS_ITERATIONS → ATROUS_VARIANCE_MAX_ATROUS_ITERATIONS`) plus the runtime call `runSvgfWebGPU → runAtrousVarianceWebGPU` — Vite refused to bundle the example (`NoMatchingExport`). The example had been broken since the M2.2 rename landed. Aliased the new names back to the old local symbols at the import site to minimise churn. (2) `packages/pt-webgl/src/__tests__/materialsTextureSpectral.test.ts:33` imported the fork's `MaterialsTexture` via a relative path with 3 `../` levels — needed 5 (test → src → pt-webgl → packages → vitrum → projects → sibling). Vitest resolved fine but `tsc` errored. Bumped to 5 `../` and added `@ts-expect-error` for the JS-only import (the fork has no `.d.ts`).

- **Cornell-spectral capture replaced with post-RNG-fix render** (2c0803f): re-captured against `three-gpu-pathtracer@bbe0640` (stratified-RNG truncation fix). The previous capture was monochromatic green because hero-wavelength sampling collapsed to one λ per pixel; this version reconstructs the red and green walls and the area-light glow correctly. Mean RGB went from (4, 55, 0) → (44, 83, 0.2) at 3 SPP. Blue is still under-represented at this sample count — separate issue with Y-CMF importance sampling at low SPP, resolved by the subsequent X/Y/Z MIS host fix (bf161f4).

- **E1 — GTAO multi-bounce term (Jiménez 2016 §5.2 / Eq. 16)** (`@vitrum/walkaround-hybrid`, sweep-2026-05-12 Phase E): implements the albedo-aware multi-bounce ambient occlusion approximation that was previously deferred (the albedo G-buffer was not wired into the GTAO pass). Changes: `hdrAlbedoOut` (shade pass M9.C, binding 14 in the frame BGL) is now bound at `@group(0) @binding(3)` in `gtao.wgsl.ts`; `gtao_aoOut` storage-texture format bumped from `r16float` to `rgba16float` to carry per-channel brightened AO; the Eq. 16 polynomial `a_mb = ((2.0404ρ − 0.3324)v + (−4.7951ρ + 0.6417))v + (2.7552ρ + 0.6903))v` is applied per colour channel; `gtaoUpsample.wgsl.ts` reduces the per-channel vec3 to a luminance-weighted scalar before writing the `r16float` `aoFull` output. `resourceManager.ts` allocates `aoHalfTexture` as `rgba16float`. `getGTAOBindGroupLayout` gains a binding-3 entry; `buildGTAOBindGroup` gains an `albedoView` parameter; the call site in `WalkaroundGPUPipeline.ts` passes `this._res.albedoTexture.createView()`. 5 new tests added to `sprint15-gtao.test.ts` (boundary conditions at ρ=0 and ρ=1, output-range invariant, ρ=1 brightening, structural WGSL assertions for binding 3 and rgba16float format).

- **E2 — RC `triIntersectEpsilon` UBO-plumb via CascadeUniforms** (`@vitrum/walkaround-hybrid`, sweep-2026-05-12 Phase E): the local `const TRI_INTERSECT_EPSILON: f32 = 1e-5` in `probeRayCast.wgsl.ts` is replaced with a `triIntersectEpsilon: f32` field in `CascadeUniforms`. The field occupies the first of the two former `_pad4` padding slots; total struct size is unchanged (40 f32/u32 = 160 bytes). `intersectsTriangle`, `intersectTriangles`, `bvhIntersectFirstHit`, and `traceSunVisibility` all accept `triEps: f32` as a parameter; the kernel reads `u.triIntersectEpsilon` and threads it through all call sites. `buildCascadeUniformDataInto` gains a `triIntersectEpsilon` argument (writes to index 26); `RCDispatchOpts` gains an optional `triIntersectEpsilon?: number` field (default `1e-5`). Both call sites in `RCDispatcher` (`_buildHandles` initial pack + per-frame update) pass `opts.triIntersectEpsilon ?? 1e-5`. 5 new tests added to `rc-bindings.test.ts` (struct field presence, const removal, function-signature parameter, size invariant, u.triIntersectEpsilon usage).

- **pt-webgpu VNDF GGX sampling** (Foundations #14): replaced `glossyReflectionSample`'s ad-hoc `mix(reflectDir, cosineHemiSample, roughness²)` lerp with Heitz 2018 "Sampling the GGX Distribution of Visible Normals" JCGT 7(4) Algorithm 1. The lerp distribution was incompatible with the GGX half-vector PDF already used in `brdfDirectionalPdf`, causing a biased MC estimator at mid roughness (systematic energy loss on glossy surfaces at roughness 0.3–0.7). The new `sampleGgxVndfTangent` helper transforms `wo` to the unit-roughness configuration, samples a visible half-vector via Frisvad ONB and concentric disc remapping, then unstretch-normalises back. Signature of `glossyReflectionSample` updated to accept `(rng, wo, n, t, b, roughness)` (caller builds ONB via `buildOnb`). (`@vitrum/pt-webgpu`, sweep-2026-05-11 Item 14)
- **pt-webgpu dielectric Fresnel branch** (Foundations #16): replaced the heuristic `transProb = transmission * (1 - metallic)` random-walk split with an exact physical Fresnel-weighted partition per PBR4e §9.3 `FrDielectric()`. New `frDielectric(cosTheta_i, eta)` helper computes unpolarised Fresnel reflectance including TIR (returns 1.0 at and above the critical angle). For transmissive, non-metallic surfaces, the Monte Carlo estimator now selects specular-reflect with probability R = frDielectric(|N·wo|, ior) and refract with probability (1−R), dividing throughput by the chosen probability. TIR is automatically handled (R=1 means refract is never sampled). Thin-film transmit tint still applied to the refraction branch. Non-transmissive surfaces keep the existing heuristic diffuse/specular partition. (`@vitrum/pt-webgpu`, sweep-2026-05-11 Item 16 / D11 analogue)
- **pt-webgpu BSDF→light MIS single-light bug** (Foundations #15): `intersectRectAreaLightRay` and `intersectMeshAreaLightRay` previously hardcoded `[0]` light-buffer indexing, so BSDF-sampled direction could only connect to the first rect-area or mesh-area light. Generalised both to accept a light index `li: u32` (stride `li * 4u`). `bsdfAreaLightConnectionContribution` now loops all `params.rectAreaLightCount` and `params.meshAreaLightCount` lights, keeping the minimum-distance unoccluded hit — sum MIS over all lights is unbiased and costs O(N_lights) extra intersection tests per bounce (typical scenes ≤ 8 lights, D9 decision). (`@vitrum/pt-webgpu`, sweep-2026-05-11 Item 15)

- **equiAngular** (Engines #10): PDF now computed at the clamped `t` value (was unclamped, causing PDF/sample mismatch in degenerate geometry); guard added for near-zero `thetaRange` (light direction nearly perpendicular to the ray) returning `{ t: 0, pdf: 0 }` to avoid downstream division by zero. (`@vitrum/shared-samplers`)
- **transformNormal** (Foundations #17): added `transformNormal(m, v)` — inverse-transpose (cofactor matrix) normal transform — to `pt-webgpu/math/mat4.ts`; fixes shading normals on instances under non-uniform scale where using `transformDirection` (M·n) incorrectly distorts normals. Call site in `uploadSceneBuffers.ts` updated. (`@vitrum/pt-webgpu`)
- **halfFloat encoder** (Foundations #33-H / F9): finite f32 inputs > 65504 now saturate to ±Inf as IEEE 754 specifies, not to a quiet-NaN bit pattern. (`@vitrum/shared-denoisers`)
- **`@vitrum/pt-webgpu`**: removed arbitrary 32-unit Beer-Lambert clamp (`min(hit.dist, 32.0)` → `hit.dist`); UBO-plumbed `triIntersectEpsilon` replacing one padding slot in `FrameParams` (default 1e-5, metre-scale, host-overridable); added `safeInvDir` IEEE-safe AABB-slab helper in `common.wgsl.ts` and replaced two raw `vec3f(1.0) / ray.direction` divisions in `intersectAabb` / `intersectAabbDetailed` (Williams 2005 §4 NaN fix for axis-aligned rays coincident with AABB face planes). (Items 18, 28, 36 / D12 — sweep 2026-05-11.)
- **ReSTIR-DI p̂ consistency** (Engines #5): unify the emitter geometry term across RIS, temporal, and spatial reuse passes. Per Bitterli 2020 §4.3, p̂ must be identical across all reuse steps to keep the WRS estimator's W_q ratio computation correct. Restores the firefly suppression that the RIS-only `emitterDist2Floor` clamp was meant to provide.
- **Atrous-variance depth channel** (Engines #7 / D2): shader now reads linear depth from `.x` matching the host upload (previously read from `.w` which was zero-initialized; depth-aware filtering silently no-op'd on every real call).

- **`@vitrum/walkaround-hybrid` firefly + dim-magnitude root-cause series** (2fc4176, 8ae2be2, a25f214, e40ea9c, e499065 — Sonnet-agent audit): Three convergent findings: (A) `spatialGi.wgsl` camera-distance-ratio depth test rejected nearly every candidate in corner geometry, causing per-pixel spatial-GI lock-in (every pixel retained its initial-RIS sample, never converging with neighbours); replaced with a coplanarity check (neighbour within 5 cm of centre pixel's tangent plane). (B) `EMITTER_DIST2_FLOOR=4.0` was calibrated for cm-scale geometry but Cornell-scale dist² ∈ [1.77, 3.92] — the floor suppressed the geometry term 2–2.3× across the entire scene; lowered to 0.01 (10 cm minimum effective distance). (C) Removed an over-tight `irrAtXs > 2.0` reject guard that was masking finding A by suppressing legitimate indirect. Follow-up: capped per-pixel reservoir W at a scene-scale floor (a25f214); bilinear-blended half-res GI reservoir reads in `shade.wgsl` (e40ea9c); Welford reads total radiance for tier accuracy (e499065).
- **`@vitrum/walkaround-hybrid` pre-atrous temporal accumulator on indirect channel** (8ae2be2): New `indirect-temporal-accum` pass with ping-pong `indirectAccumPing/Pong` textures (rgba16f) and TCBB (Temporal Clamped Bounding Box) clip runs before the spatial à-trous filter on the indirect channel, preventing per-pixel dancing noise from ReSTIR-GI's per-frame sample re-roll.
- **`@vitrum/walkaround-hybrid` DDGI pipeline stabilization series** (e011b4f, 37ad134, b50865e, bff6aec, 8d6dfa6, 48c92b4, 361e20b, 1339733): End-to-end DDGI plumbing fixes: probe spacing, light bridge, blend-weight kernel, atlas EMA collapse, perpetual flicker (root-caused to bimodal emitter injection), rect-area emitter injection into ReSTIR list, SVGF iteration count tuning for clean Cornell output, high-refresh-rate dark-flash fix.
- **`@vitrum/walkaround-hybrid` + `@vitrum/shared-bvh` BVH node layout + resolve dispatch** (2508f33): `normalizeBvhInteriorOffsets` in `bvhCommon.ts` detects the source layout (absolute 0.7.x vs relative 0.9.x `rightChildOrTriOffset`) and rewrites in place so WGSL traversal sees one canonical relative-offset layout regardless of which `three-mesh-bvh` Vite deduplication picked. RC `cascadeDispatch` resolve path fixed.
- **RTX 4090 Chrome validation fixes** (176f0f6): (1) ESM temporal dead zone on `RAYS_PER_PROBE` — extracted constant to `ddgiConstants.ts` to break a circular module dependency that caused `ReferenceError` at WGSL template literal evaluation. (2) `select(WelfordVariance, WelfordVariance, bool)` invalid WGSL — replaced with component-wise conditional. (3) WebGPU pipeline layout rejected for `walkaround-hybrid` binding 13+ storage buffers — example now passes `HYBRID_WEBGPU_REQUIRED_LIMITS` from the package to `requestDevice`.
- **`@vitrum/walkaround-hybrid` SVGF variance bind group rejected every frame** (d9f70b7): bind group was recreated with stale ping-pong index each frame, causing the GPU to reject the call — now uses a stable `frameIndex & 1` selection consistent with the resource manager's allocation order.
- **`@vitrum/walkaround-hybrid` `9db0e7f` portable remediation findings**: salvaged audit findings (B2, M9, M11, M13, M14) from the stale-base remediation worktree that applied cleanly to main HEAD: `sampleSkyColor` reads `frameParams.skyTint × skyIrradiance` instead of a hardcoded daylight gradient; `NORMAL_BIAS` and glass-step derived from `gridParams.spacing`; `maxProbesPerAxis` host-overridable; DDGI_MAX_MATERIALS compile-time injectable.
- **`@vitrum/pt-webgl` caustic capability reported consistently** (a4cc5f7): `reportCapabilities()` now reflects the active `causticStrategy` rather than always returning `'none'`.
- **`@vitrum/pt-webgl` cross-THREE type mismatches** (61a1441): resolved TypeScript type conflicts from mismatched THREE version re-exports across the package graph.
- **walkaround-hybrid ordered BVH traversal** (Foundations #30): `bvhIntersectAny` and `bvhIntersectFirstHit` in `common.wgsl.ts` now use split-axis ordered traversal (Wald 2007 / PBR4e §7.3.3). Low 2 bits of `splitAxisOrTriCount` give the split axis; ray direction sign on that axis determines near/far child order; far child is pushed first so near child is popped and tested first. Expected 30–50% fewer node tests for first-hit queries on coherent rays; smaller but positive gain for any-hit (shadow) queries. Hit results are identical to unordered traversal — only traversal efficiency changes. (`@vitrum/walkaround-hybrid`, sweep-2026-05-11 Item 30)
- **M7 DDGI Coherent Physical Model** (`@vitrum/walkaround-hybrid`, sweep-2026-05-11 Items 2, 4, 6, 20 + band-aid removal): Five changes that jointly redefine the DDGI energy contract end-to-end. Must be read as a single atomic milestone:
  1. **Item 6 — Unfreeze `randomRotation` (Shoemake SO(3))**: `probeUpdatePass.ts` replaces the all-zeros per-frame rotation with a deterministic Halton-{2,3,5} quasi-random SO(3) sequence (Shoemake 1992). Decorrelates probe ray samples across frames so the EMA temporal hysteresis accumulates an effectively larger ray budget.
  2. **Item 20 / D5 — Paper Lambertian cosine blend kernel**: `probeUpdateBlend.wgsl.ts` irradiance blend kernel changes `pow(w, 8.0)` → `w` (Majercik 2019 §3 Algorithm 1). Visibility blend changes `pow(w, 50.0)` → `pow(w, 2.0)`. Atlas now accumulates true irradiance E instead of a directionally-filtered radiance approximation.
  3. **Item 2 — Remove `* albedo / PI` from producer**: `probeUpdateRays.wgsl.ts` stores raw incoming radiance L_i. Combined with the cosine blend kernel (Item 20), the atlas holds true irradiance E. The receiver applies `(albedo/π) · E` — eliminating the `albedo²/π²` double-multiply that required the commit `3fb63e3` band-aid gain reduction.
  4. **Item 4 / D1 — Fix RC GI BRDF injection**: `giReceiver.ts` changes `nm.emissiveNode = giNode` → `nm.emissiveNode = mul(giNode, mul(materialColor, PI_INV))`. GI signal is now correctly interpreted as irradiance E; the receiver applies the Lambertian BRDF before injecting via `emissiveNode`. `three/tsl` imports extended with `mul`, `uniform`, `materialColor`.
  5. **Band-aid removal + comment update**: `applyDDGIShading.ts` receiver comment updated to document the M7 energy contract (atlas holds irradiance E; consumer applies `albedo/π`).
  - Visual verification pending GPU run. If results look incorrect, investigate the math (e.g., check cosine basis implementation in blend) — do NOT add band-aid gain factors. Reference equation: `L_o_indirect = (albedo/π) · E_ddgi` (Majercik 2019 §3).
