# vitrum — items to fix

**Audit date (original):** 2026-05-17
**Status reconciliation (2026-05-24):** every item in Sections A / B / C is closed, and the Section E merge-race backlog (E1–E7) is now closed as well. The descriptions below remain for posterity so future agents can see what was once broken and where the fix landed.

> **OPEN ITEMS (2026-06-06):** a new full-codebase audit filed **Section G (G-P0.1 … G-P2.7)** in [full-codebase-audit-2026-06-06.md](./full-codebase-audit-2026-06-06.md). A working-tree implementation sweep addresses the P0/P1 findings and most P2 hygiene items. **Native-WSL mechanical validation is DONE (2026-06-06):** typecheck clean, full vitest green (after re-pinning the goldens the sweep legitimately moved + fixing 2 sweep-introduced test bugs), fork shader smoke PASSES, layout codegen in sync. A second-pass 20-agent audit of the post-sweep tree fixed three further defects same-session (pt-webgpu lightTreeBuffer destroy leak; walkaround-rc dummy-TLAS buffer leak; three-bindings skinned-mesh double-world-transform — regression-pinned by `skinnedWorldTransform.test.ts`) and filed two NEW open items: ~~`updateLighting({primaryLightDir})` does not re-sync the DDGI sun direction (HybridEngine.ts:1524-1548)~~ — **RESOLVED / SOURCE-VERIFIED STALE (2026-06-12):** `HybridEngine.updateLighting()` now re-syncs DDGI sun lights through `_syncDdgiLightsFromCoreScene()` for core scenes or `orientDdgiSunLights(...)` for ctor-light-only scenes, and updates the DDGI sun-intensity multiplier with scene-directional single-count handling; and ~~the fork's always-on stained-glass shadow-ray perturbation biases NEE visibility (attenuate_hit_function.glsl.js:182-205)~~ — **RESOLVED (2026-06-08, fix landed `178f80d` + `75391f2`; verified by code-read):** the perturbation is NOT always-on. It is gated behind `#if FEATURE_STAINED_GLASS_SHADOW_NORMAL_PERTURBATION` (default `0` at `PhysicalPathTracingMaterial.js:69`), the fork shader-smoke-check asserts it must never be enabled by default (`shader-smoke-check.js:200-205`), and a tangent-length guard skips it when no valid TBN exists (`attenuate_hit_function.glsl.js:185`). The default shadow/NEE path is unbiased; the perturbation is an opt-in caustic-relief feature with a documented bias trade-off only when a host compiles it in. **GPU A/B captures DONE (2026-06-06, `daa9716` — full numbers in HARDWARE-VALIDATION-NEEDS.md top block):** G-P0.1 PASS, G-P0.2 PASS, G-P0.3 PASS, G-P1.2 PASS-on-rig (hardware-GL BDPT connections runtime = Windows-Chrome follow-up), G-P1.1 wiring FIXED but radiometric INSPECT (PPG live + unbiased; no variance gain on the open Cornell — needs a guiding-favourable scene). The captures surfaced + `daa9716` fixed: a **pt-webgpu P0** (within-leaf closest-hit last-writer-wins — face-on thin slabs black; **all pre-`daa9716` pt-webgpu reference renders/baselines embed it → re-capture before the next fidelity gate**), the **PPG enable-wiring gap**, and a **fork P0 HEAD blocker** (`activeLayerWeight` dangling call; GLSL call-closure gate added to the fork smoke). ~~NEW INSPECT: walkaround TLAS path produced zero ReSTIR signal headless~~ **RESOLVED (2026-06-06, `0bedd92`) — and INVERTED:** TLAS was fine (the original report conflated an up-facing-emitter scene confound); the REAL bug was **ReSTIR merged mode black since PR-3 (29942f9, 2026-05-26)** — the merged-mode scene-BGL placeholders were 16 bytes vs binding 6's 32-byte `array<BVHNode>` minimum → invalid bind group → every ReSTIR pass no-opped (the DDGI-`ea88803`/RC dummy-buffer class, never applied to the ReSTIR scene BGL; hidden because the `>1 mesh → tlas` auto-rule kept merged out of every multi-mesh test). Blast radius was single-mesh core scenes + `tier:'lite'`. Fixed (dummy 16→32B) + commit-bisect + intervention-proven on dzn: merged/auto/tlas now all render `direct=0.7320` identically on the bisect Cornell (`wsl-gpu/scripts/tlas-zero-gi-bisect.ts`). Remaining non-validation work is concentrated in the larger G-P2.6 allocator/view memoization and lazy-allocation follow-up; everything in Sections A–F below remains closed.

> **DECISION-QUEUE PASS (2026-06-07).** The 5 parked GPU-evidence decisions were resolved by the user and most executed this session: (1) **DDGI bounce → Lambertian colour bounce FIXED** (`8aa444a`, `radiance=(direct+indirect)·baseColor/π`; dims indirect ~4× + adds chroma — host calibration review + DDGI reference re-capture noted in HVN); (2) **photon-map caustic DEMOTED to approximate** (`c9d88fa`, honest-labeling only); (3) **API trio LANDED** (`c9d88fa`: backend-typed factories, `engine.getScene?()`, `negotiateWebGPUDevice`); (4) **RC out-of-model weight** — user chose the full fix (gate `cRc` on RC-has-energy + extend RC's probe cast to sample emitters) — IN PROGRESS; (5) **checkerboard rendering** (ResolvePass) — design plan complete, implementation pending. NEW open items from the V17 PPG occluded-scene A/B (`queue-2026-06-07/v17-ppg`): **PPG refine-loop runaway** (stable ~7 refine cycles then ramps ~3× via GI-reservoir↔dTree positive feedback) and **base-GI size-200 estimator instability** (ppg-OFF diverges for some seeds, control var 0.485× — independent of PPG). Both need follow-up before PPG is production-ready.

> **PT-WEBGL2 NATIVE-RENDER BRINGUP (2026-06-08).** The THREE-free `@vitrum/pt-webgl2` backend now renders a **correct, converging Cornell** on a real WebGL2 driver (llvmpipe via ANGLE, numeric readback — no visual claims). Root-cause chain fixed this session (commit `3363b87` + the prior bringup series `fbc8b68`/`ed85e45`/`cda5a03`): THREE-compat GLSL3 #define bridges → individual-uniform + dummy-sampler wiring → struct-member sampler binding names → **per-vertex (not per-triangle) materialIndex** → **emissive-emitter fold** (three-bindings strips emissive into a `mesh-area` emitter for NEE backends, but the fork integrator lights by hitting `surf.emission` AND `packLightsTexture` filters `mesh-area` out → light fell through the crack → black; `foldMeshAreaEmittersIntoMaterials` re-attaches radiance pt-webgl2-only). Convergence proof: 8spp meanLum 0.444/nonBlack 0.277 → 128spp meanLum 0.449 (stable)/nonBlack 0.986/0 NaN = unbiased MC convergence. 83/83 vitest green. Harness: `wsl-gpu/webgl2-capture/run-ptwebgl2-probe.mjs` (`SPP=<n>` env to vary samples). **FORK-VS-NATIVE A/B PASS (2026-06-08):** same Cornell through the original `three-gpu-pathtracer` `WebGLPathTracer` vs pt-webgl2 at matched bounces(8)/spp(128)/res/camera → converged ratios native/fork meanLum 1.0023, maxLum 1.0002, per-channel R 1.0037 / G 1.0025 / B 0.9952 (sub-percent, channels straddle 1.0 = noise not bias; ratios tightened from ~1.03@48spp as both converged). The THREE-free port is numerically faithful to the fork, colour bleed included. Harness: `wsl-gpu/webgl2-capture/run-ptwebgl2-vs-fork.mjs`.
>
> **PT-WEBGL2 A/B COVERAGE MAP (2026-06-09) — "what's left before dropping the fork".** Expanded the fork-vs-native A/B (`wsl-gpu/webgl2-capture/run-ptwebgl2-vs-fork.mjs`, `SCENE=cornell|glass|envmap`) across hard scenes: **(1) Diffuse GI — FAITHFUL** (meanLum 1.0023, per-channel <0.5%). **(2) Glass/transmission — FAITHFUL** (Cornell + glass sphere, refraction/Fresnel/IOR/attenuation; 96spp meanLum 1.0048, per-channel ~1%, channels straddle 1.0 = noise). **(3) Env-map/IBL — math FAITHFUL after a real fix** (`43a5b34`: `envMapInfo.totalSum` was never set → env-sampling GLSL early-outed on `totalSum==0` → IBL silently rendered fully black; now lit, 240spp meanLum 0.984 converging, visibility split identical to fork on a high-variance small-sun scene). **(G2) IBL data-bridge — RESOLVED** (`dc38f69`, 2026-06-09): `three-bindings` emitted `hdri` as a `THREE.Texture` (correct for the fork-wrapping `@vitrum/pt-webgl`, which reads it back via `vitrumSceneToThree`), but THREE-free backends need a raw `{width,height,data}` payload → IBL silently dropped through the on-ramp (empirically: meanLum 0). Fixed with `equirectTextureToPayload` (duck-types `.image.{data,width,height}`; RGBA→RGB stride, HalfFloat decode, rows reversed iff `flipY` — mirrors the fork's `preprocessEnvMap`) + a non-breaking `sceneFromThreeJS({ environmentPayload: 'raw' })` opt-in (default `'texture'` preserves pt-webgl). Verified: the 'raw' on-ramp lights pt-webgl2 byte-identically to the manual payload (totalSum 7891) and faithfully to the fork (ratio 0.97). pt-webgpu has the identical latent gap — the same on-ramp option fixes it. **(G3) Textures — RESOLVED** (`63a6dab` + `5eaf664`, 2026-06-09, core path): the materials packer hardcoded every map id to -1 (no atlas) → textured materials rendered flat. Wired end-to-end across 3 packages: **shared-bvh** propagates per-vertex UVs through `mergeWorldSpaceFromCore` (`WorldSpaceMergeResult.uvs`, stride 2 — without this the UV layer was zero and textures sampled one texel); **pt-webgl2** adds `texturesArray.ts` (gather unique map handles, duck-type pixels — raw `{w,h,data}` OR DataTexture `.image`, RGBA/RGB/R stride + HalfFloat — nearest-resample to a common dim, pack an RGBA32F `sampler2DArray` + handle→layer map), assigns each material map's atlas LAYER index + writes the UV-transform mat3 (`texel 55+2k`, THREE `setUvTransform`), and fixes the attributes packer to read UVs at stride 2. Verified on the textured A/B (4-quadrant-albedo panel): meanLum ratio 0.995, per-quadrant orientation matches the fork (top bright, TL<TR, BL<BR — no UV flip), bright quadrants within 1%. **G3.1 — RESOLVED** (`74b0b76`, 2026-06-09): the host-side canvas-readback bridge for `Image`/`ImageBitmap`/`Canvas` material textures. `three-bindings.materialTextureToPayload` extracts RGBA-float LINEAR pixels (DataTexture direct; drawable sources via a 2D canvas + `getImageData` — null without a DOM / on a CORS-tainted canvas), mirroring THREE's upload + the fork's CopyMaterial (rows reversed iff `flipY`, sRGB→linear iff `colorSpace==='srgb'`). A `rawifyMaterialTextures` post-pass rewrites every material-map handle when the on-ramp option is 'raw'; the G2 `environmentPayload` option is unified into `texturePayload: 'texture'|'raw'` (governs env + material maps). Verified on the canvas-texture A/B: correct UV orientation (top bright, TL<TR, BL<BR) + faithful albedo (bright quadrants within ~1%, meanLum 0.98; dark-region residual = the fork's 8-bit linear texture-array quantization vs native's float, not a bridge defect). Math fidelity for ALL implemented paths (diffuse/glass/IBL/textures incl. DOM-image sources) is proven against the fork.
>
> **RESOLVED — pt-webgl2 G1: gNormalDepth aux g-buffer diverged under the running-average regime** (fix `567a1f9`, 2026-06-09; approach (b)). `gNormalDepth = vec4(normalEnc, linearDepth)` packs depth (>1) in `.a`, and WebGL2 weights each MRT attachment's blend by its OWN output alpha → the colour-blend recurrence diverged → garbage/NaN (−46175 at 8spp, NaN at 128spp). Fixed by disabling blend on JUST the aux attachments via `OES_draw_buffers_indexed.disableiOES(BLEND, 1/2)` so they overwrite (latest sample = correct, primary-hit is deterministic) while the colour attachment keeps its EXACT running-average untouched. **LESSON:** a first attempt (`c95377b`: write g-buffer on sample 0 + freeze via `drawBuffers=[0,NONE,NONE]`) silently 3×-brightened OPEN/env-lit scenes (closed Cornell unaffected) — bisect-confirmed; a g-buffer fix that perturbs colour is net-worse than the unused-aux bug it fixes. The redo leaves the colour path byte-identical. Verified: Cornell g-buffer sane (maxDepth 4.2, normal [0,1], 0 NaN), env A/B ratio 0.972. Extension absent → g-buffer best-effort, colour always correct.

> **Health note (2026-05-24).** Historical "NOT IN HEAD" merge-race items have been re-landed: W2-C6 shared WGSL primitives, W3-D6 Mat4 branding, W3-D7 FrameOutput discriminated union, W3-D19 backend texture brands/helpers, W6-E6 ForkAccess indirection, and W7-G5 export hygiene. Section E is retained as an audit trail only.

---

## Section A — Public API surface (all closed 2026-05-17 → 2026-05-18)

These affected any consumer of `@vitrum/engine`'s top-level facade. All four
items shipped during the post-audit cleanup session — see Section D.0 for the
commit map. The descriptions are kept below for posterity.

### A1. `createEngine` proxy drops `updateEnvironment` pass-through

- **Where:** `packages/engine/src/createEngine.ts:269-334` — the `wrapWithIdempotentDispose` proxy forwards `setScene`, `updatePrimitive`, `updateEmitter`, `renderFrame`, `reset`, `pause`, `resume`, `dispose`, `onFrame`, `onProgress`, and `debug`. It does NOT forward `updateEnvironment`, even though `Engine.updateEnvironment?` exists on the contract (`packages/core/src/engine.ts:130`) and `PTEngineWebGL2.updateEnvironment` is implemented (`packages/pt-webgl/src/ptEngineWebGL2.ts:651`).
- **Symptom:** Any host using `createEngine()` / `attachVitrum()` cannot push an IBL/environment swap without a full `setScene` — even though the underlying engine supports the fast path. Hosts that bypass the facade (like stainedGlass, which uses `createPTEngine_WebGL2` directly) are unaffected.
- **Fix:**
  ```ts
  ...(engine.updateEnvironment
    ? { updateEnvironment: (env: ...) => { if (!disposed) engine.updateEnvironment!(env); } }
    : {}),
  ```
  Add to the `wrapWithIdempotentDispose` proxy alongside the existing optional-method forwarders.
- **Acceptance:** add a unit test in `packages/engine/__tests__/createEngine.test.ts` that creates a PT engine via `createEngine`, asserts `typeof handle.updateEnvironment === 'function'`, calls it with a fake env, and verifies the underlying engine's `updateEnvironment` was called.

### A2. `attachVitrum` never plumbs `swapChainView` into `FrameInput`

- **Where:** `packages/engine/src/lifecycle/vanilla.ts:118-128` builds the per-frame `FrameInput` object. There is no `swapChainView` field.
- **Symptom:** `HybridEngine.renderFrame` reads `input.swapChainView` at `HybridEngine.ts:963` and again at line 976, 1094. When undefined, the WebGPU path takes a skip branch. A WebGPU host using `attachVitrum` (the top-level documented entry point) gets a black canvas.
- **Fix:** detect the WebGPU backend in `attachVitrum`, acquire `context.getCurrentTexture().createView()` inside the RAF tick, and pass it as `input.swapChainView`. For WebGL hosts, leave the field undefined.
- **Acceptance:** an end-to-end test that drives one frame via `attachVitrum` against a `HybridEngine` and confirms a non-zero canvas readback. (Or a unit test that pins the FrameInput shape contains `swapChainView` when the underlying engine is WebGPU.)

### A3. `HybridEngine.updatePrimitive` / `updateEmitter` are `never`

- **Where:** `packages/walkaround-hybrid/src/HybridEngine.ts:785-786`:
  ```ts
  updatePrimitive?: never;
  updateEmitter?: never;
  ```
- **Symptom:** Any walkaround consumer who wants to edit a material or light without tearing down DDGI + the temporal accumulator has to call `setScene` (which calls `_teardownPipeline()` + `_initPipeline()` synchronously) or recreate the engine entirely. This is the gap stainedGlass calls out at `VitrumWalkaroundStage.tsx:42-50` as the reason no walkaround SceneSync exists.
- **Fix:** implement one of:
  - **Option (a):** `updatePrimitive(id, patch)` — re-upload one slot in the materials texture and (if topology changed) re-build only the affected BVH leaf, otherwise leave BVH alone.
  - **Option (b):** non-teardown `setScene(scene)` that diffs against the previous scene and only re-uploads what changed; document the diff contract.
- **Acceptance:** stainedGlass's `useVitrumWalkaroundEngine` can wire a `VitrumWalkaroundSceneSync` analogous to the PT version, and a Playwright test of a material-color edit shows the new color within 2 frames in walkaround mode (no engine recreation observable via a pipeline-compile counter).

### A4. `HybridEngine` ignores `FrameInput.viewport`

- **Where:** `grep viewport packages/walkaround-hybrid/src/HybridEngine.ts` returns zero hits — the file does not read `input.viewport`.
- **Symptom:** Engine size is set at construction via `HybridEngineOptions.width/height` and mutated only via `setSize()` (which exists, at `HybridEngine.ts:882`). A host using `attachVitrum` may push a new size via `FrameInput.viewport` (the documented field) and see it silently ignored.
- **Fix:** Either (a) honor `input.viewport.width/height` per frame (treating constructor values as initial), or (b) document explicitly in `Engine`/`FrameInput` JSDoc that `viewport` is informational only for `HybridEngine` and the host must call `setSize()` directly. (Recommend (b) — `setSize()` is already the cleaner API; the host needs to know.)

---

## Section B — Scaffold pretending (all closed)

Public surfaces that didn't actually do what they appeared to. All four items
shipped — B1 (PPG GPU dispatch) closed 2026-05-18; B2 (RC into HybridEngine)
closed via the W8 sprint (2026-05-18, see CLAUDE.md "Where things actually
stand"); B3 (neural inputPacker) and B4 (OIDN bridge consumers) both closed
during the items-to-fix landings. Descriptions kept below for posterity.

### B1. PPG GPU dispatch is `dispatchWorkgroups(0, 0, 0)`

- **Where:** `packages/walkaround-hybrid/src/pipeline/WalkaroundGPUPipeline.ts:829`:
  ```ts
  stubPass.dispatchWorkgroups(0, 0, 0); // no-op until sTree GPU buffer is wired
  ```
  The comment at lines 819-826 explicitly calls it a "skeleton GPU path" with no sTree producer.
- **Symptom:** `HybridEngineOptions.ppgEnabled = true` compiles the pipeline but produces zero PPG samples. A user who reads the docs and turns the option on gets nothing.
- **Fix:** Pick one:
  - **(a)** Wire the GPU-side sTree/dTree producer per `plan/sprint-ppg-rebuild-future.md`. This is a separate sprint of work.
  - **(b)** Remove `ppgEnabled` from `HybridEngineOptions`, delete the `ppg/` subtree (~1019 LOC per the 05-17 sweep, verify the LOC stat fresh), and update the walkaround README/capability matrix to stop advertising it.
- **Acceptance:** Either a Cornell-box walkaround render with `ppgEnabled: true` shows path-guiding gain over pure NEE (numerical or visual), or `grep -rn ppg packages/walkaround-hybrid/src` returns zero hits and the README no longer mentions PPG.

### B2. RC subsystem ships 1500+ LOC but is unwired

- **Where:** `grep -rn "from '../rc\|from \"../rc" packages/walkaround-hybrid/src --include="*.ts"` returns zero hits outside `rc/` itself. The walkaround README at line 9 + 18 is honest about it ("not currently added to the HybridEngine combined shading sum"), so the documentation rot here is minor. But shipping unused code is its own problem.
- **Fix:** Pick one:
  - **(a)** Wire RC into `HybridEngine`'s combined shading sum per `plan/walkaround-without-three.md`. Requires composition with DDGI + ReSTIR-DI without double-counting indirect.
  - **(b)** Move the entire `rc/` subtree to `examples/standalone-rc/` (since the README claims it's available "for standalone dispatch") and delete it from `walkaround-hybrid`'s public exports. Update the package README to remove the "RC" entries from the algorithm list — keep them only if (a) is the plan.
- **Acceptance:** Either `HybridEngine` runs a Cornell scene with `denoiser: 'atrous-variance'`, RC enabled, and visibly produces a higher-quality first-bounce indirect than DDGI-only; or `packages/walkaround-hybrid/src/rc/` is gone.

### B3. Neural denoiser layout — inputPacker module imported but may not match U-Net packing

- **Where:** `packages/walkaround-hybrid/src/neural/InferenceGraph.ts:271` references `inputPacker.ts`; line 631 says "The `inputPacker.ts` module provides the proper GPU packing shader"; line 653 says the runtime layout "is not the interleaved per-pixel layout. A production pack shader is in inputPacker.ts."
- **What to verify before fixing:** Read `InferenceGraph.ts:_runInputPack` (or equivalent — the 05-17 sweep cited the method name) and confirm whether it actually calls into `inputPacker.ts` or still does the legacy `copyBufferToBuffer` planar layout. If it's still planar, the `'neural'` mode is unusable; if it's now interleaved via `inputPacker.ts`, this item is closed.
- **Fix (only if confirmed broken):** swap `_runInputPack` to dispatch `inputPacker.ts`'s compute shader, matching the interleaved layout `unetArchitecture.ts` expects.
- **Acceptance:** `HybridEngine` with `denoiser: 'neural'` and supplied weights produces a denoised output that beats `atrous-variance` on a Cornell-with-noise test scene. The same dispose path no longer leaks weight buffers (the 05-17 finding F4).

### B4. OIDN bridge has zero non-test consumers

- **Where:** `packages/shared-denoisers/src/` exports `denoiseFinal` / `preloadOIDNModel` / `clearOIDNCache` (per 05-17 finding F7). Verify with `grep -rn "denoiseFinal\|preloadOIDNModel" packages --include="*.ts" | grep -v __tests__ | grep -v packages/shared-denoisers`. If zero non-test hits, this is dead public API.
- **Fix:** Either expose it through `HybridEngine.denoiser = 'oidn'` (or a PT-side capture hook) with a documented integration path, or delete it. ONNX runtime peer dep should not ship if nothing uses it.

---

## Section C — Doc rot (all closed)

All four items closed during the items-to-fix landings + W13 documentation
reconciliation. Descriptions kept below for posterity.

### C1. CLAUDE.md "What's done" section is at least one major work-package behind

- **Where:** `/home/jsquire4/projects/vitrum/CLAUDE.md:20-25` says "Phase 6 (Sprints 0-13) complete; Phase 7 walkaround-hybrid (Sprints 14-18) shipped." It does not mention the "M7 DDGI Coherent Physical Model" work (verified shipped via comment at `probeUpdateRays.wgsl.ts:546-548` and `applyDDGIShading.ts:145-148`) which moved albedo/π baking from producer to receiver and restored per-frame `randomRotation` (verified at `probeUpdatePass.ts:667-669`).
- **Where:** lines 29-37 list "load-bearing bugs the green test suite does not catch":
  - "DDGI receiver double-applies albedo and 1/π" — **fixed** per A1 of the old sweep (probeUpdateRays.wgsl.ts:549 no longer premultiplies).
  - "DDGI atlas border padding is allocated but never written" — **fixed**; `borderIrrPipeline` and `borderVisPipeline` exist (`probeUpdatePass.ts:133-135, 317-379`).
  - "What ships as SVGF is à-trous + a variance scalar lookup" — **fixed**; `svgfReprojection.wgsl.ts`, `svgfVarianceFromMoments.wgsl.ts`, `svgf7x7SpatialFallback.wgsl.ts` exist in `packages/shared-denoisers/src/wgsl/` and `svgfRealWebGPU.ts` uses dedicated `r32float` depth textures.
  - "PPG enable hard-throws at pipeline compile" — **fixed** (PPG no longer throws; it now compiles cleanly and dispatches 0 workgroups — see B1 above).
  - "`pt-webgpu` glossy BSDF" — still likely real, but `pt-webgpu` is explicitly labeled pre-alpha prototype; the documentation should clarify it's not the production PT.
  - "Neural denoiser is decorative scaffolding (no 'neural' mode)" — **partially stale**; the `'neural'` mode is in the `HybridEngineOptions.denoiser` union (`HybridEngine.ts:176`). What may still be broken is the input-packing layout (B3 above).
- **Fix:** Rewrite the "What's done" section to reflect M7 (DDGI coherent physical model), the new SVGF-real pipeline, the shipped `HybridEngine.updateLighting()` and `HybridEngine.setSize()` methods (lines 813, 882). Rewrite the "Where things actually stand" bullet list to only contain bugs that survive re-verification (the A/B sections of this file).

### C2. `memory/in-flight-sweep.md` is mostly stale and misleading

- **Where:** `~/.claude/projects/-home-jsquire4-projects-vitrum/memory/in-flight-sweep.md`. Direct verification on 2026-05-17 (this audit) confirmed that at least 9 of the ~12 "verified by direct read" bugs listed there have since been fixed: DDGI double-albedo, atlas border, randomRotation freeze, RC GI emissive bypass, ReSTIR p̂ inconsistency across RIS/temporal/spatial, SVGF depth channel mismatch, SVGF variance bindings, equiAngular PDF mismatch, light-tree mislabeling, bdptConnectionMIS naming, nodePowerPrefixSum naming.
- **Fix:** Either delete the file or replace its contents with a one-line redirect: "See `vitrum/items_to_fix.md` and `stainedGlass/items_to_fix.md` for current verified state. The 2026-05-11 audit's findings have been largely closed." Optionally archive the old contents under `memory/archive/in-flight-sweep-2026-05-11.md`.

### C3. CHANGELOG.md likely missing recent entries

- **Where:** `CHANGELOG.md`. Verify: when was the last entry written? Compare to the M7 commit messages.
- **Fix:** Walk `git log` since the last CHANGELOG entry; add bullets for the M7 DDGI work, `updateLighting`, `setSize`, the SVGF-real pipeline, and any other Phase-7+ shipped work.

### C4. Per-package READMEs may overclaim

- **Sample:** `packages/walkaround-hybrid/README.md:5` says "WebGPU **ReSTIR DI** walkaround engine with **DDGI** probe updates and atlas sampling. **Radiance Cascades (RC)** are implemented under `src/rc/` for standalone dispatch and material-wrapper flows; composition back into `HybridEngine`'s shade pass is tracked (see file header and plan/walkaround-without-three.md)." — This is **honest** (verified). No change required.
- **Fix needed elsewhere:** Audit each package's README for similar honest-vs-overclaim distinctions. Headline test: "if a new consumer reads the README, will they expect a feature that isn't actually wired into the engine?"

---

## Section E — Open items discovered 2026-05-19

### E1–E7 — seven sprint-of-2026-05-17 commits lost via merge races

All seven landed as standalone feature commits in the May 17 sprint but their
changes did not survive into HEAD because subsequent commits were branched
from pre-feature parents and silently overwrote them on merge. The features
still exist as commits in `git log`; they just have zero footprint in the
current code. All seven are type-system / structural / API-hygiene changes —
none is a runtime correctness bug, the pre-sprint behaviour is what's
running.

**Status as of 2026-05-24:**
- E1–E7 — **closed/re-landed**. Keep these entries as historical provenance.

The audit so far has spot-checked W2/W3/W6/W7 sub-bullets. W4/W11/W12/W13
have not been comprehensively audited — additional losses may surface.

### E1. W3-D7 FrameOutput discriminated union — closed (re-landed)

- **Where:** `packages/core/src/frame.ts`. Commit `40cd837` (W3-D7, 2026-05-17) introduced `FrameOutput = FrameSkipped | FrameRendered` with a `kind` discriminant. Commit `9ea12c9` (W3-D18, ~50 min later) branched from `80c2388` (D17, pre-D7) instead of from D7, and when its frame.ts changes merged they overwrote D7's. The net result: D7's code is unreachable from HEAD even though the commit is in the history.
- **Symptom:** the `FrameOutput` contract is back to `primaryRadiance: BackendTexture | null` + `samplesAccumulated === 0` skip sentinel — the exact pattern D7 was supposed to eliminate. Hosts that didn't check `samplesAccumulated > 0` before reading `primaryRadiance` would silently dereference null; the type system can't catch them.
- **Verification:** `git show HEAD:packages/core/src/frame.ts | grep -c FrameRendered` → 0. `grep -rn "FrameRendered\|FrameSkipped\|kind: 'rendered'" packages --include="*.ts"` → 0 consumers anywhere.
- **Fix:** re-apply D7's contract change, then propagate to:
  - producers: `walkaround-hybrid/src/HybridEngine.ts`, `pt-webgl/src/ptEngineWebGL2.ts`, `pt-webgpu/src/index.ts` (and any examples returning FrameOutput from a stub).
  - consumers: `engine/src/createEngine.ts` (the post-dispose stub at the `disposed` guard), examples (`cornell-box`, `two-engines-one-scene`, `hero-product-viz`), tests that fake `renderFrame()`.
  - new test: re-add `packages/engine/__tests__/frameOutputShape.test.ts` pinning the union.
- **Closure note (2026-05-24):** re-landed in the single-wave contract migration; producers and test stubs now emit `kind: 'rendered' | 'skipped'`.

### E2. W3-D6 Mat4 brand — closed (re-landed)

- **Where:** `packages/core/src/scene/math.ts:10`. Commit `e845cc5` (W3-D6, 2026-05-17) brand-typed `Mat4` as `Float32Array & __mat4Brand` and added an `asMat4(arr): Mat4` constructor that throws on length ≠ 16. Commit `cead5ab` (2026-05-18) split the monolithic `scene.ts` into 6 sibling files (`scene/{math,emitters,environment,primitives,material,index}.ts`). The split was done against a pre-D6 working copy, and the new `scene/math.ts` was created with the unbranded `Mat4 = Float32Array` form.
- **Symptom:** any `Float32Array` of any length silently satisfies the `Mat4` type. The D6 motivation — preventing 9-element upper-3×3 normal matrices from being passed as 16-element Mat4 — is gone. `asMat4` doesn't exist; callers that should have routed through it for length validation just `as Mat4` cast.
- **Verification:** `grep -c "__mat4Brand" packages/core/src/scene/math.ts` → 0; `grep -rn "asMat4" packages/*/src --include="*.ts"` → 0 callers.
- **Fix:** re-port the D6 changes to `scene/math.ts` (brand + asMat4), then re-update the 21 implicit-cast construction sites that D6 originally fixed (three-bindings, pt-webgpu, engine).
- **Closure note (2026-05-24):** re-landed with `asMat4`/`isMat4`, and callsites migrated across packages/examples.

### E7. W7-G5 validateBvhEncoding un-export — **RE-APPLIED 2026-05-19**

Re-applied during the same session that filed the audit. `packages/shared-bvh/src/index.ts` now uses selective named exports (`buildSceneBVH`, `refitBvhBounds`, the two interface types) from `bvhCommon.js` instead of `export * from`. `validateBvhEncoding` leaves the public surface. Both shared-bvh test files that needed it now import from `'../bvhCommon.js'` directly. All downstream packages (walkaround-hybrid, walkaround-rc, pt-webgpu, pt-webgl, engine) typecheck clean — the external surface of @vitrum/shared-bvh that they actually use is unchanged.

### E6. W6-E6 ForkAccess indirection — closed (re-landed)

- **Where:** `packages/pt-webgl/src/`. Commit `a6a3c90` (W6-E6, 2026-05-17) added `forkAccess.ts` (static `ForkAccess` class with `getMaterial(tracer)` + `getRenderTexture(tracer)`) and migrated `forkUniformBridge.ts` + `ptEngineWebGL2.ts` off direct `tracer._pathTracer.material.uniforms` reach-throughs.
- **Symptom:** if the upstream three-gpu-pathtracer fork renames `_pathTracer` (or upstreams an official accessor), `forkUniformBridge.ts` and `ptEngineWebGL2.ts` break in lockstep and have to be hand-patched at every reach-through site — the exact problem the indirection was meant to solve.
- **Verification:** `packages/pt-webgl/src/forkAccess.ts` is missing. `grep -n "_pathTracer" packages/pt-webgl/src/forkUniformBridge.ts` returns the pre-E6 direct-cast form: `const tracer = pathTracer as { _pathTracer?: { material?: PathTracerMaterialLike } }; const material = tracer._pathTracer?.material ?? null;`.
- **Fix:** re-apply `a6a3c90`. Test-only helper `makeForkPathTracerStubForTests` also needs re-adding (the original commit added it).
- **Closure note (2026-05-24):** `forkAccess.ts` restored and pt-webgl bridge/engine callers now route through it.

### E5. W6-E1 reuseSharedWebGpuDevice default flip — closed (re-landed)

- **Where:** `packages/shared-denoisers/src/{atrousVarianceWebGPU,hdrLuminanceBilateralWebGPU,svgfRealWebGPU}.ts`. Commit `3dbe11a` (W6-E1, 2026-05-17) was a breaking change: flipped `reuseSharedWebGpuDevice`'s effective default from `true` to `false` so callers without an explicit `device` would error rather than silently reuse a process-wide singleton (a design-principle violation — host-owns-lifecycle). The diff changed `opts.reuseSharedWebGpuDevice !== false` to `opts.reuseSharedWebGpuDevice === true` in all three dispatchers.
- **Symptom:** the pre-E1 implicit-singleton behaviour is back. Callers without a `device` arg silently reuse the global, masking the host-ownership contract violation E1 was meant to surface.
- **Verification:** `grep -n "reuseSharedWebGpuDevice !== false" packages/shared-denoisers/src/*.ts` returns 3 hits (the pre-E1 form). The post-E1 form (`=== true`) is not in HEAD.
- **Fix:** re-apply `3dbe11a` and update any test that called the dispatchers without `device` (those would have been updated in the original commit).
- **Closure note (2026-05-24):** denoiser dispatchers now require explicit opt-in (`reuseSharedWebGpuDevice === true`) when no device is supplied.

### E4. W2-C6 shared-samplers WGSL primitives — closed (re-landed)

- **Where:** `packages/shared-samplers/src/wgsl/`. Commit `da286d7` (W2-C6, 2026-05-17) introduced `pcg.wgsl.ts` (80 LOC: `pcgInit`, `pcgNext`, `rand_f32`, `rand_f32_2`, `rand_f32_3`) and `bsdfPrimitives.wgsl.ts` (108 LOC: `buildONB`, `sampleCosineHemisphere`, `cosineHemispherePdf`, `fresnelSchlick`) as canonical sources, and migrated walkaround-hybrid + pt-webgpu off their local copies. Neither file is in HEAD; both local copies are back. Verified 2026-05-19: `ls packages/shared-samplers/src/wgsl/` returns only `hammersley.wgsl.ts`, `luminance.wgsl.ts`, `octahedralCore.wgsl.ts`. `grep -A5 "fn pcgInit" packages/pt-webgpu/src/wgsl/common.wgsl.ts packages/walkaround-hybrid/src/shaders/common.wgsl.ts` returns byte-identical 6-line definitions in both files.
- **Symptom:** parallel WGSL primitives evolving in two places. The original C6 motivation — capitalisation drift (`buildONB` vs `buildOnb`) and arg-order drift (`sampleCosineHemisphere(n, rng)` vs `cosineHemisphereSample(rng, n)`) — has had time to re-accumulate.
- **Fix:** re-apply `da286d7` cleanly. The original commit's stat: `pcg.wgsl.ts` +80, `bsdfPrimitives.wgsl.ts` +108, walkaround-hybrid common -75 LOC, pt-webgpu common -35 LOC, pathTraceBruteforce -38 LOC.
- **Closure note (2026-05-24):** canonical `pcg.wgsl.ts` + `bsdfPrimitives.wgsl.ts` restored in shared-samplers; duplicated local copies removed from walkaround-hybrid/pt-webgpu common modules.

### E3. W3-D19 BackendTexture brand — closed (re-landed)

- **Where:** `packages/core/src/frame.ts:193`. Commit `5863cda` (W3-D19, 2026-05-17) replaced `export type BackendTexture = unknown` with `BackendTexture<TBackend>` + `BackendTextureFormat<TBackend>` nominal brands keyed by `unique symbol`. The D17/D18 merge race that dropped D7 (see E1) also overwrote D19 because D19 was branched off D7.
- **Symptom:** WebGPU `swapChainView` can be silently set to a WebGL `WebGLTexture` and vice versa — the type system can't catch it. `as*BackendTexture` constructors + `narrowTo*` helpers in `walkaround-hybrid` / `pt-webgpu` / `pt-webgl` are also gone (verified 2026-05-19: `grep -rn "asWebGPUBackendTexture\|narrowToWebGPU\|asWebGLBackendTexture" packages/*/src` → 0).
- **Verification:** `packages/core/src/frame.ts:193` reads `export type BackendTexture = unknown;` — the pre-D19 form.
- **Fix:** re-apply D19 (5 of the original commit's files: core types + 3 backend brand helpers + 1 example). Companion test `packages/engine/__tests__/backendTextureBrand.test.ts` also needs re-adding.
- **Closure note (2026-05-24):** backend texture/format brand helpers restored in `core/frame.ts`, with helpers consumed at backend boundaries.

---

## Section D — Verified-stale items removed

### D.0 — Closed during 2026-05-17 cleanup session (post-audit)

The following items from Sections A / B / C of THIS audit (filed 2026-05-17) have been **closed** by branches landed during the same-day cleanup session. Verified by direct read of the cited file after the implementing branch landed.

- **A1** — `createEngine` proxy now forwards `updateEnvironment` (`feat/items-to-fix-A1-A2-A4`, commit `0a24fd2`). Verify: `grep -n updateEnvironment packages/engine/src/createEngine.ts`.
- **A2** — `attachVitrum` plumbs `swapChainView` + `swapChainFormat` into `FrameInput` (`feat/items-to-fix-A1-A2-A4`, commit `1cd8a03`). Verify: `grep -n swapChainView packages/engine/src/lifecycle/vanilla.ts`.
- **A3** — `HybridEngine.updatePrimitive` + `updateEmitter` are implemented. Transform/positions fast paths are in-tree; material/emitter edits route through incremental patch APIs with rebuild fallback where needed. Verify: `grep -n "updatePrimitive\|updateEmitter" packages/walkaround-hybrid/src/HybridEngine.ts`.
- **A4** — `HybridEngine` `FrameInput.viewport` contract documented as informational-only (host must call `setSize()` directly) (`feat/items-to-fix-A1-A2-A4`, commit `11fb8f3`). Option (b) from the audit's fix sketch.
- **B3** — `INPUT_PACKER_WGSL` wired into `InferenceGraph._runInputPack` (interleaved layout matching U-Net packing) (`feat/items-to-fix-B3-neural-inputpacker`, commit `954ed1b`); `dispose()` now destroys weights/biases/uniform buffers (F4, `72b42d2`); 137-line shape-debate header stripped from `unetArchitecture.ts` (F3, `96ab815`).
- **B4** — OIDN bridge now consumed in-engine by both `HybridEngine` (`'oidn-final'` mode, `feat/w11-oidn-wire`, `74fad35`) and `PTEngineWebGL2` (`feat/w11-pt-webgl-oidn`, `7e98d90`). Zero-consumer status closed.
- **C1** — `CLAUDE.md` "What's done" + "Where things actually stand" sections reconciled (`feat/docs-c1-c3-claude-md-changelog`, commit `9751ca9`; further updated in `chore/final-docs-catchup-and-cornell-oidn-simplify` to cover W2/W3/W4/W6/W7/W11/W12/W13 and the items-to-fix landings).
- **C2** — `memory/in-flight-sweep.md` is user-owned; the current CLAUDE.md and this file are now the authoritative open-bug list. (Stale branches + 2 zombie worktrees also removed as part of repo hygiene.)
- **C3** — `CHANGELOG.md` brought current to the W1–W7 / W11 / W12 / W13 / items-to-fix landings (`feat/docs-c1-c3-claude-md-changelog`, commit `8cacb78`).
- **C4** — per-package READMEs audited for accuracy in W13 (`chore/w13-readme-audit-plan-archive`, commit `fc882f6`).
- **B1** — PPG dispatch is now a real `dispatchWorkgroups(wgCount, 1, 1)` call wired through the W9 GPU flat-buffer traversal kernel. Verified at `packages/walkaround-hybrid/src/pipeline/passes/PPGGuidePass.ts:94` (and the file header's "no more dispatchWorkgroups(0,0,0)" callout). The `ppg-dispatch.test.ts` suite pins the wiring (5 tests passing, run 2026-05-18). The earlier "stub" comment block referenced at `WalkaroundGPUPipeline.ts:819-829` is gone — that file's PPG integration now flows through `PPGCoordinator` (post W1-R5 + walkaround-pipeline-split).

- **B2** — RC subsystem now wired into HybridEngine via the W8 sprint (Phases 1A/1B/2/3/4 all landed 2026-05-18). `RCSubsystem` constructed in `HybridEngine.ts:388` when `opts.rcEnabled === true`; cascade-0 buffer composed into `shade.wgsl` via Track-A balance-heuristic MIS; `rcAcceptance.gpu.test.ts` harness scaffold + reference-render landings in `tools/reference-renders/W8-rc-{off,on}/`. See [plan/w8-rc-mis-composition.md](./plan/w8-rc-mis-composition.md) for the full sprint trace. The W8 follow-up — extracting `walkaround-hybrid/src/rc/` into a standalone `@vitrum/walkaround-rc` package — also shipped 2026-05-18 (verify: `ls packages/walkaround-rc/src/`).

All Section A, B, and C items from this audit are closed.

### D.1 — Verified-stale items removed (original 2026-05-17 list)

For posterity, the following items were on the previous version of this list, sourced from sweep docs, and **verified-removed** on 2026-05-17:

- DDGI double-albedo bug (producer at `probeUpdateRays.wgsl.ts:477-497` no longer premultiplies; consumer at `applyDDGIShading.ts:172-175` applies `albedo · PI_INV` exactly once)
- DDGI atlas border never written (border pipelines exist)
- DDGI randomRotation hard-coded to (0,0,0) (Halton-Shoemake per-frame at `probeUpdateFrameParams.ts:19-60`)
- RC GI bypasses BRDF (giReceiver.ts:108-110 multiplies by `albedo · PI_INV` before injecting)
- ReSTIR p̂ inconsistency (all 3 sites use `emitterGeometry(nlDotL, dist2, ubo.emitterDist2Floor)`)
- SVGF "fake" (real svgfReal pipeline with reprojection / variance-from-moments / 7×7 fallback)
- SVGF depth `.r` vs `.w` channel mismatch (svgfReal uses dedicated `r32float` depth textures)
- equiAngular sampler clamp/PDF mismatch (`equiAngular.ts:144-149` clamps t and computes PDF at clamped t)
- lightTree paper mislabeling (JSDoc now correctly cites Shirley 1996)
- `bdptConnectionMIS` naming (renamed to `bdptConnectionMIS_full` + `bdptConnectionMIS_partial @deprecated`)
- `nodePowerPrefixSum` naming (renamed to `_powerPrefixSumDebug`)
- `Material.anisotropy` missing from contract (`@vitrum/core/scene.ts:321,332` declares it; `convertMaterial()` reads it at `three-bindings/src/material.ts:107-110`)
- `HybridEngine.setSize` missing (exists at `HybridEngine.ts:882`)
- `HybridEngine.updateLighting` missing (exists at `HybridEngine.ts:813`)
- `SURFACE_TEXTURE_ID` table not exported (now exported from `@vitrum/walkaround-hybrid`; verified via stainedGlass importing it at `packages/stained-glass-physics/src/baking/surfaceTextureIds.ts:17`)

This section exists to prevent re-introduction. If a future audit doc claims one of these is open, the auditor should open the cited file before relaying.

---

## How the judge (me) will verify

For each open item (A1–A4, B1–B4, C1–C4):
1. Open the cited file at the cited line and confirm the bug/gap is still present *now*, before the implementing agent starts.
2. After the agent reports done, open the same file again and confirm the change matches the proposed fix.
3. Run the listed acceptance test and confirm it passes.

A finding from a sweep doc is hearsay until I read the file myself. Same rule applies to the agent's "done" claim.

---

## Section F — Open follow-ups (post-2026-05-30 wave)

### T3.G — `engine.debug.pickPrimitive(x, y)` — DONE 2026-06-04

- **STATUS: DONE + unit-tested.** Implemented option (a) — exact CPU ray-cast (better than the "ray-AABB approximate" first sketched; triangle-exact, not just bounds). `pickPrimitiveCpu` (now lives in `@vitrum/shared-bvh`; the `walkaround-hybrid/src/debug/pickPrimitive.ts` re-export shim was deleted 2026-06-10): unproject (x,y) through the retained last-frame camera (`HybridEngine._lastFrameCamera`, copied each `renderFrame`) → `invertMat4(proj·view)` far-plane ray (NDC z=+1, robust to both GL/WebGPU depth conventions) → closest-hit over `_lastScene.primitives` — Möller–Trumbore for mesh/instanced/skinned (rest-pose; deformation ignored, debug-only), world bounding-sphere for analytic (or `fallbackMesh` triangles when present). Wired into the debug surface (`HybridEngineDebug.ts` `pickPrimitive` + new `pickScene`/`pickCamera`/`pickSize` deps). `MaterialInspector` self-wires click-to-pick via an optional `canvas` prop (CSS-pixel click → canvas backing-store pixels → `pickPrimitive` → internal `pickedId`, no external `selectedPrimitiveId` needed); the stale "T3.G followup" warning + `vanilla.ts` "stubbed" comment were corrected. 7 unit tests (`pickPrimitive.test.ts`: centre-pixel hit [the acceptance case], miss→null, depth ordering, analytic sphere, transform-honoured, empty scene, zero-viewport). Full workspace typecheck + 1216 vitest green. Option (b) (GPU primitive-ID readback) deferred — CPU is exact for meshes and adds zero GPU-readback latency.
- **Acceptance (met):** `engine.debug.pickPrimitive(x, y)` returns the correct primitive ID for a known mesh (unit-tested); `MaterialInspector` opens on canvas click without external `selectedPrimitiveId` wiring (the `canvas` prop path).

### F-RC1 — RC merged-mode GPU traversal unreliable on multi-node trees (FIXED 2026-06-04)

- **STATUS: FIXED + oracle-validated.** ROOT CAUSE (NOT "traversal flakiness" as first guessed — an F-TLAS1-CLASS STRIDE BUG, upload direction): `buildSceneBVH` emits STRIDE-3 indices (12 B/tri); `RCSubsystem._uploadBVH` uploaded them RAW; but the RC probe shader declares `rc_geom_index: array<vec4u>` and `bvhIntersectFirstHit` reads each entry at a 16-byte (stride-4) stride (`bvhIntersect.wgsl.ts:327`) → GPU reads `bvhIndex.xyz[t]` from byte 16·t while data lives at 12·t → wrong for tri≥1 (the 17.75 dB / 3× lit-slot divergence). DDGI over the SAME builder works because it pads via `padTriangleIndicesToVec4` (`ddgi/probeUpdateBvhBuffers.ts:76`); RC's merged upload was the lone consumer that forgot to. FIX (`HybridEngineRC.ts:_uploadBVH`): pad to stride-4 via the same `padTriangleIndicesToVec4` (stride-3 `bvh.indices` retained for the refit fast path; TLAS path already stride-4). PROVEN: a CPU brute-force oracle (`wsl-gpu rc-merged-bvh-bruteforce-ab.ts`, modeled on F-TLAS1's) gives FIXED **100.00%** (2304/2304) vs the re-introduced bug **47.92%** (Δ52pp); production buffer now 768 B = stride-4 (was 576). RC GPU SMOKE ADDED: the oracle is wired into the every-push T1 smoke (`wsl-gpu t1-smoke.mjs:runRcMergedOracle`) — closes the "RC had zero GPU coverage" gap that kept this latent. All re-run by the lead.

- **Where:** `packages/walkaround-rc/src/wgsl/probeRayCast.wgsl.ts` (`rcTraceFirstHit` → `BVH_INTERSECT_WGSL`/`TLAS_TRAVERSAL_WGSL`). RC's merged-mode probe traversal.
- **Symptom:** rendering the SAME geometry through two valid-but-differently-shaped SAH trees (three-mesh-bvh `MeshBVH` vs the THREE-free `buildArrayBvh`) yields a **3× difference in lit probe slots** (THREE 5756 vs core 17783) and **PSNR 17.75 dB / |Δ mean| 34%** on a 6-prim Cornell (`wsl-gpu scripts/rc-core-bvh-ab.ts` CHECK 2). A CORRECT BVH traversal returns identical closest-hits on any valid tree over the same geometry, so this is a traversal bug, not a tree bug. **Diagnosis it is RC-traversal-specific, not the builder:** `buildArrayBvh`'s multi-node tree is independently validated — the DDGI core-BVH A/B renders it at 82.66 dB (`ddgi-core-bvh-ab.ts`). So RC's traversal mishandles tree shape; DDGI's (over the same builder) does not.
- **Latency of discovery:** RC merged-mode GPU was NEVER exercised — `rcEnabled:false` in every harness, no GPU compile/bind gate (W8 pinned only CPU `packRCParams` + `wgslCompose` order). The shader didn't even compile (missing `safe_normalize`) or bind (16-byte TLAS placeholder) until `592ef7f` fixed those; THIS traversal issue is the next layer.
- **Fix:** audit `rcTraceFirstHit` / the shared `BVH_INTERSECT_WGSL` stack traversal as RC assembles it (stack depth, node-order/leaf-flag assumptions, `triEps`); compare against the DDGI/ReSTIR consumers of the same WGSL (which traverse correctly) for what RC composes differently. **Add a real RC GPU smoke** (an `rcEnabled:true` Cornell in the wsl-gpu T1 path) so RC merged-mode regressions are caught — RC currently has zero GPU coverage.
- **Acceptance:** RC merged-mode renders a multi-node Cornell to within converged-A/B tolerance of a CPU brute-force (all-triangles, no BVH) reference; an `rcEnabled` GPU smoke is wired and green.

### F-RC2 — RC merged-BVH THREE-decouple — DONE 2026-06-04 (re-attempted after F-RC1 fix, oracle-validated)

- **STATUS: DONE + landed + render-validated.** Re-attempted now that F-RC1 is fixed. The RC brute-force oracle (`rc-merged-bvh-bruteforce-ab.ts`) scores BOTH the THREE `setScene` path AND the new core `setSceneFromCore` path at **100.00%** (2304/2304) vs the independent CPU all-triangles ground truth — both hit the same 1444 triangles at the same world points through DIFFERENTLY-shaped SAH trees (tree-shape-invariant correctness; buggy stride-3 still discriminated at 47.92%). The converged THREE-vs-core RC A/B (`rc-core-bvh-ab.ts` CHECK-2, previously "inconclusive" at 17.75 dB) is now **999 dB pixel-identical** — confirming that divergence WAS the F-RC1 stride bug, not harness edge-grazing. CPU set-equivalence: 9 tests (merged tri SET + per-tri materialId + world AABB + packed cascade-material bytes, incl. ei=4→`emissive·1` + the thickness→0.1 floor). The core path inherits the F-RC1 stride-4 pad via the shared `_setSceneFromBVH`→`_uploadBVH` (768-byte index buffer confirmed). Wired into production (`HybridEngineGiPropagation` routes merged-mode RC to `setSceneFromCore` when a core Scene is present, falling back to THREE for the raw-`threeScene` escape hatch). typecheck clean; wh 1185 + shared-bvh 286 green; T1 smoke 999 dB + RC oracle gate PASS. ALL re-run by the lead. **So the RC merged-BVH decouple was RIGHT all along — it was BLOCKED by F-RC1, not wrong.**
- **History (the blocker, now resolved):** Implemented + CPU/byte-validated 2026-06-04, then REVERTED (not committed) because it couldn't be render-validated while F-RC1 stood. **F-RC1 is now FIXED** (the stride bug — which, NOTE, was the REAL cause of F-RC2's `rc-core-bvh-ab.ts` CHECK-2 17.75 dB divergence; the "harness floating-point edge-grazing" explanation in that harness was WRONG — the stride bug corrupted both THREE and core paths differently). So F-RC2 is re-attemptable: re-apply the decouple (recoverable from session `97e46b32`) + run a now-STABLE converged RC A/B (the traversal is correct + has a GPU smoke). The packing (`buildRCSceneBVHFromCore` + `packCascadeMaterialsFromCore` with the `toProductionEmissiveRadiance` ei-collapse fix — RC's probe kernel DOES read `mat.emissive`, so the fix is load-bearing here unlike DDGI) is correct: CPU set-equivalence (8 tests, incl. ei=4 → `emissive·1` and the `thickness→0.1` floor) + the GPU CHECK 1 byte-identical 999 dB. The blocker is purely that swapping `buildSceneBVH(sceneRoots)` → `mergeWorldSpaceFromCore(scene)` changes the tree shape, which F-RC1's unreliable RC traversal renders differently — so the decouple's converged render can't be proven equivalent.
- **Note on production reach:** unlike DDGI (whose standalone path is dead in-engine), production RC merged mode DOES reach `buildRCSceneBVH` for single-mesh core scenes (`HybridEngineGiPropagation` merged branch + `allowRcSceneRebuild`). So this is a real production decouple once unblocked.
- **Fix:** after F-RC1, re-apply the decouple (the diff is recoverable from session `97e46b32`) + gate it on a conclusive multi-node converged A/B. Mirrors the landed emitter (`46a0078`) + standalone-DDGI (`15070cd`) decouples.
- **Acceptance:** RC core path wired into `propagateBvhToGiSubsystems` (core-first when a core Scene is present) with a converged RC A/B (THREE vs core) at high PSNR on a multi-node scene.

### F-TLAS1 — ReSTIR-TLAS `bvhIndex.xyz` vertex lanes packed STRIDE-3 from a STRIDE-4 buffer (FIXED 2026-06-04)

- **STATUS: FIXED + validated (commit lands with this entry).** `buffersFromScenePack` now feeds the packer a stride-3 extraction (`triIndices3[t*3+k]=geo.indices[t*4+k]`). PROVEN CORRECT end-to-end: a CPU brute-force oracle (`wsl-gpu scripts/restir-tlas-bvh-bruteforce-ab.ts` — independent all-triangles closest-hit using the stride-4 ground truth + Möller-Trumbore, no BVH) gives the FIXED GPU `traceTlasFirstHit` a **100.00%** match (2304/2304 rays) vs **59.68%** for the re-introduced bug (Δ40 pp), identical on lavapipe + dzn. Unit-pinned (`materialPackingCoreEquivalence.test.ts` now asserts `bvhIndex.xyz==geo.indices[t*4+k]`). The T1 smoke golden (`wsl-gpu captures/golden/hybrid-{lavapipe,dzn}.png`) was RE-CAPTURED against the corrected render (smoke now 999 dB vs the new golden; the old golden encoded the bug, regressing 81→11 dB). The bug landed via `048a2bb` (2026-05-26); `e374a7c` preserved it byte-identically.
- **RESIDUAL — RESOLVED 2026-06-04 via wsl-gpu (NOT a hardware-Chrome host — the companion app is the GPU validator):** the 5 multi-mesh hybrid-TLAS reference renders that encoded the bug were RE-CAPTURED on dzn (RTX 4090 — the wsl-gpu deno worker DOES reach the ≥16-buffer adapter; `capture-worker/render-hybrid.ts` was generalized with `renderHybridScene(scene)` → `wsl-gpu scripts/capture-ftlas1-refs.ts`, scenes built faithfully from the real `@vitrum-examples/shared` host builders + `scenario-presets.mjs` params, verified on dzn + recognizable-geometry by-eye + the fix proven by the CPU oracle). Re-captured: `PR-hybrid/{tlas-on/PR-hybrid-tlas-10-inst,200k-static/PR-hybrid-200k-static,material-edit/PR-hybrid-material-churn}.png` + `session-20260527/hero-viewer-realtime.png` + its byte-copy `baseline/hero-viewer-realtime.png`. These are now wsl-gpu/dzn captures (byte-different from the old Chrome SHA256s — the Chrome `benchmark:pr-hybrid-refs` path can't reach a ≥16-buffer adapter in WSL, which is why wsl-gpu exists; `PR-hybrid/manifest.json` hashes updated, and those hashes are asserted by nothing). VERIFIED NOT affected: all pt-webgl/pt-webgpu refs (own packers), all `W8-rc-*` (walkaround-rc path), and anything captured < 2026-05-26.
- **BVH-index stride AUDIT (2026-06-04) — CLEAN, no new bug.** Audited EVERY storage-buffer `array<vec4u>` bvhIndex consumer for the F-TLAS1/F-RC1 stride class (CPU buffer fed to a 16-byte-stride shader read): **restir-TLAS** `packBVHIndexWFromCore(triIndices3)` (F-TLAS1-fixed); **restir-merged** `packBVHIndexW(shared.indices)` (stride-3 in, stride-3 read — correct by construction); **RC merged** `_uploadBVH` padded (F-RC1-fixed) + **RC core** (F-RC2) padded + **RC-TLAS** uses the fixed `snap.bvhIndex`; **DDGI** snapshot uses `snap.bvhIndex` + standalone pads via `padTriangleIndicesToVec4`; **pt-webgpu** uses stride-4-native `geo.indices` AND throws at upload if not 16-byte-aligned (structurally immune). `padTriangleIndicesToVec4` verified correct. **pt-webgl is OUT OF CLASS** (WebGL2 texture-packed BVH — `texelFetch1D`/`DataTexture`, no storage buffers). **HARDENING — all 3 GI subsystems now brute-force-gated on every push** (`wsl-gpu t1-smoke.mjs`): `runRcMergedOracle` (F-RC1), `runRestirTlasOracle` (F-TLAS1), AND `runDdgiOracle` — the DDGI oracle covers BOTH DDGI upload paths (TLAS `rebuildProbeBvhFromRestir(snap.bvhIndex)` + merged standalone `rebuildProbeBvhFromScene`→`padTriangleIndicesToVec4`), each FIXED 100% vs an independent CPU all-triangles ground truth, discriminating BOTH stride sub-classes (stride3of4 = F-TLAS1 + rawStride3 = F-RC1). pt-webgpu self-guards via its upload-time stride-4 throw; pt-webgl is out of class (texture-packed). So EVERY storage-buffer `array<vec4u>` BVH-index consumer is now gated — closing the missing-standing-gate gap that let F-TLAS1 + F-RC1 go latent for weeks.

- **Severity:** **CONFIRMED HIGH (production geometry).** Runtime impact RESOLVED 2026-06-04 by a decisive test: applying the fix locally regresses the T1 smoke from **81→11 dB (mae 0.22)** on BOTH lavapipe and dzn (the two fixed renders still agree, cross-check 26.81 dB). So the committed golden ENCODES the bug, and the production Cornell-DDGI render (core TLAS path) IS materially corrupted — not latent. The pre-fix render is "subtly wrong, not obvious garbage" (DDGI is diffuse GI over probe rays — mangled tri vertices skew the irradiance without an obvious silhouette break), which is why it went unnoticed and the non-regression smoke passed against a buggy golden. Surfaced by the ReSTIR-material decouple agent; **all links + the impact independently verified by the lead.** The fix (feed the packer the stride-3 `geo.indices[t*4+k]` extraction — same as `bvhIndicesStride3`) is provably correct (correct global indices → correct vertices); it CHANGES the render, so it needs the T1 golden (and any other ReSTIR/DDGI reference renders) RE-CAPTURED.
- **The bug:** `buffersFromScenePack` (`restir/sceneBvhFromCore.ts`) packs `bvhIndex` via `packBVHIndexW*(geo.indices, …)`. `geo.indices` (`ScenePackResult.indices`) is **stride-4** (`scenePack.ts:551` "stride 4 (vec4u/triangle)"; allocated `*4` at :593; written `[tri*4+i]` at :628). But `packBVHIndexWTri` (`restir/packingHelpers.ts:113-115`) reads `indices[tri*3 + k]` — a **stride-3** read. So `bvhIndex.xyz[tri]` = `geo.indices[tri*3+k]`, which is correct ONLY for tri 0 (3·0==4·0); tri ≥1 reads across triangle boundaries (e.g. tri 1 gets `[geo.indices[3],[4],[5]]` = `[tri0's .w pad, v0_tri1, v1_tri1]` instead of `[v0,v1,v2]_tri1`). The new `packBVHIndexWFromCore` faithfully reproduces this (which is WHY the decouple's byte-identity test + T1 smoke passed — both sides share the same wrong read).
- **GPU consumes it for geometry:** `shared-bvh/wgsl/bvhIntersect.wgsl.ts:327-334` — `idx = (*bvh_index)[triIdx].xyz; intersectTriangle(bvh_position[idx.x], [idx.y], [idx.z])`. `bvh_index` IS the packed buffer (the shader also reads `idxEntry.w` for material color at :340, a lane only `packBVHIndexW` produces). Reached in production TLAS mode via `tlasTraversal.wgsl.ts:190 bvhIntersectFirstHitAtRoot`. **Merged mode is FINE** — it feeds genuine stride-3 `shared.indices` (`buildSceneBVH`), so the bug is TLAS-path-only (multi-mesh / instanced scenes; `resolveReSTIRBvhMode` returns 'tlas' for >1 mesh or any instanced).
- **THE PUZZLE — RESOLVED 2026-06-04: answer (a).** The goldens were captured WITH the bug, so the T1 non-regression smoke passes against a buggy golden — which **indicts the T1 smoke's correctness value for the ReSTIR/DDGI path** (it has been pinning corrupted geometry). This means the fix's golden re-capture MUST be validated against an independent CORRECT reference (CPU brute-force / known-geometry oracle), not just "re-snapshot what the fix produces". The decisive evidence: fix-applied → smoke 81→11 dB (see Severity).
- **Fix (small) + validation (involved):** pass the already-correctly-extracted stride-3 indices to the packer in the TLAS path — `bvhIndicesStride3` is built via `geo.indices[t*4+k]` at `sceneBvhFromCore.ts:~154` (reorder so it precedes the `packBVHIndexW*` call, or add a stride param). Add a unit test pinning `bvhIndex.xyz[tri] == [geo.indices[tri*4+0..2]]`. **The fix CHANGES the render** (corrects geometry) → the T1 golden must be RE-CAPTURED against a *correct* reference (a CPU brute-force / known-geometry oracle), NOT the existing (possibly-buggy) golden. Single-mesh / tri-0-only scenes mask it — use a transformed multi-mesh ReSTIR-TLAS scene.
- **Acceptance:** the puzzle is resolved (real-vs-latent established with evidence); `bvhIndex.xyz` matches the stride-4 source per a unit test; a multi-mesh ReSTIR-TLAS render matches a CPU brute-force reference; the T1 golden is re-captured if it was buggy.

---

## Section H — 2026-06-09 second deep-read audit (11-agent line-read, lead-verified)

> Filed from a full line-by-line read of all 12 packages (~113k production LOC, 11 reading
> agents) followed by lead verification of the load-bearing claims. **Legend:** `✅` = lead
> opened the file / ran the grep and confirmed; `◻` = agent-reported with file:line, credible
> but NOT yet independently re-verified — re-verify before acting (sub-agent reports are
> hypotheses). Fidelity-ceiling/feature-completion items discovered in the same audit went to
> `plan/road-to-100.md` (addendum section); THIS section is bugs + broken/inert public surface.
>
> **STATUS 2026-06-09 (remediation batch 1 — 8 parallel agents per `plan/h-remediation-plan-2026-06-09.md`; every load-bearing fix lead-verified by code-read; full typecheck + all package suites green incl. the new examples workspaces):**
> - **CLOSED earlier same day (GPU-validated on ANGLE):** H1+H4, H2, H3, H5 (crash-fix + honest gate), H6-intensity, H7 (frameTimeMs / dispose-leak / double-partition).
> - **CLOSED in batch 1 (unit/contract-tested; render-changing ones still need a GPU A/B pass — see "pending validation"):** H7-remainder (silent-drop warns, lite-tier doc, D3: additive regime VERIFIED-dead → deleted, D2: caustic naming documented), H8, H9, H12, H13 (delta-pdf → 0, WGSL goldens re-pinned), H14-C/D/F, H15, H16 (requestFullBlend one-shot clear), H17, H19, H20-doc, H21-doc (verified: sun never enters the rect CDF — the contract comment was over-broad), H22 (phantom emitter VERIFIED-live → zero-power inert + CDF guard), H24-A/B/C, H30, H31-a…h (incl. analytic-mesh UVs), H33, H34-a…g, H36 (userDataKeys deleted; honest READMEs), H37-a/c, H38, H39/H40/H41-B (ledger truth-now + NEW `core/src/__tests__/ledgerVsCapabilities.test.ts` gate), H42, H43, H44/H45 (all READMEs incl. new pt-webgl2 README), H46, H47, H48, H49, H50 (@reserved JSDoc), H51-A/B/C, H53b (sizing GPU stub + dummy-buffer oracle), H55-a/b, H56-a/b, H57 (examples/ tree — 7 workspaces, root-typecheck-gated), H58, H59.
> - **CLOSED in batch 2 (same day; 5 parallel agents + 1 solo, all lead-verified by code-read; full typecheck + all suites green — 2289 tests):** H10, H11, H14-A (implicit emissive-mesh emitters), H14-B, H14-E (dedicated `environmentHdriIntensity` frame-param lane — HDRI no longer scaled by sun.w), H18 (DDGI probe-ray emitter NEE, RC pattern), H23 (mesh-area Le = emitter color×intensity override), H25 (dTree bottom-up interior-flux propagation, CPU oracle in lockstep, χ² sampling-vs-pdf harness), H26 (runningSum seed → 0; cameraPixelPdf lane + per-frame update landed in the tail), H27 (post-loop `r.Lo` training target + atomic slot-claim buffer), H28 (ReLU ping-pong — no more same-buffer read+read_write bind), H29 (`buildPpgUpdateWgsl(stride)`), H32 (glass-aware single-traversal `traceTlasAny`; all call sites audited), H34-h (`sceneVersionTag` merge skip; DDGI caller stays on fingerprint fallback), H35 (OIDN `onError`/`getLastError` → `FrameStats.denoiserState` on all 3 backends), H41-Option-A (point/spot analytic NEE in shade; the 2026-06-12 portability pass moved it from storage buffer to sampled rgba32float texture, so shade now declares 15 storage buffers), H51-D (spot penumbra + point/spot distance/decay; emitter strides bumped), **H52** (clearcoat + Charlie sheen + thin-film iridescence in pt-webgpu; material stride 23→26; zero-default path energy-identical; CREDITS updated).
> - **TAIL CLOSED (2026-06-09, same day; lead-verified by code-read + full suites):** H26-camera-pdf (pinhole solid-angle pdf via a new `nrcCfg.cameraPixelPdf` lane — `cotFovY²·W·H/4` from `projMatrix[5]`, updated per frame/resize; the frozen 416-byte walkaround UBO untouched), H54-VitrumCanvas-mount (`vitrumCanvasMount.test.tsx` — render/attach/dispose; vitest handles tsx natively, no config change; the old fs.existsSync assertion retired), H6-rotationY (convention defined in core JSDoc: CCW dome rotation about +Y; pt-webgl2 via `makeRotationYMat4` → the existing `environmentRotation` uniform; pt-webgpu via the previously-zero `environmentTint.w` lane + `rotateYNeg/Pos` in lookup AND importance-sampling; rotationY=0 byte-identical on both; walkaround = documented no-op, scalar tint). **The ONLY remaining §H code item is H5-Option-B (pt-webgl2 BDPT host driver) — reclassified to `plan/road-to-100.md` A5 per decision D1.** Everything else in §H is closed; the GPU-validation worksheet is **HARDWARE-VALIDATION-NEEDS.md V28**.
> - **H60–H62 (2026-06-09, from an independent external review — all three verified-real by lead code-read, then FIXED same day):** **H60** ✅ the H34-b NaN filter left a hole — ALL-non-finite input slipped past the `triCount===0` guard into an empty-record recursive build (the batch-1 test only covered mixed input, exactly as the reviewer said) → post-filter empty-records guard returns the empty-BVH shape + warn + all-invalid test; **H61** ✅ `getRestirPtResultBuffer` was added to the Engine contract (H14-C) but `wrapWithIdempotentDispose` never forwarded it — unreachable through the `createEngine` facade → added to the optional-method proxy table (noop post-dispose); **H62** ✅ `detectGpu` permanent memoization (a first-wave P2 dropped when §H was filed) → `resetGpuDetectionCache()` export + honest JSDoc on first-call-options/memo semantics (`window.__WG__` publish default kept — flipping it would break walkaround consumers). The same review's remaining "blockers" are the KNOWN road-to-100 items / signed-off decisions (reserved material fields = D16, maxBounces warn = D15, OIDN not turnkey = C1, ReSTIR-PT compositing = A1, BDPT driver = A5, directional IBL = B3) — distance that is tracked, not newly discovered. Its unverified mentions ("neural resize graph mismatch", "OIDN resize race", "out-of-range indices", progressiveHandoff optional-chaining desync) carried no citations — treat as ◻ leads for a future pass.
> - **GPU VALIDATION — CLEARED for the landed fixes (V28 campaign, 2026-06-09; full log in `HARDWARE-VALIDATION-NEEDS.md` §V28 + CLOSE-OUT).** ✅ VALIDATED on real adapters (lavapipe + dzn) or executed-deterministic: **H13** (refraction MIS, energy-conserved), **H15** (UV survives both BVH build seams — round-trip + stripped A/B), **H17/H19** (merged-mode skin vs CPU `solveSkin`, residual→0 + broken-variant divergence), **H16** (invalidate full-replace override, pinned), **H18** (emitter-lit DDGI), **H41-A** (point/spot — a real STRIDE BUG was FOUND + FIXED here, `25f4105`: point 12.5×, spot inert→lit), **H51-D-point** (distance/decay), **H52** (Disney zero-lobe identity), **H6** (rotationY, pt-webgl2), **H28** (neural ReLU alias — first-ever real-device U-Net dispatch), **H9** (code-confirmed; full-tier device-limit-gated). **STILL OPEN (◻) — never were landed fixes, do NOT chase as regressions:** H23 (mesh-area Le — deliberate: relies on material emissive), H32 (TLAS skipGlass closest-hit), H31-g (textured analytic). REMAINING nice-to-have completeness: H14-E (HDRI sun-off pt-webgpu), pt-webgpu cross-backend halves of H6/H52, V25 BDPT (pt-webgl) root-cause. The pre-push T1 smoke goldens did NOT need to move (already current); naga-gate compiled all new WGSL (probe-ray NEE, analytic NEE, Disney lobes, glass-aware traversal).
>
> **STATUS 2026-06-10 (v1-closure campaign — commits 6e90443 … caab499; 5 waves, 6 commits; every fix lead-verified by code-read; full typecheck + all suites green; shader-gate 48/48):**
> - **CLOSED by Wave 1 (6e90443):** D3 material stride 85→93 coherently landed on pt-webgl2 (packer + readMaterialInfo + surface record consumption + single-sourced stride constant `materialStride.js`; parity test added); pt-webgpu backtick parse-break fixed (`material.wgsl.ts:381`); hdri ledger held at `'approximate'` (B3 was still partial — Wave 4/5 promoted it).
> - **CLOSED by Wave 2 (0dbaff5):** H14-DDGI double-count: rect/disc-area fixture point-proxy REMOVED from `coreEmittersToDDGILights.ts:155` (code-verified: 0 hits for fixture-rect pattern); mesh-area emitter triangles now expand into the DDGI probe NEE list (`HybridEngineLifecycle.ts:545`, `collectMeshAreaEmitterTrisFromCore` + `setEmitterTris`). pt-webgpu HDRI RGBA stride-4 detection; lite-tier `supportDetails` narrowed; `consumedMaterialFields` module (+11 tests); stale reservoirGi comments corrected.
> - **CLOSED by Wave 2 round 2 (44ada1a):** pt-webgl2 `emissiveIntensity` default 0→1 (cross-backend consistent); `denoiser` option now warn-degrades on pt-webgl2; core ledger skinned-mesh demoted `native→approximate` on BOTH PT backends; pt-webgpu `procedural-sky` demoted `native→approximate`; walkaround `supportsAuxBuffers` corrected false; `partitionSceneBySupport` now honours `fallbackMesh`; caustic strides (H1/H41 class) single-sourced via POINT/SPOT_LIGHT_VEC4_STRIDE constants; photon spot emission axis negation REMOVED; `hdrLuminanceBilateral` i32/u32 fixed; CI rewritten (dead fork checkout removed); walkaround unconsumed-material-field warn wired.
> - **CLOSED by Wave 3 (1d31f0b):** skinning implemented on BOTH PT backends via `solveSkin` (pt-webgpu `applySolveSkinToScene` + mutation-router fast path; pt-webgl2 `solveSkinPrimitives` pre-pass in `buildSceneTextures`). Ledger `skinned-mesh` promoted `approximate→native` on both (code-verified `promiseLedger.ts:322,392`). pt-webgpu BDPT estimator coherence (A9/A5 fixes). walkaround `tonemap/exposure/outputColorSpace` wired — new 16-byte composite UBO, `vitrumTonemap` from shared-samplers, default aces@1.0@sRGB proven bit-identical to old hardcoded ACES. Mutation-desync cluster (both backends). In-repo shader compile gate (`tools/shader-gate`, 47→48 WGSL shaders, naga; `npm run shader-gate` + CI job).
> - **CLOSED by Wave 4 (06910e2):** A4 SPPM (real SPPM replaces 32-photon approximation — `caustic.wgsl.ts:889-922` + `sppmBindings.wgsl.ts`; fudge constants gone; 23 tests). Preetham procedural sky on pt-webgpu. pt-webgl2 tonemap present pass (DOCUMENTED BEHAVIOR CHANGE: raw linear HDR → `aces@1.0@sRGB` default; hosts wanting raw pass `tonemap:'none'`). Lifecycle-edge hardening (pipeline dispose, OIDN resize guard, neural resize, PPG resize, SampleBudget ping-pong). PPG flux atomics saturate (u32 wrap gone). Core/engine seams fixed (ResizeObserver, progressive seed dims, `advanced.device` override).
> - **CLOSED by Wave 5 (caab499):** Wave 4 env pillar complete (env as DI NEE candidate in walkaround RIS; risGiNrc parity; DDGI probe misses sample HDRI; `updateEnvironment` rebuilds CDFs). `hdri` ledger promoted `approximate→native` (code-verified `promiseLedger.ts:254`). A7 RC finished (correct MC estimator; real env map; point/spot lights; chromatic sun). pt-webgpu tonemap present pass + spectral MIS connection halves + restirPtSpatial `pdfSrc` fix. pt-webgl2 context-loss handler + MAX_TEXTURE_SIZE guards. `packEmitterArrays`/`environmentParams` dedup (no longer runs twice per setScene).
> - **VALIDATION TAIL — V28-B (pending real-GPU A/B):** every render-changing Wave 2–5 landing (tonemap default, SPPM, skinning poses, env NEE, B16 VNDF, spectral MIS, Preetham bake, RC irradiance estimator, DDGI probe HDRI) needs a V28-B GPU A/B pass against fresh baselines. None of these are regressions (prior baselines embed the broken behaviour); the A/Bs confirm improvement. See `HARDWARE-VALIDATION-NEEDS.md`.
> - **STILL OPEN after campaign (road-to-100 items, not regressions):** H5 pt-webgl2 BDPT orchestration (kernel exists, host driver not yet wired); H9 `bdptAdvanceFrame` full-tier bind-group invalidation; H20-H24 cluster (walkaround lifecycle/phantom-emitter — partially fixed by the above mutation-desync work, verify residue); H31 cluster residue (engine/core lifecycle edges); H32 TLAS skipGlass; H34 cluster residue; H35 OIDN dispatcher; H36 orphaned packages; H53/H55/H56 test-infrastructure gates. See per-item notes below for current state.

### H-A — pt-webgl2 (the package furthest from its label)

- **H1 ✅→FIXED P0 — `lights.count` uniform is NEVER uploaded → the entire analytic-light system is inert.** `gl/glResources.ts:308` binds only `lights.tex`; no `uniform1ui`/setter for `lights.count` exists anywhere (lead grep: zero setters). GLSL default `0u` disables the NEE branch (`direct_light_contribution_function.glsl.js:9`), the forward light-hit loop (`composeTraceGlsl.ts:513`), and the BDPT subpath bail-out check. All six advertised emitter kinds light NOTHING except `mesh-area` via the emissive fold. The 263-line `lightsTexture.ts` packer output is uploaded-but-dead. Capability ledger advertises all six kinds. Fix is ~one uniform call **plus H2-adjacent MIS reconciliation (H4)**.
  - **FIXED 2026-06-09 (GPU-validated):** added `GlProgram.setUint` (`uniform1ui` — WebGL2 requires it for `uint` uniforms; `setInt` on a `uint` location is a GL type error, which is why the count could never be driven) + `prog.setUint('lights.count', scene.lightCount)` in `#bindSceneTextures`. **A/B on ANGLE (real WebGL2):** the rect-area-analytic-light `cornell-box` (no emissive mesh, env none) went black → **meanLum 0.073, 99.8% pixels lit, lightCount=1, 0 NaN**; the emitter-stripped control is **meanLum 0 (fully black)** — the analytic emitter is now the sole working light source. Harness: `wsl-gpu/webgl2-capture/run-ptwebgl2-h1.mjs`.
- **H2 ✅→FIXED P1 — `spectral: true` renders black.** `GlProgram.setFloatArray` (the CMF upload path, `glProgram.ts:207`) has ZERO callers (lead grep). `uCmfX/Y/Z`, the three CDFs, and the integrals stay 0 → `wavelengthPdf = 0` → `wavelengthToRGB` returns `vec3(0)`. ◻ also never set: `u_jakobCoeffs`, `iorCauchyA/B/C` (Jakob-Hanika + dispersion gates permanently false).
  - **FIXED 2026-06-09 (GPU-validated):** `glResources.ts` now uploads the CIE CMF tables/CDFs/integrals (imported from `@vitrum/shared-samplers` — `CIE_{X,Y,Z}_TABLE`, `{X,Y,Z}_CMF_CDF`, `{X,Y,Z}_CMF_INTEGRAL`; Float32 copies precomputed at module scope) via `setFloatArray`/`setFloat`, gated on `frame.spectralEnabled`. **A/B on ANGLE:** spectral `cornell-box` went black → **meanLum 0.07298, 99.3% lit, 0 NaN**, and its luminance matches the RGB path to **0.02%** (`spectralOverRgbRatio 0.9998`) — i.e. the hero-wavelength CMF reconstruction is luminance-unbiased. Harness: `wsl-gpu/webgl2-capture/run-ptwebgl2-h2.mjs`. **Follow-on (separate, lower-pri):** `u_jakobCoeffs` (RGB→spectral reflectance upsampling) + `iorCauchyA/B/C` (dispersion) stay at their flat-spectrum/no-dispersion defaults — spectral is lit + luminance-correct but reflectance is achromatic-flat until per-material Jakob coeffs are uploaded; that's spectral *colour/dispersion* fidelity, not black-vs-lit.
- **H3 ✅→FIXED P1 — `backgroundAlpha` uniform never uploaded (defaults 0) → directly-visible sky/HDRI pixels never accumulate** (blend `src*0 + dst*1` in the normal regime; zeroed in alpha-composite). `composeTraceGlsl.ts:550` + `gl/blend.ts:49-51`. Only indirect env survives.
  - **FIXED 2026-06-09 (GPU-validated):** added `backgroundAlpha?: number` to `EngineOptions` (default 1, clamped [0,1]); `glResources` uploads it via `setFloat('backgroundAlpha', …)` each frame; the regime now routes `backgroundAlpha !== 1 → alpha-composite` (mirrors the fork's `needsAlphaComposite = bgAlpha !== 1 || !floatBlend`, which blend.ts already documented). Default 1 = opaque visible env that accumulates like geometry. **A/B on ANGLE (blue-sky HDRI scene + floor, `run-ptwebgl2-h3.mjs`):** the directly-visible sky (top region, NO_HIT first ray) went black → **r=0.1000 g=0.3000 b=0.8000 = the env colour exactly** (topLum 0.294, 0 NaN); the `none`-env control top region is pure black. So directly-visible env now accumulates with correct chroma. (A chroma anomaly in the first run caught a test-data bug — the HDRI payload is read STRIDE-3 RGB by `equirectHdrInfo.ts:118`, not RGBA — not a code bug.)
- **H4 ✅→FIXED P1 — MIS pdf mismatch, latent behind H1:** NEE uses power-weighted light selection (`light_sampling_functions.glsl.js:196-217`) but the forward-hit MIS weight assumes uniform selection (`composeTraceGlsl.ts:524`, `discretePdf` hardcoded 1.0 in `intersectLightAtIndex`). Becomes a live bias the moment H1 is fixed with ≥2 unequal-power lights. Fix together with H1.
  - **FIXED 2026-06-09 (with H1):** the forward-hit MIS now mirrors NEE's power-weighted discrete selection exactly — `p_light = lightRec.pdf / lightsDenom * count * (power_i / sumPower)`. The forward loop computes `sumLightPower` once and the per-hit `discreteSelectPdf = power_i / sumPower` (with the same `sumPower>0 ? … : invCount` fallback as `randomLightSample`), replacing the old `lightRec.pdf / lightsDenom` (which silently assumed `count·discretePdf == 1`). Provable no-op for a single light or equal powers (so the H1 single-rect-area A/B above is unaffected — confirmed: rendered, 0 NaN); corrects the bias only for ≥2 unequal-power area lights. A dedicated ≥2-unequal-power-area-light converged A/B is the remaining validation refinement.
- **H5 ◑ P1 — `bdpt: true` compiles kernels the host never drives.** No subpath passes are issued, `uBdptLightSubpathPass`/`uBdptVertexCol`/`uBdptMaxLightBounces` never set (loop never iterates), and `uBdptLightPathTex` is left unbound on unit 0 colliding with the usampler there — by `glResources.ts:309-312`'s own comment that is GL_INVALID_OPERATION → likely a black frame. The dummy-binding list (`glResources.ts:313-320`) omits it.
  - **CRASH FIXED + honestly gated 2026-06-09 (GPU-validated):** added `uBdptLightPathTex` to the dummy-binding list (no-op when bdpt off; binds a dummy when on) so `bdpt: true` no longer leaves an unbound sampler on unit 0 → no more GL_INVALID_OPERATION black frame. The constructor now `console.warn`s that BDPT is not yet host-driven. **A/B on ANGLE (`run-ptwebgl2-h5.mjs`):** `bdpt:true` cornell-box went (black, GL-errored) → **glErr 0, 0 NaN, meanLum 0.07142 = BIT-IDENTICAL to the unidirectional render** — i.e. it renders safely and, because the light subpath is still undriven, exactly equals unidirectional (honest: BDPT is inert-but-safe, not silently-wrong).
  - **STILL OPEN (feature, tracked):** the actual BDPT light-subpath ORCHESTRATION — a multi-pass pipeline that runs the shader with `uBdptLightSubpathPass=1` writing vertices into the `uBdptLightPathTex` ping-pong (setting `uBdptVertexCol`/`uBdptMaxLightBounces` per bounce), then connects in the main pass. The GLSL kernels exist (`bdpt_connection.glsl.js`, `writeLightSubpathVertex`); only the host orchestration is missing. This is a feature sprint with its own caustic/hard-transport A/B, not an upload gap.
- **H6 ◑ P2 — env contract fields ignored:** `environment.intensity`/`rotationY` hardcoded to 1 / identity (`index.ts:298-299`).
  - **intensity FIXED 2026-06-09 (GPU-validated):** `#frameUniforms` now reads `env.intensity ?? 1` for HDRI environments (mirrors pt-webgpu `environmentPacking.ts:54`). A/B on ANGLE (`run-ptwebgl2-h6.mjs`): the directly-visible blue sky scaled (0.1,0.3,0.8)→(0.2,0.6,1.6) at intensity 1→2, **blue ratio exactly 2.0**.
  - **rotationY STILL OPEN — but it is a CROSS-BACKEND gap, not pt-webgl2-only:** verified 2026-06-09 that NO backend honours `environment.rotationY` (zero `rotationY→matrix` wiring in pt-webgpu either). Needs a chosen Y-rotation convention applied consistently across backends; tracked as a cross-backend contract item, left as `IDENTITY_MAT4` with an honest in-code note rather than an inconsistent unilateral guess.
- **H7 ◑ P2 cluster:** ~~`'additive'` accumulation regime unreachable (`index.ts:89` never selects it — overlaps road-to-100 D1)~~ closed 2026-06-12: `FEATURE_ADDITIVE_ACCUM` was removed from `featureDefines` and the emitted GLSL, with `composeTraceGlsl.test.ts` gating the absence; `dof/fog/backgroundMap/randomType/cameraType/debugMode` feature flags exist in `DEFAULT_TRACE_FEATURES` but no option plumbs them (sobol/stratified textures are 1×1 dummies); ~~`onFrame` reports hardcoded `frameTimeMs: 0`~~; ~~dummy textures never deleted in `dispose()`~~; ~~`partitionSceneBySupport` runs twice per setScene~~; caustic options named `mnee*` drive a cone-walk heuristic, not MNEE (`composeTraceGlsl.ts:743-827` — the "photon-map" variant ADDS energy for rays that hit nothing, line 817); silent drops: unreadable texture handles, unreadable HDRI → no warning; `lite` tier doc promises bounce/texture caps it doesn't implement (overlaps D9).
  - **3 sub-items FIXED 2026-06-09 (typecheck + 82 vitest green):** (a) `onFrame frameTimeMs` now reports the real per-frame CPU/submit cost (measured around `drawAccumStep`; the no-draw paused/converged fast-outs honestly report 0; full GPU-execution time would need `EXT_disjoint_timer_query`); (b) `dispose()` now `deleteTexture`s the two lazily-allocated dummy textures — they leaked on every engine teardown (Canvas-remount churn accumulated them); (c) `partitionSceneBySupport` now runs ONCE per `setScene` — `buildSceneTextures` returns its `supported` subset so the redundant second partition (it re-filtered the already-filtered scene) is gone. **Remaining H7:** additive-regime reachability, the unplumbed feature flags, the `mnee*`-named cone-walk heuristic, silent texture/HDRI drops, lite-tier doc — lower-tier, several overlap road-to-100.
- **H8 ◻ P2 — per-vertex materialIndex aliasing:** material id splatted per vertex, lookup reads corner x only (`bvhTextureAdapter.ts:127-139`, `composeTraceGlsl.ts:581`) — correct only while the merge never dedups vertices across material boundaries; unenforced invariant.

### H-B — pt-webgpu

- **H9 ✅ P1 — `bdptAdvanceFrame()` kills rendering on the full tier.** It nulls `pathTraceBindGroup2` (`index.ts:1279-1286`), but `buildBindGroups` early-returns while `pathTraceBindGroup` is cached (`gpuResources.ts:869-870`) so group 2 is never rebuilt with the external buffer, and the dispatch gate (`index.ts:850`) then sets NO groups 1–3 → validation-discarded submits while `#samplesAccumulated` keeps counting (silent stale/black frames "converging"). The WG-7 host-buffer seam cannot work as written.
- **H10 ◻ P1 — emitter/material fast paths desync the camera-visible emissive fold** (`cameraVisibleEmitters` default-ON): `updateEmitter` fast path re-uploads emitters + light tree but never touches `materialsBuffer` (stale folded Le, `sceneMutationRouter.ts:392-421`); a material patch packs the RAW material, wiping the fold entirely (`sceneMutationRouter.ts:360`) — every `InverseSession` step calls `patchMaterial`, so an inverse fit on an emissive-mesh scene makes the emitter camera-invisible mid-session.
- **H11 ◻ P1 — mesh-area emitter triangles go stale on geometry/transform/refit fast paths** — emitter tris are baked world-space at pack time (`emitterPacking.ts:182-260`); the refit/resize/transform paths never re-run `packEmitterArrays`/light-tree rebuild. The skinned-emitter workflow hits this every frame.
- **H12 ✅ CLOSED (source-verified 2026-06-12) — lite tier capability overstatement is stale.** Current `pt-webgpu` distinguishes lite support in `index.ts`: lite supports merged meshes, directional/point/spot/rect-area emitters, procedural-sky/HDRI via B12 texture packing, and explicitly keeps TLAS/analytics/disc-area/mesh-area/BDPT/caustics out of the lite path. `liteTierCapabilities.test.ts` pins supportDetails/warnings; `webgpuLimits.test.ts` pins the sampled-texture budget for `liteEnvTex`/`liteEnvCdfTex`/`liteLightTex`.
- **H13 ✅ CLOSED (source-verified 2026-06-12) — delta-refraction pdf mismatch is stale.** `brdfDirectionalPdf` now returns `0.0` for the opposite-hemisphere deterministic Snell refraction branch, with the old finite `eta²` approximation absent; `wgslContract.test.ts` pins the H13/D4 branch and `wgslLiteContract.test.ts` records the shared BSDF repin.
- **H14 ◻ P2 cluster:** emissive BVH meshes absent from `meshAreaLights` emit only on camera/refraction paths (`connect.wgsl.ts:214-237` + the emissive-on-hit gate); ReSTIR-PT producer NEE omits spot + mesh-area lights (`restirPtProducer.wgsl.ts:120-230`); `getRestirPtResultBuffer` unreachable from any typed public surface (compounds road-to-100 A1); mapAsync-rejection leaks readback buffers (`rgba16fReadback.ts:112-115`, adjoint transients `index.ts:1176-1215`); HDRI requires CPU float pixels — handle/ImageBitmap payloads fall to procedural sky with a misleading "lacks CPU pixel data" warning (also emitted for an all-black HDRI), and env radiance is scaled by `params.environmentSun.w` (HDRI black when sun.w=0) (`environmentPacking.ts:76-144`, `connect.wgsl.ts:69,119`); texture path v1: raw data assumed rgba8 (Float32 uploads garbage, no validation), `mipmapFilter:'linear'` with no mips ever generated, UV transform honored for baseColor only (`materialTextureArray.ts:160,71`, `materialTextures.ts:74-80`); out-of-range tuning silently coerced to defaults (`bdptOptions.maxLightBounces=0` → 3) while `maxBounces<1` throws — inconsistent; per-frame un-gated console.warn spam over buffer ceilings (`gpuResources.ts:488-498,544-552`); inverse: adjoint ignores `req.samples` and is single-bounce point+rect-only vs a full-path loss (quietly biased gradients on spot/mesh-area/env/indirect-lit scenes); inverse render while paused silently reads zeros (`index.ts:1230-1276` vs the paused fast-out).

### H-C — walkaround-hybrid

- **H15 ✅ P0 — UVs silently ZEROED at both production BVH build sites.** `restir/bvhCore.ts:169` and `:246` call `packUVIntoPositionW(geo.positions, undefined, vertCount)` — the UV source hardcoded `undefined` → every vertex packs UV(0,0) → every UV-driven surface texture samples one texel. shared-bvh DOES produce real UVs (`WorldSpaceMergeResult.uvs` since `63a6dab`); they're discarded at the seam. This is the concrete locus of the known "silent texture drops" item for the walkaround path.
- **H16 ✅ P1 — `DDGI.invalidateProbeCache()` does not do what its contract says.** Doc (`DDGI.ts:224-234`) promises the next blend fires with alpha=1 ("effectively clearing" the atlases); code sets `_frame=0; _ready=false` only, and hysteresis is the unconditional constant 0.97 (`probeUpdateFrameParams.ts:85,93` — lead-verified). After `updateLighting`, stale GI decays over hundreds of frames instead of clearing. Compounds the open `updateLighting({primaryLightDir})` DDGI sun-desync item (Section G banner, 2026-06-06).
- **H17 ✅ P1 — GPU skin kernel doubles world translation in merged mode.** `gpuSkinBvh.wgsl.ts:93` seeds `sp = vec4f(0,0,0,1)` then accumulates `sp += wi * p4` where `p4.w == 1` for affine bones → `sp.w == 2` with normalized weights; `:123` `(matrixWorld * sp).xyz` scales the translation column by 2. Bites merged mode (`applyWorld=1`) with a non-identity world transform; hidden by identity-transform tests and TLAS mode. CPU `solveSkin` accumulates from zero (correct). One-line fix (`vec4f(0)` seed or use `sp.xyz` then transform as a point).
- **H18 ✅ CLOSED (2026-06-12) — DDGI probe rays now see area emitters and plain material-emissive surfaces.** Earlier waves added rect/disc/mesh-area triangle NEE through `ddgiEmitterNEE`; this follow-up closes the leftover plain material-emissive path by adding packed `mat.emissive` to direct probe-hit radiance in `probeUpdateRays.wgsl.ts` after the glass mix and before `out.hitRadiance`. `probeUpdateMaterials.ts` now documents that DDGI reads the emissive lane, and `ddgiGlossyProbeBounce.test.ts` pins both the shader ordering and the core-material convention that `emissive` is final radiance rather than `emissive * emissiveIntensity`. The light packer also filters unsupported `DDGILight.kind` values before the count/cap, warns with the unknown kind names, and `probeUpdateLights.test.ts` pins that unknown kinds do not consume GPU light slots. `MAX_LIGHTS=16` truncation remains intentionally capped but warned.
- **H19 ✅ CLOSED (2026-06-12) — transform/positions fast paths keep GPU per-vertex normals current.** Earlier H19 work added inverse-transpose normal-slice uploads for transform refits and merged-mode positions+normals refits. This follow-up closes the TLAS residual: `HybridEngine.updatePrimitive` now routes count-preserving `positions` plus optional `normals` through `positionsRefit` unless structural topology fields are present, and the TLAS branch uploads the matching `bvhNormals` slice for replacement local normals. `positionsRefitTlas.test.ts` pins the TLAS normals upload; `primitiveUpdatesFixes.test.ts` continues to pin inverse-transpose transform normals.
- **H20 ✅ CLOSED (source-verified 2026-06-12) — empty ready scenes present sky-only frames.** The empty-scene init path still intentionally sets `'ready'` without BVH/pipeline (`HybridEngineLifecycle.ts`), but `runHybridEngineFrame` now detects `pipeline == null && bvh == null && state === 'ready' && lastScene.primitives.length === 0` and calls `presentSkyOnly()` with `skyTint × skyIrradiance` when a swap-chain view is available. `skyOnlyPresent.test.ts` pins rendered output for empty ready scenes, skip-without-view, and the non-empty mid-init case.
- **H21 ✅ P2 — `updateLighting({primaryLightIntensity})` emitter-CDF desync claim is source-verified stale.** Directional/sun emitters never enter `collectRectAreaEmitterTrisFromCore` / the rect-area emitter CDF; the live UBO comment in `pipelineFrameInputs.ts` documents that `updateLighting({primaryLightIntensity})` correctly mutates the shade-side sun intensity without rebuilding the emitter list. DDGI receives the matching sun-intensity multiplier via `HybridEngine.updateLighting()`.
- **H22 ✅ CLOSED (source-verified 2026-06-12) — zero-emitter placeholder is structural but inert.** `emitterList.ts` still emits one placeholder triangle to satisfy non-zero WebGPU storage-buffer binding requirements, but it now packs `Le=[0,0,0]`, `intensity=0`, and `power=0`; the zero-total-power CDF branch writes finite uniform CDF values instead of NaN. The shader-side RIS weight remains zero because `pHat=luminance(Le...)` is zero. `hRemediationItems.test.ts` pins the zero-emitter placeholder `Le=[0,0,0]` and the one-real-emitter non-zero guard.
- **H23 ✅ CLOSED (source-verified 2026-06-12) — `mesh-area` emitter `color`/`intensity` now drives walkaround emitter Le.** `bvhCore.ts` builds mesh-area material-slot Le overrides from `emitter.color * emitter.intensity`, applies them to the ReSTIR emitter-list production materials, and applies the same override to TLAS/merged emissive-Le glow buffers. `directLightEmitterCore.test.ts` pins that a mesh-area emitter overrides the referenced mesh's material emissive with `[100,100,100]` for `color=[10,10,10]`, `intensity=10`, and that total emissive power follows the override. Caveat retained in code: material-slot dedup means primitives sharing a structural material slot share the override.
- **H24 ◻ P2 cluster (engineering):** `materialResolver` unknown-id/duplicate-id fallback is ✅ closed (`restir/bvhCore.ts`, `bvhCoreMaterialResolver.test.ts`: duplicate mesh-like primitive ids now throw before TLAS packing can reuse the first material slot, and impossible unknown resolver calls throw instead of falling back to material 0); legacy `SceneBvh` equal-length vertex edit skip is ✅ closed (`SceneBvh.updateFromCore()` uses `fingerprintBuffersExact`, pinned by `sceneBvhVersionTag.test.ts` for equal-length vertex edits and large unsampled-byte edits); DDGI frame-loop error swallowing is ✅ closed (`DDGI.ts`, `ddgiErrorReporting.test.ts`, `engineErrorSurface.test.ts`: init/BVH/runFrame failures emit non-fatal `EngineError` via `HybridEngine.onError`, and failed probe frames no longer advertise `ready`); GpuSkinning host-buffer absence is ✅ closed (`GpuSkinningSubsystem.ts`, `gpuSkinningBindRouting.test.ts`: absent GPU position/normal buffers or mesh ranges now CPU-skin all skinned meshes instead of returning silently), and the stale count-only cache-key note is source-verified closed by live `boundPositionBuffer`/`boundNormalBuffer` identity checks; emitter-count magic stride is ✅ closed (`EMITTER_TRI_STRIDE_BYTES`, `BvhBufferHost.ts`, `bvhBufferHost.test.ts`: upload/update paths validate byte alignment and declared count instead of dividing by a literal 80); beer/emissive texture dimension guarding is ✅ closed (`bvhTextureLimits.ts`, `bvhTextureLimits.test.ts`: Beer/rough-metal/emissive uploads check `maxTextureDimension2D`, refreshes check existing capacity, and huge counts no longer truncate through `|0`); NRC training failure swallowing is ✅ closed (`WalkaroundGPUPipeline.ts`, `nrcTrainingDiagnostics.test.ts`: train readback/step rejections emit deduped non-fatal `EngineError`s while the pipeline is live and remain suppressed after dispose); denoiser `state()` zero-consumer claim is ✅ source-verified stale (`WalkaroundGPUPipeline.getActiveDenoiserState` → `HybridEngineFrameOrchestrator` → `FrameStats.denoiserState`, pinned by `frameStatsDenoiserState.test.ts`); RC per-frame sun RGB is ✅ closed (`HybridEngineFrameOrchestrator.ts`, `hybridEngineFrameOrchestrator.test.ts`: scene directional color × intensity reaches `RCSubsystem.dispatchFrame`, legacy scalar grey only when no scene directional exists); SVGF object-id unconditional allocation is ✅ closed by Road D7 (`createSvgfFrameResources.ts`, `svgfObjectIdResources.test.ts`, `gpuMemoryEstimate.test.ts`); remaining memory residue: GRIS reservoir lanes (10 of 30 u32/px) + dead `nrcEnabled` UBO byte carried on the default path (~30 MB+ at 4K for an off feature), and `hdrTotalTexture`/variance-aux textures allocated unconditionally for conditional consumers.

### H-D — opt-in subsystem structural defects (PPG / NRC / neural)

- **H25 ✅ CLOSED — PPG dTree interior flux propagates bottom-up before refinement.** `PPGCoordinator._mergeFluxAndRefine` merges fresh leaf flux, zeroes interiors, then reverse-scans children into subtree sums before `refineDTree`; `ppgPdf.wgsl.ts` uses those child flux values for proportional descent and evaluates `(leafFlux / totalFlux) / solidAngle`. `dTreeInteriorFlux.test.ts` pins depth-1 and depth-2 sampling/pdf parity, including a chi-square harness and zero-flux fallback.
- **H26/H27 ✅ CLOSED — NRC spread semantics, training target, and record claims are wired.** `risGiNrc.wgsl.ts` seeds `runningSum` from `0.0`, computes the primary footprint from `nrcCfg.cameraPixelPdf`, reads roughness from `bvh_material`, tracks the first fired suffix candidate, and writes one post-loop record with `r.Lo`. `nrcQuery.wgsl.ts` uses `atomicCompareExchangeWeak` on `nrcSlotClaims`, `NrcSubsystem.updateCameraPixelPdf()` updates the UBO lane from projection/resolution, and `RISGIPass` clears slot claims immediately before the NRC GI-RIS dispatch. `spreadTermination.test.ts` pins the production-default spread arithmetic, camera-pdf source, post-loop target structure, and no hardcoded `xsRough=1.0` regression.
- **H28 ✅ CLOSED — neural U-Net ReLU layers no longer alias read and read_write storage bindings.** `layerResourceAllocator.ts` detects in-place ReLU specs, allocates a distinct `${layer.name}_out` buffer for binding 3, preserves the original input for disposal, and remaps downstream tensor reads. `reluPingPong.test.ts` records bind groups and asserts no ReLU layer uses the same buffer at bindings 0 and 3; V28 also records a real-device neural dispatch for this fix.
- **H29 ✅ CLOSED — PPG max dTree nodes per cell is now end-to-end configurable and snapshot-compatible.** `HybridEngineOptions.ppgMaxDTreeNodesPerCell` threads through `deriveHybridEngineConfig`, `_initStaticConfig`, `PipelineInitCoordinator`, `WalkaroundGPUPipeline.initialize`, `compilePipelines`, `PPGCoordinator.initialize/onResize`, and `allocatePPGResources`; `buildPpgUpdateWgsl` uses the same cap as the flux/dTree buffers. `PPGCoordinator.exportSTree/importSTree` and GI snapshot v5 now record/check `maxDTreeNodesPerCell` (v4 snapshots default to the historical 341). Tests: `ppgUpdateBuilder.test.ts`, `hybridEngineCfgCharacterization.test.ts`, `hRemediationItems.test.ts`, `lifecycleEdges.test.ts`, `giStateSnapshot.test.ts`.

### H-E — engine / core (host integration)

- **H30 ✅ CLOSED — `attachVitrum` / `<VitrumCanvas>` now manage the canvas backing store before engine construction.** `attachVitrum` synchronously writes `canvas.width/height = clientSize × devicePixelRatio` before calling `createEngine`, so WebGPU swapchain/history resources allocate at the visible size instead of the browser default 300×150. `attachVitrumLoop.test.ts` pins both initial sizing and CSS×DPR behavior; `<VitrumCanvas>` inherits the fixed attach path.
- **H31 ✅ CLOSED (source-verified 2026-06-12) — engine/core lifecycle residue reconciled, with backend-scoped advanced options now explicit.** The stale claims are closed in source: `createEngine` returns `EngineWithBackendId` via non-enumerable `backendId`; `createProgressiveEngine` routes `configureWebGpuCanvas` failures through `onError`; `idempotentDispose` reports dispose/post-dispose throws through `onDisposeError`; `detectWebGL2Sync()` returns `false` on exceptions; analytic primitives without fallback meshes are skipped for AABB instead of contributing a zero-volume origin point; `analyticPrimitiveToMesh` now emits UVs; `pickBackend` routes TLAS + WebGPU quality scenes to `pt-webgpu`; and `Material.anisotropy` documents the glTF KHR_materials_anisotropy convention. The remaining real `opts.advanced` ambiguity is now closed by `advancedByBackend` plus legacy `advancedBackend`: backend constructors call `resolveAdvancedForBackend()`, keyed options consume only the selected backend's bag, tagged legacy bags are ignored with `createEngine.advanced-target-backend-mismatch` when auto/fallback chooses another backend, and untagged legacy `advanced` under auto emits `createEngine.advanced-auto-backend-ambiguous`. `createEngineConstruction.test.ts` pins the resolver and the pt-webgpu device-limit path.

### H-F — shared packages

- **H32 ✅ CLOSED (source-verified 2026-06-12) — `traceTlasAny` threads `skipGlass` through closest-hit traversal.** The old any-hit pre-test + non-glass-aware closest-hit pair has been replaced by one `bvhIntersectFirstHitAtRoot(..., skipGlass)` per TLAS instance in `tlasTraversal.wgsl.ts`; `bvhIntersectFirstHitAtRoot` mirrors the any-hit transmission-nibble filter before triangle tests. `tlasWgslPipeline.test.ts` now pins the TLAS `skipGlass` argument and the closest-hit `trans4` filter.
- **H33 ✅ CLOSED (2026-06-12 Wave 2) — Beer-Lambert material fingerprint is coherent.** `worldSpaceMerge.materialSig` already included `attenuationColor` / `attenuationDistance` / `thickness`; the stale gap was `SceneBvh.materialSetHashFloats`, which omitted distance/thickness and could skip a DDGI content rebuild when only Beer-Lambert depth changed. `sceneBvh.ts` now hashes packed attenuation distance plus thickness, and `sceneBvhVersionTag.test.ts` pins attenuationDistance-only and thickness-only rebuilds.
- **H34 ✅ CLOSED (source-verified 2026-06-12) — shared-bvh pack/refit safety cluster reconciled.** Current source already guards leaf tri/instance count overflow in BLAS/TLAS forced-leaf paths (`leafCountGuard.test.ts`), filters non-finite and out-of-range triangles before they poison root bounds (`nanTriangleFilter.test.ts`, `oobIndexFilter.test.ts`), skips zero-instance instanced meshes before BLAS concatenation, auto-upgrades `tlas:false` multi-primitive packs with a warning, and skips non-invertible transforms during initial TLAS packing (`scenePack.test.ts`). This wave closes the remaining refit asymmetry: `refitTlasTransforms()` now returns `ok:false` when an updated transform becomes non-invertible, forcing callers onto rebuild/skip behavior instead of refitting that instance as identity-at-origin. `refitBvhBounds()` already accepts `indexStride: 3 | 4` and has stride coverage. `validateBvhEncoding` and `expandIndicesToStride4` remain exported diagnostics/adapters rather than required production calls; production paths use their own stride-specific packers where appropriate.
- **H35 ◻ P2 denoiser cleanup residue:** The P1 OIDN runtime diagnostic claim is ✅ closed/source-verified. `OIDNDispatcherCore` already had `onError` + `getLastError`; pt-webgpu now exposes that hook through `OIDNFinalDispatcher`, and `PTEngineWebGPU` converts async OIDN readback/model/ORT failures into non-fatal structured `EngineError { kind:'denoiser' }` while `FrameStats.denoiserState` remains `failed`. `oidnFinalIntegration.test.ts` pins the wrapper hook. Walkaround's in-engine `OIDNFinalDenoiser` already reports `state(): failed` and destroys readback buffers in `finally`. Remaining lower-priority cleanup: one-shot shared-denoiser pipelines still need try/finally audits around readback/transient resources; one-shot SVGF still needs verification/fix for demodulating previous-frame radiance by current-frame albedo; `ATROUS_WGSL` standalone compilation still needs a direct luminance include or a documented composer-only boundary.

### H-G — orphaned/stranded surface (THREE-removal debris)

- **H36 ◻ P1 — two packages have zero production consumers:** `@vitrum/scene-lighting` (index header claims "consumed by the native path-tracing and walkaround renderers" — false; zero imports repo-wide; content is host-app-specific: panel-backlit sun, 22° altitude cap, inches) and `@vitrum/stained-glass-extensions` (`packCameUBO` has no GPU-shader counterpart anywhere; userData keys target the deleted three-bindings layer; walkaround-hybrid declares the dep but imports nothing). Decide: re-wire, relocate to a host repo, or delete (per the dead-code policy: classify, don't blind-delete).
- **H37 ✅ CLOSED (2026-06-12 Waves 4-6) — `walkaround-rc` exported surface, dispatcher lifecycle, and direct-light glass visibility are reconciled.** The README/package-boundary test verify no `/three` bridge is advertised/exported; `validateCascadeDims()` guards custom cascade overrides; `RCSubsystem.refitCascadeBounds()` invalidates bindings when probe origin/room size changes; raw `RCDispatcher.dispatchFrameRaw()` auto-invalidates cached bind groups when `bvhMode`, buffer sets, env bindings, device, cascade output buffers, or cascade bounds change; stale RC mapping/"Used by" docs are reconciled to the current raw package boundary. The probe-cast direct-light residual is also closed: `rcEmitterNEE()` and `evalRCPointSpotLights()` now use `rcTraceAny(..., skipGlass=true)` so transmissive geometry does not fully occlude coarse RC direct light, and merged-mode RC uploads the canonical `bvhIndex.w` payload so the `trans4` glass filter works outside TLAS mode too. `cascadeDispatchInvalidation.test.ts` pins stable-frame reuse plus TLAS/bounds rebuild behavior, `rcLightEvalWgsl.test.ts` pins the glass-skip direct-light shadow calls, and `rcMergedRefit.test.ts` pins merged RC glass `trans4` packing.
- **H38 ✅ CLOSED (2026-06-12) — stale fork-era comments and the sample-budget first-frame bug are reconciled.** Source-read verified the old `risGi.wgsl.ts` / `risGiNrc.wgsl.ts`, `shared-samplers/luminance.ts`, and `probeUpdatePass.ts` citations were already corrected in HEAD. This wave removed the remaining stale `probeGrid.ts` three/webgpu coupling comment and fixed the real behavior bug in `sampleBudget.wgsl.ts`: frame 0 reaches the shader as `sampleCount=1`, so `welfordVariance(n < 2)` returned 0 and previously classified every first-frame pixel as tier 1 despite the contract saying low-confidence tier 4. The shader now forces tier 4 for `n < 2`; `sprint9-10a-welford.test.ts` and `staleCommentGate.test.ts` pin the behavior/comment closure.

### H-H — Claims surfaces: capability ledger / fidelity matrix / READMEs / CHANGELOG (2026-06-09 second wave)

> Five claims-focused agents (ledger-vs-code, READMEs-vs-code, examples/tools, option
> efficacy matrix, test blind spots) ran after H-A…H-G were filed; lead spot-verified the
> load-bearing items. The pattern: **the machine-readable contract surface and the docs
> both overstate the code in the same places the H-A…H-G bugs live.**

- **H39 ✅ P0 — promiseLedger rows contradict shipped runtime capabilities (same backend, same fields).** pt-webgl2: ledger claims all 5 analytic shapes `'fallback-generated-mesh'` (`promiseLedger.ts:299-305`) but `pt-webgl2/src/capabilities.ts:36` ships an EMPTY `supportedAnalyticShapes` set — analytic primitives are warn-and-DROPPED (`analyticPrimitiveToMesh` has exactly one caller repo-wide: walkaround). ◻ Also: ledger mutations rows say `'native'` in-place (`promiseLedger.ts:306-314`) while runtime overrides ALL to `'fallback-rebuild'` (`capabilities.ts:71-81`); `supportsAuxBuffers:false` vs tier-dependent true. The ledger header calls itself a "machine-checkable truth table asserted by backend tests" — no test catches these.
- **H40 ✅ P1 — ledger grades pt-webgl2 emitters all `native`** (`promiseLedger.ts:282,293`) — false per H1 (analytic-light NEE inert; only mesh-area-via-emissive-fold lights, and that has no NEE either).
- **H41 ✅ P1 — walkaround point/spot graded `native` but have NO direct-lighting term.** ReSTIR-DI harvests only rect-area/disc-area (`restir/bvhSceneHelpers.ts:323,350` — lead-verified) + mesh emissive; point/spot reach DDGI fixtures only (`coreEmittersToDDGILights.ts:180-211`) → no sharp shadows, no specular highlights, diffuse probe irradiance only. The ledger's own comment (:204-205) admits the routing but grades `native` anyway.
- **H42 ◻ P1 — `plan/renderer-fidelity-matrix.md` is structurally stale:** the entire `pt-webgl` column describes the package deleted 2026-06-09; the shipping `pt-webgl2` backend appears NOWHERE in the matrix; row "Hero-wavelength + CMF — pt-webgpu: supported" overstates the tint-over-RGB regime; evidence gate `npm run fork-shader-smoke` no longer exists.
- **H43 ✅ P1 — CHANGELOG `[Unreleased]` advertises deleted packages with no `Removed` entry** (lead-verified: pt-webgl BDPT light-path fill :40, TLAS tags :43-52, three-bindings entries :65, patch-surface claims :87). A consumer reading the only release section expects APIs that no longer exist.
- **H44 ✅ P1 — README claims vs code:** root README:49-51 advertises pt-webgl2 "point/dir/area/sky" lights (dead per H1), "spectral hero-MIS" (black per H2), and "manifold-NEE" caustics (◻ actually a heuristic refraction-walk whose "reachedLight" means the ray ESCAPED, weighted by an ad-hoc `pow(dot,10)` focus — `composeTraceGlsl.ts:742-791`; the real Newton-solve MNEE is pt-webgpu-only — same name, different algorithm). walkaround-hybrid README: ✅ documents the deleted `examples/neural-denoiser` workspace (:63-69) and a `three/webgpu` peer dep its package.json doesn't declare (:169-174); ◻ labels PPG/neural "production-grade" (:3); ◻ RC "not GPU-verified" warning is a stale UNDERSELL (RC was GPU-validated 2026-06-07). ✅ `pt-webgl2` and `stained-glass-extensions` ship NO README at all (the only two; pt-webgl2 is a headline backend). ◻ `scene-lighting` README "shared by every vitrum render backend" — zero production consumers (H36). ◻ root README perf table presents an unverifiable living-room ms/frame figure alongside "validation pending" rows.
- **H45 ◻ P2 — README undersells:** dev README still calls click-to-pick "a future addition" (shipped, T3.G); pt-webgpu README:134 "textured PBR is a pt-webgl-only path" (deleted name; native pt-webgl2 has the atlas).

### H-I — Accepted-but-ignored public option/field surface (per-backend efficacy matrix)

> Full matrix in the 2026-06-09 audit transcript; the load-bearing rows, lead-spot-verified
> where marked. The headline: **the core MaterialSpec advertises ~60 fields; the walkaround
> default path consumes ~8 of them (partially), and a dozen fields have ZERO consumers in
> any backend.**

- **H46 ✅ CLOSED — walkaround no longer silently accepts inherited core options it cannot honour.** Source-verified 2026-06-12: `maxBounces` is a real DDGI indirect-feedback control surface (`1` = direct-only probes, `>=2` = diffuse equilibrium), is reflected in capabilities, and is pinned by `h46aMaxBounces.test.ts`; invalid `<1` values warn and clamp to the direct-only regime. `maxSamplesPerPixel` now emits `walkaround-hybrid.max-samples-per-pixel-ignored` because walkaround does not accumulate SPP. Non-`none` `causticStrategy` emits `walkaround-hybrid.unsupported-caustic-strategy`, and any `causticOptions` object emits `walkaround-hybrid.unsupported-caustic-options`; `hRemediationItems.test.ts` pins the console + structured warning paths.
- **H47 ✅ CLOSED — `ppgMaxSpatialCells` is an active PPG capacity knob.** The public option threads through `deriveHybridEngineConfig`, `HybridEngineLifecycle`, `WalkaroundGPUPipeline.initialize`, `PPGCoordinator.initialize/onResize`, `allocatePPGResources`, STree import/export compatibility, and GI state snapshots. `hRemediationItems.test.ts`, `lifecycleEdges.test.ts`, and `giStateSnapshot.test.ts` pin config preservation, resize retention, and snapshot metadata.
- **H48 ✅ CLOSED — `denoiser` is no longer a silent no-op on PT backends.** Source-verified 2026-06-12: pt-webgpu warns through `pt-webgpu.unsupported-denoiser` for every non-`none` / non-`oidn-final` request and degrades to no-denoise (`packages/pt-webgpu/src/index.ts`, pinned by `unsupportedDenoiserDegrade.test.ts`); pt-webgl2 warns through `pt-webgl2.unsupported-denoiser` for every non-`none` request because it has no denoiser pipeline (`packages/pt-webgl2/src/index.ts`, pinned by `engineContract.test.ts`). `denoiser:'none'` remains silent on both.
- **H49 ✅ CLOSED — pt-webgl2 consumes authored `specularColor`/`specularIntensity`.** Source-verified 2026-06-12: `materialsTexture.ts` packs scalar `specularColor` into texel 10 RGB and `specularIntensity` into texel 11 R, with defaults pinned by `materialsTexture.test.ts`; `material_struct.glsl.js` decodes scalar + map ids/transforms; `get_surface_record_function.glsl.js` samples `specularColorMap` RGB and `specularIntensityMap` alpha into `SurfaceRecord`; `bsdf_functions.glsl.js` uses them in glossy F0/F90; `promiseLedger.ts` grades both pt-webgl2 scalar fields native.
- **H50 ✅/◻ SOURCE-RECONCILED 2026-06-12 — the old "ZERO consumers" list was stale.** Current source evidence: pt-webgpu consumes anisotropy scalars/map, AO, bump, lightMap, envMapIntensity, and primitive/emitter `castShadow`; pt-webgl2 consumes scalar + map specular, bump/light maps, castShadow lanes, authored tangent XYZW, and HDRI intensity/rotation. The real residuals are narrower and honestly ledgered: displacement trio is unsupported on all shipping backends (`displacementMap`/`Scale`/`Bias` rows are `unsupported` and backends warn), `receiveShadow:false` is a reserved non-physical field with structured warnings on all three backends, pt-webgpu `normalMap` stays approximate until authored tangent handedness is consumed, and remaining backend-specific approximations stay in `BACKEND_PROMISE_LEDGER` / Road material buckets rather than this stale global-zero row.
- **H51 ✅ CLOSED as a silent-coercion row — remaining unsupported fidelity is tracked in Road material/environment buckets.** Source-verified 2026-06-12: pt-webgpu `maxBounces` emits `pt-webgpu.max-bounces-clamped`; distinct roughness/metallic map handles warn and document ORM precedence; unknown extensions warn while graduated legacy extension keys are accepted without unknown-key noise (`h51WarnCoercions.test.ts`, `contractHonestyWarns.test.ts`). walkaround emissive intensity is folded into emissive radiance and then normalized to `emissiveIntensity:1` by design so emitter Le and camera-visible glow agree. pt-webgl2 HDRI `intensity` and `rotationY` are wired (`uploadGapGuard.test.ts`, `mat4.test.ts`); `procedural-sky` is not advertised by pt-webgl2 capabilities (`supportedEnvironmentKinds=['none','hdri']`) and is filtered/warned by `partitionSceneBySupport` rather than silently rendered black. pt-webgpu point/spot `distance`/`decay` and spot `penumbra` are packed and consumed (`emitterStride.test.ts`, `kernel.wgsl.ts`, `kernelLite.wgsl.ts`). walkaround alpha fields (`alphaMode`, `alphaCutoff`, `opacity`, `alphaMap`) are still unsupported, but `collectUnconsumedMaterialFields` now explicitly surfaces them through the unconsumed-material warning path; full alpha rendering remains a Road material-completion task, not a silent drop.
- **H52 ✅/◻ SOURCE-RECONCILED 2026-06-12 — scalar lobes and full-tier extension maps are closed; sampler/schema parity remains.** pt-webgpu now packs/consumes clearcoat, sheen, iridescence, anisotropy, and KHR_materials_specular scalar fields. `specularColor`/`specularIntensity` live in material vec4 #27 and feed ordinary PT BRDF/PDF paths, MNEE/SPPM receiver paths, and BDPT light-subpath surface scattering. The full-tier material texture descriptor now also packs/samples clearcoat factor/roughness, sheen color/roughness, iridescence factor/thickness, and specular color/intensity maps with per-map UV metadata; those rows are honestly `approximate` until ReSTIR-PT visible-vertex texture-map payload/resolve parity, clearcoat/sheen source-lobe sampler/PDF schemas, and BDPT light-subpath scatter PDFs are closed. `clearcoatNormalMap` / `clearcoatNormalScale` remain unsupported.

### H-J — Test-suite blind spots (why all of the above is green)

- **H53 ✅ P0(class) — the suite structurally cannot see the H1-class:** pt-webgl2's mock GL returns a non-null location for EVERY uniform and no-ops unknown calls (`mockGl.ts:49,56-66`); every `glProgram` setter silently no-ops on null locations (`glProgram.ts:173-204`) so even real-GL typos are silent. ◻ pt-webgpu/walkaround stub devices validate no sizes/usages/layouts — the exact class of the thrice-recurred 16B-vs-32B dummy-buffer bug. ◻ NO vitest run compiles any shader (gpu tests excluded from default configs); the only behavioral gates (T1 smoke, wsl-gpu A/Bs) live OUTSIDE `npm test`, so CI/agents/refactor loops see green with zero shader-compile or radiometric coverage.
- **H54 ✅ P1 — pure false-confidence tests:** `engine/__tests__/lifecycle.test.ts` is `typeof` checks + a literal `fs.existsSync(VitrumCanvas.tsx)` (lead-read in full) — the documented host entry points (attachVitrum loop, ResizeObserver, VitrumCanvas mount) have zero executed coverage (compounds H30).
- **H55 ◻ P1 — goldens whose reference shares the code path (no independent oracle):** WGSL self-SHA contracts, `scenePack.golden.json` (pre-refactor same-lineage — the F-TLAS1 pattern), `frameParamsPacker.golden` re-deriving bytes from the same constants, `cpuTracer.ts` "mirrors" converging on themselves. The WGSL behavior layer has zero in-repo independent oracle. ◻ ~80 of 258 test files assert substrings of never-compiled shader text (worst: welford 69, wgslContract 62, mneeNewton 43).
- **H56 ◻ P1 — default-config test residue:** the NRC `spreadC=0.01` production path is now characterized in `spreadTermination.test.ts` with the H26 camera-pdf and `runningSum=0.0` fixes. Residual: `hybridLiteTier.test.ts` still leans on private `_cfg` and constant ordering for lite limits rather than proving the lite pass graph fits those limits.
- **Top missing tests (from the audit, priority order):** (1) GL uniform-upload completeness via a recording mock (catches H1/H2/H3 + the whole future class in one test); (2) in-repo naga/wgpu parse gate over composed shader variants in plain vitest; (3) size/usage-validating GPU stub (kills the dummy-buffer recurrence class); (4) NRC default-c characterization; (5) attachVitrum loop under happy-dom; (6) VitrumCanvas mount with tsx transform; (7) FrameParamsSlot↔WGSL struct cross-check; (8) lite-tier binding-count budget; (9) cpuTracer↔WGSL drift tripwire; (10) OIDN dispatch-time failure degradation.

### H-K — Examples / tools / scripts (the front door)

- **H57 ✅ CLOSED / SOURCE-RECONCILED 2026-06-12 — examples are restored and root-typecheck-gated.** The old "NO `examples/` directory" paragraph is stale: `examples/` now contains runnable Vite apps for `createEngine`, `attachVitrum`, `VitrumCanvas`, `createProgressiveEngine`, direct `pt-webgpu`, direct `pt-webgl2`, and the newly added `gltf-viewer` path for `loadGltfWithEngine()` / backend recommendation / `textureDecodeReport` / controller attachment. Future scenario-specific examples are product polish, not the original H57 blocker.
- **H58 ✅ P1 — `tools/benchmark-runner/capture-adapter-playwright.mjs:3` imports the deleted `playwrightWebGpu.mjs`** (lead-verified) — the GPU-capture adapter that gap-closure verification and three READMEs direct users to crashes on launch. ◻ `run-gap-closure-verification.mjs:186` tells users to use a "built-in capturePtWebgpu adapter" that `defaultCaptureCommand()` unconditionally returns `''` for.
- **H59 ◻ P1 — ~6 tool READMEs document a dead workflow:** `capture:refs*` npm scripts, `capture-all-refs.sh`, `benchmark:qualitymodes`/`lifecycle-soak`/`wave4-hardening`/`pt-webgl-fidelity`/`wave0-baseline`/`pr-hybrid`/`seed-wg0` and the `@vitrum-examples/*` dev servers — none exist. `scenario-presets.mjs:82` still targets `backend:'pt-webgl'`. Every dated reference-render sub-README cites deleted refresh commands. `tools/reference-renders/pt-webgl-fidelity/` (12 PNGs) baselines a deleted backend with no remaining capture path (possibly intentional as the cutover A/B reference per `plan/three-removal/06-validation.md` — decide and label). ◻ `rfe09-bridge-global-cmf.png` is the 5th pre-`daa9716` baseline — re-capture/byte-identity status unrecorded (the 4774c33 note covers only 4). ◻ neural-training README + train.py repeat the WRONG 426,075 param count (canonical 535,107 per `neural.test.ts:699-717`); dataset_spec.md cites the deleted fork + examples.
- **Healthy (verified by the agent):** the mechanical no-GPU chain (`verify:mechanical`, rc-acceptance, validate-gpu, githooks, wgsl-layout codegen in sync), `diff-baselines.mjs`, gpu-env probes.

### H verification protocol

Same as ever: every ◻ item is hearsay until the fixing session opens the cited file and
confirms the gap is present NOW. ✅ items were lead-verified 2026-06-09 but re-confirm the
line numbers before editing (the tree moves fast). Fixes that change rendering need the
before/after A/B per the testing protocol; H1/H3/H15/H17 in particular WILL change renders
and require golden re-captures against independent references (the F-TLAS1 lesson).
