// React entrypoint for @vitrum/engine. `react` is an OPTIONAL peer dep —
// hosts that don't use React never load this module (they import from
// the main `@vitrum/engine` entry instead).

export { VitrumCanvas, type VitrumCanvasProps } from './VitrumCanvas.js';
