# @vitrum/core

`@vitrum/core` defines the backend-agnostic contract for Vitrum engines.

- Scene graph and material types
- Engine lifecycle/state contract
- Per-frame input/output contracts
- GPU capability detection helpers

This package intentionally contains interfaces and shared utilities only; concrete rendering implementations live in backend packages.
