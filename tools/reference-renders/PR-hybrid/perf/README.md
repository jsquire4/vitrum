# PR-hybrid perf JSON

Historical perf JSON from the archived PR-hybrid benchmark flow. The old
PR-hybrid benchmark script no longer exists; use `npm run validate:gpu:smoke`
for the current hybrid GPU smoke, or the `tools/reference-renders/README.md`
capture-adapter flow for new image/perf capture sessions.

- `latest.json` — most recent full bench report (overwritten each run)
- Timestamped copies remain under `tools/benchmark-runner/results/pr-hybrid/`
