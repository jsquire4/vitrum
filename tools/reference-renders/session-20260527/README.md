# Session captures 2026-05-27

Quick (`512×512`, 64 SPP) GPU reference renders captured during the 2026-05-27 reference session.

The old quick-capture npm script no longer exists. For a new session, use the
capture-adapter flow documented in `tools/reference-renders/README.md`.

Promote selected PNGs into `tools/reference-renders/baseline/` after visual review:

```bash
cp tools/reference-renders/session-20260527/cornell-*.png tools/reference-renders/baseline/
```
