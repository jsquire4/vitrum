{
  "name": "@vitrum-examples/attach-vitrum",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal example: attachVitrum() — RAF loop + resize + camera forwarding wired by the lifecycle helper.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vitrum/engine": "file:../../packages/engine",
    "@vitrum-examples/cornell-scene": "file:../cornell-scene"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.40",
    "typescript": "^5.5.0",
    "vite": "^5.0.0"
  }
}
